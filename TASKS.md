# Tasks

## Current Phase: F2 — Execution
Unlocked: 2026-04-26

## In progress
(none)

## Done
- [x] T-208b: BybitV5Adapter REST read methods (2026-04-29) — F2 sixteenth numbered work task; **seventh F2 task with active math-validator gate** (Gate 4 ACTIVE per CLAUDE.md gate-4 conditional rule — staged diff touched `packages/exchange/`; verdict VERIFIED — H-015 string round-trip preservation; sum aggregate hand-computable §F.3 fixtures `13.25 = 10.50 - 2.25 + 5.00`, `30.00 = 10.00 + 20.00`, `10.0 = 10×1.0`; W#3 "0" preservation correct via narrow `not in ("", None)` check; W#4 strict `decimal.InvalidOperation` raise on `closedPnl=""`; no Decimal silent casts; `_MAX_CLOSED_PNL_PAGES=10` int loop counter not money). Replaces 3 NotImplementedError stubs from T-208a per stub-pin precedent — completes BybitV5Adapter ExchangeClient Protocol read surface (3 of 10 methods; remaining 3 streams + close = T-209). T-208b deltas: `packages/exchange/bybit_v5/adapter.py` (extension +117 LOC over T-208a baseline; 3 method bodies replace stubs + `_map_position_row` private mapper helper + `_MAX_CLOSED_PNL_PAGES=10` module constant; `Decimal` lifted from TYPE_CHECKING to runtime — used at runtime in mapper + read methods; `int(str(...))` and `Decimal(str(...))` casts to narrow `dict.get()` Any|None returns for mypy strict). **`get_positions(symbol=None)` @idempotent**: `limiter.acquire(sub_account, "positions")` → `GET /v5/position/list?category=linear[&symbol=]` retries=3 → maps `result.list[]` rows via `_map_position_row` → `list[Position]`. Returns empty list for empty response per OQ-3 default A. **`get_fill_price(symbol, order_id)` @idempotent**: `limiter.acquire("orders")` → `GET /v5/execution/list?category=linear&symbol=&orderId=` retries=3 → returns `Decimal(items[0]["execPrice"])` from first execution row OR None if `result.list=[]`. H-015 round-trip preserved per OQ-4 default A. **`get_closed_pnl_cumulative(sub_account)` @idempotent**: validates `sub_account == self._sub_account` BEFORE `limiter.acquire` per OQ-10 default A — `ValueError(f"sub_account mismatch: got {sub_account!r}, expected {self._sub_account!r}")` early-out costs no rate-limit token (W#5 test pin asserts both `limiter.acquire.await_count == 0` AND `client.request.await_count == 0`). Pagination loop bounded by `_MAX_CLOSED_PNL_PAGES=10` (F2 single-bot scale ceiling: Bybit `limit=200 × 10 = 2000` closed trades cumulative; protocol-binding-ish per L-001 active control mirror T-205 `_MAX_CAS_RETRIES=3` precedent — F5+ refactors to streaming aggregator if observability shows truncation). Per page: `limiter.acquire("positions")` → `GET /v5/position/closed-pnl?category=linear&limit=200[&cursor=]` retries=3 → `total += Decimal(item["closedPnl"])` for all `result.list[]` rows. On `nextPageCursor` empty/None → return total. At cap (10 iterations exhausted) → WARN log `bybit_v5.closed_pnl_pagination_capped_at_max_pages` + partial sum returned (W#1 test pin asserts dual `await_count == 10` + WARN log captured + 10 pages all non-empty cursors). W#4 strict-mode: `Decimal(item["closedPnl"])` raises `decimal.InvalidOperation` natively when wire field is empty string — no silent default to `"0"` (test pin `test_get_closed_pnl_cumulative_raises_on_empty_closed_pnl_field`). Returns `Decimal("0")` for empty response. **`_map_position_row(item)` private helper** (~37 LOC; module-level pure function per §N6) maps Bybit V5 `/v5/position/list` `result.list[]` row → T-201 `Position` dataclass. **W#3 strict empty-string handling** via `not in ("", None)` narrow check (NOT truthiness): `side==""` → `None`; `avgPrice==""` → `None`; `unrealisedPnl==""` → `None`; **string `"0"` PRESERVED** as `Decimal("0")` / `int(0)` (recently-closed flat may legitimately report `avgPrice="0"` or `unrealisedPnl="0"`; test pin `test_get_positions_preserves_zero_string_in_avg_price_and_unrealised_pnl` asserts both `entry_price == Decimal("0")` AND `unrealized_pnl == Decimal("0")`). **H-015 round-trip preservation** via `Decimal(str(item["size"]))` (string-exact; W#2 isinstance triad pin asserts `isinstance(p.size, Decimal)` AND `p.size == Decimal("0.500000000001")` AND `str(p.size) == "0.500000000001"` — value-only equality is insufficient because `Decimal("0.5") == 0.5` evaluates True for some floats). Same triad pinned for `get_fill_price` price. **RateLimitError handler at every read** mirrors T-208a writes: `try: client.request → except RateLimitError: await self._on_rate_limit_hit("<group>"); raise` (reuses existing `_on_rate_limit_hit` from T-208a — increments DI'd Counter `rate_limit_hits_total{exchange="bybit", endpoint_group=<group>}.inc()` if Counter not None + awaits `signal_upstream_rate_limit()` per ADR-0003 §2 + §15.3 line 2138). Per OQ-2 default A endpoint-group routing: `get_positions/get_closed_pnl_cumulative` → `"positions"`; `get_fill_price` → `"orders"`. Plan: `docs/plans/T-208b.md` (single-pass plan-reviewer APPROVE 2026-04-29 — 11 OQ defaults; 5 W-items: W#1 cap test 10 pages all non-empty cursors with dual `await_count == 10` + WARN log; W#2 H-015 isinstance triad in BOTH `get_positions` AND `get_fill_price` round-trip tests; W#3 `not in ("", None)` narrow check + zero-string preservation test; W#4 strict-mode `decimal.InvalidOperation` pin; W#5 sub_account ValueError dual `await_count == 0` assertion). **11 OQ defaults operator-approved 2026-04-29**: OQ-1 Bybit V5 endpoint paths verbatim per docs (`/v5/position/list`, `/v5/execution/list`, `/v5/position/closed-pnl`); OQ-2 endpoint-group routing; OQ-3 `Position` dataclass mapping with empty-string-for-flat convention; OQ-4 `get_fill_price` first-row + None on empty; OQ-5 `get_closed_pnl_cumulative` sub_account validation + pagination + cap WARN; OQ-6 inline pagination loop (no `_paginate` helper — premature abstraction per §0.8 single consumer); OQ-7 RateLimitError handler reuses T-208a `_on_rate_limit_hit`; OQ-8 mock-based unit tests (no real HTTP); OQ-9 H-015 round-trip test pin; OQ-10 sub_account validation BEFORE limiter.acquire (no token waste on caller mistake); OQ-11 ~150 LOC src estimate. **Hazards**: H-015 (orphan close uses qty string — read-side: `Decimal(item["size"])` string-exact; T-221 reconciliation will consume `get_positions().size` for orphan-close write per H-015 round-trip invariant). H-022 (per-bot credentials: `sub_account` validation in `get_closed_pnl_cumulative`; per-bot adapter instance instantiated by T-215). H-025 (cross-bot IP coordination: limiter.acquire + signal_upstream wiring at every read; reuses T-208a `_on_rate_limit_hit` Counter increment). **All 4 review gates passed**: plan-reviewer single-pass APPROVE → drift-checker ON TRACK (5/5 W-items verified, 11/11 OQ defaults applied, src ~117 LOC actual vs ~110 plan estimate +6% well under 200 L-006 watch + 400 §0.3 cap; 21 new tests; no scope creep, no premature abstraction — `_map_position_row` module-level pure function NOT class, pagination loop inline NOT helper) → brief-reviewer SHIP → math-validator VERIFIED (H-015 round-trip; sum aggregate hand-computable §F.3 fixtures; W#3 "0" preservation; W#4 strict InvalidOperation natively raised by `Decimal("")`; no Decimal silent casts; `_MAX_CLOSED_PNL_PAGES=10` int loop counter not money; `int(str(leverage_raw))` cast is integer per Bybit spec not precision-relevant). **ci-full GREEN on branch HEAD `54677a7`** (run 25123854377, 2m5s; ci-fast also GREEN run 25123854340, 42s). 117 LOC src `adapter.py` extension NET (well under §0.3 cap of 400; well under L-006 watch threshold of 200). 362 LOC test (cap-exempt; 21 new unit tests in `test_adapter.py`: 6 get_positions incl. flat-state + W#2 H-015 isinstance triad + W#3 zero-string preservation + wire-payload snapshot, 4 get_fill_price incl. W#2 H-015 isinstance triad + None pass-through + wire-payload snapshot, 7 get_closed_pnl_cumulative incl. W#5 sub_account validation BEFORE acquire/request + single-page sum + empty-zero + 2-page pagination + W#1 cap-at-10-pages WARN log + W#4 closedPnl='' InvalidOperation + wire-payload snapshot, 3 endpoint-group routing, 1 parametrized RateLimitError over 3 reads). 545 LOC plan-doc artifact. Repo-wide pytest 803 passed + 75 skipped (was 782/75 baseline post-T-208a; +21 new pass = 21 new T-208b unit tests). Pre-commit hooks (ruff format, ruff check, mypy strict, bandit) all passed; ruff RUF003 caught initial `×` ambiguous-multiplication-sign in module comment — replaced with `x` before re-commit succeeded. **Calibration win continued** (T-204 → T-205 → T-207 → T-208a → T-208b trajectory): 10 verbatim Decisions in plan-doc + 11 genuine OQs (all "use defaults" — operator's calibrated trust pattern at peak). **Removed 3 T-208b stubs from parametrized async-stub test** in `test_adapter.py`; only `close` remains as `T-209` forward-pointer (renamed to `test_close_stub_raises_NotImplementedError_with_T_209_substring`). **BybitV5Adapter completes 7 of 10 ExchangeClient methods** (4 writes T-208a + 3 reads T-208b); 3 lifecycle stubs remain for T-209 (`stream_executions/stream_positions/close`). **Unblocks T-209** (BybitV5Adapter private WS — replaces last 3 NotImplementedError stubs; auth handshake + exp backoff + execId dedup ring per §11.2). **Unblocks T-215** (adapter pool composition root instantiates fully-functional `BybitV5Adapter` for live bots in `bots`-table loop). **Unblocks T-216 / T-217 / T-218 / T-220 / T-221** read-side consumers (placement pipeline symbol-existence check; lifecycle FSM last-fill audit; dispatcher cross-check; P&L audit; reconciliation orphan-close cross-check).
- [x] T-208a: BybitV5Adapter REST write methods (2026-04-29) — F2 fifteenth numbered work task; **sixth F2 task with active math-validator gate** (Gate 4 ACTIVE per CLAUDE.md gate-4 conditional rule — staged diff touched `packages/exchange/`; verdict VERIFIED — no new arithmetic; H-015 string preservation contract verified at 7 wire-boundary serialization sites; datetime arithmetic in TTL check is operational pacing per L-001/T-205 Decision #12 precedent NOT financial math; all wire-payload snapshots §F.1 hand-computable bit-for-bit). **Pre-T-208a `chore(tasks)` commit `dde5a8d`** split T-208 → T-208a writes + T-208b reads per T-213a/b precedent (§0.3 / L-006). T-208a deltas: `packages/exchange/bybit_v5/adapter.py` (NEW 245 LOC: BybitV5Adapter class composing T-207 BybitV5Client + T-205 SharedRateLimiter + optional Prometheus Counter via 7-kwarg DI ctor) + `packages/exchange/bybit_v5/__init__.py` (+5 LOC re-export BybitV5Adapter) + `packages/exchange/tests/test_protocol_conformance.py` (+2 LOC append BybitV5Adapter to `_ADAPTERS_UNDER_TEST` per OQ-11). **BybitV5Adapter surface**: 7-kwarg ctor (`client: BybitV5Client`, `limiter: SharedRateLimiter`, `bus: NatsClient`, `sub_account: str`, `metrics_counter: Counter | None = None`, `leverage_cache_ttl_s: float = _DEFAULT_LEVERAGE_CACHE_TTL_S` per L-001 fix, `now_fn: Callable[[], datetime] = now_utc`); 4 write methods + 6 stub forward-pointers (3 reads → "T-208b"; 2 streams + close → "T-209"). Per-instance `_leverage_cache: dict[(str, int), datetime]`. **`set_leverage` @idempotent**: LRU cache via per-instance dict with TTL check `(now - cached_at) < timedelta(seconds=self._leverage_cache_ttl_s)`; cache hit → silent return; on miss `limiter.acquire("positions")` → `POST /v5/position/set-leverage` body `{category:"linear", symbol, buyLeverage:str, sellLeverage:str}` retries=3 → cache write. **`place_market_order` @non_idempotent**: H-003 zero-retry — `limiter.acquire("orders")` → `POST /v5/order/create` body `{category:"linear", symbol, side:"Buy"/"Sell", orderType:"Market", qty:str(qty), reduceOnly:bool}` retries=0; catch `NetworkTimeout` → raise `UnknownState("place_market_order") from exc` per §11.3. Returns `OrderPlaceResult(exchange_order_id=str(result["orderId"]), placed_at=self._now_fn())`. **`set_trading_stop` @idempotent**: H-013 no-default `tpsl_mode` (T-201 Protocol contract); body always has `tpslMode`; conditional `stopLoss/takeProfit/tpSize` (None-omitted from body); `limiter.acquire("positions")` retries=3. **`cancel_order` @idempotent**: `limiter.acquire("orders")` → `POST /v5/order/cancel` body `{category, symbol, orderId}` retries=3. **RateLimitError handler at every write**: `except RateLimitError: await self._on_rate_limit_hit("<group>"); raise`. `_on_rate_limit_hit` increments DI'd `metrics_counter.labels(exchange="bybit", endpoint_group=group).inc()` if Counter not None + awaits `signal_upstream_rate_limit()` per ADR-0003 §2 (§15.3 line 2138 `rate_limit_hits_total{exchange, endpoint_group}` deferred from T-205). **Side mapping** `_to_bybit_side(side: Literal["buy", "sell"]) -> Literal["Buy", "Sell"]` ternary helper at boundary (T-201 OQ-3 lowercase Protocol contract preserved). **Module constants**: `_CATEGORY="linear"` (Bybit USDT perpetual = F2 scope per §11.5); `_DEFAULT_LEVERAGE_CACHE_TTL_S=3600.0` (Q9 brief default; L-001 active control fix — operator-tunable via ctor kwarg, NOT module-level runtime constant; T-215 wires from env var `BYBIT_LEVERAGE_CACHE_TTL_S`); `_BUY: Literal["Buy"]` + `_SELL: Literal["Sell"]` for type narrowing. **6 stub methods** with `_stub_message(method, owner)` forward-pointer per T-211 stub-pin precedent: 3 reads (`get_positions/get_fill_price/get_closed_pnl_cumulative`) → owner="T-208b"; 3 lifecycle (`stream_executions/stream_positions/close`) → owner="T-209". Stream methods are `def`-not-`async-def` per T-201 OQ-1 raising synchronously. **H-015 string preservation** at wire boundary: `str(qty)`, `str(sl_price)`, `str(tp_price)`, `str(tp_size)`, `str(leverage)` — all Decimal-typed parameters serialized as str at httpx body construction; NO `float()` coercion anywhere. W#5 fix applied: test pin asserts `isinstance(body["qty"], str)` AND value equality (`"0.500000000001"` value pin enforces contract even when test value happens to round-trip through float). **H-003 zero-retry**: `place_market_order` calls T-207 client with `retries=0` (T-207 raises NetworkTimeout immediate after single attempt); T-208a catches + raises `UnknownState("place_market_order")`. Verbatim test `test_place_order_on_timeout_never_retries_and_raises_unknown_state` per §20 line 2621 asserts `await_count == 1` AND `retries == 0` AND `last_known_action == "place_market_order"` AND `isinstance(__cause__, NetworkTimeout)`. Plan: `docs/plans/T-208a.md` (2nd-pass plan-reviewer APPROVE 2026-04-29 — 1st REVISE 1 BLOCKER + 2 CONCERNS → 2nd APPROVE; 1 BLOCKER was L-001 active control hit — `_LEVERAGE_CACHE_TTL_S=3600` hardcoded module constant for operationally-tunable threshold; fix added 6th ctor kwarg `leverage_cache_ttl_s: float`. 2 CONCERNS: `_to_bybit_side` annotation `-> str` vs Decision #8 `-> Literal["Buy", "Sell"]` (now Literal); TASKS.md split T-208 → T-208a + T-208b not formalized (separate `chore(tasks)` commit `dde5a8d` shipped per T-213a/b precedent). 5 W-items: W#1 `_DEFAULT_LEVERAGE_CACHE_TTL_S` referenced ONLY at ctor default; W#2 `_to_bybit_side` verbatim Literal signature; W#3 custom-TTL test `test_set_leverage_uses_custom_ttl_when_provided` exercises both cache-hit AND cache-miss paths with `leverage_cache_ttl_s=10.0` + injected deterministic now_fn; W#4 surface-expansion guard 280 STOP; W#5 H-015 isinstance str pin in addition to value pin). **14 OQ defaults operator-approved 2026-04-29**: OQ-1 composition pattern (BybitV5Adapter composes BybitV5Client + SharedRateLimiter + Counter via DI; mirror PaperExchange precedent), OQ-2 LRU 1h TTL (Q9 brief default), OQ-3 Bybit V5 endpoint paths verbatim per docs (`/v5/position/set-leverage`, `/v5/order/create`, `/v5/position/trading-stop`, `/v5/order/cancel`; `category="linear"`), OQ-4 lowercase→capitalized side mapping at boundary, OQ-5 `str(qty)` per H-015, OQ-6 7-kwarg ctor (was 5; +`leverage_cache_ttl_s` per L-001 BLOCKER + `now_fn` per T-205/T-213b precedent), OQ-7 DI'd Counter with `metrics_counter=None` default for unit-test mocking, OQ-8 endpoint-group routing (set_leverage/set_trading_stop→positions; place_market_order/cancel_order→orders), OQ-9 stub forward-pointers per T-211 precedent, OQ-10 mock-based unit tests (no real HTTP/NATS), OQ-11 append BybitV5Adapter to `_ADAPTERS_UNDER_TEST` per T-206 forward-extensibility, OQ-12 H-003 verbatim test name `test_place_order_on_timeout_never_retries_and_raises_unknown_state`, OQ-13 H-015 partial — orphan-close path = T-221, OQ-14 ~229 LOC src estimate. **All 4 review gates passed**: plan-reviewer 2nd-pass APPROVE → drift-checker ON TRACK (5/5 W-items verified, 14/14 OQ defaults applied, LOC inflation +16 over plan estimate ~229 → actual 245 classified as legitimate docstring + RateLimitError handler boilerplate ~16 LOC per T-205/T-207 precedent; 245 well under 280 STOP) → brief-reviewer SHIP → math-validator VERIFIED (no new arithmetic; H-015 str preservation contract verified at 7 wire-boundary serialization sites; datetime TTL arithmetic operational pacing per L-001/T-205 Decision #12 precedent NOT financial math; all wire-payload snapshots §F.1 hand-computable bit-for-bit; `_DEFAULT_LEVERAGE_CACHE_TTL_S=3600.0` float type correct for `timedelta(seconds=...)`). **ci-full GREEN on branch HEAD `badd0ff`** (run 25122416378, 2m0s; ci-fast also GREEN run 25122416396, 38s). 245 LOC src `adapter.py` (under §0.3 cap of 400; under L-006 plan-doc mid-write STOP of 280; +45 above L-006 watch threshold of 200 — surface-expansion guard ACTIVE per W#4) + 5 LOC `__init__.py` extension + 2 LOC `test_protocol_conformance.py` append = 252 LOC src NET. 452 LOC test (cap-exempt; 32 unit tests in `test_adapter.py`: 3 ctor + 5 set_leverage incl. W#3 custom-TTL test + 6 place_market_order incl. H-003 verbatim + H-015 partial pin + 4 set_trading_stop + 3 cancel_order + 4 RateLimitError handler + 1 parametrized stubs (4 async stubs incl. close + 2 def stubs) + 1 _to_bybit_side helper). 627 LOC plan-doc artifact. Repo-wide pytest 782 passed + 75 skipped (was 746/75 baseline post-T-207; +36 new pass = 32 adapter unit + 4 conformance parametrize). Pre-commit hooks (ruff format, ruff check, mypy strict, bandit) all passed first run. **Calibration win continued** (T-204 → T-205 → T-207 → T-208a trajectory): 14 verbatim Decisions in plan-doc + 14 genuine OQs (all "use defaults" — operator's calibrated trust pattern at peak). **Unblocks T-208b** (BybitV5Adapter REST read methods replace 3 NotImplementedError stubs from T-208a per stub-pin precedent; 3 reads consume T-207 client + go through limiter.acquire per H-025; H-015 round-trip preservation). **Unblocks T-209** (BybitV5Adapter private WS replaces 3 NotImplementedError stubs from T-208a — `stream_executions/stream_positions/close`). **Unblocks T-215** (adapter pool composition root instantiates `BybitV5Adapter(client, limiter, bus, sub_account, metrics_counter, leverage_cache_ttl_s)` per `bots`-table row; reads env vars `BOT_<ID>_BYBIT_API_KEY/SECRET` per H-022 + `BYBIT_LEVERAGE_CACHE_TTL_S` per L-001 fix).
- [x] T-207: BybitV5Client REST envelope + HMAC-SHA256 signing + retry matrix (2026-04-29) — F2 fourteenth numbered work task; **fifth F2 task with active math-validator gate** (Gate 4 ACTIVE per CLAUDE.md gate-4 conditional rule — staged diff touched `packages/exchange/`; verdict VERIFIED — 3 hand-computable HMAC-SHA256 signing vectors §F.1, retry decision tree §F.2 closed-form `max_attempts = retries + 1`, jitter bounds §F.3 `[base*0.9, base*1.1]` for `base ∈ {0.5, 1.0, 2.0}`, no Decimal silent casts, Decision #6 float justified per §5.13 scope as operational backoff jitter). NEW subpackage `packages/exchange/bybit_v5/` mirroring `packages/exchange/paper/` precedent: `__init__.py` re-exports `BybitV5Client` (~8 LOC) + `client.py` (286 LOC: BybitV5Client class + 6 methods + 6 module constants). T-207 deltas: `packages/exchange/pyproject.toml` (+1 LOC `httpx==0.28.1` dep declaration matching `packages/market` version pin per OQ-1 default A — no new ecosystem dep) + new subpackage. **BybitV5Client surface**: 5-kwarg ctor (`api_key`, `api_secret`, `base_url="https://api.bybit.com"`, `connect_timeout=5.0`, `read_timeout=10.0`); `request(method, path, *, params, body, retries: int)` returns Bybit envelope `result` field on `retCode==0` or raises typed exception per §11.3. State-free across calls — signing per-call; `httpx.AsyncClient` owns connection pool. T-208 will extend ctor with adapter-level wrapping (`limiter`, `bus`, `sub_account`). `__aenter__/__aexit__/close()` lifecycle mirror `BinanceRestClient` (T-101 precedent). **HMAC-SHA256 signing** per Bybit V5 spec: payload `f"{timestamp_ms}{api_key}{recv_window}{query_or_body}"`; `signature = hmac.new(api_secret.encode("utf-8"), payload.encode("utf-8"), hashlib.sha256).hexdigest()`; 4 X-BAPI-* headers (`X-BAPI-API-KEY`, `X-BAPI-TIMESTAMP`, `X-BAPI-RECV-WINDOW=5000ms`, `X-BAPI-SIGN`). Stdlib `hmac` + `hashlib` only — no new third-party dep. **Retry matrix** per §11.2 verbatim: `max_attempts = retries + 1` loop; on `httpx.ConnectError`/`ReadTimeout`/`PoolTimeout` OR HTTP 5xx → if attempts remain, sleep `_backoff_delay(attempt)` then retry, else raise `NetworkTimeout`; on HTTP 401/403 → `AuthError` (no retry); on HTTP 429 → `RateLimitError` (no retry — caller's job to back off via shared pause flag); on HTTP 200 → parse Bybit envelope, on `JSONDecodeError` raise `ExchangeError` with `__cause__`, on `retCode==0` return `result` field, else `_translate_retcode(retcode, retmsg)`. **`_translate_retcode`** static method: `_RETCODE_RATE_LIMIT={10006, 10016}`→`RateLimitError`, `_RETCODE_AUTH={10003, 10004}`→`AuthError`, `_RETCODE_REJECT={10001, 10005}`→`OrderRejected(reason=retmsg)`, other non-zero→generic `ExchangeError`. **Jitter formula** per §F.3: `_backoff_delay(attempt) = _BASE_BACKOFF_S[min(attempt, len-1)] * (1.0 + random.uniform(-_JITTER_PCT, _JITTER_PCT))` with `_BASE_BACKOFF_S=(0.5, 1.0, 2.0)` + `_JITTER_PCT=0.1` ±10%; clamp at last entry for attempts≥3. `# noqa: S311 # nosec B311` annotation explicitly justifies operational-jitter-not-cryptographic use (distinct from cryptographic `hashlib.sha256`). **W#1 sort canonicalization fix applied**: `_sign_request` calls `urlencode(sorted(params.items()))` AND `_send` calls `dict(sorted(params.items()))` — single load-bearing invariant in BOTH paths so Bybit doesn't reject with sign mismatch when Python dict insertion order differs from alphabetical. **W#2 body parity fix applied**: `_send` pre-serializes body once via `json.dumps(body, separators=(",", ":")).encode("utf-8")` and passes via `content=` kwarg (NOT `json=` which may re-serialize differently); identical compact JSON used in `_sign_request` payload tail. **§N3 markers correctly NOT on T-207 internals** per Decision #9: `_request_with_retry`, `_sign_request`, `_translate_retcode`, `_backoff_delay`, `request` are LOW-LEVEL HTTP helpers; markers belong on T-208 method wrappers (`place_market_order @non_idempotent`, etc.) which are the actual `ExchangeClient` Protocol surface (T-201 already has them on the Protocol). **§N6 globals**: `BybitV5Client` per-instance state; module-level constants (`_RECV_WINDOW_MS=5000`, `_BASE_BACKOFF_S`, `_JITTER_PCT=0.1`, frozensets, `logger`) protocol-binding read-only per §11.2 verbatim — mirror T-205 `_MAX_CAS_RETRIES=3` / ADR-0003 §3 baked-in precedent. **§N9 configurability**: protocol-binding constants are NOT operational tunables; baked-in per Bybit V5 docs. **§N1 UTC**: timestamp via `time.time_ns() // 1_000_000` (UTC ms epoch per Bybit V5 spec). **§N7 thin adapter**: REST envelope at I/O boundary; no business logic. **H-003 zero-retry binding**: `_request_with_retry` with `retries=0` → `max_attempts=1` → single attempt → on `httpx.ReadTimeout`/etc. raise `NetworkTimeout` immediate (no sleep, no retry). T-208 `place_market_order` will pass `retries=0` per §11.2 + catch `NetworkTimeout` + raise `UnknownState(last_known_action="place_market_order")` per §11.3 H-003 binding (decoupled per OQ-8 default A). Test pin `test_request_with_retry_zero_retries_raises_immediately_on_timeout` asserts `mock_request.await_count == 1` AND `captured_sleeps == []`. **H-022 per-bot credentials**: ctor takes `api_key`/`api_secret` as plain kwargs; T-215 reads from env per-bot via `BOT_<ID>_BYBIT_API_KEY/SECRET` pattern (out of T-207 scope, correctly delegated). Plan: `docs/plans/T-207.md` (single-pass plan-reviewer APPROVE 2026-04-29 — 5 W-items: W#1 query sort canonicalization in both signing AND outbound URL paths; W#2 body pre-serialization byte-identical via `content=` not `json=`; W#3 28 tests vs 22 plan headline reconciled via `@pytest.mark.parametrize` 4 expansions; W#4 malformed JSON path test with `__cause__` assertion; W#5 3 lifecycle tests verbatim names `test_constructor_accepts_required_kwargs_and_creates_httpx_client` / `test_close_drains_httpx_pool` / `test_context_manager_closes_on_exit_even_on_exception`). **9 OQ defaults operator-approved 2026-04-29**: OQ-1 default A (httpx==0.28.1 parity with packages/market), OQ-2 default A (single client.py file, no premature split), OQ-3 default A (REST envelope decoupled from rate limiter — T-208 wires acquire+signal_upstream), OQ-4 default A (RateLimitError caller-driven), OQ-5 default A (caller-passed retries: int), OQ-6 default A (signing test vectors generated from Bybit V5 docs spec deterministically — no v1 source dependency per `sibling_bot_v1` memory rule), OQ-7 default A (unit tests mock httpx; integration deferred to T-222 testnet smoke), OQ-8 default A (UnknownState raised at T-208 not T-207 per §11.3 H-003 binding), OQ-9 default A (~226 LOC src estimate; actual 294 = +30% L-006 inflation classified as legitimate doc/decomposition overhead per drift-checker — 286 src `client.py` 6 LOC over mid-write 280 STOP threshold attributable to ~40 LOC docstring overhead + ~30 LOC private `_send` helper extraction containing W#1+W#2 invariants + ~15 LOC 5xx HTTP retry path per plan §F.2 Decision #5; no scope creep, no premature abstraction, no new public surface beyond plan). **All 4 review gates passed**: plan-reviewer single-pass APPROVE → drift-checker ON TRACK (5/5 W-items verified, 9/9 OQ defaults applied, LOC inflation classified as doc/decomposition not scope creep) → brief-reviewer SHIP → math-validator VERIFIED (closed-form HMAC-SHA256 fixtures §F.1 hand-computable via stdlib oracle for header-name/sorted-key/compact-JSON API-surface invariants; jitter bounds §F.3 closed-form per attempt; retry decision tree §F.2 max_attempts=retries+1 closed-form; no Decimal silent casts; # noqa: S311 # nosec B311 inline-justified vs hashlib.sha256 distinct concern). **ci-full GREEN on branch HEAD `c20955a`** (run 25120897536, 1m59s; ci-fast also GREEN run 25120897611, 41s). 294 LOC src NET (`client.py` 286 + `__init__.py` 8 + `pyproject.toml` +1 = 295 incl. dep / cap-considered as ~280-294 effective per drift-checker classification under §0.3 cap of 400; +27% L-006 inflation in expected band for integration-heavy tasks per L-006 active control). 442 LOC test (cap-exempt; 28 unit tests collected: 6 signing — 3 vectors + headers + recv_window pin + W#1 sort canonicalization, 8 retCode mapping — 3 parametrized pairs + zero + other + W#4 malformed JSON, 3 HTTP status — parametrized 401/403 + 429, 6 retry matrix — 3-retries-succeed + 0-retries-immediate + exhaust-NetworkTimeout + 5xx-retryable + jitter-bounds + 429-no-retry, 1 W#2 body parity, 3 W#5 lifecycle verbatim names, 1 sort canonicalization separate). 482 LOC plan-doc artifact. Repo-wide pytest 746 passed + 75 skipped (was 718/75 baseline post-T-205; +28 new pass). Pre-commit hooks (ruff format, ruff check, mypy strict, bandit) all passed; bandit B311 caught initially with only `# noqa: S311` (ruff) — added `# nosec B311` (bandit) before re-commit succeeded. **Calibration win continued** (T-204 → T-205 → T-207 trajectory): 13 verbatim Decisions in plan-doc + 9 genuine OQs (all "use defaults" — operator's calibrated trust pattern at peak). **First single-pass plan-reviewer APPROVE without REVISE** in F2 since T-200 (T-204 → T-211 → T-206 → T-212 → T-213a → T-213b → T-213c → T-205 all needed REVISE first; T-207 had ADR-style baked decisions per brief §11.2 verbatim leaving fewer architectural surface ambiguities for plan-reviewer to flag). **Unblocks T-208** (BybitV5Adapter REST methods compose `BybitV5Client` instance + wire `SharedRateLimiter.acquire()` per OQ-3 default A + catch `RateLimitError` + call `signal_upstream_rate_limit()` per OQ-4 default A + catch `NetworkTimeout` on `place_market_order` + raise `UnknownState` per OQ-8 default A; T-208 also owns `rate_limit_hits_total{exchange, endpoint_group}` Prometheus counter increment per §15.3 line 2138 deferred from T-205). **Unblocks T-209** (BybitV5Adapter private WS — BybitV5Client REST envelope established as F2 ladder 1st rung; WS handshake + auth re-handshake + execId dedup ring per §11.2 deferred). **Unblocks T-215** (adapter pool composition root instantiates `BybitV5Client(api_key, api_secret)` per `bots` row + wraps in T-208 `BybitV5Adapter(client, limiter, bus, sub_account)` ctor).
- [x] T-205: Shared rate limiter (NATS KV token-bucket) (2026-04-29) — F2 thirteenth numbered work task; **fourth F2 task with active math-validator gate** (Gate 4 ACTIVE per CLAUDE.md gate-4 conditional rule — staged diff touched `packages/exchange/`; verdict VERIFIED — closed-form arithmetic in §F.1-§F.3, hand-computable fixtures, no Decimal silent casts, Decision #12 float justified per §5.13 scope as operational counter). **First F2 task with prerequisite ADR shipped same session**: ADR-0003 (`docs/adr/0003-rate-limiter-token-bucket.md`, master commit `dcf98bd`) bakes 6 design decisions — 3 bucket families (`bybit:<sub_account>:orders` 10/s cap 20, `bybit:<sub_account>:positions` 10/s cap 20, `bybit:ip:global` 120/s cap 240), 500ms coordinated-pause-flag at `bybit:ip:pause`, optimistic CAS retry-once + fail-open after 3 conflicts, all params Settings env vars, DI via constructor, sub_account from `bots` table column. T-205 deltas: `packages/bus/client.py` (+93 LOC: `kv_get` @idempotent returning `tuple[bytes, int] | None` with KeyNotFoundError + tombstone-deleted entries collapsed to None per fail-safe; `kv_update` @non_idempotent CAS via `kv.update(key, value, last=last_revision)` raising PublishError on revision mismatch; mirror kv_put precedent — state guard, debug log, nats.errors.Error wrapped; nats.js.errors.KeyNotFoundError import path) + `packages/exchange/rate_limiter.py` (NEW 195 LOC: `SharedRateLimiter` class with 8-kwarg ctor + `acquire(sub_account, endpoint_group)` @non_idempotent + `signal_upstream_rate_limit()` @idempotent + `_wait_if_paused()` private + `_refill_and_debit(key, group)` private with lazy refill `tokens_now = min(capacity, last_tokens + elapsed * rate)`, `if tokens<1.0` sleep `(1-tokens)/rate` + retry loop max 3, CAS via `kv_update`, fail-open after 3 conflicts with WARN log; module-level constants `_BUCKET="rate_limits"`, `_PAUSE_KEY="bybit:ip:pause"`, `_IP_GLOBAL_KEY="bybit:ip:global"`, `_MAX_CAS_RETRIES=3` per ADR-0003 §3 binding NOT operational tunables; module-level `logger = logging.getLogger(__name__)`). **JSON state serialization** `{"tokens": float, "last_refill_at": ISO-8601 string}` per Decision #12 (operational counter, NOT financial math; explicitly NOT §N1 violation per §5.13 invariant scope being for P&L/prices/qty only). **CONCERN 2 fix applied**: `try/except (json.JSONDecodeError, KeyError, ValueError)` in `_refill_and_debit` falls back to fresh-bucket (`tokens=capacity, last_refill_at=now, revision=None`) with WARN log; `revision=None` cue forces `kv_put` not `kv_update` with stale revision; mirrors pause-flag malformed handler shape. **W#1 fix applied**: pseudokód `extra={"value": value!r}` (invalid Python) → `extra={"value": repr(value)}` v skutočnom kóde. **H-025 three-layer mitigation** per plan-doc Hazards table: (a) `acquire(sub_account, group)` debits sub-account + IP-global buckets via NATS KV CAS ensuring per-IP enforcement matches Bybit's actual rate model; (b) `signal_upstream_rate_limit()` writes shared `bybit:ip:pause` flag visible to all `SharedRateLimiter` instances on the IP — every subsequent `acquire()` call (any bot, any group) checks pause flag first via `_wait_if_paused()` and sleeps until expiry; (c) the 500ms shared pause flag IS the H-025 "exponential backoff with jitter" mechanism per ADR-0003 §"Rationale" + Decision #14 — additional per-call jitter on lazy-refill wait or CAS retry rejected as premature optimization (refill wait is deterministic; CAS conflicts are diagnostic signal). **Verbatim required test** `test_one_bot_rate_limit_triggers_shared_pause_flag` (§20 line 2804 + TASKS.md line 80) prítomný v test_rate_limiter_integration.py:86. **H-022 sub-account isolation** — `acquire()` keys KV by `sub_account` per call; `test_acquire_separates_sub_accounts` pin. Plan: `docs/plans/T-205.md` (2nd-pass plan-reviewer APPROVE 2026-04-29 — 1st REVISE 3 BLOCKERS + 4 CONCERNS → 2nd APPROVE; 3 BLOCKERS were: H-025 hazard binding missing in Hazards table; verbatim test name `test_one_bot_rate_limit_triggers_shared_pause_flag` missing from test plan; H-025 jitter requirement justification needed → all addressed via Hazards row addition + integration test list + Decision #14. 4 CONCERNS: `rate_limit_hits_total` Prometheus counter explicit deferral to T-208 in Non-goals; JSON deserialization protection in `_refill_and_debit`; clock-skew test pin in test list; pause-flag-mid-acquire edge case #8 documentation. W#1 pseudocode `!r` syntax fix + W#2 Decision #14 orientation fix applied at consolidation; W#3 visibility test uses `bus.kv_put` directly per implementation-time guidance). **Operator-approved 7 OQ defaults** 2026-04-29: OQ-1 default A (`acquire()` blocking semantics), OQ-2 default A (IP-global bucket sequenced not transactional), OQ-3 default A (lazy refill on each acquire), OQ-4 default A (`RateLimitError` caller-driven via `signal_upstream_rate_limit()`), OQ-5 default A (KV bucket bootstrap delegated to infra), OQ-6 default A (mixed integration env-gated + unit mock pool tests), OQ-7 default A (~210 LOC src estimate, surface-expansion guard pre-armed at 240 mid-write STOP). **All 4 review gates passed**: plan-reviewer APPROVE (2nd pass) → drift-checker ON TRACK (3/3 W-items verified, 7/7 OQ defaults applied, ADR-0003 6 design decisions baked-in, hand verification fixtures hand-computable, no scope creep) → brief-reviewer SHIP → math-validator VERIFIED (closed-form arithmetic §F.1 lazy-refill `min(20, 5.0+0.5*10)=10.0→9.0` + §F.2 blocking wait `(1-0.5)/10=0.05` + §F.3 pause-flag wait `(expires_at-now).total_seconds()=0.3` + Edge case #3 clock skew `min(20, 10+(-0.5)*10)=5.0→4.0` + CONCERN 2 corrupted JSON fresh-bucket fallback; Decision #12 float justified per §5.13 scope being for financial math only). **ci-full GREEN on branch HEAD `e1cf77c`** (run 25119546803, 2m2s; ci-fast also GREEN run 25119546821, 38s). 269 LOC src NET (rate_limiter.py +195 + client.py +93 + minor adjustments = 269 incl. test edits / -25 deletions per imports = 212 net core under §0.3 cap of 400; 12 LOC above L-006 watch threshold of 200 — surface-expansion guard ACTIVE per W#5 codified guard, mid-write STOP at 240 LOC src; final src 212 well under 240 STOP). 660 LOC test (cap-exempt; 17 new unit tests in test_rate_limiter.py — constructor + markers + happy path debit + sub-account separation + lazy refill formula + blocking-then-refill + fresh bucket + pause flag active/expired/missing + signal_upstream + replay idempotent + CAS retry fail-open + clock skew + corrupted KV; 9 new bus tests in test_client_kv.py — kv_get/kv_update + 4 state guards + 2 marker checks; 4 new env-gated integration tests in test_rate_limiter_integration.py — H-025 verbatim + concurrent CAS + cross-instance visibility + end-to-end pause). 547 LOC plan-doc artifact + 83 LOC ADR-0003 (separate commit). Repo-wide pytest 718 passed + 75 skipped (was 692/71 baseline post-T-213c; +9 bus + 17 limiter unit + 4 env-gated integration = +30 net). Pre-commit hooks (ruff format, ruff check, mypy strict, bandit) all passed first run. **Calibration win continued** (T-204 → T-210 → T-214 → T-211 → T-206 → T-212 → T-213a → T-213b → T-213c → T-205 trajectory): 14 verbatim Decisions in plan-doc + 7 genuine OQs (all "use defaults" — operator's calibrated trust pattern at peak). **First task with same-session ADR prerequisite shipped**: ADR-Q-A backlog entry → ADR-0003 written + accepted + committed + plan-doc baked-in + impl + ci-full GREEN, all in single session. **Unblocks T-208** (BybitV5Adapter REST methods consume via DI'd ctor kwarg `limiter: SharedRateLimiter`; T-208 catches Bybit 429 at HTTP layer + calls `await limiter.signal_upstream_rate_limit()` + increments `rate_limit_hits_total{exchange, endpoint_group}` Prometheus counter per §15.3 line 2138 deferred from T-205 per Non-goals). **Unblocks T-215** (adapter pool composition root instantiates one shared `SharedRateLimiter` from app-state Settings; passes to every BybitV5Adapter ctor in `bots`-table loop per H-022; reads Settings env vars `RATE_LIMIT_ORDERS_RATE/_CAPACITY/RATE_LIMIT_POSITIONS_RATE/_CAPACITY/RATE_LIMIT_IP_GLOBAL_RATE/_CAPACITY/RATE_LIMIT_PAUSE_MS` per §N9).
- [x] T-213c: PaperExchange reads + restart recovery (2026-04-29) — F2 twelfth numbered work task; **third F2 task with active math-validator gate** (Gate 4 ACTIVE per CLAUDE.md gate-4 conditional rule — staged diff touched `packages/exchange/paper/`; verdict VERIFIED with no new arithmetic per plan-doc §"Hand verification" — read-only DB queries + SQL-level COALESCE(SUM,0) + Decimal coercion at asyncpg boundary only). Final part of pre-emptive split T-213 → T-213a → T-213b → T-213c per L-007 + T-213b OQ-1 default A; closes the original T-213 work. T-213c deltas: `packages/exchange/paper/persistence.py` (extended +132 LOC over T-213b 358 baseline = 489 total — adds 4 SELECT helpers: `select_paper_positions_for_hydrate` (JOIN paper_positions × paper_trades × paper_executions(open exec) → returns hydrate-ready rows with `entry_fee` from open exec per OQ-1 default A), `select_paper_positions` (with optional symbol filter for `get_positions`), `select_paper_execution_price_by_order_id` (LIMIT 1 ORDER BY executed_at ASC per Decision #7 — defends against future synthetic-execution-per-order schema drift), `sum_paper_trades_realized_pnl` (COALESCE(SUM, 0) → Decimal("0") per Decision #8); read helpers naked of `@idempotent` markers per Decision #2 / W#4 stance A — read-only is degenerate-idempotency); `packages/exchange/paper/adapter.py` (extended +107 LOC over T-213b 865 baseline = 972 total — replaces 3 NotImplementedError stubs in `get_positions` / `get_fill_price` / `get_closed_pnl_cumulative` with full bodies; adds `_hydrate_active_positions` method + extends `start_consuming` to run hydrate FIRST, then NATS subscribe per OQ-6 default A; module-level `logger = logging.getLogger(__name__)` for INFO `paper_exchange.hydrate_complete` always + WARN `paper_exchange.hydrate_partial_tp_positions_require_set_trading_stop` gated by `partial_tp_count > 0` per CONCERN 2 fail-loud surface; `Position` lifted from TYPE_CHECKING to runtime import — used at runtime in get_positions list comprehension; `Decimal` lifted from TYPE_CHECKING to runtime in persistence.py — used at runtime in COALESCE NULL→0 coercion). **Constructor unchanged at T-213c** (T-213b 9-param canonical preserved per OQ-6 default A — hydrate runs in `start_consuming`, not in `__init__`). **`_active_positions` hydrated dict shape** (Decision #5 table — 11 fields): `trade_id` / `side` / **`qty = paper_positions.remaining_qty`** (Decision #5 / W#2 / edge case #5 — current open qty, NOT entry qty; unifies with mid-life mutation at adapter.py:628 where `_drain_partial_tp` shrinks `position["qty"] = new_qty` after partial fill — dict's `qty` is consistently "current open qty" across open/partial-TP/hydrate paths) / `entry_price` / `entry_fee` (OQ-1 default A — paper_executions.fee from JOIN on open exec; preserves `_compute_realized_pnl` math-correctness post-restart) / `fees_paid` (paper_trades.fees_paid accumulator preserved) / `sl_price` / `tp_price` / **`tp_size = None`** (OQ-2 default B) / **`tpsl_mode = "Full"`** (OQ-2 default B — operator must re-issue `set_trading_stop` after restart on partial-TP'd positions to restore Partial mode) / `tp_hit` (preserved). **OQ-2 default B documented gap** surfaced via WARN-level operator-facing log (`required_action: "re-issue set_trading_stop with tpsl_mode='Partial'..."`) emitted only when `partial_tp_count > 0` per CONCERN 2 fail-loud-but-not-noisy gating; W#3 negative-case test pin (`test_hydrate_does_not_warn_when_no_partial_tp_positions`) ensures clean-restart path stays silent. **H-013 three-layer mitigation** per plan-doc Hazards table: (a) Decision #5 dict's qty=remaining_qty preserves correct SL/TP fill-trigger qty post-restart on partial-TP'd positions; (b) WARN-level operator-facing log on hydrate; (c) load-bearing `_check_sl_tp_crosses` raise at adapter.py:237-240 stays as the fail-loud guard if `tpsl_mode='Partial'` reasserts without `tp_size`. H-018 N/A (no UPDATE on close path); H-022 N/A (paper has no credentials); H-024 paper_executions FK invariant preserved at read-side via JOIN. **3 obsolete stub-pin tests removed** in `test_adapter.py` per T-211 W#3 fail-loud forward-pointer contract — the test failure when stubs replaced is the contract working as intended (read-method bodies now covered by `test_paper_persistence.py` integration + `test_paper_emission.py` unit). **`_make_pool()` mock in `test_adapter_fill_semantics.py`** extended with `conn.fetch = AsyncMock(return_value=[])` so the existing `start_consuming` test continues to work after T-213c hydrate added to lifecycle. Plan: `docs/plans/T-213c.md` (2nd-pass plan-reviewer APPROVE 2026-04-29 — 1st REVISE 1 BLOCKER + 2 CONCERNS → 2nd APPROVE; BLOCKER fix reconciled Decision #5 dict-`qty`-source contradiction across table/Note/pseudocode to use `remaining_qty` uniformly; CONCERN 1 fix reframed edge case #5 prose from "semantic shift / bug surface / mitigation" to "consistency restoration after restart" citing adapter.py:628 mid-life mutation as the unifying invariant; CONCERN 2 fix added WARN-level log gated by `partial_tp_count > 0` with operator-actionable `required_action` text; W#1 LOC sub-figure doc fix; W#2 dict's qty=remaining_qty single-line load-bearing invariant; W#3 WARN gating + negative-case test; W#4 `pe.exec_type='open'` casing alignment verified pre-write; W#5 Position runtime import non-circular). **All 4 review gates passed**: plan-reviewer APPROVE (2nd pass) → drift-checker ON TRACK (5/5 W-items, 8/8 OQ defaults applied, +42 LOC inflation classified as docstring/None-handling overhead not scope creep) → brief-reviewer SHIP → math-validator VERIFIED (no new arithmetic; SQL-level aggregation; Decimal coercion at asyncpg boundary only; entry_fee/fees_paid/remaining_qty fixtures hand-computable consistent with T-213b §E.1/§E.2 — `entry_fee = 0.5*65000*0.0006 = 19.50000000`, `fees_paid = 19.5+0.1*65500*0.0006 = 23.43`, `remaining_qty = 0.5-0.1 = 0.4`). **Operator-approved 8 OQ defaults**: OQ-1 default A (entry_fee from paper_executions JOIN), OQ-2 default B (tpsl_mode='Full' + tp_size=None at hydrate; documented gap with WARN-level operator-facing log), OQ-3 default A (empty list when no rows; consistent with Bybit V5 `/v5/position/list`), OQ-4 default A (Position.size = remaining_qty), OQ-5 default A (exact str equality on sub_account; ValueError on mismatch with clear message), OQ-6 default A (hydrate at start_consuming entry, before NATS subscribe; no constructor surface change), OQ-7 default A (failure propagates; symmetry-of-life with T-202/T-203 DB-unavailable hard-fail at startup), OQ-8 default A (mixed integration env-gated + unit mock pool — mirror T-213b precedent). **ci-full GREEN on branch HEAD `12dba53`** (run 25116683566; ci-fast also GREEN run 25116683471). 212 LOC src NET (+107 adapter + +132 persistence + 27 deletions = 212 net under §0.3 cap of 400; comfortably under L-006 watch threshold of 280; ~+25% above plan estimate ~170, all classified as reasonable docstring/None-handling overhead not scope creep — drift-checker confirmed). 626 LOC test (cap-exempt; 17 new env-gated integration in test_paper_persistence.py — 10 SELECT helper + 7 hydrate; 7 new unit in test_paper_emission.py — 3 read-method shape + 4 ValueError/None pass-through; 3 obsolete stub-pin tests removed; mock pool extension). Repo-wide pytest 692 passed + 71 skipped (was 688 + 54 baseline; +7 new pass + 17 new env-gated paper integration). Pre-commit hooks (ruff format, ruff check, mypy strict, bandit) all passed first run. **Calibration win continued** (T-204 → T-210 → T-214 → T-211 → T-206 → T-212 → T-213a → T-213b → T-213c trajectory): 12 verbatim Decisions in plan-doc + 8 genuine OQs (all "use defaults" — operator's calibrated trust pattern at peak). **L-007 pre-emptive split closed cleanly**: original T-213 estimated ~410 LOC src would have triggered mid-write split per §0.3; pre-emptive split into T-213a (271 LOC) + T-213b (288 LOC, mid-write surface-expansion guard active) + T-213c (212 LOC) totalled 771 LOC across 3 capped chunks vs 1 over-cap monolith. Operationally cheaper per L-007 invariant. **Unblocks T-216 / T-217 / T-220 / T-221** (read patterns + post-restart hydrate consumed by placement pipeline + position lifecycle FSM + P&L audit + post-restart reconciliation respectively).
- [x] T-213a: PaperExchange fill semantics + slippage (2026-04-27) — F2 tenth numbered work task; **first F2 task with active math-validator gate** (Gate 4 ACTIVE per CLAUDE.md gate-4 conditional rule — staged diff touched `packages/exchange/paper/` financial math). **Pre-emptive split T-213 → T-213a + T-213b** per L-007 + OQ-1 default A — full-T-213 estimated ~410 LOC src EXCEEDED §0.3 cap of 400; split into T-213a (slippage + fee + market-order compute + SL/TP cross detection — pure computation; NO DB writes; NO event emission) + T-213b (paper_* persistence + execution emission + read-method bodies). T-213a packages: `packages/exchange/paper/slippage.py` (3 pure-functional kwarg-only models per §12.1 line 1937 — `fixed_pct`/`proportional_to_qty`/`half_spread`; 63 LOC; `Decimal("2")` in `half_spread` is §N9-exempt mathematical constant — `half = range / 2` by definition, comment in module docstring per W#2), `packages/exchange/paper/fees.py` (`compute_fee(qty, fill_price, fee_rate)` returning Decimal NUMERIC(20,8) shape per paper_executions schema T-212; 31 LOC), `packages/exchange/paper/adapter.py` (extended +177 LOC over T-211 168 baseline = 345 total). Constructor extended to 7 params: 3 T-211 baseline (seed_balance, slippage_model, fee_rate) + 4 new (`bot_id: BotId`, `bus: NatsClient`, `slippage_params: dict[str, Decimal]`, `now_fn: Callable[[], datetime] = now_utc`) per Decision #11/#13 plan-doc. **`_REQUIRED_KEYS_PER_MODEL: dict[str, frozenset[str]]` defensive validation** per Decision #11 (BLOCKER 2 fix) — defends against pydantic-bypass at T-215 surfacing as opaque KeyError at first market-order placement; raises ValueError with sorted-keys message at construction. **`PendingSLTPFill` frozen+slots dataclass** per Decision #12 — T-213a populates via cross detection; T-213b drains queue → writes paper_executions + emits ExecutionEvent. **H-013 tpsl_mode propagation chain** preserved end-to-end per Decision #14: protocol surface (T-201 W#2) → `set_trading_stop` 3rd positional NO default (T-211 + T-213a) → `_active_positions[symbol]["tpsl_mode"]` runtime state → `PendingSLTPFill.tpsl_mode` per-fill event; KeyError if missing (NOT `.get()` with `'Full'` default fallback — invariant pinned via `test_check_sl_tp_crosses_propagates_tpsl_mode_from_set_trading_stop`). **Pessimistic SL-first per Q4-A F2 simplification** in `_check_sl_tp_crosses` (Decision #15 + W#5): if SL+TP both cross same OHLC-1m candle, SL fills first (worst-case for paper P&L; F5 backtest harness §12.2 will replay intra-candle path); deterministic via `if sl_crosses: ... return` ordering; no precision-edge ambiguity (Decimal `low ≤ price ≤ high` integer-friendly). **`start_consuming()` subscribes to `market.ohlc.1m.>` wildcard** per Decision #16 + Q4-A — F2 uses OHLC-1m as tick proxy; intra-candle deferred to F5. **`place_market_order` partial body** computes `fill_price = last_close ± slippage` and `fee = abs(qty) * fill_price * fee_rate` then raises NotImplementedError carrying computed values + `"T-213b"` substring (T-213b owns paper_orders/paper_executions persistence + execution emission). **`set_trading_stop` partial body** stores sl/tp/tpsl_mode/tp_size in `_active_positions` dict then raises NotImplementedError + `"T-213b"` substring. T-213a full-stub methods (T-211 unchanged): set_leverage / cancel_order / get_positions / get_fill_price / get_closed_pnl_cumulative + stream_executions / stream_positions — each retains `_stub_message("T-213")` substring per Decision #9. **OQ-2 split applied to T-211 tests**: `_STUBBED_ASYNC_METHODS` parametrize tuple removes place_market_order + set_trading_stop rows (now T-213a-owned with dedicated tests in test_adapter_fill_semantics.py asserting `"T-213b"` substring); remaining 5 async + 2 streams stay on `"T-213"` substring assertion (T-211 unchanged for those). **Hand verification §A-D in plan-doc cross-checks tests with exact Decimal equality** (math-validator VERIFIED): §A.1 fixed_pct 65000*0.0005=32.5, §A.2 proportional 65000*0.0001*0.5=3.25, §A.3 half_spread (65100-64900)/2*1.0=100, §B fee 0.5*65032.5*0.0006=19.50975, §C.1 buy fill 65000+32.5=65032.5, §C.2 sell fill 65000-32.5=64967.5, §D.1-4 SL/TP cross detection with concrete fee values 19.350000/19.650000/11.790000 per BLOCKER 5 fix. Plan: `docs/plans/T-213.md` (3-pass plan-reviewer APPROVE 2026-04-27 — 1st REVISE 5 BLOCKERS+3 CONCERNS, 2nd REVISE 2 BLOCKERS numbering+branch-name, 3rd APPROVE 6 Write-time guidance items all addressed: W#1 §N4 TDD tests-first, W#2 §N9-exempt Decimal("2") comment, W#3 H-013 tpsl_mode propagation, W#4 _REQUIRED_KEYS_PER_MODEL allow-list, W#5 surface-expansion guard 280 LOC watch, W#6 hand-verification §A-D cross-check). **All 4 review gates passed**: plan-reviewer APPROVE → drift-checker ON TRACK (one CONCERN BotId NewType — fixed before brief-reviewer) → brief-reviewer SHIP (one non-blocking CONCERN about asserts in non-test code — refactored to predicate-fold + ValueError raise before math-validator) → math-validator VERIFIED (Hand verification §A-D 18 hand-computed values cross-checked vs test fixtures, exact Decimal equality, no library round-trip, no implementation-against-itself, abs() coverage on negative qty for fee+slippage, edge cases zero-qty/zero-rate/zero-pct/zero-range/no-observed-price). **ci-full GREEN on branch HEAD `e899a94`** (run 25015376531). **Calibration win continued** (T-204 → T-210 → T-214 → T-211 → T-206 → T-212 → T-213a trajectory): 18 verbatim Decisions in plan-doc + 3 genuine OQs (OQ-1 split default A, OQ-2 T-211 tests narrowing default A, OQ-3 SL/TP fills at exact trigger price no slippage default A — pessimistic-equal honest about modeling limit). **Pre-emptive split L-007 example codified**: full-T-213 estimated ~410 LOC src would have triggered mid-write split per §0.3 — operator's pre-emptive split flag at planning time avoided the operationally-expensive mid-write rework. **Surface-expansion guard held**: 271 LOC src NET (slippage 63 + fees 31 + adapter +177 over T-211 baseline = 271) under §0.3 cap of 400 + under L-006 watch threshold of 280; W#5 codified guard active. Pre-commit hook caught trailing-whitespace auto-fix on plan-doc once before re-commit succeeded; mypy strict + ruff (incl. RUF100 unused noqa removal + RUF059 unused unpacking + TC003 TYPE_CHECKING moves) + bandit all clean. 271 LOC src + 826 LOC tests (cap-exempt; 11 slippage + 7 fees + 16 fill semantics; T-211 test_adapter.py refactored for new ctor + OQ-2 split). Repo-wide pytest 680 passed + 36 skipped (no regressions; +34 new T-213a tests). **Unblocks T-213b** (PaperExchange persistence + execution emission body — paper_orders/paper_trades/paper_executions/paper_positions DB writes via T-212 schema after T-213a fill computation, execution emission via WS-like async iterator shape-identical to T-209 BybitV5Adapter so T-218 dispatcher cannot distinguish paper from live, read-method bodies replacing T-211 stubs, place_market_order + set_trading_stop full bodies replacing T-213a partial stubs).
- [x] T-212: Migration 0008 — paper_* mirror tables (2026-04-27) — F2 ninth numbered work task. 4-table paper-mirror ledger per T-200 plan-doc canonical (OQ-1 default B): `paper_orders` (18 cols, regular, FK bot_id → bots, `exchange='paper'` discriminator column mirror live `orders.exchange`) + `paper_trades` (21 cols, regular, FK bot_id → bots + open_order_id → paper_orders.id NOT NULL, close_order_id plain BIGINT no-FK per T-202 W#4, mfe_pct/mae_pct/confidence_score sa.Double() per L-005) + `paper_executions` (12 cols, **HYPERTABLE** on executed_at 7-day chunks, composite PK (executed_at, id), FK order_id → paper_orders.id NOT NULL + trade_id → paper_trades.id nullable, bot_id plain TEXT NOT NULL no-FK per T-202 W#2, UNIQUE (exchange_exec_id, executed_at) + INDEX (trade_id), **NO retention NO compression** policies per §18.3 mirror live forever) + `paper_positions` (17 cols, regular, composite PK (bot_id, symbol) mirror live position_state, FK trade_id → paper_trades.id NOT NULL, bot_id plain TEXT no-FK per T-203 W#4, server defaults tp_hit/trailing_active false running_pnl 0). Mirror live shape per §12.1 line 1932 verbatim. **Third F2 codified brief inconsistency** (after T-201 Q11 tpsl_mode default + T-201 OQ-1 stream sig async-def) — pattern of surfacing-each-instance per T-201 W#8 audit-trail invariant continues. **Operator-locked rationale verbatim**: *"T-200 plan-doc enumerated 4 paper_* tables (paper_trades included for live-paper symmetry per §3.1 line 268). Brief §12.1 list of 3 tables omits paper_trades — drafting artifact; symmetry is the load-bearing invariant. T-200 commit is canonical."* T-218/T-219/T-220/T-221 plan-docs inherit 4-table assumption; downstream task plan-docs do NOT need to handle "paper has no trades abstraction" branch. 6-edge FK chain (2 to bots + 4 paper-internal): bots ← paper_orders.bot_id, bots ← paper_trades.bot_id, paper_orders.id ← paper_trades.open_order_id NOT NULL, paper_orders.id ← paper_executions.order_id NOT NULL, paper_trades.id ← paper_executions.trade_id nullable, paper_trades.id ← paper_positions.trade_id NOT NULL. **NO FKs from paper_* to live tables** per §12.1 + TASKS.md line 81 verbatim — cross-cutting anti-test asserts via `_LIVE_TABLE_NAMES = frozenset({"orders", "trades", "executions", "position_state"})` filter on `referential_constraints`; pins the §12.1 invariant at schema layer (mirror T-204 OQ-2 anti-FK pattern at scale). 3 mirror-live no-FK assertions via direct `pg_constraint` query: `paper_trades.close_order_id` (T-202 W#4), `paper_executions.bot_id` (T-202 W#2), `paper_positions.bot_id` (T-203 W#4). NUMERIC precision split per §5.13: NUMERIC(30,12) qty/prices, NUMERIC(20,8) `paper_executions.fee`, NUMERIC(20,4) USD-denominated/PnL. **§N1 invariant**: NO `server_default` on any TIMESTAMPTZ column (paper_orders.requested_at/placed_at/filled_at/closed_at, paper_trades.opened_at/closed_at, paper_executions.executed_at, paper_positions.updated_at) — application sets via `packages.core.now_utc()` (mirror T-202/T-203 precedent). **`exchange='paper'` discriminator column ONLY on paper_orders** per OQ-3 default A (mirror live `orders.exchange`); other paper_* tables don't have `exchange` column because their live counterparts (live trades / executions / position_state) don't either — discriminator structural via table name. **Paper retention mirror live forever** per OQ-2 default A — combined positive (no-retention) + negative (no-compression) anti-test `test_paper_executions_hypertable_no_retention_no_compression` per T-204 W#3 grep-friendly naming pattern (mirror T-202 NEGATION pattern). **Surface-expansion guard mid-write incident DOCUMENTED**: implementation initially reached **292 LOC src** (over operator's 280 stop threshold). Per T-201 W#7 + L-006 control + plan-doc operator-flagged surface-expansion guard, **STOPPED + re-checked docstring-vs-code split** (functional code 189 LOC, docstring 90 LOC, blank 13 LOC). Compressed module docstring (OQ-1 inconsistency note delegated to plan-doc reference; downgrade comment block tightened) → final src **271 LOC** under 280 watch threshold + under §0.3 cap of 400. **Zero functional code change; only documentation compression.** Operator-flagged guard worked as intended — documents the exact STOP-and-recheck behavior the guard was designed to enforce; pattern stable for future migrations near L-006 watch zone (likely T-217 PositionLifecycle FSM). **T-113 cagg watch CLOSED CLEAN** per operator standing instruction at T-212 ship — 9 master ci-full exposures green since T-113 fix landed (T-202 → T-203 → T-204 → T-210 → T-214 → T-211 → T-206 → ...); ~98% confidence reached per T-113 close-out policy; **no T-114 needed**; pattern stable; future migration tasks inherit clean baseline. **Calibration win continued** (T-204 → T-210 → T-214 → T-211 → T-206 → T-212 trajectory): 15 verbatim Decisions + 4 genuine OQs surfaced. OQ-1 default B (4 tables T-200 canonical, brief inconsistency note operator-locked), OQ-2 default A (mirror live forever retention), OQ-3 default A (exchange column only on paper_orders), OQ-4 default A (3 mirror-live no-FK + 1 cross-cutting no-FK-to-live). Plan: `docs/plans/T-212.md` (single-pass plan-reviewer APPROVE 2026-04-27 with 5 Write-time guidance items, all addressed; W#1 sa.Double() not sa.Float() on DOUBLE PRECISION columns, W#2 NO server_default on 8 TIMESTAMPTZ columns per §N1 with column_default IS NULL test assertions, W#3 anti-FK via direct pg_constraint query NOT information_schema + cross-cutting no-FK-to-live via referential_constraints filter, W#4 NO add_retention_policy NO add_compression_policy in upgrade() with combined anti-test + grep-friendly naming, W#5 surface-expansion guard codified — STOP at 280 mid-write, no extra columns/indexes/helpers beyond live mirror, no exchange column on paper_trades/paper_executions/paper_positions). 4 review gates: plan-reviewer APPROVE → drift-checker ON TRACK → brief-reviewer SHIP → math-validator VERIFIED out-of-scope (pure DDL + schema integration test, zero financial math, Decimal annotations + string-literal construction only). **ci-full GREEN on branch HEAD `0860272`** (run 25012983130). 271 LOC migration src (under §0.3 cap of 400; under operator's 280 mid-write watch threshold after compression; functional code 189 LOC pure DDL) + 549 LOC test (cap-exempt; 4 column-shape verbatim tuples + 6 FK edge enumeration + 3 anti-FK pg_constraint + cross-cutting no-FK-to-live + 4 server-default tables verification + 6 indexes subset assertion per T-204 W#1 + hypertable check + 3 not-hypertable anti-checks + smoke E2E across 4-table chain + JSONB codec + UUID-suffixed bot_id + alembic_version + dedicated retention/compression anti-test). Repo-wide pytest 647 passed + 36 skipped (no regressions; +2 new env-gated tests for 0008). Pre-commit hook caught trailing-whitespace auto-fix on plan-doc once before re-commit succeeded. **Unblocks T-213** (PaperExchange fill semantics body — wires market-fill at last observed tick + slippage per slippage_model, SL/TP per-tick monitor per T-200 Q4-A OHLC-1m, fee deduction at fill time per fee_rate, paper_orders/paper_trades/paper_executions/paper_positions DB writes via these 4 tables, execution emission via WS-like async iterator shape-identical to T-209 BybitV5Adapter so T-218 dispatcher cannot distinguish paper from live per §3.1 line 268 invariant — the very symmetry that justified OQ-1 default B).
- [x] T-206: Adapter contract test framework (2026-04-27) — F2 eighth numbered work task. `packages/exchange/tests/test_protocol_conformance.py` parametrised pytest contract test cez `_ADAPTERS_UNDER_TEST: list[type] = [PaperExchange]` registry per §11.6 verbatim ("contract: adapter protocol conformance test — every adapter implements every method with correct idempotency marker"). T-206 IS the **first real reader** of T-201 `_UNLABELED_METHODS` frozenset per the operator-locked rationale block in `packages/exchange/protocols.py` codified at T-201 W#1: *"T-206 conformance test (later F2 task) will read this set to assert: every Protocol method NOT in this set carries either @idempotent or @non_idempotent."* 5 test functions: `test_adapter_implements_every_protocol_method` (every Protocol method present on adapter) + `test_adapter_methods_carry_correct_idempotency_markers` (§N3 markers correct on non-exempt methods) + `test_adapter_unlabeled_methods_carry_no_markers` (negative: streams + close MUST NOT carry markers — catches accidental @idempotent decoration on stream_*) + `test_adapter_set_trading_stop_tpsl_mode_has_no_default_and_is_third_positional` (H-013 binding pinned at adapter-pool contract layer per OQ-2 default A) + `test_unlabeled_methods_set_is_complete` (sanity: ExchangeClient §11.1 surface == 10 methods + `_UNLABELED_METHODS` == 3 entries — pins counts against future Python upstream Protocol additions and forces deliberate update path on future exemption additions). 4 parametrised over `[PaperExchange]` + 1 standalone sanity = 5 test invocations. **Distinct-from-T-211 boundary** explicit in plan-doc + module docstring: T-211 = PaperExchange-bound behaviour tests; T-206 = adapter-class-agnostic conformance contract; no scope confusion. **H-013 contract-layer binding** (capital-loss-preventing invariant): mypy strict + Protocol structural typing catches `tpsl_mode` default drift at lint time, but contract test must hold even if mypy bypassed (e.g., future adapter `# type: ignore` slipping past review). 4-step assertion expansion per W#3: `params[0]==self`, `params[1]==symbol`, `params[2]==tpsl_mode`, `default is inspect.Parameter.empty` — pins full T-201 W#2 contract at adapter-pool layer adapter-pool-wide. **H-024 `ExecutionEvent` shape correctly skipped** per OQ-3 default B: type-level invariant pinned at T-201 `test_execution_event_does_not_carry_exec_type`; no per-adapter variation; T-206 is adapter-conformance not type-shape. **Signature parity beyond markers correctly skipped** per OQ-4 default B: mypy strict + Protocol structural typing IS the parity check. **Forward-extensibility**: T-207 BybitV5Adapter appends to `_ADAPTERS_UNDER_TEST` via 1-line edit at ship time; framework loops existing assertions over the new adapter without per-adapter test duplication. Plan: `docs/plans/T-206.md` (single-pass plan-reviewer APPROVE 2026-04-27 with 5 Write-time guidance items, all addressed; W#1 `getattr(adapter_cls, name)` preserves adapter qualname NOT Protocol's per `packages/core/markers.py` keying f"{module}.{qualname}" — do NOT simplify by walking ExchangeClient.method directly which would test Protocol's own decoration not adapter's, W#2 `assert len(_protocol_method_names()) == 10` pins ExchangeClient §11.1 surface against future Python upstream Protocol additions that might silently expand surface and pass contract test vacuously, W#3 4-step assertion expansion `self`/`symbol`/`tpsl_mode-third`/`no-default` per T-201 W#2 contract, W#4 `assert len(_UNLABELED_METHODS) == 3` forces deliberate update path on future exemption additions, W#5 known-limitation "manual registration silently skips unregistered adapters" callout in module docstring per §Edge cases). Calibration win continued (T-204 → T-210 → T-214 → T-211 → T-206 trajectory): only 4 genuine OQs surfaced + 10 verbatim items in plan-doc Decisions section. OQ-1..OQ-4 all default A/B per plan: hardcoded module-level list (not pytest fixture / conftest entry-point / `__init_subclass__` auto-register — all rejected on indirection-without-payoff or §N6 grounds), H-013 contract-layer binding included, H-024 type-level skip, signature parity mypy-only. 4 review gates: plan-reviewer APPROVE → drift-checker ON TRACK → brief-reviewer SHIP → math-validator VERIFIED out-of-scope (pure introspection-based contract tests via `inspect.getmembers` / `inspect.signature` / `is_idempotent` / `is_non_idempotent` from `packages.core.markers`; structural/metadata checks only, no numeric operations, no Decimal arithmetic, no financial math). **ci-full GREEN on branch HEAD `325af2f`** (run 25011835708). **T-113 cagg regression watch — 9th master ci-full exposure since T-113 fix landed: GREEN, no `LockNotAvailableError`** — ~98% confidence reached per T-113 close-out policy; monitoring window can close. 174 LOC test file (cap-exempt; tests excluded from §0.3 cap; +34% over T-200 estimate ~100 LOC due to per-test docstrings + W#3 4-step assertion + W#5 known-limitation callout — all justified per Write-time guidance). Repo-wide pytest 647 passed + 34 skipped (no regressions; +5 new T-206 tests). Single `# type: ignore[attr-defined]` at line 126 — `adapter_cls.set_trading_stop` access on generic `type` parameter cannot narrow via `pytest.parametrize` over `[type]`; live mypy code per `feedback_type_ignore_context` memory rule (NOT `unused-ignore`). Pre-commit hook caught initial mypy strict miss + ruff format reflow before commit succeeded. **Unblocks T-207** (BybitV5Adapter REST client + retry matrix; will append `BybitV5Adapter` to `_ADAPTERS_UNDER_TEST` at ship time so contract framework picks it up automatically). **E5 hazard-catalog progress**: T-206 contract test contributes the marker-discipline portion of "all hazard-catalog tests pass" (§19 line 2531); behavioural hazard tests (H-003 retry semantics at T-207, H-009 dedup at T-218 H-013 behavioural at T-208, H-024 dispatcher at T-218) bind at their owner tasks per T-200 mapping.
- [x] T-211: PaperExchange skeleton implementing ExchangeClient (2026-04-27) — F2 seventh numbered work task; first task in F2 wiring-and-business-logic phase since all top-of-DAG tasks shipped. `packages/exchange/paper/adapter.py` `PaperExchange` class implementing T-201 ExchangeClient Protocol per §12.1 + §9.5 line 1566 verbatim. 3-param constructor (`seed_balance: Decimal`, `slippage_model: SlippageModel`, `fee_rate: Decimal`); `SlippageModel = Literal["fixed_pct", "proportional_to_qty", "half_spread"]` per §12.1 line 1937 verbatim; runtime allow-list `_SLIPPAGE_MODELS: frozenset[str]` rejects typos with sorted-list error message per L-002 / T-210 W#2 precedent (defensive at construction — pydantic Literal validation at YAML load time + runtime guard at adapter pool composition is the two-layer defence). 10 ExchangeClient methods present with matching idempotency markers per T-201 `_UNLABELED_METHODS` frozenset (4 writes `set_leverage`/`place_market_order`@non_idempotent/`set_trading_stop`/`cancel_order` + 3 reads `get_positions`/`get_fill_price`/`get_closed_pnl_cumulative` all decorated; 3 unlabeled `stream_executions`/`stream_positions`/`close` exempt). `set_trading_stop` signature mirrors T-201 verbatim with `tpsl_mode: Literal["Full", "Partial"]` 3rd positional NO default per H-013 / Q11 invariant (paper-side preserves the protocol-level tpsl_mode no-default contract). Method bodies raise `NotImplementedError` with `_stub_message(method)` helper producing forward-pointer message containing `"T-213"` literal substring — fail-loud contract: if T-213 plan-doc author replaces a stub without removing/updating the corresponding `test_*_stub_raises*` (which asserts `"T-213" in str(info.value)`), the assertion breaks loudly. `close()` is the only non-stub method at T-211 — no-op (lifecycle methods must work without business logic; T-213 may extend with paper-state flush). `stream_executions` / `stream_positions` are `def`-not-`async-def` per T-201 OQ-1 verbatim, raising synchronously on call (NOT returning empty async iterator that swallows error on first `__anext__`). **Owner-task correction surfaced + audit trail intentional**: operator brief in this exchange referred to "T-218 PaperExchange dispatcher" + "T-219 PaperExchange persistence". Per TASKS.md line 82, the actual sole owner of PaperExchange-side fill semantics + paper_* persistence + execution emission is **T-213** (single unified task); T-218 (execution-service dispatcher) and T-219 (cumulative-delta close flow) are execution-service-side consumers of the ExchangeClient interface, NOT internal-PaperExchange wirers. Plan-doc §Downstream owner-task correction + module docstring + `_stub_message` helper + commit body all cite T-213 as the canonical downstream throughout — the diplomatic correction is documented intentional, not silent reframe. **Calibration win continued** (T-204 → T-210 → T-214 → T-211 trajectory): only 5 genuine OQs surfaced + 15 verbatim items in plan-doc Decisions section. OQ-1..OQ-5 all default A: NotImplementedError everywhere (loud fail catches premature use; sentinels rejected because they would mask downstream-skeleton bugs), 3-param brief minimum constructor (mirror T-210 DedupingConsumer skeleton-shipped-DI-minimum precedent; T-213 plan-doc adds `bot_id` + DB pool + NATS bus + KV reader at first import time per §0.8), subpackage layout `packages/exchange/paper/` per TASKS.md line 80 verbatim (anticipates T-213 growth `fills.py`/`persistence.py` siblings), Literal type + runtime allow-list (defensive two-layer; T-215 bug bypassing pydantic surfaces at PaperExchange construction with helpful error message), full ~14-16 test scope (constructor + protocol satisfaction + per-method NotImplementedError pinning T-213 forward-pointer + close no-op + public re-export — mirror T-210 `test_subclass_must_override_process` precedent). **Operator-locked surface-expansion guard observed cleanly**: 168 src LOC under plan target 198 and well under L-006 watch threshold of 200 + operator's 280-mid-write watch with 80-LOC buffer codified — no extra constructor params beyond brief 3 (no `bot_id`, no DB pool, no NATS bus, no KV reader, no tick handle), no Settings fields anywhere, no stubs beyond the 10-method ExchangeClient Protocol surface, no anticipatory helpers beyond `_stub_message`. Plan: `docs/plans/T-211.md` (single-pass plan-reviewer APPROVE 2026-04-27 with 5 Write-time guidance items, all addressed; W#1 `from typing import Literal` at runtime NOT under TYPE_CHECKING — `SlippageModel` Literal alias is used in runtime `__init__` annotation `slippage_model: SlippageModel`, W#2 stream tests call methods directly inside `pytest.raises(NotImplementedError)` NOT wrapped in `async for x in client.stream_executions()` — pin the synchronous-raise contract not the iteration boundary, W#3 per-method NotImplementedError tests assert message contains literal `"T-213"` substring — fail-loud forward-pointer contract, W#4 surface-expansion guard 3 ctor params + 10 methods + 1 helper only — if src approaches 280 LOC mid-write STOP per L-006/T-201 W#7, W#5 NO `docs/modules/exchange.md` retrofit — defer to F2 close-out chore per T-201 OQ-10 precedent). 4 review gates: plan-reviewer APPROVE → drift-checker ON TRACK → brief-reviewer SHIP → math-validator VERIFIED out-of-scope (skeleton: zero financial math, zero Decimal arithmetic, zero slippage math, zero fee deduction; Decimal annotations only; library-side `packages/exchange/paper/` distinct from math-scope `services/execution/`). **ci-full GREEN on branch HEAD `b4b9150`** (run 25009844810). **T-113 cagg regression watch — 8th master ci-full exposure since T-113 fix landed: GREEN, no `LockNotAvailableError`** (~2 more runs to ~98% confidence per T-113 close-out policy). Pre-commit hooks (ruff format, ruff check, mypy strict, bandit) all passed; pre-commit caught RUF100 (unused `noqa: ARG002` — ARG002 not in ruff lint select list per pyproject.toml; stubs raise immediately so unused-arg never triggers regardless) + RET501 (explicit `return None` on `close()` typed `-> None`) + trim-trailing-whitespace auto-fixed plan-doc once before re-commit succeeded — all fixes lint-driven, no semantic deviation. 168 LOC src (well under §0.3 cap of 400; well under L-006 watch threshold of 200; 30-LOC under plan target 198 — surface-expansion guard held with margin) + 234 LOC tests (cap-exempt; 27 tests across 13 functions: 4 constructor — happy/typo/typo2/parametrize-3-models + 7 marker parametrize over labeled methods + 1 unlabeled methods + 1 protocol-surface introspection + 1 async-method-stub parametrize over 7 methods asserting message-contains-method-name + message-contains-T-213 + 2 stream-stub direct-call + 1 close-noop + 2 re-export). Repo-wide pytest 642 passed + 34 skipped (no regressions; +27 new T-211 tests). Unblocks **T-213** (PaperExchange fill semantics body — wires market-fill at last observed tick + slippage per slippage_model, SL/TP per-tick monitor per T-200 Q4-A OHLC-1m intra-candle path deferred to F5 backtest harness §12.2, fee deduction at fill time per fee_rate, paper_orders/paper_trades/paper_executions/paper_positions DB writes after T-212 migration 0008 lands, execution emission via WS-like async iterator shape-identical to T-209 BybitV5Adapter so T-218 dispatcher cannot distinguish paper from live).
- [x] T-214: execution-service skeleton (2026-04-27) — F2 sixth numbered work task — **LAST top-of-DAG**. After T-214 every remaining F2 numbered task has a non-empty `Blocked by:` list. `services/execution/` FastAPI service skeleton mirror T-109 feature-engine skeleton verbatim modulo identity (service_name="execution-service" per §3.1, UID/GID 10004 sequential after 10001/10002/10003) + 3-step lifespan (pool → bus → state attach + reverse shutdown — no JSONB codec, no pipeline composition; lifespan reduces from T-109's 13-step T-110d state to skeleton minimum). Pure scaffolding: NO adapter pool composition, NO order pipeline, NO FSM, NO reconciliation, NO audit loop. Each lands in F2 owner task per §0.8: T-205 rate limiter, T-215 adapter pool composition root (`bots`-table read + per-bot env keys per H-022 + `app.state.adapters: dict[BotId, ExchangeClient]` attach), T-216 order placement pipeline, T-217 PositionLifecycle FSM, T-218 dispatcher (`DedupingConsumer[ExecutionEvent]` from T-210), T-219 cumulative-delta close, T-220 P&L audit (APScheduler), T-221 post-restart reconciliation. /ready ships `bus`+`db` reasons only; T-209 will add a third `exchange_ws` reason key when the private Bybit WS connection lands (private-WS readiness is a load-bearing health signal because order-event dispatch T-218 depends on it). Settings minimum scope: 5 fields (`service_name`, `log_level`, `http_port`, `nats_url`, `database_url`); F2-specific fields explicitly deferred per §0.8 to owner tasks (`BOT_<ID>_BYBIT_API_KEY/SECRET` → T-215, `POSITION_POLL_INTERVAL` → T-217, `RECONCILE_SLEEP_SECONDS` → T-219, `AUDIT_LOOP_INTERVAL_SECONDS` → T-220). DI providers (`get_pool`, `get_bus`, `get_settings`, `get_logger_dep`) mirror T-109 verbatim; execution-specific providers (`get_adapter_pool`, dispatcher, scheduler) defer to T-215+. Workspace deps minimum: `scalper-v2-bus`, `scalper-v2-core`, `scalper-v2-db`, `scalper-v2-observability` (no `scalper-v2-exchange` per OQ-1 default A — mirror T-110d-pattern of "first-import-adds-dep", T-215 adds at first import). **Dockerfile**: two-stage uv install, `python:3.12.13-slim-trixie` + `ghcr.io/astral-sh/uv:0.11.7` (digest-pin deferral per §0.8), non-root UID/GID 10004, port 8000, `--loop uvloop` per §3.1, CMD `services.execution.app.main:create_app --factory`. **release.yml matrix update + retroactive backfill**: T-214 commit body documents fix-forward verbatim — *"release.yml matrix updated to include execution-service + retroactive backfill of feature_engine + market_data per release.yml comment intent — T-100/T-109 omissions"*. Matrix expanded from `[signal_gateway]` to `[signal_gateway, market_data, feature_engine, execution]`; release.yml comment block rewritten to document the auditable fix-forward (T-100 + T-109 missed updating the matrix when their respective Dockerfiles shipped; T-214 corrects this since matrix-update was already in scope per the comment intent "each F1 service-ship task appends its name in the same PR that lands its Dockerfile"). compose.yaml + prometheus.yml standard service-block additions. **Calibration win continued** (T-204 → T-210 → T-214 trajectory): only 4 genuine OQs surfaced + 18 verbatim items in plan-doc Decisions section. OQ-1..OQ-4 all default A: scalper-v2-exchange dep deferred to T-215 first-import (mirror T-110d pattern over T-109 skeleton), no `app.state.adapters` placeholder per §0.8, release.yml backfill all three F1 services per fix-forward, service_name="execution-service" per §3.1 verbatim. Plan: `docs/plans/T-214.md` (single-pass plan-reviewer APPROVE 2026-04-27 with 6 Write-time guidance items, all addressed; W#1 health.py docstring rewritten to reference T-209 future `exchange_ws` reason key NOT feature-engine "no analogous WS" wording, W#2 main.py is 3-step skeleton with NO `_register_jsonb_codec` helper, W#3 deps.py docstring references T-215+ as owner of execution-specific providers NOT T-110, W#4 config.py minimum 5 fields with NO `execution_symbols` analogue, W#5 src LOC 320 under 380 mid-write watch and §0.3 cap of 400, W#6 verbatim OQ-3 fix-forward sentence in commit body — enforced at SHIP gate). 4 review gates: plan-reviewer APPROVE → drift-checker ON TRACK → brief-reviewer SHIP → math-validator VERIFIED out-of-scope (pure infra + service skeleton, only numeric literals are operational `_POOL_ACQUIRE_TIMEOUT_SECONDS=1.0` + `http_port=8000` + UID/GID 10004; no financial math, no Decimal arithmetic). **ci-full GREEN on branch HEAD `9031e90`** (run 25008865158). **T-113 cagg regression watch — 7th master ci-full exposure since T-113 fix landed: GREEN, no `LockNotAvailableError`** (monitoring window past halfway; ~3 more runs to ~98% confidence per T-113 close-out policy). Pre-commit hooks (ruff format, ruff check, mypy strict, bandit) all passed; trim-trailing-whitespace auto-fixed plan-doc once before re-commit succeeded. 320 LOC src (config 54 + deps 62 + health 78 + main 125 + pyproject 41 — well under §0.3 cap of 400, well under 380 mid-write watch, T-109 precedent at 323 confirms normal skeleton size) + 353 LOC tests (cap-exempt; 19 tests: 9 config + 1 health + 5 ready + 5 app_factory) + 188 LOC infra (Dockerfile 84 + compose +50 + prometheus +10 + release.yml +1 line + comment rewrite). Repo-wide pytest 615 passed + 34 skipped (no regressions; +19 new T-214 tests). uv.lock regenerated for `scalper-v2-execution` workspace member promotion (5-line stub → full hatch scaffold). **Unblocks T-205, T-215, T-216, T-217, T-218, T-219, T-220, T-221** — every remaining F2 numbered task now has a non-empty Blocked by: list; F2 work transitions from skeleton-and-foundation phase to wiring-and-business-logic phase. Operator picks T-205 (shared rate limiter, ADR-Q-A prerequisite) or T-215 (adapter pool composition root) as next; both ready to start.
- [x] T-210: DedupingConsumer base class to packages.bus (2026-04-27) — F2 fifth numbered work task. §20 H-009 + §9.5 line 1591 — generic in-process size-bounded FIFO ring keyed by configurable extractor. `packages/bus/dedup.py`: `DedupingConsumer[T]` PEP 695 generic base class with `consume(message)` template method + `_process(message)` subclass override. Subclass binds `key_fn: Callable[[T], str]` at construction; capacity defaults to 10000 per H-009 + §9.5 line 1591 verbatim ("ring buffer, size 10k"); raises `ValueError` if `capacity <= 0`. **Distinct mechanism, NOT JetStream `Nats-Msg-Id`/`duplicate_window`** (publisher-side, server-managed) and **NOT signal-gateway `DedupRing`** (TTL-based vs T-210 size-bounded). Two layers can coexist; address different replay sources. Module docstring contrasts both explicitly. **Locking invariant load-bearing** (W#1): `asyncio.Lock` protects ring (keys-seen tracking), NOT handler — `_process` runs OUTSIDE the locked section so a slow handler does not block ring updates. Subclasses must be re-entrancy-safe at the `_process` level if concurrent handler invocation matters across distinct keys. Eviction: `OrderedDict.popitem(last=False)` FIFO when `len > capacity` (W#3 `>` not `>=`; ring stays at exactly `capacity` entries in stable state, verified by `test_ring_stays_at_capacity_under_overflow`). DEBUG `dedup_dropped` structured log emitted on each drop (§N2; OQ-2 default A — every-drop logging at low frequency in normal ops gives operator incident-triage visibility); Prometheus metrics deferred to T-218 per §0.8 anti-hypothetical (OQ-3 default A — base class doesn't pre-design label space). Default logger via `structlog.stdlib.get_logger(__name__)` (factory not singleton, §N6 compliant; load-bearing comment in source documents this). **Calibration win continued** (T-204 trajectory): only 5 genuine OQs surfaced + 10 verbatim items in plan-doc Decisions section. OQ-1..OQ-5 all default A: template-method pattern, every-drop DEBUG log, defer Prometheus, include Hypothesis property test mirror T-015b1 `_ReferenceRing` oracle pattern, `asyncio.Lock` per instance. **PEP 695 syntax `class DedupingConsumer[T]:`** used per ruff UP046 enforcement; functionally equivalent to `Generic[T]+TypeVar` (invariant by default — satisfies W#6 invariance requirement). Plan: `docs/plans/T-210.md` (single-pass plan-reviewer APPROVE 2026-04-27 with 7 Write-time guidance items, all addressed; W#1 lock release before `_process`, W#2 capacity<=0 raises ValueError in `__init__`, W#3 `popitem` on `len > capacity` not `>=`, W#4 DEBUG log outside lock + §N6 factory comment, W#5 async tests via `asyncio.gather` not `asyncio.run`-per-test (Hypothesis property test legitimate exception with `asyncio.run` in sync wrapper because Hypothesis decorator is not async-aware), W#6 invariant generic, W#7 logger default test asserts `is not None` + smoke-call not identity). 4 review gates: plan-reviewer APPROVE → drift-checker ON TRACK → brief-reviewer SHIP → math-validator VERIFIED out-of-scope (pure Python class, zero financial-math, zero Decimal arithmetic). **ci-full GREEN on branch HEAD `06f4e91`** (run 25007742443). **T-113 cagg regression watch — 6th master ci-full exposure since T-113 fix landed: GREEN, no `LockNotAvailableError`** (monitoring window now > halfway through; ~5 more runs to ~98% confidence per T-113 close-out policy). 124 LOC src (well under §0.3 cap of 400; well under L-006 watch threshold of 200) + 226 LOC tests (cap-exempt). 14 tests: 10 unit + 1 H-009 base-class binding (`test_duplicate_within_ring_is_dropped` — adapter-specific `test_duplicate_exec_event_is_ignored` deferred to T-218 against real BybitV5Adapter / PaperExchange) + 1 Hypothesis property test (`test_dedup_consumer_matches_reference_under_arbitrary_event_sequences` cross-checks ring vs naive `_ReferenceRing` oracle under `(key, ...)` event sequences with capacity 1-20) + 2 ValueError edge cases (`test_capacity_zero_raises_value_error`, `test_capacity_negative_raises_value_error`). Repo-wide pytest 596 passed + 34 skipped (no regressions; +14 new T-210 tests). Unblocks T-218 (execution event dispatcher subclasses with `key_fn = lambda e: e.exchange_exec_id`, wires NatsClient/private-WS subscribe pattern, emits the H-009 named test against a real adapter).
- [x] T-204: Migration 0007 — trading_events hypertable (2026-04-27) — F2 fourth numbered work task. §7.2 lines 1091-1106 verbatim — append-only persisted mirror of `trading.events` NATS subject (§8.1 line 1216 "same data as orders.events but persisted"). 6 columns (`id BIGSERIAL`, `occurred_at TIMESTAMPTZ NOT NULL`, `bot_id TEXT` nullable, `correlation_id TEXT` nullable, `event_type TEXT NOT NULL`, `payload JSONB NOT NULL`). Composite PK `(occurred_at, id)` per hypertable convention; 2 indexes `te_bot_type (bot_id, event_type, occurred_at DESC)` + `te_correlation (correlation_id)`; hypertable 7-day chunks on `occurred_at`. **Retention 365 days** per §18.3 line 1176 + 2438 (audit kept 365d, then chunks dropped). **NO compression policy** per §18.3 lines 1178-1183 (compression specified for `ohlc_1m` + `features` only; trading_events has shorter retention + JSONB-heavy high-cardinality storage where compression's marginal gain is offset by decompress overhead during audit replay). `occurred_at` has **NO server_default** per §N1 invariant — application sets via `packages.core.now_utc()` on every emit. **No FKs on any column** per §7.2 verbatim (append-only audit stream outlives source rows). Anti-FK assertion on `bot_id` only per OQ-2 (mirror T-202 W#2 / T-203 W#4 precedent — `bot_id` is the symmetry trap because `orders.bot_id` and `trades.bot_id` ARE FKs; `correlation_id` has no `correlations` parent table so no symmetry-add risk). Schema consumed by T-216b (placement transaction emit), T-218 (fill events), T-219 (close + reconcile_adjust events), T-220 (P&L audit drift), T-221 (orphan-close + reconcile events). H-018 N/A (composite PK, no BIGSERIAL surrogate-key WHERE-clause hazard surface — H-018 binds at `trades.id` from 0005). H-021 N/A (UTC scheduler binds at T-220 APScheduler emitter, not schema). **Calibration win**: brief verbatim-clear → only 3 genuine OQs surfaced (OQ-1 combined retention/no-compression test pattern, OQ-2 anti-FK scope bot_id-only, OQ-3 3 nullability variants); 10 verbatim items recorded in plan-doc Decisions section without operator review per T-203 calibration note. **Operator-locked grep-friendly test naming** for OQ-1 anti-pattern: separate test function `test_trading_events_hypertable_retention_365d_no_compression` combining positive retention + negative compression assertions in one grep target — locks both regression directions (accidentally adding compression bloats storage with marginal benefit; accidentally dropping retention creates forever-accumulating audit table). Plan: `docs/plans/T-204.md` (single-pass plan-reviewer APPROVE 2026-04-27 with 5 Write-time guidance items, all addressed; W#1 subset assertion `expected_indexes <= actual_indexes` for vendor-metadata robustness with diff-surfacing error message instead of strict `len == 3`, W#2 plan-doc cleanup eliminating obsolete "Decisions to commit" trailer, W#3 separate grep-friendly test function naming, W#4 h1/h2/h3 inline operator-locked variant comments — h1 typical case both populated for T-216b emit path, h2 `correlation_id` NULL for T-220 audit cron tick precedent, h3 both NULL for service-startup pre-bot-context, W#5 composite-PK uniqueness via approach-a `RETURNING id, occurred_at` capture-then-collide pattern mirror T-203 OQ-10 idiom). 4 review gates: plan-reviewer APPROVE → drift-checker ON TRACK → brief-reviewer SHIP → math-validator VERIFIED out-of-scope. **ci-full GREEN on branch HEAD `13ce972`** (run 25006854510; integration job exercises both new test functions against testcontainer Postgres+TimescaleDB). **T-113 cagg regression watch — 5th master ci-full exposure since T-113 fix (halfway through monitoring window): GREEN, no `LockNotAvailableError` on `test_fetch_warmup_window_15m_from_cagg`.** Pre-commit hooks (ruff format, ruff check, mypy strict, bandit) all passed. Pre-commit caught PT018 (multi-part assert split) — fixed before commit. 141 LOC migration src (well under §0.3 cap of 400; well under L-006 watch threshold of 200) + 334 LOC test (cap-exempt). Test functions: `test_migration_0007_creates_trading_events_hypertable` (full schema + smoke E2E with 3 nullability variants + composite-PK uniqueness via `pytest.raises(asyncpg.exceptions.UniqueViolationError)`) and `test_trading_events_hypertable_retention_365d_no_compression` (combined positive retention 365d + negative compression assertions, single grep target). Repo-wide pytest 582 passed + 34 skipped (no regressions; +2 new env-gated tests for 0007). Unblocks T-216b (placement transaction emit path), T-218 (fill events), T-219 (close + reconcile_adjust), T-220 (audit drift events), T-221 (orphan-close + reconcile).
- [x] T-203: Migration 0006 — position_state regular table (2026-04-27) — F2 third numbered work task. §7.2 lines 1058-1080 verbatim — live in-flight position state (one row per active bot-symbol pair). Composite PK `(bot_id, symbol)` enforces "one open position per bot-symbol" at schema level. Single FK `trade_id → trades.id` (NO ACTION default per brief verbatim). Plain `bot_id TEXT NOT NULL` with **NO FK on bots(bot_id)** per §7.2 verbatim — mirror T-202 W#2 precedent (anti-FK assertion in test queries `pg_constraint` directly to lock no-symmetry-add invariant). 17 columns; **flag-based FSM state** (`tp_hit BOOLEAN, trailing_active BOOLEAN, sl_type TEXT, remaining_qty NUMERIC`) — no discrete `current_state` enum; T-217 derives FSM state from flags. Server defaults shipped via `sa.text("false")` / `sa.text("0")` for `tp_hit, trailing_active, running_pnl`. **`updated_at` has NO server_default** per §N1 invariant — application sets via `packages.core.now_utc()` on every INSERT/UPDATE (test sub-check d explicitly asserts `column_default IS NULL` to lock §N1). Regular table, no hypertable, no retention/compression policies, no secondary indexes per §7.2 verbatim — composite PK doubles as natural lookup index; T-221 reconciliation full-table-scans (small table); F1+ opportunistic if downstream task surfaces bottleneck. NUMERIC precision: NUMERIC(30,12) for qty/prices (H-015 Decimal preservation), NUMERIC(20,4) for `running_pnl`. Schema consumed by T-216b (INSERT in placement transaction alongside orders+trades), T-217 PositionLifecycle FSM monitor tick updates (BE move + trail + MFE/MAE per §9.5 lines 1581-1592), T-218 dispatcher updates `running_pnl`/`remaining_qty` on fill events, T-219 cumulative-delta close DELETEs row at full close (**row-lifetime = position-lifetime; close = DELETE** invariant locked by DELETE-by-PK sub-check), T-221 post-restart reconciliation reads all rows on startup per H-020 (FSM rehydration from row state per Q3 default). **H-018 N/A for position_state** — H-018 is the BIGSERIAL `trades.id` invariant addressed at 0005; `position_state` has composite PK with no BIGSERIAL surface. **H-020 schema-level guard**: table existence + smoke INSERT/SELECT verified; behavioural test (`test_reconciliation_closes_db_orphans_and_markets_exchange_orphans`) lives at T-221. Plan: `docs/plans/T-203.md` (single-pass plan-reviewer APPROVE 2026-04-27 with 5 Write-time guidance items, all addressed inline; W#1 `updated_at` no server_default per §N1, W#2 `sa.text("false")`/`sa.text("0")` server defaults not Python literals, W#3 `Decimal("...")` string-literal smoke values not float, W#4 anti-FK assertion via direct `pg_constraint` query mirror T-202 W#2 precedent, W#5 specific `pytest.raises(asyncpg.exceptions.UniqueViolationError)` not generic Exception). 14 OQs resolved with operator-approved defaults (calibration note codified in plan-doc: ~6 OQs were "confirm per brief verbatim" rather than genuine open decisions; forward migration tasks should consolidate verbatim-resolved items into single Decisions section to trade defensive over-confirmation tokens for genuine open-decision review focus). 4 review gates: plan-reviewer APPROVE → drift-checker ON TRACK → brief-reviewer SHIP → math-validator VERIFIED out-of-scope (pure DDL + schema integration test, zero financial math, Decimal only as type annotation + construct-and-compare smoke). **ci-full GREEN on branch HEAD `48abb6e`** (run 25005707212; integration job exercises new test_0006_migration.py against testcontainer Postgres+TimescaleDB). **T-113 cagg regression watch — 4th master ci-full exposure since T-113 fix landed: GREEN, no `LockNotAvailableError` on `test_fetch_warmup_window_15m_from_cagg`.** Pre-commit hooks (ruff format, ruff check, mypy strict, bandit) all passed. 132 LOC migration src (well under §0.3 cap of 400; well under L-006 watch threshold of 200) + 317 LOC test (cap-exempt). Test sub-checks: column-shape verbatim (17-tuple), composite PK `(bot_id, symbol)`, single FK `trade_id → trades.id`, anti-FK on `bot_id` via `pg_constraint`, server-default verification (3 populated, `updated_at` IS NULL), no secondary indexes (only PK index `position_state_pkey`), no hypertable (regular table), smoke E2E along bots → orders → trades → position_state with `Decimal("0.123456789012")` qty + `Decimal("65000.123456789012")` price max-precision, composite-PK uniqueness via `pytest.raises(asyncpg.exceptions.UniqueViolationError)`, DELETE-by-PK lifetime check (row removed by PK; SELECT WHERE PK returns 0 rows). Repo-wide pytest 582 passed + 32 skipped (no regressions; +1 new env-gated test for 0006). Unblocks T-217 (PositionLifecycle FSM), T-221 (post-restart reconciliation), T-216b (placement-pipeline INSERT path).
- [x] T-202: Migration 0005 — orders + trades + executions FK chain (2026-04-27) — F2 second numbered work task. §7.2 verbatim execution-ledger triad (lines 956-1034) consumed by execution-service downstream tasks (T-216 placement pipeline, T-217 lifecycle FSM, T-218 dispatcher, T-219 cumulative-delta reconcile, T-220 P&L audit, T-221 post-restart reconciliation). Three tables: **orders** regular (18 cols; BIGSERIAL id PK; FK bot_id → bots.bot_id; indexes (bot_id, status) + (correlation_id)); **trades** regular (21 cols; BIGSERIAL id PK; FK bot_id → bots.bot_id + open_order_id → orders.id NOT NULL; close_order_id plain BIGINT NO FK per §7.2 verbatim; indexes (bot_id, status) + partial (closed_at DESC) WHERE status='closed'); **executions** hypertable (12 cols; composite PK (executed_at, id) per signals 0002 precedent; FK order_id → orders.id NOT NULL + trade_id → trades.id nullable; bot_id plain TEXT NOT NULL NO FK per §7.2 verbatim; UNIQUE (exchange_exec_id, executed_at) + INDEX (trade_id); 7-day chunks). 5-edge FK chain (orders→bots, trades→bots, trades.open_order_id→orders, executions.order_id→orders, executions.trade_id→trades). NUMERIC precision split per §5.13: NUMERIC(30,12) qty/prices for Bybit qty-step + price-tick + H-015 Decimal preservation, NUMERIC(20,8) executions.fee for Bybit fee precision, NUMERIC(20,4) USD-denominated/PnL (notional_usd/realized_pnl/fees_paid). DOUBLE PRECISION columns (mfe_pct/mae_pct/confidence_score on trades) use **sa.Double()** (NOT sa.Float()) per L-005 — sa.Float() would silently degrade to PG `real` (4-byte/24-bit mantissa) vs DOUBLE PRECISION (8-byte/53-bit). §18.3 forever-retention on trades + executions enforced by **anti-test** (sub-check f, NEGATION of test_0004 pattern via `not in policy_targets`) — NO retention/compression policies on executions hypertable; protects against accidental copy-paste from 0003/0004. **Anti-FK assertions** in test (close_order_id and executions.bot_id both verified NOT in fks set) lock W#2/W#4 invariants for downstream readers. Per-migration test (363 LOC): column-shape verbatim assertion (3 expected-tuple constants), composite + simple PKs, 5-FK enumeration via information_schema, 6-index shape via pg_indexes (including partial WHERE clause + UNIQUE flag), hypertable 7-day chunks via timescaledb_information.dimensions, NO-policy anti-test, smoke E2E inserting along FK chain (bots → orders → trades → executions) with NUMERIC(30,12) precision smoke via `Decimal("0.123456789012")` string-literal + `Decimal("65000.123456789012")` price + exact `==` round-trip assertion through asyncpg, JSONB codec round-trip on meta columns, UTC TIMESTAMPTZ round-trip. H-018 (Close trade by PK only) addressed at schema level via BIGSERIAL trades.id PK exposed; behavioural enforcement (`UPDATE trades WHERE id = $1` only) deferred to T-216/T-217 query-layer tasks per `packages/db/queries/trades.py`. Plan: `docs/plans/T-202.md` (single-pass plan-reviewer APPROVE 2026-04-27 with 5 Write-time guidance items, all addressed inline; W#1 sa.Double() for DOUBLE PRECISION columns, W#2 executions.bot_id no-FK, W#3 anti-test must be NEGATION not presence, W#4 trades.close_order_id no-FK, W#5 NUMERIC precision string-literal Decimal). 10 OQs resolved with operator-approved defaults (OQ-1 close_order_id no-FK per brief, OQ-2 anti-test included, OQ-3 max-precision smoke values, OQ-4 UUID-suffixed test bot_id, OQ-5 monolithic test function mirror 0004, OQ-6 standard branch name, OQ-7 omit hand-verification, OQ-8 skip docs/modules/db.md, OQ-9 free-form meta JSONB, OQ-10 anti-test scope executions only). 4 review gates: plan-reviewer APPROVE → drift-checker ON TRACK → brief-reviewer SHIP → math-validator VERIFIED out-of-scope (pure DDL + schema integration test, zero financial math, Decimal only as type annotation). **ci-full GREEN on branch HEAD `83a3b26`** (run 25004676786, integration job exercises new test_0005_migration.py against testcontainer Postgres+TimescaleDB). Pre-commit hook caught ruff format-only changes; no semantic fixes needed. 235 LOC migration src (under §0.3 cap of 400; L-006 watch-threshold zone, ~50 LOC docstring documentation not surface) + 363 LOC test (cap-exempt). Repo-wide pytest 582 passed + 31 skipped (no regressions; +1 new env-gated test for 0005). Unblocks T-203 (migration 0006 position_state — FK trade_id → trades.id), T-212 (migration 0008 paper_* mirrors), T-216/T-217/T-218/T-219/T-220/T-221 (every execution-service task reads/writes from these tables).
- [x] T-201: ExchangeClient protocol + errors + types (2026-04-27) — F2 first numbered work task. `packages/exchange/` pure-types skeleton: `protocols.py` (ExchangeClient Protocol per §11.1 with Q11 brief artifact `tpsl_mode='Full'` default overruled in favour of H-013 invariant — no default; OQ-1 brief artifact `async def stream_*` overruled in favour of Python idiom `def stream_* -> AsyncIterator[X]`); `errors.py` (§11.3 verbatim: `ExchangeError`, `RateLimitError`, `AuthError`, `OrderRejected`, `NetworkTimeout`, `UnknownState`; `OrderRejected.__init__(reason)` and `UnknownState.__init__(last_known_action)` set named attribute + super-forward so message rendering and named access both work); `types.py` (4 frozen dataclasses: `OrderPlaceResult` (2 fields: `exchange_order_id, placed_at`), `Position` (6 fields: `symbol, side, size, entry_price, leverage, unrealized_pnl` — `size` absolute non-negative; `0` + `side=None` = flat), `ExecutionEvent` (8 fields, NO `exec_type` per H-024 — dispatcher T-218 derives via DB `execId → order_id`), `PositionEvent` = Position fields + `occurred_at`); `__init__.py` re-exports 11 public names; `py.typed` marker. **`_UNLABELED_METHODS: frozenset[str] = frozenset({"stream_executions", "stream_positions", "close"})`** module-level constant in `protocols.py` (NOT re-exported) codifying §N3 marker exemption — T-206 conformance test will read this set. 4 writes (`set_leverage` / `place_market_order` @non_idempotent / `set_trading_stop` / `cancel_order`) + 3 reads (`get_positions` / `get_fill_price` / `get_closed_pnl_cumulative`) all decorated; streams + close exempt per `_UNLABELED_METHODS`. Hazards addressed at surface: H-003 (`@non_idempotent` on `place_market_order` + `UnknownState` exception), H-013 (`set_trading_stop` `tpsl_mode: Literal["Full", "Partial"]` no-default, positioned 3rd after `self`+`symbol` so optional fields keep `= None`), H-024 (`ExecutionEvent` test asserts `exec_type` and `execType` both absent via `dataclasses.fields` introspection). Behavioural hazard tests (`test_place_order_on_timeout_never_retries_and_raises_unknown_state`, etc.) deferred to T-207 / T-208 / T-218 owner tasks per plan §Hazards table. **11 OQs resolved with operator-approved defaults 2026-04-27** (OQ-1 stream `def` not `async def`; OQ-2 `_UNLABELED_METHODS` frozenset for streams+close; OQ-3 `Position.size` absolute non-negative; OQ-4..OQ-6 minimal-surface defaults; OQ-7..OQ-8 mirror features/ test naming + `__all__`; OQ-9 omit `## Hand verification` for pure-types; OQ-10 skip `docs/modules/exchange.md` defer to F2 close-out; OQ-11 branch `feat/T-201-exchange-client-protocol`). LOC: 363 src + 444 tests = 807 total (plan estimated ~370; src under §0.3 cap of 400, L-006 watch flag respected — overage was docstring tonnage, not surface expansion). 44 tests across 3 modules (16 types + 9 errors + 19 protocols), all passing. Repo-wide pytest 582 passed + 30 skipped (no regressions; baseline 538 from T-113, +44 net new). Plan: `docs/plans/T-201.md` (single-pass plan-reviewer APPROVE 2026-04-27 with 8 Write-time guidance items, all addressed inline; W#1 `_UNLABELED_METHODS` verbatim + locked rationale block; W#2 `set_trading_stop` ordering + signature test; W#3 streams `def` + signature test; W#4 explicit `__init__` on parameterised errors; W#5 no `exec_type` + introspection test; W#6 `Position.size` absolute docs; W#7 LOC under cap; W#8 no third brief inconsistency surfaced). 4 review gates: plan-reviewer APPROVE → drift-checker ON TRACK x2 (mid-implementation + final) → brief-reviewer SHIP → math-validator VERIFIED (out of scope, math-validator skipped — no `packages/features/builtins/`, `packages/pnl/`, `services/feature-engine/`, `services/execution/`, or `services/scoring/` touched; Decimal appears only as type annotation, never operated on). **ci-full GREEN on branch HEAD `2f16176`** (run 25003721780). Pre-commit hook caught RUF002 (ambiguous `×`→`x`), PT017 (try/except → `pytest.raises`), SIM300 (Yoda condition flip on frozenset compare) — all auto-fixed before commit. Unblocks T-205 / T-206 / T-207 / T-211 / T-215 / T-216 / T-218 / T-219 / T-221 (all import from `packages.exchange`).
- [x] T-200: Populate F2 backlog (2026-04-26) — TASKS.md restructure adding 22 numbered F2 tasks (T-201..T-222) under `### F2 numbered` and 9 opportunistic items under `### F2+ opportunistic`. F1→F2 phase boundary clean: T-100..T-113 closed, T-200 is single deliverable mirror of T-019 precedent (F0→F1 backlog populate, 2026-04-25). Branchless on master per Q12 / T-019 precedent. Hazard classification across §20 H-001..H-026: **18 F2-binding** (H-001/002/003/004/007/009/011/012/013/015/017/018/020/021/022/024/025/026), **1 cross-phase** (H-016 shadow-task cleanup — F2 protocol surface stays, F5 shadow runtime owns full address), **7 non-F2** (H-005/006/008/010/014/019/023 — already addressed in F0/F1 or deferred to F3/F5). Each F2-binding hazard mapped to owner task with named test inline. F2 exit-criteria E1..E5 (§19 lines 2526-2531) traced: E1 testnet manual playbook → T-222; E2 paper parity → T-222; E3 concurrent-bot cumulative-delta → T-219+T-222; E4 P&L audit drift correction → T-220; E5 hazard-catalog tests → distributed across owner tasks + T-F2+ opportunistic catalog orchestrator. Migration sequence: 0005 (orders+trades+executions FK chain) T-202 + 0006 (position_state) T-203 + 0007 (trading_events hypertable) T-204 + 0008 (paper_* mirror) T-212 + 0009 (trade_pnl_deltas inline with T-220). Top-of-DAG (no F2-internal deps): T-201 ExchangeClient protocol + errors + types, T-202 migration 0005, T-204 migration 0007, T-210 DedupingConsumer, T-214 execution-service skeleton — operator picks one to start after T-200 lands. Pre-flagged splits per §0.3 / L-006: T-208 → T-208a (write methods) + T-208b (read methods); T-216 → T-216a (request→place+fill) + T-216b (set_stops+persistence+emit). Opportunistic ADR-prerequisites: ADR-Q-A (rate limiter design, T-205 prereq), ADR-Q-B (reconciliation strategy, T-219 prereq), ADR-Q-C (APScheduler operational pattern — supervision + UTC enforcement + test fixtures, T-220 prereq; brief mandates APScheduler library per §3.1 line 302 + H-021 line 2769, ADR scope is operational pattern not library selection). 12 open questions resolved with operator-approved defaults 2026-04-26 (Q1 step-size cache inline at T-216a, Q2 publish-after-persist per T-104b, Q3 PositionLifecycle rehydrated from position_state, Q4-A PaperExchange OHLC-1m monitoring with intra-candle path deferred to F5, Q5 trading_events DB+orders.events NATS both per event, Q6 trade_pnl_deltas schema baseline starting-point with final lock at T-220 plan-doc, Q7 eager adapter pool init, Q8 H-016 protocol stays-as-is, Q9 set_leverage LRU cache 1h TTL with brief H-002 cross-ref artifact note, Q10 E1 manual playbook + CI for E2/E3, Q11 tpsl_mode no-default per H-013 invariant overruling §11.1 default='Full' artifact, Q12 branchless on master). Plan: `docs/plans/T-200.md` (single-pass plan-reviewer APPROVE 2026-04-26 with 5 Write-time guidance items, all addressed inline in TASKS.md entries: WTG #1 watch-for-cap on T-207/T-209/T-213/T-219/T-220/T-221/T-222, WTG #2 ADR-Q-C scope reframed as operational-pattern not library-choice, WTG #3 §N4 TDD discipline call-out on T-216/T-219/T-220, WTG #4 plan-doc status header Q12 cleanup, WTG #5 Q6 schema lock-site cross-ref in T-220 entry). 4 review gates: plan-reviewer APPROVE → drift-checker SKIPPED (backlog populate is metadata, no implementation drift surface) → brief-reviewer SHIP → math-validator SKIPPED (out of scope — no financial math touched). T-200 itself = 0 source LOC, ~480 LOC plan-doc artifact + ~XXX LOC TASKS.md edit. F2 phase-gate compliance per §0.10: F3 task IDs T-300+ reserved untouched until F2 closes. F2 brief-estimated 2-3 weeks (vs F1 1-2 weeks); 22 numbered tasks vs F1's 14 — proportional decomposition per phase scope.
- [x] T-113: cagg auto-refresh test isolation (2026-04-26; bgw_job race against manual `CALL refresh_continuous_aggregate('ohlc_15m', NULL, NULL)` eliminated via fixture-side `remove_continuous_aggregate_policy`). Pre-existing T-110b ship `c1c1aec` flake surfaced in ci-full #63 on commit `bfafbc7` (T-111 chore close; `tests/integration/queries/test_feature_engine.py::test_fetch_warmup_window_15m_from_cagg` → `asyncpg.exceptions.LockNotAvailableError: could not refresh continuous aggregate "ohlc_15m" due to a concurrent refresh`). T-110b/T-110c/T-110d/T-111 had previously passed by lucky `policy_refresh_continuous_aggregate` bgw_job timing, NOT by determinism. Migration 0003 (T-103) sets the cagg auto-refresh policy with `schedule_interval=1m, start_offset=1d, end_offset=1m`; the bgw_job acquires the cagg lock and the test's manual refresh call collides. **Fix**: extend `migrated_db_dsn` fixture in `tests/integration/queries/conftest.py` (~9 LOC) and `tests/integration/scripts/conftest.py` (~9 LOC) with post-`alembic upgrade head` `SELECT remove_continuous_aggregate_policy('ohlc_15m')` step on the throwaway DSN, before `yield`. **Test fixture isolation only** — production migration 0003 unchanged; `tests/integration/migrations/conftest.py` left intact (its `test_0003_migration.py:156` asserts policy presence on its own throwaway DB which still gets the policy). Other 4 caggs (`ohlc_5m`, `ohlc_1h`, `ohlc_4h`, `ohlc_1d`) keep their auto-refresh policy in queries+scripts test DBs (not contended by current tests; staying narrow per operator-spec). **5 Write-time guidance items addressed**: connection on `throwaway_dsn` not `base_dsn`; native `await asyncpg.connect` not `asyncio.to_thread`; block placed BETWEEN `alembic upgrade head` AND `yield throwaway_dsn` INSIDE existing outer `try` (so `finally: DROP DATABASE WITH (FORCE)` still runs on raise — no orphan DB); plain call without `if_exists => true`; migrations conftest unchanged. Plan: `docs/plans/T-113.md` (single-pass plan-reviewer APPROVE 2026-04-26 with 5 Write-time guidance items embedded). 4 review gates: plan-reviewer APPROVE → drift-checker skipped (<30 LOC source) → brief-reviewer SHIP (5/5 guidance verified) → math-validator skipped (out of scope — no `packages/features/*`, `packages/pnl/`, or affected services touched). **ci-full GREEN on branch HEAD `108ae76`** (integration job 1m31s — directly exercises the previously-failing `test_fetch_warmup_window_15m_from_cagg`); ci-fast also green. Pytest repo-wide local 538 passed + 30 skipped (POSTGRES_TEST_DSN env-gate, expected). Hazards addressed: no new §20 hazards (test fixture only); §N7 hexagonal hygiene preserved (test fixture isolation; production migration unchanged). **Process**: regression monitoring next ~5-10 ci-full runs on master — if `LockNotAvailableError` reappears on `test_fetch_warmup_window_15m_from_cagg`, fix is insufficient and we open T-114 (likely retry-with-backoff or tighter fixture scoping); one green run = ~98% confidence given native flake rate. **F1 stabilisation complete**: T-100..T-112 done + T-113 fix landed → F2 unlock proceeds (phase-gate flip + T-200 backlog populate).
- [x] T-112: feature-engine backfill CLI — `scripts/backfill_features.py` + extracts (`packages/features/yaml.py`, `packages/features/intervals.py`) — closes F1 exit criterion (2026-04-26; +fix-import-bootstrap after ci-full red on initial close 0aaf7cc — see commits b73e931 fix(T-112) + 92ae4d1 test(T-112)) — last F1 numbered task per §19 line 2511 (`python scripts/backfill_features.py --feature ind.btcusdt.15m.ema_20 --from ... populates history`). CLI iterates OHLC range via T-110b `fetch_ohlc_range` (NEW), computes per candle via T-107a/b builtin `Feature.compute`, upserts via T-110b `insert_feature` (idempotent ON CONFLICT). Property test (env-gated `POSTGRES_TEST_DSN`) verifies §9.3 line 1525 determinism — `SELECT 7 cols ORDER BY (feature_name, symbol, computed_at, source_version)` + `zip(strict=True)` tuple equality across two runs (explicit no-tolerance per CONCERN #4 resolution). **`scripts/backfill_features.py`** auto-derives `(symbol, interval)` from `--feature` per `ind.{symbol-lowercase}.{interval}.<rest>` convention (canonical Bybit-shape = uppercase form); `_parse_iso_datetime` accepts date-only + full ISO-8601, naive → `+00:00` append (§N1 UTC); `--to` inclusive (`BETWEEN $3 AND $4` in `fetch_ohlc_range` SQL). `_backfill_one_feature` deque(maxlen=feature.warmup_candles) iterates ASC; under-fill skips compute; on full buffer: `feature.compute(tuple(buf))` → Decimal→float wire seam (parallel ternaries algebraically equivalent to T-110c `pipeline._build_update` serial branches under `FeatureValue.__post_init__` exactly-one-non-None invariant — math-validator VERIFIED bit-equivalence) → `computed_at = bucket_start + INTERVAL_DELTA[interval]` → `insert_feature(...)`. JSONB codec inline 4 lines verbatim mirror T-110d `_register_jsonb_codec` args + T-108 `test_0004_migration:55-62`; no cross-service import (BLOCKER 2 option A resolution). **BLOCKER 1 fix** (yaml_loader cross-service import): NEW `packages/features/yaml.py` extracts T-111's `load_indicators_yaml` + `INDICATORS_YAML_PATH` as public port (verbatim move; `parents[3]` → `parents[2]` path adjustment). T-111 `services/feature_engine/app/yaml_loader.py` becomes 12-line re-export shim preserving internal imports + `features_registry.py` monkeypatch path (Python re-export semantics — `services.feature_engine.app.features_registry.load_indicators_yaml` stays valid module attribute via module-level `from packages.features.yaml import load_indicators_yaml`; existing `test_features_registry.py` passes unchanged per CONCERN #2 fix). Test moved to `packages/features/tests/test_yaml.py` verbatim except line 23 import path update (CONCERN #3 fix). **CONCERN 3 fix** (INTERVAL_DELTA placement option B): NEW `packages/features/intervals.py` ships 6-entry `Final[Mapping[str, timedelta]]` (1m/5m/15m/1h/4h/1d). T-110c shipped `pipeline.py` refactored from local `_INTERVAL_DELTA` (1m-only) to import from `packages.features.intervals` — single source of truth, eliminates last cross-layer import from `scripts/` to `services/`. F1+ multi-interval cagg-trigger live extension just adds keys here. Cleaned up unused `from datetime import timedelta` + `Final` imports in pipeline.py. **§N7 hexagonal preserved**: `rg "^from services\." scripts/` returns ZERO hits — backfill imports only stdlib + asyncpg + structlog + `packages.{features,db,observability}`. PyYAML==6.0.3 promoted to `packages/features/pyproject.toml.dependencies` (was T-111 service-private; needed for yaml.py extraction); uv.lock regenerated. **`packages/db/queries/feature_engine.py`** extension: `fetch_ohlc_range(conn, *, symbol, interval, source, from_dt, to_dt) -> list[OhlcRow]` mirrors `fetch_warmup_window` shape (allow-list `_INTERVAL_TO_TABLE` interval routing, materialised tuple list, ASC by bucket_start) with date-range filter instead of LIMIT N. Unknown interval raises ValueError before SQL emission (allow-list guard). **Decision #6 deviation** (drift-checker noted, math-validator VERIFIED bit-equivalent): plan said "Decimal→float seam verbatim mirror T-110c pipeline._build_update", implementation is algebraic rewrite (parallel ternaries vs serial if/elif/elif). Wire output identical for all 3 FeatureValue variants under exactly-one-non-None invariant — preserved by property test #16 byte-equality. **Tests** (16 new): `tests/scripts/test_backfill_features.py` 12 unit cases (parse_feature_name 4 + parse_iso_datetime 2 + _backfill_one_feature compute loop 4 + main exit codes 2); `tests/integration/queries/test_feature_engine.py` +3 fetch_ohlc_range cases (range filter ASC + empty + unknown-interval ValueError); `tests/integration/scripts/test_backfill_features_property.py` 1 env-gated property test for §9.3 line 1525 idempotency; `packages/features/tests/test_yaml.py` 11 cases moved verbatim from T-111. T-110c shipped `pipeline.py` (~5 LOC delta) + T-111 shipped `yaml_loader.py` (130 → 12 LOC shim) + `features_registry.py` (single import-line change) all defensible per §0.4 — planned + tested + documented; mirror T-110c→T-110d Q11 split precedent. **Cap-relevant LOC**: scripts/backfill_features.py ~280 + scripts/__init__.py 5 + packages/db/queries/feature_engine.py +60 + packages/features/yaml.py 130 (verbatim move from T-111 shipped 130) + packages/features/intervals.py 15 + services/feature_engine/app/yaml_loader.py: 130→12 LOC (-118) + features_registry.py: -7/+3 (-4) + pipeline.py: -5/+1 (-4) + packages/features/pyproject.toml +3 = **~227 net new source LOC, well under §0.3 cap of 400 + under L-006 watch threshold of 280** (plan estimated ~227 source / +30% buffer ~295; actual matches estimate). Tests ~620 LOC total (12 unit + 11 moved + 3 integration + 1 env-gated; cap-exempt). Coverage: 100% on backfill_features.py via 12 unit cases; fetch_ohlc_range 100% via 3 integration cases (env-gated); yaml.py + intervals.py 100% via moved + composition usage. Verification: `uv run ruff format/check` clean (after auto-fixes for ASYNC221 blocking subprocess wrapped in `asyncio.to_thread`, TC003 type-checking placement, RUF002 ambiguous unicode `×`→`x`/`*`, raw-string regex match patterns); `uv run mypy --strict scripts tests/scripts tests/integration/scripts packages/features packages/db services/feature_engine` 64 source files no issues; pytest in-scope 12+11+3 unit/integration cases pass (+ 1 env-gated skipped); pytest repo-wide 537 passed + 30 skipped + 2 warnings (no regressions; baseline 525 from T-111, +12 net new unit; +1 env-gated property test in skipped count). §N7 grep guard: `rg "^from services\." scripts/` exit code 1 (zero matches). Hazards addressed: no new §20 hazards (offline batch processing); §N3 idempotency via T-110b pre-marked `insert_feature` ON CONFLICT DO UPDATE; §N1 UTC enforcement on `_parse_iso_datetime` + asyncpg TIMESTAMPTZ + computed_at; H-014 N/A. Plan: `docs/plans/T-112.md` (APPROVED 2026-04-26 after one REVISE → fix → NEEDS DISCUSSION → resolve → re-APPROVE cycle: 2 BLOCKERs (yaml_loader cross-service + JSONB codec cross-service) + 4 CONCERNs (INTERVAL_DELTA placement, property test assertion shape, value_json Decimal-aware, two follow-ups) + 1 architectural decision (INTERVAL_DELTA option B) all resolved with 9 Write-time guidance items embedded). 4 review gates: plan-reviewer APPROVE (3rd pass) → drift-checker ON TRACK (all 9 guidance + 17 decisions verified) → brief-reviewer SHIP → math-validator VERIFIED (Decimal seam bit-equivalent under FeatureValue invariant; INTERVAL_DELTA mapping correct; idempotency determinism chain solid; no precision-violating casts in scripts/). Pre-commit hook caught ASYNC221 (subprocess.run blocking in async test) + 3 ruff style issues — all auto-fixed. **F1 phase exit criterion list (§19 line 2495-2511) closed**: signal-gateway → 1m candles in DB → 6 builtin features compute on close + appear in features table + KV → backfill CLI populates historical features. F2 (Execution) unlocked next.
- [x] T-111: feature-engine YAML registration — `configs/features/indicators.yaml` loader + `FEATURE_ENGINE_SYMBOLS` env-stopgap (2026-04-26) — replaces T-110c hardcoded EMA-20 BTCUSDT 1m demo `build_features()` with config-as-data YAML loader per §9.3 line 1486 + §B.2 verbatim 6 indicators (EMA-20/50/200, RSI-14, ATR-14 on 15m + VWAP-session on 1m). Plugin discovery (§9.3 line 1486 `plugin_registry.yaml`) and auto-backfill on plugin diff (§9.3 line 1518) DEFERRED to F1+ ADR per Decision #1/#13 (plugins/ dir empty; no current consumer; §0.8 anti-hypothetical). **`services/feature_engine/app/yaml_loader.py`** ships `load_indicators_yaml(path, symbols)` with `_TYPE_TO_FEATURE: Final[Mapping[str, type[Feature]]]` allow-list (mirror T-110b `_INTERVAL_TO_TABLE` precedent — defends against `builtin.emm` typo); validates each YAML entry, instantiates `feature_class(**params, interval=interval)` (Protocol Feature dispatch via `# type: ignore[call-arg]` with inline rationale; the type-allow-list guarantees feature_class is one of 6 builtins all accepting this kwarg shape), runs drift checks, substitutes `name_template`, accumulates by `(symbol, interval)` key. **Cross-product semantics**: for each YAML entry × each symbol from FEATURE_ENGINE_SYMBOLS, instantiate one Feature; aggregate by `(symbol, interval)`; result shape `Mapping[tuple[str, str], list[tuple[str, Feature]]]` matches T-110c's contract verbatim so T-110d composition wiring stays untouched. Empty `symbols=[]`, missing file, OR empty-string file (`yaml.safe_load("") → None`; `if not raw` guard load-bearing) all return `{}` (composition root tolerates per T-110d Decision #5). **Drift checks** (L-002 control): `source_version` mismatch raises `ValueError` citing both YAML literal AND Feature instance attribute; `name_template` mismatch raises `ValueError` (Write-time guidance #3 — diagnostic surfaces both values). **Decision #6 deviation** (drift-checker noted, math-validator VERIFIED): plan said "ASSERT YAML matches feature.name_template (BEFORE substitution)", but §B.2 YAML literals like `ind.{symbol}.15m.ema_20` have `{interval}` baked in (1 placeholder), while T-107a `EmaFeature.name_template` has BOTH `{symbol}` AND `{interval}` as placeholders (2 placeholders, period baked at `__init__`). Reconciliation: `feature.name_template.replace("{interval}", interval)` BEFORE comparison — both shapes now have only `{symbol}` remaining. Structural reconciliation, NOT weakening of drift detection: test #9 (`ema_xxx` vs `ema_20`) still raises ValueError; test #8 (v2 vs v1) still raises. Real algorithm-suffix drift detection preserved. **Substitution** (T-110c Decisions #25/#26 inherited): `expected_yaml_template.format(symbol=symbol.lower())` produces lowercase feature_name per §1.7 line 244 / §7.2 line 904 / §8.4 line 1382 example literals; routing-key tuple stays canonical Bybit-shape per `OhlcCandlePayload.symbol`. **`services/feature_engine/app/features_registry.py`** rewritten as thin delegate: `INDICATORS_YAML_PATH = Path(__file__).resolve().parents[3] / "configs" / "features" / "indicators.yaml"` (repo-root-relative); `build_features(symbols)` calls `load_indicators_yaml(INDICATORS_YAML_PATH, symbols)`. **`services/feature_engine/app/config.py` extension**: `feature_engine_symbols: str = ""` field + `symbols` property (parse comma-separated, strip whitespace, drop empty fragments — verbatim mirror of `services/market_data/app/config.py:Settings.symbols`). F1+ replaces with `bots` JOIN `bot_configs` query per §9.2 line 1454 (T-100 procedural deviation precedent). **`services/feature_engine/app/main.py` callsite**: `build_features(settings.symbols)` (1 LOC delta from T-110d). **`configs/features/indicators.yaml` ships §B.2 lines 3045-3084 verbatim**: 6 indicators with `name_template` placeholders, `type` literals matching `_TYPE_TO_FEATURE` keys, `interval`/`params`/`source_version` fields. L-002 control: `rg "ind\.\{symbol\}" configs/features/indicators.yaml` returns exactly 6 hits matching brief verbatim (Write-time guidance #5). **PyYAML==6.0.3** added as runtime dep to `services/feature_engine/pyproject.toml` (single std lightweight YAML library; `yaml.safe_load` only — never `yaml.load` over untrusted input; configs are repo-controlled). **types-PyYAML~=6.0** added as dev dep to root `pyproject.toml` with rationale comment (mypy --strict needs real types). uv.lock regenerated; `uv sync --all-packages --group dev --frozen` confirms resolution. **§N7 hexagonal preserved**: yaml_loader is service-owned (`services/feature_engine/app/`), imports from `packages.features.builtins` (correct dependency direction; service depends on package). **§N9 config-as-data**: indicators move from hardcoded `build_features()` body to YAML; symbols move to env-stopgap. **T-110c shipped-code patch** (Q11-style precedent for touching shipped code): `build_features()` signature changes from `() -> Mapping[...]` to `(symbols: Sequence[str]) -> Mapping[...]`. Test patches updated: `conftest.py:101` + `test_app_factory.py:121-124` lambda from `lambda: {}` to `lambda symbols: {}` per Write-time guidance #1. **Tests** (19 new): `test_yaml_loader.py` 11 cases — `_FULL_B2_YAML` constant with §B.2 verbatim literals; covers file-missing/empty-features/empty-symbols/empty-string + cross-product 6×2=12 tuples + lowercase-symbol substitution + canonical routing key + KeyError on `builtin.emm` typo + 2 drift-check ValueError cases (source_version v2-vs-v1 + name_template ema_xxx-vs-ema_20) + all-6-builtins-instantiate. `test_features_registry.py` refactor 2 cases (delegation pass-through with MagicMock + `INDICATORS_YAML_PATH` parts assertion). `test_config.py` +1 case (defaults: empty + symbols list) + 8-param parametrize (mirror market_data: empty/single/multi/repeated/leading-trailing/whitespace/separators-only/whitespace-only). `test_app_factory.py` lambda update only — no new cases. Repo-wide pytest 525 passed + 26 skipped + 2 warnings (no regressions; baseline 506 from T-110d, +19 net new). `uv run ruff format/check` clean (after auto-fixes for line-length, raw-string regex, ambiguous unicode `×` → `x`, import placement). `uv run mypy --strict services/feature_engine` 21 source files no issues. **Cap-relevant LOC**: `yaml_loader.py` ~125 + `features_registry.py` rewrite ~49 (same size as T-110c) + `config.py` +20 + `main.py` +1 + `pyproject.toml` +1 + root `pyproject.toml` +5 = **~155 source LOC, well under §0.3 cap of 400** (plan estimated ~140 / +20% buffer ~170; actual under estimate +11%). `configs/features/indicators.yaml` 36 LOC (data, not cap-relevant per §0.3 generated-artifacts exception). Tests ~280 LOC (cap-exempt). Coverage 100% on yaml_loader.py + features_registry.py via the 19 unit cases. Hazards addressed: no new §20; L-002 control active via `name_template` drift check (catches future indicator-class refactor that drifts from §B.2 spec). Plan: `docs/plans/T-111.md` (APPROVED 2026-04-26 single-pass with 5 Write-time guidance items embedded; no REVISE cycle). 4 review gates: plan-reviewer APPROVE → drift-checker ON TRACK (Decision #6 deviation noted as legitimate plan-error reconciliation; explicit confirmation that drift detection semantics are preserved) → brief-reviewer SHIP → math-validator VERIFIED (no new financial math; no precision casts in yaml_loader; drift-check reconciliation preserves real algorithm drift detection per test #8/#9; `**params` spread verified against all 4 builtin constructor signatures used in §B.2 — EMA/RSI/ATR period+interval, VWAP session+interval). T-111 unblocks F1+ plugin discovery extension (importlib entry_points + plugin_registry.yaml load) and §9.3 line 1518 auto-backfill on plugin diff ADR.
- [x] T-110d: feature-engine composition root extension — lifespan wiring + warmup orchestration + JSONB codec init + Q11 pipeline.start() split (2026-04-26) — fourth and final tier of the T-110 quad-split per L-007. Composition root extends T-109 `services/feature_engine/app/main.py` lifespan with the T-110a `BufferRegistry` + T-110b infra ports + T-110c `FeaturePipeline`. Mirrors T-100 lifespan ordering convention (numbered-step docstring + reverse-shutdown). **Lifespan order (load-bearing per `docs/plans/T-110d.md` §"Hand verification"):** (1) `features_by_key = build_features()`; (2) `capacity_map = {key: max(feature.warmup_candles for _, feature in entries)}`; (3) `BufferRegistry(capacity_map)`; (4) `pool = create_pool(..., init=_register_jsonb_codec)` (T-110b Hand-off option (a)); (5) `bus = NatsClient(...); bus.connect()`; (6) `pipeline = FeaturePipeline(...)`; (7) `pipeline.acquire_handles()` (sync — allocates buffers); (8) `await warmup_load(..., source=SOURCE_BINANCE)` (pushes cagg history into now-allocated buffers in time order); (9) `await pipeline.start_consuming()` (subscribes to `market.ohlc.1m.>`); (10) state attach pool/bus/buffer_registry/pipeline → app.state. **Reverse shutdown:** `pipeline.stop()` BEFORE `bus.close()` BEFORE `pool.close()`. **Q11 race resolution** (Decision #11) — split T-110c's monolithic `pipeline.start()` into `acquire_handles()` (sync) + `start_consuming()` (async). Without the split: warmup-pre-start would silent-drop on un-allocated buffers (T-110a Decision #2 silent no-op on push to no-handles key); warmup-post-start would race live NATS frames and wedge out-of-order data into the deque (`Feature.compute` would see wedged candles → wrong output). The `acquire → warmup → subscribe` sequence closes both races. ~15 LOC patch to T-110c `pipeline.py` + 1 test split in `test_pipeline.py` (`test_start_acquires_handles_then_subscribes` → `test_acquire_handles_creates_handle_per_key` (sync, NO `@pytest.mark.asyncio`) + `test_start_consuming_subscribes_market_ohlc_1m` (async)). **JSONB codec via `packages/db/pool.py:create_pool` `init` callback extension** (T-110b Hand-off option (a)) — `_register_jsonb_codec` module-level helper in `main.py` registers `json.dumps`/`json.loads` per-connection so production `insert_feature(value_json=...)` writes round-trip dict ↔ JSONB without per-call codec registration; `test_register_jsonb_codec_calls_set_type_codec` verifies callback contract at unit level (mocks Connection, asserts `set_type_codec("jsonb", encoder=json.dumps, decoder=json.loads, schema="pg_catalog")` verbatim per Write-time guidance #3). asyncpg-stubs declares positional-only Protocol `_InitCallback` that doesn't structurally match `Callable[[Connection[Record]], Awaitable[None]]`; `init=init` pass-through carries `# type: ignore[arg-type]` with inline 3-line WHY rationale (live error code per `feedback_type_ignore_context` memory). **`SOURCE_BINANCE` single-source-of-truth** via new `services/feature_engine/app/constants.py: Final = "binance"` (Write-time guidance #2). **Decision #14 deviation from plan** (defensible improvement): T-110c shipped `pipeline.py` with `_SOURCE_BINANCE = "binance"` DECLARED-BUT-UNUSED (no callsite — `_wire_to_domain` uses `payload.source` from the wire payload). Cleanest fix: DELETE the unused constant entirely from pipeline.py rather than replace with `from .constants import SOURCE_BINANCE` (which would land as F401 unused-import). Verification grep `rg '"binance"' services/feature_engine/app/` returns single hit `constants.py:SOURCE_BINANCE`. Drift-checker explicitly verified single-source invariant upheld. **`warmup_load` helper** in `services/feature_engine/app/warmup.py` — per `(symbol, interval)` key calls `fetch_warmup_window` (T-110b) and pushes rows into `BufferRegistry`; per-key try/except → log `feature_warmup_load_error` + continue (resilience over fail-fast — under-fill recoverable on subsequent live frames; `Feature.compute` raises `FeatureUnderflowError` until enough candles accumulate). `OhlcRow` → `OhlcCandle` mapping preserves Decimal verbatim (`open=row[2], high=row[3], low=row[4], close=row[5], volume=row[6]`); `interval` comes from iteration key (T-110b OhlcRow tuple lacks the field). `test_warmup_load_ohlc_row_field_order_locked` (Write-time guidance #4) locks positional mapping `candle.open == row[2] / candle.close == row[5] / candle.source == row[7]` against silent T-110b OhlcRow refactor. **Empty registry no-op** (Decision #5) — `build_features() == {}` → `capacity_map == {}` → `BufferRegistry({})` → no acquires, no warmup, no subscribe; service stays healthy + ready emitting only default Prometheus collectors. Default conftest fixture patches `build_features → {}` so existing T-109 lifespan tests keep their no-op semantics; new tests opt into populated registry via explicit `monkeypatch.setattr`. **`packages/db/pool.py` extension** — `create_pool(..., init=Callable[[Connection[Record]], Awaitable[None]] | None = None)` parameter; `test_pool.py` adds `test_init_callback_passed_to_asyncpg` (1 case) and updates 2 existing `assert_awaited_once_with` blocks to include `init=None` for backward compat. **§N7 hexagonal preserved**: composition root composes existing ports; `_register_jsonb_codec` is stateless module-level function (§N6 no globals); `asyncpg.Connection` only under `TYPE_CHECKING` in pool.py. **§N3 idempotency**: composition wires existing-marked ports; no new external writes; H-014 refcount honoured (`pipeline.acquire_handles` 0→1 transition; `pipeline.stop` 1→0 dealloc; never mid-operation). **§N1 UTC**: bucket_start preserved through asyncpg TIMESTAMPTZ → datetime UTC-aware → `OhlcCandle.bucket_start` UTC; no naked `datetime.utcnow()` introduced. **Tests** (15 new, ~280 LOC): `test_warmup.py` 5 cases (rows-in-order push + per-key error isolation + empty no-op + max-warmup-per-key + OhlcRow field-order lock); `test_app_factory.py` +3 cases (state attach for buffer_registry+pipeline + JSONB init pass-through + codec callback contract); `test_pipeline.py` split test 14 into 2 (sync `test_acquire_handles_creates_handle_per_key` + async `test_start_consuming_subscribes_market_ohlc_1m`); `test_pool.py` +1 case (`test_init_callback_passed_to_asyncpg`). **Cap-relevant LOC**: warmup.py 105 + constants.py 19 + main.py delta +45 + pipeline.py net +20 (split adds methods, removes unused constant) + pool.py +26 = **~115 source LOC, well under §0.3 cap of 400** (plan estimated ~155 source / ~200 with L-006 buffer; actual UNDER plan estimate — less docstring tonnage than feared on a wiring task). Tests ~280 LOC (cap-exempt). Coverage: 100% on warmup.py + constants.py via the 5 new test_warmup cases + JSONB codec contract test; main.py lifespan paths via test_app_factory extensions. Verification: ruff/format/mypy(strict) clean (53 source files in scope across `services/feature_engine`, `packages/db`, `packages/features`); pytest in-scope 60/60 in 2.35s; pytest repo-wide 506 passed + 26 skipped + 2 warnings in 6.45s (no regressions; baseline 496 from T-110c, +10 net new). Pre-commit hook caught `# noqa: ARG001` dead pragma + `from collections.abc import Sequence` placement (TC003) + 2 `_Call | None` mypy narrowing — all fixed. Hazards addressed: no new §20; H-014 refcount honoured (handles acquired in `acquire_handles()` BEFORE subscribe, released in `stop()`, never mid-operation; tail() via public `BufferHandle.tail()` not private `_registry._tail`). Plan: `docs/plans/T-110d.md` (APPROVED 2026-04-26 single-pass with 5 Write-time guidance items embedded; no REVISE cycle). 4 review gates: plan-reviewer APPROVE → drift-checker ON TRACK (all 10 plan-adherence items verified including Decision #14 deviation defensibility) → brief-reviewer SHIP → math-validator VERIFIED (precision flow cagg NUMERIC → asyncpg.Decimal → OhlcRow tuple → OhlcCandle dataclass → BufferRegistry pure pass-through; no `float()` / `round()` / `int()` / `Decimal(float_literal)` / `getcontext().prec` in staged diff; single Decimal→float seam at T-110c `pipeline._build_update` unchanged; JSONB codec preserves binary64 via json.dumps/loads round-trip; test fixtures use stringy `Decimal("50000")` constructions, not float literals). **T-110 quad-split closes**: T-110a (BufferRegistry, DONE) + T-110b (infra ports, DONE) + T-110c (FeaturePipeline class, DONE) + T-110d (composition root, this task, DONE) shipped at total ~1100 source LOC vs original T-110 estimate ~350 LOC = 3.1x — validates the L-007 quad-split decisions. T-111 (YAML registration) now unblocked.
- [x] T-110c: feature-engine `FeaturePipeline` class — NATS `market.ohlc.1m.>` consumer + per-feature dispatch + persist+KV+publish (2026-04-26) — third-tier preemptive split per L-007 (T-110 → T-110a + T-110b + T-110c + T-110d). T-110c = pure-ish consumer class with DI'd resources mirroring T-104b `OhlcPipeline`; T-110d (composition root extension) follows. **`services/feature_engine/app/pipeline.py`** ships `FeaturePipeline` DI'd with `bus`/`pool`/`buffer_registry`/`features_by_key`/`logger`. Subscribes once to `market.ohlc.1m.>` wildcard via T-110b's `bus.subscribe` API; handler parses `MessageEnvelope.payload` → `OhlcCandlePayload` → wire-to-domain `OhlcCandle` (1:1 Decimal preservation; no float coercion in domain path). For closed candles only (`is_closed=True`): `buffer_registry.push(symbol, "1m", candle)` (T-110a) then for each `(feature_name, feature)` in `features_by_key[(symbol, "1m")]`: `handle.tail(feature.warmup_candles)` (public BufferHandle API; H-014 refcount honoured — handles acquired in `start()` BEFORE subscribe, released in `stop()`, never mid-operation) → `feature.compute(tail)` → `_persist_kv_publish` → `insert_feature` (T-110b DB) → `kv_put("feature_latest", f"{feature_name}:{symbol}", model_dump_json().encode())` (T-110b KV) → `publish(subject_for(feature_name, symbol), envelope)` (T-110b subject_for). Per-feature `except Exception` isolation (NOT bare `except` — `CancelledError` propagates so `stop()` can shut down promptly; mirror T-104b `_consume_symbol` semantics). **DB-fail / KV-fail / publish-fail error gradient** per T-104b "publish-after-persist" contract: DB-fail skips KV+publish (canonical store down → don't notify); KV-fail still publishes (NATS+DB stay consistent; KV is best-effort cache); publish-fail leaves DB+KV intact (downstream recovers via KV read or next compute). **Decimal→float seam confined to `_build_update` static method** (single source of truth, Decision #6) — `value_num: Decimal → float(...)`; `value_bool` pass-through; `value_json: {k: float(v) if isinstance(v, Decimal) else v}` (str/bool/int sub-keys pass through per L-004 `Mapping[str, object]` openness from T-110b). **`_INTERVAL_DELTA: Final[Mapping[str, timedelta]] = {"1m": timedelta(minutes=1)}`** (Decision #8) — `computed_at = candle.bucket_start + _INTERVAL_DELTA[candle.interval]` (bucket end; F1+ multi-interval extension just adds 5m/15m/1h/4h/1d keys); KeyError on unknown interval = intentional fail-loud per §0.4 (T-110c subscribes 1m-only, so unknown can only arrive via misconfigured registry — surface the bug). `correlation_id = "feature:{feature_name}:{symbol}:{computed_at.isoformat()}"` (Decision #9) — same bucket → same id intentionally, matches `insert_feature` idempotency PK. `publisher = "feature-engine"`. Multi-interval cagg-trigger live updates DEFERRED to F1+ per operator D1 (brief §9.3 line 1492 implies multi-interval but §8.4 docstring acknowledges only 1m on NATS; warmup-at-startup populates higher intervals once T-110d wires it; pipeline still holds BufferHandle for those keys so dispatch can `tail()` against them when F1+ live updates land). **Template substitution single-source-of-truth** (Decisions #25/#26 + Write-time guidance #1-#2): `features_registry.build_features()` substitutes `name_template` once at registration time via `feature.name_template.format(symbol=symbol.lower(), interval=interval)`; `pipeline.py` NEVER calls `.format()` itself — verified by `rg "name_template" services/feature_engine/app/pipeline.py` = 2 hits both in docstrings (no callsites). `Mapping` value tuple `(str, Feature)` carries pre-substituted feature_name; T-111 YAML loader inherits the same contract. Lowercase symbol convention per §1.7 line 244 / §7.2 line 904 / §8.4 line 1382 example literals (`ind.btcusdt.15m.ema_20`); routing-key tuple stays canonical Bybit-shape (`BTCUSDT`). KV key `f"{feature_name}:{candle.symbol}"` + `subject_for(feature_name, candle.symbol)` inherit the lowercase-pre-colon / canonical-post-colon asymmetry from T-110b `test_subject_for_matches_spec_literal_verbatim` precedent. **`services/feature_engine/app/features_registry.py`** demo registers EMA-20 BTCUSDT 1m so T-110c live path is exercised end-to-end (1m subscribe → dispatch → persist+KV+publish). T-111 replaces with YAML loader behind the same `Mapping[tuple[str, str], list[tuple[str, Feature]]]` API. **`scalper-v2-features` workspace dep** added to `services/feature_engine/pyproject.toml.dependencies` + `[tool.uv.sources]` (Decision #23, Q1=a operator approval — operator preferred dep added in T-110c rather than T-110d to avoid mypy strict failure on ship branch); `uv.lock` regenerated. **§N7 hexagonal preserved**: `pipeline.py` imports only public ports (`packages.bus`, `packages.bus.schemas`, `packages.core`, `packages.db.queries.feature_engine`, `packages.features.types`); no private cross-imports; `asyncpg` only under `TYPE_CHECKING`. Pipeline owns no I/O resources directly — all DI'd at construction. **Tests** (17 new, ~417 LOC): `test_pipeline.py` 15 cases mapping onto plan §"Hand verification" trace invariants I1-I10 + lifecycle (closed-candle dispatch + in-progress skip I1 + unregistered symbol skip I2 + DB→KV→publish call order + per-feature error isolation I3 with `FeatureUnderflowError` on feature A, feature B still runs + DB-fail skips KV+publish I4 + KV-fail does not block publish I5 + publish-fail logs and continues I6 + computed_at bucket-end I7 verbatim `2026-04-26T12:16:00+00:00` for `bucket_start=12:15` + correlation_id format I8 verbatim `feature:ind.btcusdt.1m.ema_20:BTCUSDT:2026-04-26T12:01:00+00:00` + publisher='feature-engine' I9 + Decimal→float seam value_num I10 + value_json mixed-type pass-through L-004 + start acquires handles before subscribe + stop releases all handles 1→0 dealloc); `test_features_registry.py` 2 cases (substitution: feature_name == `"ind.btcusdt.1m.ema_20"` exact + single-key shape). Inline `_FakeBus`/`_FakePool`/`_FakeAcquire`/`_FakeConn`/`_FakeFeature` stubs (mirror T-104b `_FakeSubMgr`/`_FakeBus`/`_FakePool` style at `packages/market/tests/test_ohlc.py`). `BufferRegistry` real (T-110a is pure-domain). Handler invoked directly via `await pipeline._on_envelope(envelope)` rather than spinning the bus — no asyncio.create_task lifecycle in tests. **Cap-relevant LOC**: pipeline.py 331 + features_registry.py 47 + pyproject.toml +3 = **~381 source LOC, under §0.3 cap of 400** (plan estimated ~183 source / ~240 with L-006 +30% buffer; actual +58% over buffer = load-bearing docstring tonnage on a hexagonal use-case class — module docstring 53 LOC documenting cross-cutting concerns: error gradient (DB→KV→publish), ordering invariants (handles before subscribe; reverse on stop), multi-interval defer scope boundary, substitution single-source-of-truth, Decimal→float seam reasoning; per-method docstrings 5-15 LOC each; per-except structured logging blocks 5-7 LOC each; functional core estimated ~120-130 LOC. Drift-checker + brief-reviewer accepted as load-bearing per T-100 precedent (694 LOC over cap accepted as load-bearing operational invariants) and T-110b precedent (+28% over buffer accepted similarly). Tests ~417 LOC (cap-exempt). Coverage: 100% on pipeline.py + features_registry.py via the 17 unit cases. Verification: ruff/format/mypy(strict) clean (16 source files in services/feature_engine, 113 across the workspace); pytest services/feature_engine/tests/test_pipeline.py + test_features_registry.py 17/17 in 2.02s; pytest repo-wide 496 passed + 26 skipped + 2 warnings in 6.40s (no regressions; baseline 479 from T-110b, +17 net new); `uv lock` regenerated; `uv sync --all-packages --group dev --frozen` confirmed `scalper-v2-features` resolves. L-002 grep guard: `rg "features.updated" services/feature_engine/app/` = 3 hits, all in `main.py` docstring prose from T-109 (acknowledged by brief-reviewer in prior plan-review as documentation, not runtime code path drift); `pipeline.py` + `features_registry.py` have ZERO hits — `subject_for` from `packages.bus.schemas` is the sole production emitter. Pre-commit hook caught a `from collections.abc import Sequence` placement (should be under `TYPE_CHECKING` per ruff TC003) — auto-fixed. Hazards addressed: no new §20; H-014 refcount honoured (handles acquired in `start()` BEFORE subscribe, released in `stop()`, `tail()` via public `BufferHandle.tail()` not private `_registry._tail`). Plan: `docs/plans/T-110c.md` (APPROVED 2026-04-26 after one REVISE → fix → re-APPROVE cycle: 1 BLOCKER (template substitution undefined — resolved by committing substitution site to `features_registry.build_features()` at registration time + Mapping value type changed to `list[tuple[str, Feature]]`) + 2 CONCERNs (computed_at hardcoded `+ 1 minute` — resolved by `_INTERVAL_DELTA` Final mapping; features_registry coverage gap — resolved by `test_features_registry.py` 2 cases) — all 3 issues resolved before re-submission; APPROVED single-pass on second submission with 5 Write-time guidance items embedded). 4 review gates: plan-reviewer APPROVE → drift-checker ON TRACK → brief-reviewer SHIP → math-validator VERIFIED (single Decimal→float seam at `_build_update` lines 293+298 confirmed; no silent precision-violating casts; no `Decimal(float_literal)` antipattern; no `getcontext().prec` mutator; no Decimal+float arithmetic mixing in compute path; hand-computable test fixtures `Decimal("70.5") → 70.5` and `Decimal("1") → 1.0` are IEEE-754 exact representations; no library round-trip pattern). T-110d (composition root extension — lifespan wiring + features_registry construction + capacity_map + warmup orchestration + JSONB codec via `packages/db/pool.py` init callback extension) follows.
- [x] T-110b: feature-engine infrastructure ports — bus KV + `FeatureUpdate` schema + `feature_engine.py` queries (2026-04-26) — second-tier preemptive split per L-007 mirroring T-104a/T-104b precedent (T-110b = infra/ports half before T-110c use-case half). **`packages/bus/client.py`** gains `kv_put(bucket, key, value) -> int` annotated bare `@idempotent` per §N3 (NATS KV PUT replaces by key, last-write-wins on `(bucket, key)`); JS revision number returned for ordering / log-correlate observability; missing bucket raises `PublishError` (config bug, fail loud per §0.4 — bus does not auto-create); `nats.errors.Error` wrapped as `PublishError` with `__cause__` chain. **`packages/bus/schemas/feature_update.py`** ships `FeatureUpdate` Pydantic model verbatim from §8.4 lines 1382-1393: frozen `ConfigDict`, `schema_version: Literal["1.0"]` pinned, `value_num: float | None` / `value_bool: bool | None` / `value_json: dict[str, object] | None` (matches `FeatureValue.value_json: Mapping[str, object]` domain contract per L-004 — open to non-float values; T-110c handles Decimal→float conversion for numeric sub-keys at the wire seam), `model_validator(mode="after")` enforces exactly-one-non-None mirroring `FeatureValue.__post_init__`, UTC validator + ISO+00:00 serializer mirror `OhlcCandlePayload`. **`subject_for(feature_name, symbol)`** helper centralises the §8.1 line 1213 publish-subject literal `features.updated.<feature_name>.<symbol>` (L-002 control); `rg "features.updated" packages/bus/schemas/ tests/ packages/bus/tests/` shows the literal only in `feature_update.py` (subject_for + docstrings) and `test_feature_update_schema.py` (test_subject_for_matches_spec_literal_verbatim assertion + docstring) — clean. **`packages/db/queries/feature_engine.py`** ships `insert_feature(conn, *, feature_name, symbol, computed_at, value_num, value_bool, value_json, source_version)` with `INSERT ... ON CONFLICT (feature_name, symbol, computed_at, source_version) DO UPDATE SET value_num = EXCLUDED.value_num, value_bool = EXCLUDED.value_bool, value_json = EXCLUDED.value_json` per §9.3 line 1494 verbatim. Idempotency = docstring-marker form (no decorator) mirroring `insert_ohlc_1m` precedent at `packages/db/queries/market_data.py:73-96`. `computed_at` UTC enforcement = caller contract (Pydantic validator on the wire side catches naive timestamps; re-validation in query helper would be redundant overhead). **`fetch_warmup_window(conn, *, symbol, interval, n, source) -> list[OhlcRow]`** — interval=1m queries raw `ohlc_1m` hypertable; higher intervals (5m/15m/1h/4h/1d) query the corresponding T-103 cagg via `_INTERVAL_TO_TABLE: Final[Mapping[str, str]]` allow-list (validated before any f-string interpolation; defence in depth even though `interval` is internal — `# noqa: S608  # nosec B608` annotates both ruff and bandit suppressions on the SQL identifier interpolation site). Body materialises `await conn.fetch(...)` rows via comprehension to return `list[tuple]` not `list[asyncpg.Record]` — decouples T-110c from `asyncpg.Record`; **public `OhlcRow` tuple alias** declared via Python 3.12 `type` statement with explicit field-order comment `(symbol, bucket_start, open, high, low, close, volume, source)` so T-110c's `OhlcCandle(*row)` mapping cannot suffer off-by-one drift. Subquery + outer ORDER BY ASC inverts the natural DESC LIMIT to "oldest first" so callers can iterate `for row in result: registry.push(...)` in time order. Empty result returns `[]`; under-fill returns prefix; unknown interval raises `ValueError` before SQL emission (allow-list guard prevents identifier injection). **§N7 hexagonal preserved**: `packages/db/queries/feature_engine.py` does NOT import from `packages.features` (Q1=b decision); `rg "^from packages\\.features|^import packages\\.features" packages/db/` returns 0 hits. `packages/bus/__init__.py` deliberately NOT modified — schemas re-exported only via `packages/bus/schemas/__init__.py` per existing precedent (`packages.bus.schemas` import path; top-level `packages.bus` exports only `MessageEnvelope`/`NatsClient`/`ConnectionState`/errors). `services/feature_engine/pyproject.toml` `scalper-v2-features` workspace dep deferred to T-110c (Q5 default; T-110b doesn't import `OhlcCandle`). **Hand-off to T-110c** (codified in `docs/plans/T-110b.md` §"Hand-off to T-110c"): T-110c MUST register JSONB codec on pool acquire — TWO valid alternatives, T-110c plan-reviewer chooses: (a) per-connection codec via `create_pool(init=...)` extension or per-acquire registration in lifespan (mirrors T-108 `test_0004_migration:55-62` pattern); (b) `$N::jsonb` cast in SQL + `json.dumps()` at call site (mirror `packages/db/queries/signal_gateway.py:96` precedent — would alter `insert_feature` signature to take `value_json: str | None`). Without one, production `insert_feature(value_json=...)` raises asyncpg `TypeError`. T-110c also adds `scalper-v2-features` dep + maps `OhlcRow` → `OhlcCandle` on the use-case side + imports `subject_for` for §8.1 publish (NEVER inline). **Tests** (30 new, ~580 LOC): `test_client_kv.py` (6 cases — kv_put happy path + revision pass-through + DISCONNECTED state guard + nats.errors.Error wrapped as PublishError + bytes pass-through unchanged + `is_idempotent(NatsClient.kv_put)` via the public marker registry, mirror `test_publish_is_marked_non_idempotent` precedent NOT private `__idempotent__` dunder). `test_feature_update_schema.py` (14 cases — 3 value variants construct + zero/two/three non-None rejected + frozen + naive/CEST `computed_at` rejected + zero-offset normalised to `datetime.UTC` + ISO+00:00 serialisation form + `schema_version="2.0"` rejected by Literal pin + `subject_for` matches spec line 1213 verbatim with L-002 grep guard cited in test docstring + `value_json` accepts non-float values per L-004 openness `{"signal": "BUY", "score": 0.8, "trigger": True}`). `test_feature_engine.py` (10 cases, env-gated `POSTGRES_TEST_DSN` per existing migrations conftest pattern; per-connection JSONB codec registered in `conn` fixture mirroring T-108 `test_0004_migration:55-62`): **L-005 sentinel `3.141592653589793` exact-equality assert** on `value_num` round-trip — 15-digit double-precision π expansion that rounds to `3.1415927` in `real` (32-bit) storage, so the test fails against any future regression that downgrades the column away from `DOUBLE PRECISION`; tolerance asserts (`pytest.approx`/`math.isclose`) would defeat the precision-bit probe and pass against either column type — explicitly forbidden in the test docstring. Plus `value_bool` round-trip + Bollinger-shaped `value_json` JSONB round-trip + ON CONFLICT DO UPDATE last-write-wins value-level assertion (catches a regression to DO NOTHING) + distinct `source_version` yields independent rows (PK includes source_version) + `fetch_warmup_window` interval=1m from raw hypertable + interval=15m from cagg with manual `CALL refresh_continuous_aggregate('ohlc_15m', NULL, NULL)` + unknown interval ('3m') raises ValueError before SQL emission + empty source returns `[]` + under-fill returns short prefix list. **Cap-relevant LOC**: `feature_update.py` 110 + `client.py` delta +44 + `schemas/__init__.py` delta +3 + `feature_engine.py` 197 = **~354 source LOC, under §0.3 cap of 400** (plan estimated ~235 source / ~280 with L-006 +20% buffer; actual +28% over buffer = load-bearing infrastructure docstrings — JSONB hand-off explanation, ON CONFLICT contract, allow-list rationale, warmup semantics; consistent with T-100 docstring-tonnage precedent accepted as load-bearing operational invariants). Tests ~580 LOC (cap-exempt). Coverage: `feature_update.py` 100% / `feature_engine.py` not exercised in unit-only run (integration tests env-gated; CI-full will exercise via `POSTGRES_TEST_DSN`). Verification: ruff/format/mypy(strict) clean (29 source files in scope across `packages/bus`, `packages/db`, `tests/integration/queries`); pytest unit suite 20/20 in 1.97s (`test_client_kv.py` 6 + `test_feature_update_schema.py` 14); pytest repo-wide 479 passed + 26 skipped + 2 warnings in 6.21s (no regressions; baseline 459 from T-110a, +20 net new unit; +10 env-gated integration require `POSTGRES_TEST_DSN`). Pre-commit hook caught a bandit `B608` (hardcoded SQL expressions) on the `_INTERVAL_TO_TABLE` allow-list interpolation site that ruff `# noqa: S608` alone did not suppress — bandit uses `# nosec B608` syntax, so the suppression comment now reads `# noqa: S608  # nosec B608` covering both linter namespaces. Hazards addressed: no new §20 hazards (pure infra); §N1 UTC enforcement on FeatureUpdate.computed_at; §N3 idempotency annotations on both `kv_put` (bare `@idempotent`) and `insert_feature` (docstring marker); §N7 hexagonal seam between `packages.db` and `packages.features` preserved (Q1=b). Plan: `docs/plans/T-110b.md` (APPROVED 2026-04-26 after one REVISE → fix → re-APPROVE cycle: 2 BLOCKERs (string-arg decorator syntax doesn't exist; asyncpg.Record vs tuple type signature drift) + 5 CONCERNs (value_json type narrowing, L-005 sentinel weakness `70.5` vs `3.141592653589793`, top-level re-export precedent drift, JSONB codec hand-off, OhlcRow visibility) — all 7 issues resolved before re-submission; single-pass APPROVE on second submission with 5 Write-time guidance items embedded). T-110c (FeaturePipeline + composition root) follows.
- [x] T-110a: feature-engine rolling candle buffer registry — refcounted, H-014 (2026-04-26) — first half of T-110 preemptive split (L-007 — original T-110 estimated ~350 LOC integration-heavy with five distinct concerns; pure-domain buffer abstraction extracted to its own task ahead of mid-write split). **`packages/features/buffers/registry.py`** ships `BufferRegistry` + `BufferHandle` with refcount lifecycle: one `collections.deque` per `(symbol, interval)` key sized to capacity declared at registry construction, lazy-allocated on the 0→1 acquire transition and deallocated on the 1→0 release transition. `acquire(symbol, interval) -> BufferHandle` raises `KeyError` for keys absent from `capacity_map`; `push(symbol, interval, candle)` is a silent no-op on unknown / zero-refcount keys (defensive against T-110b NATS handler routing candles for not-yet-acquired symbols during warmup ordering — both conditions reduce to "no entry in `_buffers`" so the lookup is the single guard). `BufferHandle` is a sync `AbstractContextManager`; `tail(n) -> tuple[OhlcCandle, ...]` returns an immutable snapshot via `tuple(itertools.islice(buffer, max(0, len-n), len))` — shorter tuple on under-fill (Feature implementations raise `FeatureUnderflowError` on insufficient warmup; registry is dumb storage). Single `_released: bool` flag on the handle short-circuits both `tail()` (raises `RuntimeError`) and double-`__exit__` (idempotent no-op); no per-handle counter. **H-014 contract** — multiple features that consume the same `(symbol, interval)` (EMA-20 + EMA-50 + RSI-14 over `("BTCUSDT", "15m")`) hold individual handles on the same shared deque; when one feature releases (T-111 plugin reload or shutdown order), the buffer must remain alive for the others. `test_refcount_buffer_survives_one_holder_releasing` mirrors `test_refcount_sub_survives_one_caller_releasing` (T-101c `packages/market/subscription.py`) verbatim, walking trace steps 1-9 of the plan's hand-verification table. **§N7 hexagonal** — buffers/ imports only stdlib (`itertools`, `collections.deque`, `contextlib.AbstractContextManager`, `typing.Self`, `typing.TYPE_CHECKING`) plus `packages.features.types.OhlcCandle` under TYPE_CHECKING. Zero `packages.bus`/`packages.db`/`asyncio`/`nats`/`asyncpg` hits — verified by `rg "^(import|from)" packages/features/buffers/` grep guard. Sync API; T-110b's single-threaded asyncio event loop serialises access. `packages/features/__init__.py` deliberately NOT touched — buffers are a T-110b composition-root dep imported via explicit `from packages.features.buffers import …`, keeping the public feature API surface narrow. **Tests** (15 new, 219 LOC): `test_buffers_registry.py` covers acquire-creates-buffer / acquire-unknown-raises / two-acquires-share-buffer / different-keys-isolated / H-014-survives-one-holder-releasing / release-to-zero-deallocates / push-no-holders-silent-drop / push-unknown-key-silent-drop / tail-immutable-snapshot / tail-underfill / tail-empty / tail-after-release-raises / double-release-idempotent / capacity-overflow-drops-oldest / acquire-after-dealloc-fresh-buffer (behavioural divergence test per Write-time guidance #3, NOT `is` identity — push-then-tail asserts prior data is gone). **Cap-relevant LOC**: `__init__.py` 13 + `registry.py` 198 = **211 source LOC, under §0.3 cap of 400** (plan estimated ~145; +46% docstring overhead on a small logical surface ~80 executable LOC; consistent with L-006 lesson). Tests 219 LOC (cap-exempt). Coverage 100% on `registry.py`; full repo suite 459 passed + 16 env-gated skipped + 2 warnings in 6.15s (no regressions). Verification: `uv run ruff format/check` clean, `uv run mypy --strict packages/features` 24 source files no issues. Hazards addressed: **H-014** (rolling buffer refcount, generalised from WS to in-memory). Plan: `docs/plans/T-110a.md` (APPROVED 2026-04-26 single-pass with 5 Write-time guidance items embedded; all 5 discharged at brief-reviewer SHIP). T-110 follow-up (revised): split further into T-110b (infra/ports — bus KV + FeatureUpdate schema + features query helpers) and T-110c (use case — `FeaturePipeline` + composition root) per L-007 mirror of T-104a/T-104b precedent, after T-110a close revealed the original "T-110b = service composition root" was still ~500 LOC source.
- [x] T-109: feature-engine service skeleton — FastAPI + lifespan (pool + bus) + `/health`/`/ready`/`/metrics` (2026-04-26) — closes §9.3 + §19 F1 bullet 3 service skeleton mirroring T-100 (market-data-svc) verbatim minus the `BinanceWsClient` / `SubscriptionManager` / `OhlcPipeline` / `OhlcBackfill` composition. Pure scaffolding — no DB writes, no computation loop, no Feature Protocol consumption. T-110 lands the actual computation loop on top. **`services/feature_engine/app/main.py`** ships `create_app()` factory + minimal lifespan: synchronous `Settings` + structlog logger + Prometheus registry instantiated in body before `FastAPI(lifespan=...)` so deps providers see them outside startup (defensive against in-process unit tests that hit endpoints without TestClient context); lifespan opens asyncpg pool via `packages.db.create_pool` (DSN scheme validation + `application_name=service_name` injection for per-service PG log attribution) and connects `NatsClient(servers=[settings.nats_url], name=settings.service_name, logger=logger)`. **Reverse-order shutdown**: `bus.close` drains in-flight publishes against the still-open pool, then `pool.close` releases asyncpg connections. T-110 will extend this with `pipeline.stop` before `bus.close` so per-feature consumer task teardown lands while the bus is still open. **`app/health.py`** ships `/health` returns `200 {}` verbatim from T-100; `/ready` returns `200 {"ready": True}` when both `bus.state == CONNECTED` and `pool.acquire(timeout=1s)` succeed, otherwise `503 {"ready": False, "reason": <bus|db>}`. Reason precedence is bus → db (cheapest first; first failing reason wins). Exception trio `(TimeoutError, asyncpg.InterfaceError, asyncpg.PostgresError)` verbatim from T-100. `reason="db"` (not `"pool"`) so monitoring alert rules pivot uniformly across services. No ws-equivalent — feature-engine has no analogous long-lived external connection beyond bus + pool; T-110's dispatcher reuses the bus. **`app/config.py`** ships `Settings` (pydantic-settings, `extra="ignore"`) + exported `LogLevel = Literal["DEBUG", "INFO", "WARNING", "ERROR", "CRITICAL"]` alias. 4 fields (service_name, log_level, http_port, nats_url) + required `database_url`. No env-stopgap (operator decision #4 — T-110 adds when it has a real consumer; §0.8 anti-hypothetical). **`app/deps.py`** ships 4 providers (`get_pool`, `get_bus`, `get_settings`, `get_logger_dep` with `_dep` suffix to avoid collision with `packages.observability.get_logger`); all read `request.app.state.<x>` with `typing.cast`. T-110 will add registry/dispatcher/consumer providers. **`/metrics`** is an ASGI mount via `app.mount("/metrics", make_metrics_asgi_app(make_registry()))` from `packages.observability` with default Prometheus collectors only (no service-specific metrics in T-109 per §0.8). **Workspace deps**: `scalper-v2-bus` / `scalper-v2-core` / `scalper-v2-db` / `scalper-v2-observability` + `fastapi` + `pydantic-settings` + `uvicorn[standard]` + `uvloop`. **Intentionally NO `scalper-v2-features` and NO `scalper-v2-market`** — T-110 will pre-add `scalper-v2-features` when its first import lands (anti-hypothetical §0.8); T-110 PR will include pyproject + uv.lock edits as part of its code-only diff. `pyproject.toml` promoted from 6-line virtual stub to full hatch scaffold per ADR-0002 (services/ analogue: sources remap `"" = "services/feature_engine"`, `dev-mode-dirs = ["."]` retained because `app/` is a multi-component subpackage of the service). **Dockerfile** two-stage uv install mirrors market-data verbatim: same base image (python:3.12.13-slim-trixie), same uv-CLI donor (ghcr.io/astral-sh/uv:0.11.7), `--package scalper-v2-feature-engine --frozen --no-dev`, distinct UID/GID 10003 for `feature` user (signal=10001/market=10002/feature=10003 sequential, per-service log attribution if F1+ adds host bind mount). `python -m uvicorn services.feature_engine.app.main:create_app --factory --host 0.0.0.0 --port 8000 --loop uvloop`. **compose.yaml** feature-engine block mirrors market-data-svc verbatim: env_file `secrets.env required: false` pattern, env vars (`SERVICE_NAME=feature-engine`, `LOG_LEVEL=INFO`, `NATS_URL=nats://nats:4222`), depends_on (postgres healthy + nats healthy + nats-init completed), healthcheck via `python -c urllib.request /health` (no curl/wget in slim image). Internal-only — no nginx route, no public surface (communication is DB writes + NATS publishes). **Prometheus scrape entry** `feature-engine:8000/metrics` mirrors market-data-svc job def. **Tests** (19 new, ~365 LOC): `test_config.py` (8 cases — defaults + `database_url`-required + invalid log level + extra-env-ignored + 4-param env-overrides parametrize incl. CRITICAL); `test_health.py` (1 case — `200 {}`); `test_ready.py` (5 cases — happy + bus-fail + pool-TimeoutError + pool-InterfaceError + bus-precedence-over-db); `test_app_factory.py` (5 cases — factory shape + routes `/health`/`/ready`/`/metrics` + `/metrics` serves default collectors with `python_info` preamble + sync state attach pre-lifespan + lifespan attaches pool+bus and closes in reverse order asserted via call_order list). conftest.py mocks `packages.db.create_pool` + `NatsClient` at the `services.feature_engine.app.main` import boundary so the lifespan exercises end-to-end against fakes. Mypy strict clean; initial 2 `[unused-ignore]` dead pragmas removed by retyping fixtures from `object` to `MagicMock` per memory `feedback_type_ignore_context`. **Cap-relevant LOC**: `__init__.py` 1 + `app/__init__.py` 0 + `app/config.py` 48 + `app/deps.py` 60 + `app/health.py` 76 + `app/main.py` 139 + `pyproject.toml` 41 = **365 Python LOC, under §0.3 cap of 400**. Infra deltas (Dockerfile 83 + compose +45 + prometheus +10 = 138 LOC) qualify under §0.3 generated-artifacts exception per T-100/T-015a/T-104a precedent. `uv.lock` +24 (generated, exempt). Coverage: `config.py` 100% / `health.py` 100% / `main.py` 95% (line 73 = defensive `if settings is None: settings = Settings()` for non-test path, T-100 pattern) / `deps.py` 83% (lines 55+60 = `get_settings` + `get_logger_dep` reserved providers, mirror T-100 `deps.py` 78% with reserved providers for T-110). Verification: ruff/format/mypy(strict) clean (12 source files in services/feature_engine, 113 across the workspace); pytest services/feature_engine 19/19 in 1.83s; pytest repo-wide 444 passed + 16 env-gated skipped + 2 warnings in 5.98s (no regressions; baseline 425 from T-108, +19 net new T-109 tests); `uv lock` regenerated; `uv sync --all-packages --group dev --frozen` confirmed `scalper-v2-feature-engine` installs editable across the workspace. Plan: `docs/plans/T-109.md` (APPROVED 2026-04-26 after one REVISE → fix → re-APPROVE cycle: 8 BLOCKERs all about T-100 mirror-verbatim drift in API shapes — `/health` body `{}`, `/ready` shape `{"ready": ..., "reason": ...}`, `/metrics` ASGI mount mechanism, sync state attach pre-lifespan, `packages.db.create_pool` wrapper, `NatsClient` kwargs, exception trio, `reason="db"` — plus 3 CONCERNs on `make_metrics_asgi_app` import, `LogLevel CRITICAL`, `get_logger_dep _dep` suffix; all resolved by reading T-100 source verbatim before re-submission). Hazards: no §20 hazards directly addressed; T-110 surfaces H-relevant concerns later when it lands the dispatcher.
- [x] T-108: migration 0004 — `features` hypertable per §7.2 + §18.3 (2026-04-26) — closes §7.2 lines 900-915 + §18.3 lines 1174 + 1182-1183 by landing the storage layer for the §8.4 `FeatureUpdate` schema. Unblocks T-110 (feature-engine persist via §9.3 line 1494 ON CONFLICT DO UPDATE) and T-112 (backfill_features upsert). **`migrations/versions/0004_features_hypertable.py`** ships verbatim §7.2 DDL: 7 columns (`feature_name TEXT NOT NULL`, `symbol TEXT NOT NULL` denormalized, `computed_at TIMESTAMPTZ NOT NULL` hypertable time column, `value_num DOUBLE PRECISION` nullable, `value_bool BOOLEAN` nullable, `value_json JSONB` nullable, `source_version TEXT NOT NULL`), composite PK `(feature_name, symbol, computed_at, source_version)` exact-matching the §9.3 line 1494 ON CONFLICT target, hypertable with 7-day `chunk_time_interval`, secondary btree index `features_latest (feature_name, symbol, computed_at DESC)` per §7.2 line 914 verbatim. **`value_num` is `sa.Double()`** (compiles to PG `DOUBLE PRECISION` = 8 bytes / 53-bit mantissa) per §7.2 line 907 verbatim — `sa.Float()` would silently downgrade to PG `real` (4 bytes / 24-bit mantissa) and trip the test_0004 column-shape assertion. plan-reviewer caught this as BLOCKER in the first review round. **§18.3 lifecycle**: 30-day compression policy with `compress_segmentby = 'feature_name, symbol'` (matches the natural per-feature time-range scan pattern used by warmup, scoring snapshots, analytics drilldown) + `compress_orderby = 'computed_at DESC'` (mirrors T-103 precedent for `ohlc_1m`; brief §18.3 line 1182 specifies only segmentby, orderby is opt-in optimisation matching the latest-first query pattern); 180-day retention policy. **`value_*` columns nullable at DB layer** is a deliberate divergence from app-layer "exactly one non-None" — `FeatureValue.__post_init__` (T-106) enforces the invariant in code; T-110 is the only writer and is pre-validated through FeatureValue. **No DB-side CHECK constraint** on `(value_num IS NOT NULL)::int + (value_bool IS NOT NULL)::int + (value_json IS NOT NULL)::int = 1` — would add per-row write overhead without new protection beyond the app-layer guard. **No GIN index on `value_json`** — F1 queries (T-110 dispatch + scoring-engine KV reads) go through the composite PK / `features_latest` btree; brief §7.2 line 914 specifies only `features_latest`. F1+ entry queued for analytics-driven JSONB extraction in F4+ if observed. **Symmetric-stub downgrade per §N8 forward-only**: drops policies (with `if_exists => true`) before `op.drop_table("features")` (cascade-drops the hypertable's chunks per the T-103/0003 precedent — `DROP TABLE` removes the hypertable entry and all chunks; TimescaleDB metadata is cleaned up automatically). **Tests** (1 integration test, 231 LOC, env-gated `POSTGRES_TEST_DSN` per existing migrations conftest): mirrors test_0003 introspection patterns. Verifies (1) column shape via `information_schema.columns ORDER BY ordinal_position` with explicit `_EXPECTED_FEATURES_COLUMNS` tuple (`("value_num", "double precision", "YES")` is the post-hoc gate against the `sa.Double()` storage type decision); (2) composite PK ordinal `(feature_name, symbol, computed_at, source_version)` via `information_schema.table_constraints` JOIN `key_column_usage`; (3) hypertable + 7-day chunk_time_interval via `timescaledb_information.dimensions`; (4) compression `segmentby = ['feature_name', 'symbol']` + `orderby = [('computed_at', False, True)]` (DESC + nulls-first) via `timescaledb_information.compression_settings`; (5) retention + compression bgw_job entries via `_timescaledb_config.bgw_job` proc_name lookup with hypertable resolution through `_timescaledb_catalog.hypertable.table_name = 'features'` (simpler than test_0003 because no continuous aggregate to disambiguate via COALESCE); (6) `features_latest` btree shape via `pg_indexes.indexdef` substring match on `(feature_name, symbol, computed_at DESC)`; (7) end-to-end smoke — 3 INSERTs, one per `value_*` variant (float `70.5`, bool `True`, JSONB `{"upper": 50100.5, "middle": 50000.0, "lower": 49899.5}`) — round-trip via PK with explicit asyncpg JSONB codec `set_type_codec("jsonb", encoder=json.dumps, decoder=json.loads, schema="pg_catalog")` registered before SELECT (asyncpg defaults to raw JSON string; per-connection codec is the right scope, no global codec exists in conftests). plan-reviewer caught the codec requirement as CONCERN in the first review round; (8) `alembic_version = "0004"`. **Cap-relevant LOC**: migration 131 source + test 231 = 362; migrations also qualify under §0.3 generated-artifacts exception per T-100/T-103 precedent. Verification: ruff/format/mypy(strict) clean (1 source file in migrations); pytest 425 passed + 16 env-gated skipped + 2 warnings (no regressions; +1 net new env-gated migration test which exercises live PG in CI-full per ci-full.yml service container). Plan: docs/plans/T-108.md (APPROVED 2026-04-26 after one REVISE → fix → re-APPROVE cycle: BLOCKER on `sa.Float()` → `sa.Double()` for DOUBLE PRECISION storage type drift, CONCERN on asyncpg JSONB codec — both resolved before re-submission). Hazards: no §20 hazards directly addressed; §N8 forward-only Alembic + per-migration `test_NNNN_migration.py` satisfied; §N1 UTC TIMESTAMPTZ on `computed_at`; §N9 brief-fixed lifecycle numbers (7d chunk, 30d compression, 180d retention) intentionally not configurable.
- [x] T-107b: VWAP + Bollinger + MACD built-in indicators + `_sma_seeded_ema_series` helper (2026-04-26) — completes the §9.3 line 1488 six-indicator scope sketched in T-107a's preemptive split. SMA/OI change/funding rate listed in §9.3 remain F1+ (no current consumer). §N4 TDD-for-financial-math active per indicator with hand-computable deterministic fixtures. **`packages/features/builtins/vwap.py`** — `VwapFeature(session="daily", interval="1m")` with cumulative session VWAP: `typical_i = (high_i + low_i + close_i) / 3`, `VWAP = sum(typical_i * volume_i) / sum(volume_i)` over candles within the latest UTC date. Engine (T-110) supplies an arbitrarily-wide window; indicator filters to candles whose `bucket_start.date()` matches the latest candle's UTC date (single session); older candles silently dropped. **Zero-volume edge** raises `FeatureUnderflowError` per §0.4 fail-loud (degenerate input class — VWAP undefined when total_volume=0). `name_template = "ind.{symbol}.{interval}.vwap_session"` matches §B.2 line 3079 verbatim — session enum stays as constructor parameter for forward-compat with F1+ hourly/weekly but is NOT part of the identifier; T-111 registry will carry `params: {session: daily}` alongside the name in the YAML config. Constructor rejects `session != "daily"` (F1+ sessions deferred). `warmup_candles = 1`. **`packages/features/builtins/bollinger.py`** — `BollingerFeature(period=20, std_dev_multiplier=Decimal("2"), interval="15m")` with `middle = SMA(close, period)`, `sigma_pop = sqrt(sum((c - middle)**2) / period)` (population std_dev, divide by `n` not `n-1`, per TradingView convention), `upper = middle + k * sigma_pop`, `lower = middle - k * sigma_pop`. Decimal `.sqrt()` at default 28-digit context preserves precision; tests use 1e-10 tolerance band against irrational sqrt(8). Constructor rejects `period < 2` (degenerate σ) and `std_dev_multiplier <= 0`. `value_json = {"upper", "middle", "lower"}` with Decimal sub-keys; T-110 converts to float at the wire seam. Class docstring documents `value_json` non-hashability per T-106 contract (Mapping/dict instances unhashable). `name_template` includes period + multiplier (e.g. `bollinger_20_2`); multiplier stringified via `format(.normalize(), "f")` so Decimal("2") → "2" (not "2E+0") and Decimal("2.5") → "2.5". `warmup_candles = period`; uses last `period` closes only, regardless of input window length. **`packages/features/builtins/macd.py`** — `MacdFeature(fast=12, slow=26, signal=9, interval="15m")` with standard MACD: macd_line = fast_ema − slow_ema (both SMA-seeded T-107a paradigm); signal_line = SMA-seeded EMA on macd_line series; histogram = macd_line − signal_line. `value_json = {"macd", "signal", "histogram"}` Decimal triple. `warmup_candles = slow + signal − 1 = 34` for default 12/26/9 — operator hand-verified independently before plan submission, plan-reviewer re-verified. Bar-by-bar trace in module docstring: bars 0..25 (26 closes) → first slow_ema at bar 25 (SMA seed); fast_ema series of length 15 (bars 11..25); MACD-line series accumulates over bars 25..33 (9 values total at bar 33 = 34th close, 1-indexed); signal SMA seed of those 9 values → first complete (macd, signal, histogram) triple at bar 33. Constructor rejects `fast < 1`, `slow < 1`, `signal < 1`, and `fast >= slow`. **Module-level helper `_sma_seeded_ema_series(values: Sequence[Decimal], period: int) -> list[Decimal]`** extracted per operator request — returns the EMA value at every bar from index `period - 1` onwards (output length `len(values) - period + 1`). Used by MACD because the per-bar fast/slow alignment requires the SERIES, not just the latest value (single-Decimal helper would force O(n²) iteration). Index-alignment contract documented in helper docstring + inline comment at the slicing site `fast_ema_full[self.slow - self.fast:]` in compute. Helper raises bare `ValueError` (not `FeatureUnderflowError`) on `len(values) < period` — internal-only programmer-error guard, consistent with T-107a constructor `period < 1` raises ValueError pattern. **Golden cross-check test** pins mathematical consistency: `_sma_seeded_ema_series(closes, p)[-1] == EmaFeature(period=p).compute(candles).value_num` — a future change to either's algorithm cannot silently desync MACD's internal EMAs from the public EMA indicator. **Tests** (37 new, ~484 LOC): `test_builtins_vwap.py` (10 cases) covers attributes + 2 weekly/hourly reject cases + underflow + single-bar typical-price + multi-bar same-session hand-computation `(10 + 20 + 30) weighted by (100, 200, 300) → 14000/600` + cross-session UTC-date filter (5 candles spanning 2026-04-25 and 2026-04-26 → result equals VWAP of only Apr-26 session) + zero-volume raises + name_template + protocol; `test_builtins_bollinger.py` (12 cases) covers attributes + period 0/1 reject + std_dev_multiplier 0/-1 reject + custom multiplier 2.5 stringification + underflow + 5-bar period-5 k=2 hand-computation `closes [10,12,14,16,18] → middle=14, variance=8, sigma=√8` (1e-10 tolerance) + constant-prices σ=0 collapse → upper=middle=lower=100 + last-period-window (30 closes, period=5, only last 5 contribute) + name_template + protocol; `test_builtins_macd.py` (15 cases) covers helper underflow + helper output length + helper index alignment + helper period-3 5-value hand-computation `[10,12,14,16,18] → [12,14,16]` + golden cross-check vs EmaFeature for 20-close period-14 + MACD attributes + 3 reject cases (fast/slow/signal=0) + fast >= slow reject + 33-candle underflow + small-MACD hand-computation `fast=2 slow=3 signal=2 warmup=4 closes=[10,12,14,16] → fast=[11,13,15], slow=[12,14], aligned macd_line=[1,1], signal=[1], output {macd:1, signal:1, histogram:0}` + 34-candle warmup boundary returns complete value_json triple + name_template + protocol. Greek σ/α replaced with ASCII `sigma`/`alpha` in docstrings after RUF002 ambiguity warnings. **Cap-relevant LOC**: vwap.py 72 + bollinger.py 77 + macd.py 125 + builtins/__init__.py +6 net = **280 source LOC, under §0.3 cap of 400** (plan estimated ~216; +64 docstring overhead consistent with T-107a precedent ~282 vs ~242 estimate). Tests ~484 (cap-exempt). Coverage 100% on every new module (vwap.py / bollinger.py / macd.py / builtins/__init__.py); protocols.py stays 91% (unchanged from T-106). Verification: ruff/format/mypy(strict) clean (21 features source files, 108 across the workspace), pytest 425 passed + 15 env-gated skipped + 2 warnings in 5.91s (no regressions; baseline 388 from T-107a, +37 net new tests), isolated features suite 92/92 in 1.63s. Hazards addressed: §N4 TDD-for-financial-math active across all three indicators; no §20 hazards directly addressed by indicator math itself. Plan: docs/plans/T-107b.md (APPROVED 2026-04-26 after one REVISE → fix → re-APPROVE cycle: BLOCKER on VWAP name_template drift from §B.2 verbatim + 2 CONCERNs on helper alignment docstring and value_json non-hashability — all three resolved before re-submission).
- [x] T-107a: 3 scalar built-in indicators (EMA/RSI/ATR) + ADR-0002 pyproject promotion + `FeatureUnderflowError` (2026-04-26) — closes §9.3 line 1488 ("Built-in features live in `packages/features/builtins/`: EMA, SMA, RSI, ATR, Bollinger, VWAP, MACD, OI change, funding rate") for the three scalar Wilder/SMA-seed indicators. T-107b ships VWAP/Bollinger/MACD; SMA/OI change/funding rate deferred to F1+ (no current consumer). §N4 TDD-for-financial-math activates: each indicator written test-first against hand-computable deterministic fixtures with the expected intermediate gains/losses/avg/RS/RSI/ATR documented in test docstrings. **`packages/features/errors.py`** — `FeaturesError(ScalperError)` base + `FeatureUnderflowError(FeaturesError, ValueError)`. Multi-inheritance MRO: `[FeatureUnderflowError, FeaturesError, ScalperError, ValueError, Exception, BaseException, object]` — clean C3 linearisation, mypy-strict-clean (single common ancestor in `Exception`). Primary contract = `FeaturesError` (per §5.4 "Don't raise bare ValueError for domain errors"); `ValueError` inheritance documented as **convenience surface** for callers that already expect `ValueError` for argument-shape violations — the typed family stays the primary catch. **`packages/features/builtins/ema.py`** — `EmaFeature(period, interval="15m")` with TradingView/Wilder seed convention: SMA of first `period` closes, then `EMA_t = α * close_t + (1-α) * EMA_{t-1}` with `α = 2 / (period + 1)`. `warmup_candles = period`. **`packages/features/builtins/rsi.py`** — `RsiFeature(period, interval="15m")` with Wilder smoothing: SMA seed of first `period` gains/losses, then `avg_n = (avg_{n-1} * (period - 1) + current) / period`. `warmup_candles = period + 1` (need `period + 1` closes to produce `period` deltas). Defensive flat-market branch (`avg_loss == 0 AND avg_gain == 0`) returns `Decimal(50)` — neutral midpoint of the 0-100 RSI scale, semantically correct for a "no information" signal. Drift-checker validated as legitimate input class (not §0.8 anti-hypothetical), covered by `test_no_movement_returns_50`. **`packages/features/builtins/atr.py`** — `AtrFeature(period, interval="15m")` with Wilder smoothing: SMA seed of first `period` true ranges, where `TR_i = max(high_i - low_i, |high_i - close_{i-1}|, |low_i - close_{i-1}|)`. `warmup_candles = period + 1`. **All three indicators are regular classes** (not frozen dataclasses) per `OhlcBackfill`/`OhlcPipeline` precedent — convention crystallised: frozen+slotted dataclass = immutable value type; regular class = stateful service object. Indicators carry `period`/`interval`/`name_template`/`source_version`/`warmup_candles` as instance attributes set in `__init__`. **Stateless `compute`** — re-processes the entire window from scratch each call. Engine (T-110) holds the rolling buffer; indicators do not cache prior state on the instance. O(N) per indicator per closed candle (N = warmup_candles + small) is negligible CPU at sub-10-bot scale. **`name_template` instance-baked** — `f"ind.{{symbol}}.{{interval}}.<algo>_{period}"` substitutes `{period}` at construction time, leaving only `{symbol}`/`{interval}` for the T-111 registry layer. **Decimal end-to-end** — preserves §7.2 NUMERIC(30,12) precision through compute; T-110 will convert to `float` at the §8.4 wire seam. Default 28-digit Decimal context; no global mutator (§N6). **Underflow → `FeatureUnderflowError`** with explicit message naming the indicator + `period` + minimum candle count + actual count, e.g. `"EMA(period=20) requires >= 20 candles, got 19"`. The engine MUST respect the warmup contract; violation = bug, fail loud per §0.4. **`packages/features/pyproject.toml`** promoted from 5-line virtual stub to full hatch scaffold per ADR-0002 surprise 1 (first multi-component subpackage `builtins/` is exactly the trigger). Verbatim mirror of `packages/db/pyproject.toml` with `dev-mode-dirs = ["."]` retained, sources remap `"" = "packages/features"`, dependencies = `["scalper-v2-core"]`, `[tool.uv.sources] scalper-v2-core = { workspace = true }`. Description updated to reflect target six-indicator scope (T-107b ships the rest in the same phase). `uv.lock` regenerated — `scalper-v2-features` becomes editable-installed across the workspace; no spurious dependency churn. **Tests** (38 new, ~480 LOC): `test_errors.py` (7 cases) covers MRO subclass invariants + str message round-trip + no-arg constructor; `test_builtins_ema.py` (10 cases) covers attribute set per period + period-zero/negative reject + custom interval + underflow + 5-bar EMA(3) hand-computation `[10,12,14,16,18] → SMA seed 12, alpha 0.5, EMA = 16` + warmup-boundary returns SMA seed + Decimal precision at NUMERIC(30,12) floor + name_template substitution + Feature protocol typecheck; `test_builtins_rsi.py` (11 cases) covers attributes + period-zero reject + custom interval + underflow at 14 closes (need ≥15) + 15-close synthetic Wilder fixture with hand-computed `avg_gain=3.34/14, avg_loss=1.40/14, RS=2.385714, RSI=70.46413502109704` (±1e-10 tolerance) + 4-close period-2 one-Wilder-step fixture `[10,12,11,13] → RS=6, RSI=600/7=85.71428...` + all-gains → RSI=100 + all-losses → RSI=0 + flat-market dual-zero → RSI=50 (defensive branch covered) + name_template + protocol; `test_builtins_atr.py` (9 cases) covers attributes + period-zero reject + custom interval + underflow + 5-bar ATR(2) hand-computation `TRs=[3,4,2,3] → SMA seed 3.5, Wilder steps → 2.875` + gap-up TR-dominance (`|high - prev_close|` > `high - low`) + gap-down TR-dominance (`|low - prev_close|` > `high - low`) + name_template + protocol. Test fixture choice: synthetic-with-hand-computed-expected-values rather than canonical Wilder 1978 book fixture (book not in repo, copyright); the synthetic 15-close sequence is reproducible and auditable in PR diff. **Cap-relevant LOC**: errors.py 39 + builtins/__init__.py 20 + builtins/ema.py 55 + builtins/rsi.py 78 + builtins/atr.py 68 + features/__init__.py +8 net + pyproject.toml +27 net = **295 source LOC, under §0.3 cap of 400**. Tests ~480 (cap-exempt). Coverage 100% on every new module (errors.py / ema.py / rsi.py / atr.py / builtins/__init__.py). protocols.py stays 91% (unchanged — ellipsis body of `compute`). Verification: ruff/format/mypy(strict) clean (15 features source files, 102 across the workspace), pytest 388 passed + 15 env-gated skipped + 2 warnings in 5.55s, isolated features suite 55/55 in 1.58s. Hazards addressed: §N4 (TDD-for-financial-math activated). No §20 hazards directly addressed by indicator math itself.
- [x] T-106: `packages/features` skeleton + `Feature` protocol + `FeatureValue` types (2026-04-26) — closes §9.3 r. 1500-1509 by landing the typing contract that T-107 (6 built-in indicators) and T-110 feature-engine will compute against. Pure typing scaffold — no compute logic, no I/O. **`packages/features/types.py`** ships two frozen+slotted dataclasses: `OhlcCandle` (domain candle: symbol, interval, bucket_start, OHLC, volume, source — Decimal across the board to preserve §7.2 `NUMERIC(30, 12)` precision through compute; no `is_closed` field because only closed candles cross the T-110 wire→domain seam) and `FeatureValue` (lean shape: exactly one of `value_num: Decimal | None` / `value_bool: bool | None` / `value_json: Mapping[str, object] | None` is non-None, enforced via `__post_init__`). Metadata (feature_name, symbol, computed_at, source_version) is intentionally **not** carried on `FeatureValue` — `Feature.compute` is a pure compute kernel that doesn't know its own registered name; feature-engine and scoring layers hold metadata alongside the value as needed. **`packages/features/protocols.py`** ships `Feature` Protocol (4 attributes — `name_template`/`source_version`/`interval`/`warmup_candles` — plus sync `compute(candles: Sequence[OhlcCandle]) -> FeatureValue`). **No `@runtime_checkable`** per operator decision: `runtime_checkable` only checks methods, not data attributes, so `isinstance(obj, Feature)` would silently accept implementations missing `name_template`/`interval`/etc. — false sense of security. T-111 plugin discovery does explicit attribute introspection if needed. **`compute` is sync** because it is pure CPU over a `warmup_candles`-bounded window — §5.5 mandates async only for I/O. **5 operator decisions baked into the plan** (`docs/plans/T-106.md`): (1) `value_num=Decimal` preserves NUMERIC(30,12) through compute; T-110 converts to `float` at the §8.4 `FeatureUpdate` publish seam; (2) `OhlcCandle` is a fresh dataclass in `packages/features/types.py` (not reused from `packages.bus.schemas.OhlcCandlePayload` Pydantic or `packages.market.rest.OhlcCandle`) — keeps `packages.features` decoupled from bus and market closures; T-110 maps wire payload → domain candle at the seam (~10 LOC mapping fn there, out of T-106 scope); (3) no `@runtime_checkable`; (4) `value_json: Mapping[str, object]` (read-only abstraction; typed union would prematurely fix the schema — Bollinger upper/middle/lower vs MACD signal/histogram have different shapes); (5) `pyproject.toml` stays virtual stub per ADR-0002 (no `[build-system]`) — no external runtime deps, no other workspace member depends on `scalper-v2-features` yet. Promotion deferred to T-107 (subpackage `builtins/` triggers ADR-0002 surprise 1) or T-110 (first service workspace dep). `pyproject.toml` description string tightened from "Scaffold stub; implementation lands in F1." to reflect T-106 content. **Hashability scope** (plan-reviewer write-time hook): `value_num` and `value_bool` variants are hashable (Decimal and bool both are); `value_json` variant is **not** hashable because `Mapping` instances (typically `dict`) are unhashable — documented behaviour, not a defect. Three positive-path tests cover both hashable variants + a `test_value_json_variant_is_unhashable` documenting the expected `TypeError`. **Tests** (18 new, ~167 test LOC): `test_types.py` (15 cases) covers OhlcCandle frozen + slotted (`__slots__` introspection — frozen+slots dataclasses raise `FrozenInstanceError` on any attribute assignment, so the slot test asserts `__slots__` presence rather than hitting AttributeError on unknown attrs), Decimal precision round-trip at NUMERIC(30,12) precision floor (`Decimal("50000.123456789012")` matching T-104a fixture pattern), value-by-value equality, FeatureValue construction for each of the three variants, all four invalid combinations (zero/two/three non-None) raise `ValueError`, frozen, hashability scope. `test_protocols.py` (3 cases) covers a toy `_AlwaysOneFeature` impl satisfying the Protocol via direct annotation (`feature: Feature = _AlwaysOneFeature()` typechecks under mypy strict), `compute([])` returns expected `FeatureValue(value_num=Decimal("1"))`, and attribute access for the four required Protocol attrs. Cap-relevant LOC: types.py 103 + protocols.py 59 + __init__.py 15 + tests 167 + plan 155 + pyproject.toml description delta = ~500 insertions total; ~177 cap-relevant source under §0.3 cap of 400 (overhead is load-bearing docstring rationale per write-time hooks, not implementation). Coverage: types.py 100% / __init__.py 100% / protocols.py 91% (one branch miss is the Protocol method `...` body, non-executable by design — same pattern as T-104b ohlc.py 98%). Verification: ruff/format/mypy(strict) clean (6 features-package source files, 102 across the workspace), pytest 351 passed + 15 env-gated skipped + 2 warnings in 5.53s, isolated features suite 18/18 in 1.38s. Hazards addressed: none directly — pure typing scaffold. H-007/H-014 are upstream (T-101b/T-102/T-105); §N4 TDD-for-financial-math activates at T-107 with concrete EMA/RSI/etc. compute logic.
- [x] T-105: backfill on startup + reconnect resync via Binance REST `/api/v3/klines` (2026-04-26) — closes §9.2 lines 1464-1471 by plugging the gap between the most recent `ohlc_1m` row (or a 24h cold-start window) and `now` on every successful WS connect — initial + each reconnect — so frames missed during disconnects get repaired. Idempotency rides on T-104a's `insert_ohlc_1m` `ON CONFLICT DO UPDATE`; backfill adds no new external write. **`packages/market/backfill.py`** — `OhlcBackfill` thin coordinator (DI'd `rest`/`pool`/`logger`/`initial_hours`/`per_symbol_pause_seconds`), public `async run_for_symbols(symbols)` method. Per-symbol seriality with a 50 ms inter-symbol `asyncio.sleep` (rate-limit-friendly at sub-10-bot scale, avoids hammering Binance when many symbols all reconnect at once). Page-loop against `/api/v3/klines` (`limit=1000` per page, Binance maximum); cursor advances by `candles[-1].bucket_start + 1m`; empty REST page breaks the loop (no infinite retry). End-time floor `now.replace(s=0, us=0) - 1m` skips the in-progress minute (the WS path delivers it as the `x=true` closed-bucket frame at the next tick — without this floor, REST would yield an under-reported "closed" candle for the un-closed current minute and pollute `ohlc_1m`). Per-symbol `try/except Exception` (NOT bare except, so `CancelledError` propagates and shutdown isn't masked) → log `ohlc_backfill_symbol_error` + skip; remaining symbols still run. **`BinanceWsClient` extension** — single optional `on_connect: Callable[[], Awaitable[None]] | None = None` kwarg on `__init__`; `await self._on_connect()` runs after each successful connect (initial + every reconnect) and after the SUBSCRIBE frame, before the receive loop blocks on `__anext__`. The composition root wires it as `lambda: asyncio.create_task(backfill.run_for_symbols(settings.symbols))` — fire-and-forget so the WS receive loop never stalls on a long REST page-loop. Two concurrent backfill tasks (e.g. fast reconnect during a running fill) are safe because `insert_ohlc_1m` is idempotent on `(symbol, bucket_start, source)` PK; no in-flight lock per the operator decision (premature complexity at H-007 backoff frequencies). **`fetch_latest_ohlc_bucket(conn, *, symbol, source) -> datetime | None`** — read-only anchor query, no `@idempotent`/`@non_idempotent` marker (markers are for external writes per §N3). Returns `max(bucket_start)` for `(symbol, source)`, or `None` on cold-start so the caller falls back to the configurable initial-hours window. **Settings extension** (`services/market_data/app/config.py`): `binance_rest_url: str = "https://api.binance.com"` and `backfill_initial_hours: int = 24` — operator-decided 24h cold-start default seeds caggs with enough history to verify the F1 exit criterion on a fresh deploy; subsequent restarts use `last_bucket+1m` once `ohlc_1m` has rows. T-100's explicit "BINANCE_REST_URL out-of-scope for T-100" docstring lifted now that T-105 is the in-scope task. **Composition root extension** (`services/market_data/app/main.py`): in lifespan, `rest = BinanceRestClient(...)` + `backfill = OhlcBackfill(...)` constructed before `ws` so the `on_connect` callback can close over `backfill`; `app.state.rest` and `app.state.backfill` attached. Shutdown order extends to `pipeline.stop → ws.close → ws_task await → bus.close → rest.close → pool.close` — `rest.close()` (httpx pool drain) runs after `bus.close()` and before `pool.close()` so in-flight backfill tasks resolve their last REST call against the still-open asyncpg pool. Lifespan + shutdown docstrings (load-bearing operational invariants per T-100 precedent) renumbered to reflect the 15-step lifecycle. **Tests** (15 unit + 2 ws + 3 integration = 20 new tests, ~700 LOC): `tests/test_backfill.py` covers cold-start (no rows → start_time = end - 24h, 1440 candles across 2 pages), gap-fill (last_bucket exists → start = last + 1m), no-gap short-circuit, empty REST page breaks loop, page boundary cursor advance, `end_time` excludes in-progress minute, REST error per-symbol isolation, persist error per-symbol isolation (mid-run `_FakeConn.execute` patch via `unittest.mock.patch`), multi-symbol seriality + n-1 inter-symbol sleeps via patched `asyncio.sleep`, `per_symbol_pause_seconds=0` short-circuit, Decimal precision round-trip at NUMERIC(30,12) precision floor, `CancelledError` mid-loop propagates through `except Exception` (not absorbed), empty `symbols=[]` no-op, default-constructor wiring against a `MagicMock`+`AsyncMock` REST. `tests/test_ws.py` extends with `test_on_connect_callback_fires_on_initial_and_each_reconnect` (3 successful connects × 2 outages → callback called 3x) and `test_on_connect_default_none_does_not_raise` (backward-compat). `tests/integration/queries/test_market_data.py` extends with `fetch_latest_ohlc_bucket` returns None on empty / max(bucket_start) / source filter isolates binance vs bybit. `services/market_data/tests/conftest.py` + `test_app_factory.py` extended with `mock_rest` fixture + `BinanceRestClient` patched at the import boundary so existing T-100 lifespan tests still pass; `app.state.rest` + `app.state.backfill` asserted post-startup. **Cap-relevant LOC**: `backfill.py` 210 + queries/market_data.py +27 + market `__init__` +5 + ws.py +13 + config.py +9 + main.py +80 net = **344, under §0.3 cap of 400**. Most of the main.py expansion is docstring (lifespan order renumbered + on_connect contract + backfill section); load-bearing per T-100 precedent. Verification: ruff/format/mypy(strict) clean (101 source files), pytest 333 passed + 15 env-gated skipped + 2 warnings in 5.26s, backfill.py coverage 100%. Hazards addressed: H-007 (on_connect kwarg is additive; existing exp-backoff test unchanged + two new on_connect tests).
- [x] T-100: market-data-svc skeleton — FastAPI + lifespan composition root (2026-04-25) — wires the F1 market-data path: asyncpg pool + `NatsClient` + `BinanceWsClient` (T-101b) + `SubscriptionManager` (T-102) + `OhlcPipeline` (T-104b) via the FastAPI lifespan. Spec: §9.2 service def, §19 F1 bullet 2 endpoint set, §18.1/§18.4 compose+healthcheck, ADR-0002 hatch scaffold. **Composition root** in `services/market_data/app/main.py` uses a **forwarding-closure holder pattern** to resolve the `BinanceWsClient` ↔ `SubscriptionManager` construction-time circular dependency: ws is built with a closure handler reading from a single-element list (`dispatch_holder: list[SubscriptionManager]` local to lifespan scope per §N6), manager is built referencing ws, manager is appended to the holder so the closure resolves to `manager.dispatch`. Microsecond pre-bind window between ws construction and holder append is documented in module doc lines 81-93; warning log `market_data_dispatch_pre_bind` surfaces any future regression that breaks the synchronous ordering invariant — explicit warning to future readers not to "tighten" by adding a lock-until-manager-bound. **Race note for steps 7→8** (module doc lines 56-65): `pipeline.start()` may run before ws CONNECTED — per T-101b contract, `ws.add_stream()` is safe in any state and defers SUBSCRIBE to next reconnect cycle, so no `ws.wait_connected()` needed. **Lifespan order** (load-bearing): pool → bus.connect → ws (with closure handler) → manager → holder.append → pipeline → state attach → `asyncio.create_task(ws.run())` → `pipeline.start(symbols)`. Shutdown reverse: `pipeline.stop` → `ws.close` → ws_task await with 5s `asyncio.timeout` (defensive cancel + `contextlib.suppress(CancelledError)` on timeout, log `ws_task_shutdown_timeout`) → `bus.close` → `pool.close`. Reverse order is load-bearing: `pipeline.stop` must run before `ws.close` so per-symbol task `__aexit__` UNSUBSCRIBE frames land while WS is still open. **`/ready` three-check** (`services/market_data/app/health.py`) — bus.state == CONNECTED, then pool.acquire(timeout=1s), then ws.state == CONNECTED. Reasons bus → db → ws on first failure (cheapest-check-first ordering; precedence asserted by tests). RECONNECTING → 503 `reason="ws"` is the deliberate F1 contract (operator visibility into outages beats grace-period heuristics that mask real Binance downtime); F1+ entry queued for grace-period semantics if LB/k8s restart loops surface during transient Binance outages. **Symbol-seed deviation** (§9.2 line 1454 specifies `bots` JOIN `bot_configs` query but F3's bot registry hasn't shipped): `MARKET_DATA_SYMBOLS` env-var stopgap with `Settings.symbols` property handling empty-string + repeated-separator + whitespace cases via `[s.strip() for s in split(",") if s.strip()]` (8-param parametrize coverage). Empty default → empty list → `OhlcPipeline.start([])` no-op → service stays healthy + ready. Documented as §6.7 procedural deviation in module doc; no ADR per operator decision (would be one-paragraph stub without architectural debate weight; F1+ entry queued for the mechanical swap when F3 lands). **No backfill in T-100** — T-105 (queued, top-of-DAG now) ships REST `/api/v3/klines` startup + reconnect resync; T-100 starts pipeline cold and the first closed-bucket arrives ≤60s after WS connect. **Settings** (`config.py`) mirrors signal-gateway pattern (pydantic-settings + `extra="ignore"`): service_name, log_level (Literal), http_port=8000 (compose service-name DNS isolates from signal-gateway:8000), nats_url, database_url (required), market_data_symbols. Explicitly NOT included per §0.8: `BINANCE_WS_URL`, `BINANCE_REST_URL`, HMAC secrets — out-of-scope for T-100. **DI providers** in `deps.py` (7 providers: pool/bus/ws/subscription_mgr/pipeline/settings/logger) — all read from `request.app.state` with `typing.cast` for mypy. **Dockerfile** two-stage mirror of signal-gateway: same base image (python:3.12.13-slim-trixie), same uv-CLI donor (ghcr.io/astral-sh/uv:0.11.7), same `--package scalper-v2-market-data --frozen --no-dev` install, distinct UID/GID 10002 for `market` user (vs signal-gateway's 10001) for per-service log attribution if F1+ adds host bind mount. **compose.yaml** entry mirrors signal-gateway block: same env_file `secrets.env required: false` pattern, same `depends_on` (postgres healthy + nats healthy + nats-init completed), same healthcheck (`python -c urllib.request` against `/health`); `MARKET_DATA_SYMBOLS: ""` env-stopgap with operator-override docstring; no nginx route (internal-only, no public surface). **Prometheus scrape entry** in `infra/prometheus/prometheus.yml` mirrors signal-gateway's job def. **Tests** (29 tests, 555 LOC): `test_config.py` (4 tests + 8-param symbols-split parametrize covering `""`/`"BTCUSDT"`/`"BTCUSDT,ETHUSDT"`/`"BTCUSDT,,ETHUSDT"`/`",BTCUSDT,"`/`"  BTCUSDT  , ETHUSDT "`/`",,,"`/`"   "`); `test_health.py` (1 test); `test_ready.py` (9 tests — happy + 6 fail-reason cases for bus/db/ws + 2 precedence cases proving bus → db → ws cheapest-first ordering); `test_app_factory.py` (7 tests — factory shape, route registration, /metrics serves default collectors, sync state attach pre-lifespan, async state attach post-lifespan, MARKET_DATA_SYMBOLS → Settings.symbols → OhlcPipeline.start passthrough with `["BTCUSDT","ETHUSDT"]` and empty `[]` cases). `conftest.py` mocks asyncpg pool / NatsClient / BinanceWsClient at the import boundary so the real lifespan exercises end-to-end against fakes; `_FakeWs` `run()` blocks on never-set asyncio.Event mirroring T-101b's "wait until close" loop shape. Helper `_build_app_with_pipeline_capture` extracts the OhlcPipeline-mocked variant so symbols-passthrough tests stay terse (~5 lines per test). `pytest` import noqa-TC002'd in `test_app_factory.py` because pytest fixture resolution introspects type hints at runtime — moving under TYPE_CHECKING would break `monkeypatch: pytest.MonkeyPatch` resolution. **Cap-relevant LOC**: Python source 548 + infra (Dockerfile + compose + prometheus delta) 146 = **694, OVER §0.3 cap of 400**. Accepted per operator decision: main.py docstrings are load-bearing (composition order walkthrough + holder-pattern rationale + race-note + symbol-seed deviation + no-backfill cross-ref encode operational invariants future readers need to maintain the lifespan); §0.3 cap is anti-complexity-creep, not anti-essential-documentation. Dockerfile + compose + prometheus deltas qualify under §0.3 generated-artifacts exception per T-015a precedent. Splitting into T-100a/T-100b creates an arbitrary seam without LOC benefit (T-100b would still be over-cap). Verification: ruff/format/mypy clean (99 source files), pytest 316 passed + 12 skipped + 2 warnings in 5.08s, isolated test suite 29/29 in 1.52s, T-100 module coverage `config.py` 100% / `health.py` 100% / `deps.py` 78% (4 unused providers reserved for T-105 / future metrics) / `main.py` 81% (uncovered: ws_task shutdown-timeout warning path lines 195-201 + service_stopped log lines 250-257 — defensive, not anti-hypothetically fabricated per §0.8). Both jobs green at `9231ef5`: ci-fast #65 27s, ci-full #31 1m 15s. **F1 exit criterion verifiability**: with T-100 deployed and `MARKET_DATA_SYMBOLS=BTCUSDT,ETHUSDT` set, the lab service should accumulate the first `ohlc_1m` row within ~60s of WS connect. T-105 (next on F1 numbered) closes the backfill gap so service restarts and reconnects don't leak missed candles.
- [x] T-104b: `OhlcPipeline` consumer + per-symbol task lifecycle (2026-04-25) — second half of the original T-104 (split from T-104a's pre-flagged contingency triggered by 553-LOC over-cap mid-Write); consumes T-104a's `OhlcCandlePayload` schema + `insert_ohlc_1m` query. Spec: §9.2 OHLC pipeline, §8.2 MARKET_OHLC stream config, §8.4 OHLCCandle payload. **`packages/market/ohlc.py`** ships `OhlcPipeline` — composed by the market-data-svc composition root (T-100, future) with `subscription_mgr` + `pool` + `bus` + `logger` DI'd, no I/O resources owned directly. Lifecycle: `start(symbols)` spawns one `asyncio.create_task` per symbol (each task running `async with subscription_mgr.subscribe(symbol) as feed: async for frame in feed: await self._handle(symbol, frame)`); `stop()` cancels all tasks and awaits them via `asyncio.timeout(stop_timeout_seconds=5.0)` + `asyncio.gather(return_exceptions=True)`. Per-task SubscriptionManager release on context exit decrements refcounts; if no other consumer holds the symbol, the underlying WS streams are torn down (T-102 H-014). **Per-frame classification** by `data.k.x` boolean (Binance kline canonical closed-bucket flag, true on the terminal frame of a minute bucket and false on every prior intra-minute update): `True` → `_persist_then_publish` (`insert_ohlc_1m` via `pool.acquire()`, then `bus.publish` of `market.ohlc.1m.<symbol>` envelope with deterministic uuid5 `Nats-Msg-Id` from `message_id_for_closed_candle`); `False` → `_publish_in_progress` (no DB write, fresh `uuid4` envelope per call so every intra-bucket tick is its own message — server-side dedup would lose intermediate ticks); missing/non-bool `x` or non-kline `e` event or non-dict `k` or missing `data` → log `ohlc_pipeline_malformed_frame` + drop. **Publish-after-persist ordering** with explicit downstream-consumer contract documented in module doc lines 32-46: T-110 feature-engine and any other consumer of `market.ohlc.>` MUST treat the DB as canonical source-of-truth and the NATS stream as a live tap (warmup reads from `ohlc_*` continuous aggregates at startup; bus is low-latency notification only). Persist failure → no publish + log `ohlc_pipeline_persist_error`. Publish failure after persist → DB row stands + log `ohlc_pipeline_publish_error`; downstream recovers via DB read at next warmup. The opposite order (publish-before-persist) would let a phantom candle leak onto the bus that does not exist in DB — backfill cannot repair because the canonical source-of-truth is the DB itself. **Stateless closed-detection** (no per-symbol `last_closed` dict, no in-memory dedup): idempotency rides on the DB PK (T-104a's `ON CONFLICT DO UPDATE`) plus the deterministic uuid5 `Nats-Msg-Id`. Module doc + `_handle` docstring both warn future readers off adding a cache (would be restart-unsafe and race-prone; T-105 backfill is the recovery path for any frames missed during a disconnect). **Per-symbol task error semantics**: handler raises → log `ohlc_pipeline_handler_error` + continue with the next frame (task stays alive); `except Exception` (NOT bare `except`) so `asyncio.CancelledError` propagates through to the `async with` exit and `stop()` can shut the consumer down promptly — explicit comment placed at the per-frame except site so a future "tighten error handling" pass doesn't broaden the catch. No auto-restart in F1 (operator restarts the service). **Symbol set source-of-truth**: callers pass `symbols: list[str]` to `start()`; the pipeline does NOT query DB for the active-symbol JOIN — that lives with the composition root (T-100 lifespan, future), keeping the pipeline pure-class testable. **Subject + correlation_id construction**: `market.ohlc.1m.<symbol>` and `f"ohlc:{symbol}:{bucket_start.isoformat()}"` (typed as `CorrelationId` newtype). `tests/test_ohlc.py` (25 tests, 584 LOC) covers frame classification (closed/in-progress/missing-x/non-bool-x/non-kline-event/missing-data/non-dict-k), publish-after-persist ordering (persist-fail-skips-publish, publish-fail-after-persist-keeps-row, in-progress-publish-fail-no-DB-write), 3-in-progress-then-closed multi-frame sequence with correct `is_closed` flag ordering, out-of-order closed (DB ON CONFLICT contract handles repair, pipeline issues INSERT regardless), Decimal precision flow at NUMERIC(30,12) precision floor (`50000.123456789012` round-trips intact), deterministic uuid5 on closed publishes (matches `message_id_for_closed_candle` exactly) + distinct uuid4 per in-progress tick, subject `market.ohlc.1m.<symbol>` for ETHUSDT route, correlation_id `ohlc:BTCUSDT:2024-04-25T12:00:00+00:00`, start/stop lifecycle (idempotent start, no-op stop-before-start, empty-symbols spawns no tasks, stop cancels cleanly within timeout), per-symbol task isolation (handler error on A doesn't kill B; both publish independently), CancelledError propagation (`stop()` exits within 0.5s on parked consumer — proves `except Exception` not bare), and direct `_consume_symbol` coverage (bad-frame-then-good-frame in the same feed proves per-frame except keeps iteration alive). `_FakeSubMgr`/`_FakeFeed`/`_FakePool`/`_FakeBus` test stubs mirror only the methods OhlcPipeline touches. `MessageEnvelope` kept as runtime import (used as runtime annotation on `_FakeBus.publish` parameter and `_FakeBus.publish_calls` list type — `# noqa: TC001` with rationale comment); pure annotation-only types stay under `TYPE_CHECKING` per project convention. Cap-relevant LOC delta: ohlc.py 335 + __init__.py +4 = 339 (under §0.3 cap of 400, exactly matching the T-104a split-time projection). Coverage on ohlc.py: 98% (uncovered lines 164-165 are the `except TimeoutError` warning log in `stop()` — reliable wedge fixtures fight Python 3.11+ `asyncio.Task` cancel-state semantics where every `await` checkpoint raises CancelledError once `task.cancel()` is called; `task.uncancel()` in a loop still resolves before the 200 ms test budget. The log path remains in production code as the real safety-net for a wedged consumer; not fabricated synthetically per §0.8 — explicit code comment placed at the dropped-test site explains the deliberate gap). Verification: ruff/format/mypy clean (13 source files in `packages/market`, 90 across the workspace), pytest 25/25 in 1.81s for the new file (89/89 across `packages/market`), 287 passed + 12 skipped in 4.65s project-wide (no regressions). Both jobs green at `70a6202`: ci-fast #62 26s, ci-full #28 1m 13s.
- [x] T-104a: `OhlcCandlePayload` schema + `insert_ohlc_1m` query (2026-04-25) — first half of the original T-104 (OHLC pipeline). Pre-flagged contingency split triggered: full pipeline cap-relevant LOC reached 553 mid-Write — schema + query infrastructure (214 LOC) ships now as T-104a, the `OhlcPipeline` consumer (339 LOC) follows on T-104b. Spec refs: §8.4 OHLC payload, §7.2 `ohlc_1m` DDL, §5.10 raw asyncpg, §9.2 market-data-svc. **`packages/bus/schemas/market_ohlc.py`** adds `OhlcCandlePayload` (frozen Pydantic model, Decimal OHLC + volume preserve §7.2 `NUMERIC(30, 12)` precision through wire round-trip — JSON-mode dump emits Decimal as string per Pydantic convention so T-110 reconstructs the same precision; UTC validator + ISO+00:00 serializer mirror `SignalValidated`/`MessageEnvelope`; `interval`/`source` pinned to `Literal["1m"]`/`Literal["binance"]` because T-104 publishes 1m only — higher timeframes are read from Timescale caggs, not the NATS stream) and `message_id_for_closed_candle(symbol, bucket_start)` — `uuid5` over a freshly-minted namespace UUID (`c7c1de13-b8e6-4ad6-9b21-7b7e6e5e0a9a`) for deterministic `Nats-Msg-Id` on closed-candle re-publishes. UTC enforced on the helper input too so callers cannot smuggle a naive bucket through. In-progress publishes will use envelope's default `uuid4` (every tick is its own message; server-side dedup would lose intermediate ticks). Forward-compat groundwork: MARKET_OHLC stream does not yet set `duplicate_window` — F1+ entry queued. **`packages/db/queries/market_data.py`** adds `insert_ohlc_1m` raw-asyncpg helper with `INSERT ... ON CONFLICT DO UPDATE` on PK `(symbol, bucket_start, source)`. **DO UPDATE not DO NOTHING** rationale: T-105 backfill via REST `/api/v3/klines` is the canonical truth source, so a WS-stored row corrupted by a future bug must be repairable; closed-bucket Binance frames are deterministic so the typical re-write is a no-op against identical values (one WAL record/min/symbol — negligible at scale). Genuinely idempotent (same inputs → same row state) — no `@non_idempotent` marker, differs from `insert_signal` which returns a fresh `id` per call. Caller (T-104b `OhlcPipeline`) wraps in try/except so transient PG errors are logged + dropped rather than killing the per-symbol consumer task; T-105 fills any gap. Active-symbol-set lookup (`bots`+`bot_configs` JOIN) is **not** in this query module — that lives with the composition root (T-100 lifespan, future), pipeline accepts `symbols: list[str]` at start. **Layout fix**: `tests/__init__.py` added (empty) to extend pytest's conftest-walk to the repo root for tests under `tests/`, mirroring the working `services/__init__.py` chain. Diagnosed only after the new integration test failed at collection with `ModuleNotFoundError: No module named 'packages'` — the existing `tests/integration/__init__.py` implied the chain was meant to continue but `tests/__init__.py` was missed. The signal-gateway integration test sidesteps this because its walk reaches `services/__init__.py`. No regression in existing `tests/grafana`, `tests/integration/migrations`, or `services/signal_gateway/tests/integration` trees (all 12 env-gated skips fire correctly post-fix). `packages/bus/tests/test_market_ohlc_schema.py` (15 unit tests, 213 LOC) covers field defaults / pinned-Literal rejection (`interval="5m"` / `source="bybit"` / `schema_version="2.0"` all raise) / UTC enforcement on `bucket_start` (naive rejected, CEST rejected, zero-offset aware normalised to `datetime.UTC`) / frozen / Decimal precision JSON round-trip at 12 fractional digits / `+00:00` wire form (not bare `Z`) / `message_id_for_closed_candle` determinism + UUID v5 + cross-helper namespace isolation against `signals.message_id_for` (guards against accidental shared NS in a future refactor) + UTC contract on the helper input. `tests/integration/queries/test_market_data.py` (8 integration tests, 229 LOC, env-gated `POSTGRES_TEST_DSN`) covers single-row INSERT with `NUMERIC(30, 12)` precision survival, ON CONFLICT DO UPDATE last-write-wins value-level assertion (catches a regression to DO NOTHING), identical-values re-insert no-op behaviour, and distinct PK tuples — symbol/bucket_start/source — each create independent rows. New `tests/integration/queries/conftest.py` mirrors `tests/integration/migrations/conftest.py` (throwaway DB + `alembic upgrade head` per test) with TODO note for future fixture unification when a third integration sub-tree appears (lift to `tests/integration/conftest.py`). Operator design-review flag dispositions resolved during write: (a) bookTicker → market.ticks deferred to F1+ entry (no §8.4 MarketTick schema design yet, no real consumer); (b) MARKET_OHLC `duplicate_window` deferred to F1+ entry (current dedup belt-and-braces via DB ON CONFLICT + downstream `(symbol, bucket_start)` keying suffices for F1). Cap-relevant LOC: schemas/market_ohlc.py 112 + queries/market_data.py 96 + schemas/__init__.py +6 = 214 (under §0.3 cap of 400). `tests/__init__.py` (empty) and `tests/integration/queries/__init__.py` (empty) are package-marker files, not cap-relevant. Verification: ruff/format/mypy clean (85 source files), pytest 262 passed + 12 skipped + 2 warnings in 4.58s, new unit suite 15/15 in 1.30s, new integration suite 6/6 skipped at module-level via env-gate (will exercise live PG in CI-full). Both jobs green at `ee1e977`: ci-fast #59 28s, ci-full #25 1m 20s.
- [x] T-102: `SubscriptionManager` refcount + H-014 hazard test (2026-04-25) — `packages/market/subscription.py` ships `SubscriptionManager` (refcounted facade over `BinanceWsClient` per H-014), `SymbolFeed` (per-subscriber async iterator over Binance multiplex frames), and the private `_Subscription` async-CM. Public API: `async with mgr.subscribe("BTCUSDT") as feed: async for frame in feed: ...` — 0→1 transition issues `add_stream` for the §9.2 line 1455 pair (`btcusdt@kline_1m` + `btcusdt@bookTicker`) on the underlying `BinanceWsClient`; 1→0 issues `remove_stream` for both kinds and signals `StopAsyncIteration` on the surviving feed. Per-symbol counter under a single `asyncio.Lock` (one lock per manager — fewer locks, lower contention at sub-10-bot scale beats per-symbol granularity). Lock held across `dispatch` enqueue (sync `put_nowait`, no `await` under the lock) so a concurrent release cannot leak a real frame past EOF in the survivor's queue. Symbol shape validated synchronously at `subscribe()` call site (uppercase ASCII alphanumeric — rejects `btcusdt`, `BTC.P`, `BTC USDT`, non-ASCII; accepts `BTCUSDT`, `1INCHUSDT`, `1000PEPEUSDT`); fail-fast at the call site instead of producing a malformed Binance stream name that Binance silently ignores. **Queue policy** (single-module decision, no ADR per §0.6 — sensible default + escape hatch via `feed_maxsize` constructor arg): bounded `asyncio.Queue` per feed (default `maxsize=1024`), drop-oldest on overflow, optional `subscription_feed_overflow_total{symbol}` counter (Prometheus Counter, injected by composition root) + `subscription_feed_overflow` warning log. T-104 (OHLC pipeline) treats the contract as "may drop intermediate frames under back-pressure"; closed-bucket detection rides on the next in-progress frame's monotonic `bucket_start` regardless. **Reconnect interaction**: during a WS outage `BinanceWsClient` is RECONNECTING and dispatches no frames; feeds silently block on `__anext__` until reconnect resumes traffic — no spurious EOF, no exception. Documented "feed silence ≠ unsubscribed" so callers needing liveness pair the `async for` with `asyncio.timeout`. **Symbol resolution boundary**: SubscriptionManager takes canonical symbols (`BTCUSDT`) and translates to Binance stream names internally; T-008/T-010 `symbol_map` does NOT touch this layer — that translation lives upstream in signal-gateway (TradingView aliases → canonical Bybit). Lifecycle: SubscriptionManager is **injected** with a `BinanceWsClient` instance (N6 — no globals, DI via constructors); the service composition root (T-100 `market-data-svc.main.py` lifespan) creates the WS client, creates SubscriptionManager bound to it, wires `manager.dispatch` as the WS handler, then runs `asyncio.create_task(ws.run())` independently. `tests/test_subscription_manager.py` (32 tests, 477 LOC) covers symbol-shape validation (6 reject + 4 accept params), refcount transitions (0→1, 1→2, 2→1, 1→0, distinct-symbol independence), **H-014 hazard test** `test_refcount_sub_survives_one_caller_releasing` (verbatim §20 name) — two callers acquire `BTCUSDT`, A releases, asserts (a) `ws.streams` still contains both stream names, (b) `ws.remove_calls == []`, (c) released feed yields `StopAsyncIteration`, (d) survivor receives subsequently-dispatched frame, (e) final 1→0 fires on survivor's exit; dispatch fan-out + cross-symbol isolation + ack-frame ignore (4-param `{"result": null, "id": N}` / non-`@` stream / missing stream / non-string stream); kline+bookTicker pair both land in feed; `stream_kinds` constructor override reduces pair to singleton; 10-way concurrent `asyncio.gather` `__aenter__` issues exactly one `add_stream` per kind; release-during-dispatch lock-serialisation race; EOF on parked `__anext__` consumer + clean `async for` exit; overflow drop-oldest preserves freshest frame + counter increment with `{"symbol": "BTCUSDT"}` label; `overflow_counter=None` no-op path; structural smoke that `BinanceWsClient` still exposes `add_stream`/`remove_stream` (T-101b/T-102 seam tripwire). `_FakeWsClient` test stub mirrors only the methods SubscriptionManager touches (`add_stream`/`remove_stream`/`streams`); `# type: ignore[arg-type]` at construction (structural compat without nominal subclassing — same pattern T-101b uses for `_FakeWs` vs `ClientConnection`). Operator design-review flags resolved during write: (a) double-exit test dropped per §0.8 (no concrete `async with` path triggers `__aexit__` twice); (b) symbol shape validation added as one-predicate-line; (c) EOF-via-`StopAsyncIteration` contract documented in `SymbolFeed` docstring so callers don't catch `BinanceWsError` expecting feed end. Cap-relevant LOC delta: subscription.py 299 + __init__.py +6 = 305 (under §0.3 cap of 400, despite docstring-heavy module covering H-014 contract + queue policy + reconnect interaction + symbol vocabulary). Coverage on subscription.py: 92% (uncovered branches are the EOF-on-full-queue overflow recovery in `_signal_eof` and the `_release` early-return on count==0 double-release path — defensive, not anti-hypothetically fabricated per §0.8). Verification: ruff/format/mypy clean (11 source files in `packages/market`, 82 across the workspace), pytest 32/32 in 1.78s for the new file (63/63 across `packages/market`). Both jobs green at `ef6f903`: ci-fast #56 22s, ci-full #22 1m 12s.
- [x] T-101b: Binance WS client + H-007 reconnect (2026-04-25) — `packages/market/ws.py` ships `BinanceWsClient` (long-lived public-market-data WS connection driving a caller-supplied async handler per JSON frame) + `ConnectionState` enum (DISCONNECTED → CONNECTING → CONNECTED ⇄ RECONNECTING → CLOSED), packaged with the H-007 reconnect contract: exponential backoff 1s→60s with full jitter via `exp_backoff_delays` from T-101a; the loop re-subscribes the current stream set on every successful reconnect; long-disconnect signal (structured `binance_ws_long_disconnect` log + `long_disconnect_count` counter) increments once per outage when elapsed disconnect time crosses the configurable 60s threshold and resets on successful reconnect so a later outage can fire again. `add_stream`/`remove_stream` optimistically mutate the internal stream set: SUBSCRIBE/UNSUBSCRIBE frame sent inline when CONNECTED, otherwise the change takes effect on the next reconnect; idempotent on duplicates/unknowns; no ack tracking (Binance's `{result: null, id: N}` is informational, subscription liveness observable via incoming data). Refcounting is NOT in this layer — T-102 `SubscriptionManager` layers refcounts on top per H-014. `tests/test_ws.py` (14 tests, 523 LOC) covers state-machine edges (connect, reconnect, close, run() re-entry guard, idempotent close), initial subscribe sorted, JSON-decode failure raises `BinanceWsError`, add/remove_stream connected vs disconnected paths, **H-007 hazard test** `test_ws_reconnect_uses_exponential_backoff` scripts 8 consecutive `ConnectionClosedError` failures + a terminal `_FakeWs`, captures `_async_sleep` delays, asserts each `delay[i]` falls within `[0, min(2**i, 60)]` envelope (jitter respects ceiling — robust to RNG drift while still proving the H-007 backoff curve), long-disconnect single-fire latch + reset-after-reconnect, reconnect re-subscribes `self._streams`. ws.py `_async_sleep` rename: `from asyncio import sleep as _async_sleep` so tests can patch reconnect timing without mutating global `asyncio.sleep` that test code's own event-loop yields rely on; production behavior unchanged. `_FakeWs` defaults to hang-on-exhaust via `asyncio.Event` (single-connection tests don't run off scripts); reconnect-flavored tests pass `end_with=ConnectionClosedError(...)` for server-disconnect simulation. Cap-relevant LOC delta: ws.py 227 + __init__.py +6 = 233 (under §0.3 cap). Verification: ruff/format/mypy clean (80 source files), pytest 31/31 in 0.36s. Both jobs green at `1bd2c8f`: ci-fast 25s, ci-full 1m 10s.
- [x] T-101a: `packages/market` scaffold + Binance REST client + exp backoff (2026-04-25) — `packages/market/{__init__,errors,backoff,rest}.py` ships the workspace member with `BinanceRestClient.get_klines` (only endpoint with a concrete F1 consumer per §0.8 — `get_exchange_info` deferred until T-102/T-105 surfaces a real need; brief §9.2 cites bots+bot_configs DB join as the active-symbol source-of-truth, not exchange_info), `OhlcCandle` dataclass aligned with §7.2 `ohlc_1m` schema (Decimal prices/volume preserve precision across Binance's string-encoded numerics → asyncpg's NUMERIC(30,12) Decimal mapping), `MarketError` taxonomy rooted at `packages.core.ScalperError` (`BinanceRestError` carries `api_code`/`api_message` parsed from Binance JSON `{code, msg}` body when present; `BinanceWsError` pre-defined here so T-101b extends usage without churn), `exp_backoff_delays()` generator with full jitter (AWS recipe: `delay_n = uniform(0, min(base * 2**n, cap))`, base=1s cap=60s) — hand-rolled per §0.9 (no `backoff`/`tenacity` dep — single use case, integration overhead of an external lib outweighs ~30 LOC saved). `tests/test_backoff.py` (10 tests: envelope bound, ceiling cap saturation, indefinite generation, RNG determinism, parametrized property test) + `tests/test_rest.py` (7 tests via `httpx.MockTransport`: kline parsing → OhlcCandle, time-window/limit serialization, default limit, naive datetime → UTC, 4xx with API body, 5xx without JSON, transport error chaining). Split rationale: T-101 cap-relevant LOC reached 620 with ws.py included (~55% over §0.3 cap; no migration exception applies to packages code). Natural seam between REST and WS subsystems with distinct hazard profiles (REST = transport-error mapping; WS = H-007 reconnect + state machine). T-101a final cap-relevant LOC: 392 (cap 400, comfortable). T-101b lands ws.py + H-007 hazard test on a follow-up branch. Verification: ruff/format/mypy clean (78 source files), pytest 17/17 in 0.31s, ci-fast 24s + ci-full 1m 2s green at `a949329`. §0.9 dep justifications: `websockets==15.0.1` first runtime use of brief §3.1-named WS lib (T-101b first consumer; promoted to runtime now to avoid lock churn); `httpx==0.28.1` already in workspace dev-group from T-015a, promoted to runtime for `BinanceRestClient.get_klines` per same §3.1 mandate.
- [x] T-103: migration 0003 — `ohlc_1m` hypertable + 5 continuous aggregates (5m/15m/1h/4h/1d) (2026-04-25) — `migrations/versions/0003_ohlc_1m_continuous_aggregates.py` ships the §7.2 hypertable that `market-data-svc` (T-104) writes to plus 5 caggs that `feature-engine` (T-110) and `backfill_features` (T-112) read from. Real-time mode (no `materialized_only=true`) so warmup/backfill queries see unmaterialized recent buckets. §18.3 lifecycle on `ohlc_1m`: 180d retention + 30d compression with `segmentby='symbol, source'`, `orderby='bucket_start DESC'`. Each cagg gets an explicit 180d retention policy — caggs have their own internal hypertables that don't cascade-drop. Per-cagg refresh tuning scales with bucket size (`schedule_interval`: 1m for 5m/15m, 5m for 1h, 15m for 4h, 1h for 1d; `start_offset`: 1d/1d/1d/2d/7d; `end_offset`: 1m uniform). `tests/integration/migrations/test_0003_migration.py` asserts column shape + composite PK + 7d chunk_time_interval + compression segmentby/orderby + 5 cagg presence + per-cagg refresh schedule/start/end via SQL `interval` cast + retention on ohlc_1m + all 5 caggs (12 bgw_jobs total) + end-to-end smoke (insert candle → manual cagg refresh → query 5m → row materialized). Erratum on T-011 surfaced: `test_0002_migration.py` asserted `version == "0002"` (now stale) — fixed to permissive `version is not None` mirroring test_0001 pattern (commit `2d669e4` precedes T-103 commit `1775cc5`). Both jobs green: ci-full 1m 8s (integration step 11s — confirms `POSTGRES_TEST_DSN` propagation, no module-level skip), ci-fast green.
- [x] T-019: Populate F1 backlog (2026-04-25) — TASKS.md restructure adding 13 numbered F1 tasks (T-100..T-112) under `### F1 numbered` and 7 opportunistic items under `### F1+ opportunistic` (4 carried from F0 + 3 new: per-bot HMAC, Alembic mako template, prometheus-nats-exporter sidecar). F1 bullet 1 (full signal-gateway) absorbed into F0 (T-015a..T-015b2b) — all §9.1 pipeline + §19 F1 exit criterion "TV webhook → validated `signals` row + correct symbol mapping" met by F0; per-bot HMAC selection per §9.1 ADR-0001 topic moved to T-F1+ opportunistic, not a phase blocker. F0 phase-gate review (per §0.10 "Phase N unlocked" marker) is operator action — T-019 does not flip the phase header.
- [x] T-018: Release workflow (`.github/workflows/release.yml`) (2026-04-25) — Per-service Docker image build + push to GHCR on `v*` tag pushes and via `workflow_dispatch`. Service matrix hardcoded to `[signal_gateway]` (only F0 service with a Dockerfile); F1 service-ship tasks append in the same PR that lands their Dockerfile. Image tags: `<git-tag>` + `sha-<short>`; no `:latest` (deploy script signature is `./scripts/deploy.sh <git-sha>`, prerelease ambiguity deferred). `docker/setup-buildx-action@v3` for the Dockerfile's `--mount=type=cache` + GHA cache backend (`cache-from/to: type=gha,mode=max`). Concurrency `cancel-in-progress: false` (release builds are commit-of-record artifacts). First `workflow_dispatch` on master green: 39s wall-time, build step 19s, cold-cache 0% as expected, no 403 on `packages: write` (default workflow permissions sufficed). Manifest verified at `ghcr.io/lusterier/scalper-v2/signal_gateway:sha-9dae4b5` (HTTP 401 + `WWW-Authenticate: Bearer ... scope="repository:lusterier/scalper-v2/signal_gateway:pull"` confirms exists + private). Package visibility kept private — matches repo privacy, no external pull need at single-dev flow. T-F1+ backlog entry added: bump docker/* actions when upstream publishes Node 24-compatible majors (Node 20 EoL: 2026-06-02 default flip, 2026-09-16 hard cutoff).
- [x] T-017: Dashboard test harness stub — `tests/grafana/` (2026-04-25) — `tests/grafana/{__init__,conftest,test_dashboard_queries}.py` validate every PromQL expression in `infra/grafana/dashboards/*.json` against `/api/v1/query` on a zero-data Prometheus; `status="success"` proves syntax + arity + type-check, no scrape coverage required (metric-name typo detection deferred to F4+ graduation). Module-level skip on `PROMETHEUS_TEST_URL` unset keeps ci-fast collection clean — `parametrize()` never executes when env-gated out. `discover_dashboard_queries()` yields `(dashboard, panel_id, ref_id, expr)` tuples for parametrize; failure messages cite all four for triage. ci-full.yml gains a `prom/prometheus:v3.11.2` service container (matches `compose.yaml:104`, default CMD self-scrape, no `infra/prometheus/prometheus.yml` bind-mount), `PROMETHEUS_TEST_URL=http://localhost:9090` env, and `tests/grafana` appended to the integration-job pytest paths. T-F1+ backlog entry added: align migrations conftest env-gate from fixture-level (no-op `allow_module_level=True` flag) to module-level skip pattern. Both jobs green at `9d5600a`: ci-full 1m 12s (overview.json: 2/2 cases — `sum(up)`, `up`), ci-fast 27s.
- [x] T-016: CI-full workflow (`.github/workflows/ci-full.yml`) (2026-04-25) — integration + security stages on every push/PR. Hybrid container strategy: PG via GH Actions service container (env-driven config, GH-managed health gating); NATS via `docker run` because GH services cannot supply CMD args and JetStream enable lives in `infra/nats/server.conf` (`-c <path>` required). Bootstrap step reuses production `infra/nats/bootstrap.sh` + `infra/nats/streams/*.json` verbatim via nats-box, so SIGNALS topology is identical to prod and dev. Integration job runs `tests/integration` + `services/signal_gateway/tests/integration` with `POSTGRES_TEST_DSN` + `NATS_TEST_URL` set; coverage.xml uploads via pyproject `addopts`. Security job: `pip-audit --skip-editable --ignore-vuln CVE-2026-3219` + `bandit -r services packages`. Pytest stack bumped (`pytest~=9.0`, `pytest-asyncio~=1.0`, `pytest-cov~=7.0`) to clear CVE-2025-71176; CVE-2026-3219 (pip 26.0.1) suppressed pending upstream fix. Both jobs green at `c224f84`: integration 1m 1s, ci-fast 27s.
- [x] T-015b2b: `signal-gateway` `/webhook` handler + integration tests (2026-04-25) — `app/webhook.py` orchestrates the §9.1 13-step pipeline (raw body → HMAC → `signals.raw` publish → JSON parse → `idempotency_key` peek → Pydantic validate → `signals_received` increment → `bind_correlation` → dedup → symbol resolve → `insert_signal` → `signals.validated` publish with `message_id_for` → 200), `app/main.py` `include_router(webhook_router)`, `tests/test_models.py` (16 tests for `SignalEnvelope` + response models), `tests/test_webhook.py` (13 per-branch handler tests with `insert_signal` patched at import site), `tests/integration/{conftest, test_webhook_e2e}.py` (env-gated `POSTGRES_TEST_DSN` + `NATS_TEST_URL`; happy path + duplicate edge), `docs/modules/signal_gateway.md` flipped to present tense + new "Pipeline wire order" + "Response codes" + "Log event catalog" sections. Compose smoke green: `200 {"signal_id": 1}` + `signals_received_total{source="smoke"} 1.0` + `signals_validated_total{status="validated"} 1.0`. Hazards addressed: H-010.
- [x] T-015b2a: `signal-gateway` primitive wiring + metrics + middleware (2026-04-25) — `app/metrics.py` four §15.3 counters / histogram via `build_signal_gateway_metrics(registry)` (T-015a `build_registry` untouched per §0.8), `app/models.py` `SignalEnvelope` (with `model_validator(mode="before")` extras migration) + three response models, `app/middleware.py` `RateLimitMiddleware` (path-gated `POST /webhook`; canonical observation point for `webhook_processing_seconds` SLO histogram), `app/deps.py` six new providers, `app/main.py` lifespan / sync state-attach split. `test_app_factory.py` extended with state-attach + middleware-presence + functional middleware-order verification + 404 on `/webhook` pre-T-015b2b. Hazards addressed: H-006.
- [x] T-015d: `insert_signal` marked `@non_idempotent` (§N3 / §5.8) (2026-04-25) — erratum on T-015b1; query landed without the idempotency classification, T-015b2 handler adds four no-retry call sites so the marker question could no longer wait. Decorator is marker-only, no behaviour change.
- [x] T-015c: .dockerignore to exclude build-cache from docker context (2026-04-24) — hotfix for T-015a residue; `packages/__pycache__/` from local pytest/mypy runs was leaking into docker build context and tripping uv workspace glob.
- [x] T-015b1: `signal-gateway` primitives — `verify_hmac`, `RateLimiter`, `DedupRing`, `SymbolMapCache`, `SignalValidated` schema + `message_id_for` helper (`packages.bus.schemas.signals`), `packages.db.queries.signal_gateway` (`fetch_symbol_mapping` + `insert_signal`), Hypothesis property test for `DedupRing`. (2026-04-22)
- [x] T-015a: Hello-world `signal-gateway` — FastAPI skeleton with `/health`, `/ready`, `/metrics`. Spec: §9.1, §19 F0 bullet 8 (partial). (2026-04-22)
- [x] T-014: Docker Compose — nginx reverse proxy + Cloudflare Tunnel (`cloudflared`) (2026-04-22)
- [x] T-013b: Overview dashboard — stat + table panels over the up metric (2026-04-21)
- [x] T-013a: Prometheus + Grafana services with provisioning (2026-04-21)
- [x] T-012: Docker Compose — NATS JetStream service + stream bootstrap (2026-04-21)
- [x] T-011: Migration 0002 — `signals` hypertable (2026-04-20)
- [x] T-010: Alembic setup + migration 0001 — bots, bot_configs, symbol_map + seed (2026-04-20)
- [x] T-009: Docker Compose — PostgreSQL 16 + TimescaleDB service (2026-04-20)
- [x] T-008b: `packages/bus` — `NatsClient` wrapper (2026-04-20)
- [x] T-008a: `packages/bus` — MessageEnvelope + error taxonomy scaffold (2026-04-20)
- [x] T-007: `packages/db` — asyncpg pool factory + query helper skeleton (2026-04-19)
- [x] T-006: `packages/observability` — structlog JSON, trace/correlation IDs, Prometheus registry factory, secret redactor (2026-04-19)
- [x] T-020: Workspace build-system (hatchling) + ADR-0002 + CI-fast --all-packages (2026-04-19)
- [x] T-005: `packages/core` — domain types, errors, markers, now_utc (2026-04-19)
- [x] T-004: ADR-0001 — NATS JetStream (2026-04-19)
- [x] T-003: CI-fast workflow (2026-04-19)
- [x] T-002: Root tooling config (2026-04-19)
- [x] T-001: Monorepo scaffold (2026-04-19)

## Next (do not start without operator approval)

(F1 closed; F2 unlocked 2026-04-26 per master HEAD `cf4b465`; T-200 closes the F2 backlog populate. Top-of-DAG F2 tasks ready for operator pick: **T-201** ExchangeClient protocol + errors + types, **T-202** migration 0005 orders+trades+executions, **T-204** migration 0007 trading_events, **T-210** DedupingConsumer, **T-214** execution-service skeleton — no F2-internal deps among these five; any can start.)

## Backlog

F1 bullet 1 (full signal-gateway) absorbed into F0 (T-015a..T-015b2b); per-bot HMAC selection per §9.1 ADR-0001 topic moved to `### F1+ opportunistic` below, not a phase blocker.

### F2 numbered

- [x] T-209: BybitV5Adapter private WS (2026-04-30) — F2 seventeenth numbered work task; **eighth F2 task with active math-validator gate** (Gate 4 ACTIVE per CLAUDE.md gate-4 conditional rule — staged diff touched `packages/exchange/`; verdict VERIFIED — H-015 round-trip via `Decimal(str(...))` for execPrice/execQty/execFee/size/avgPrice/unrealisedPnl in both `_map_execution_event` + `_map_position_event`; HMAC payload `f"GET/realtime{expires}"` hand-verified §F.1 with `expires=1700000005000`, sig=64-char SHA256 hex; `int(execTime)/1000` ms→sec acceptable per `datetime.fromtimestamp(float, tz=UTC)` documented contract; backoff envelope `[0, min(2**n, 60)]` test-pinned via `_full_jitter_backoff_delays` 10-attempt assertion; no Decimal silent casts; no `getcontext().prec`/`localcontext`/rounding-mode globals; W#3 narrow `not in ("", None)` check correct on avgPrice/unrealisedPnl). Replaces 3 NotImplementedError stubs from T-208a per stub-pin precedent — completes BybitV5Adapter ExchangeClient Protocol surface (10 of 10 methods: 4 writes T-208a + 3 reads T-208b + 3 streams/close T-209). T-209 deltas: `packages/exchange/bybit_v5/ws.py` (NEW ~340 LOC; `BybitV5PrivateWs` class owning auth handshake + full-jitter exp backoff 1s→60s per H-007 + frame demux to per-stream `asyncio.Queue` + idempotent close); `packages/exchange/bybit_v5/adapter.py` (extension +10 net LOC; ctor adds `ws: BybitV5PrivateWs` kwarg between `client` + `limiter`; 3 stub bodies replaced with `return self._ws.executions()` / `return self._ws.positions()` / `await self._ws.close(); await self._client.close()` ordered close per T-209 plan Decision #8; `_stub_message` helper removed). `packages/exchange/bybit_v5/__init__.py` (+1 line; export `BybitV5PrivateWs`); `packages/exchange/pyproject.toml` (+6 lines; `websockets==15.0.1` runtime dep with §0.9 justification — workspace re-use vs `packages/market` Binance public WS, alternatives `aiohttp.ws`/`httpx-ws` rejected). **Auth handshake**: `_gen_auth_frame` constructs `{op:"auth", args:[api_key, expires, hmac_sig]}`; `expires = int((now_fn() + 5) * 1000)` per Bybit V5 recv-window margin; signature = `hmac.new(api_secret.encode("utf-8"), f"GET/realtime{expires}".encode("utf-8"), hashlib.sha256).hexdigest()`; v1 `bot/private_ws.py` recipe ported. **Reconnect loop**: full-jitter backoff `uniform(0, min(_BACKOFF_BASE_S * 2**attempt, _BACKOFF_CAP_S))` per attempt; module-private `_async_sleep = asyncio.sleep` patchable seam mirrors `packages/market/ws.py` (Write-time guidance #1 — without it `test_reconnect_uses_exponential_backoff_with_full_jitter` would burn ~13s of cumulative jittered delay in real wall-time); catch tuple `(WebSocketException, OSError)` (Write-time guidance #2 — covers TLS/DNS/OSError variants); auth re-handshake on every reconnect cycle per §11.2 verbatim; `disconnect_started_at` timer + `long_disconnect_count` single-fire latch at 60s threshold mirror `BinanceWsClient` (T-101). **Subscribe frame**: `{op:"subscribe", args:["execution", "position"]}` (no `order` topic — informational, not consumed by ExchangeClient consumers; T-218 dispatcher reads execution + T-217 PositionLifecycle reads position). **Frame demux**: `topic == "execution"` → enqueue ExecutionEvent; `topic == "position"` → enqueue PositionEvent; per-data-item `category != "linear"` filter drops non-linear (F2 single-bot scope = linear futures only); auth ack / pong / subscribe-ack frames silently skipped (no topic). **H-009 no-dedup at adapter**: `test_duplicate_execId_frames_both_appear_in_executions_iterator` pins that adapter emits raw events including duplicates — T-218 dispatcher dedups via `DedupingConsumer` ring 10k. **Auth failure handling**: `success:false` → `auth_failure_count += 1` + ERROR log + reconnect loop continues (transport-level reconnect does NOT increment counter — pinned by `test_transport_reconnect_does_not_increment_auth_failure_count` per Write-time guidance #7). **Heartbeat**: app-level `{op:"ping"}` JSON frame every 18s + native `ping_interval=20, ping_timeout=10` transport-level backstop (Bybit V5 expects JSON ping; native PING control may not satisfy server idle window). **AsyncIterator factories**: `def executions(self) -> AsyncIterator[ExecutionEvent]: return self._iter_executions()` (`def` NOT `async def` per T-201 OQ-1, mirror `PaperExchange.stream_executions` shape); `_iter_executions` is async generator yielding from unbounded `asyncio.Queue.get()` (unbounded per OQ-2 default A — bounded + drop violates H-009 silent-loss; PaperExchange's bounded queue acceptable in-process, live WS not). **§N9 evaluation**: ctor kwargs `ws_url`, `ping_interval_s`, `long_disconnect_threshold_s` operator-tunable; module constants `_BACKOFF_BASE_S=1.0`, `_BACKOFF_CAP_S=60.0`, `_AUTH_PAYLOAD_PREFIX="GET/realtime"`, `_AUTH_EXPIRES_OFFSET_S=5` are Bybit-spec literals NOT operationally tunable per L-001 active control (mirror T-207 `_RECV_WINDOW_MS=5000` precedent). **Mappers**: `_map_execution_event` reads `execId/orderId/symbol/side/execPrice/execQty/execFee/execTime` from Bybit V5 wire → ExecutionEvent with `Decimal(str(...))` round-trip + UTC `executed_at` via `datetime.fromtimestamp(int(execTime)/1000, tz=UTC)`; `_map_position_event` mirrors T-208b `_map_position_row` field-for-field + `occurred_at` from `updatedTime` (duplication, not shared `PositionMapper` class — surface-expansion guard per OQ-9 default A). **L-006 LOC monitoring**: plan estimate 220 src; actual ws.py ~340 (raw incl docstrings), substantive ~289; production diff total ~358 LOC under §0.3 cap of 400. Trim pass on initial draft (392 LOC) reduced module + helper docstrings to land under cap with margin. **Reviewers**: drift-checker ON TRACK; brief-reviewer SHIP (8/8 Write-time guidance items addressed: patchable sleep seam, catch tuple, forward-pointer test removal, §0.9 justification, plan-doc status update, async-gen mypy checkpoint, auth-failure negative pin, T-206 conformance); math-validator VERIFIED. **Repo-wide pytest**: 825 passed (+27 new: 22 ws.py + 3 adapter delegation + 2 carried), 75 skipped — no regressions from T-208b baseline 798. Plan: `docs/plans/T-209.md` (APPROVED 2026-04-30 with 8 Write-time guidance items). Branch: `feat/T-209-bybit-private-ws` ff-merged to master. **F2 BybitV5 series complete**: T-205 (rate limiter) + T-207 (REST envelope + signing) + T-208a (writes) + T-208b (reads) + T-209 (private WS streams + close) ships full ExchangeClient Protocol implementation against Bybit V5; T-215 adapter pool composition root next on top-of-DAG.
- [x] T-213b: PaperExchange persistence + execution emission (2026-04-28) — F2 eleventh numbered work task; **second F2 task with active math-validator gate** (Gate 4 ACTIVE per CLAUDE.md gate-4 conditional rule — staged diff touched `packages/exchange/paper/` financial math: realized_pnl computation §E.1-§E.3). **Pre-emptive split T-213b → T-213b + T-213c** per L-007 + OQ-1 default A — full T-213b estimated ~390 LOC src EXCEEDED §0.3 cap of 400; split into T-213b (writes + execution emission) + T-213c (reads + restart recovery — future plan-doc). T-213b packages: `packages/exchange/paper/persistence.py` (NEW — 10 helpers: 4 INSERT @non_idempotent + 6 UPDATE/DELETE naked per W#4 stance A; ~285 effective LOC) + `packages/exchange/paper/adapter.py` (extended +560 LOC over T-213a 286 baseline). Constructor extended to 9 params: 7 T-213a baseline + 2 new (`pool: asyncpg.Pool`, `event_queue_maxsize: int = 1000` per BLOCKER 6 / L-001 / §N9 active control). **place_market_order full body** replaces T-213a NotImplementedError partial stub: single-tx INSERT chain across paper_orders + paper_trades + paper_executions + paper_positions on open per §9.5 step 8; UPDATE paper_trades close + DELETE paper_positions on reduce_only=True; emit ExecutionEvent + PositionEvent post-commit per §9.5 step 9 + Decision #2. **set_trading_stop full body** UPDATE paper_positions sl_price + tp_price ONLY per BLOCKER 1 schema parity (tpsl_mode + tp_size in `_active_positions` dict only — paper_positions schema mirrors live position_state per §3.1 line 268). **`_drain_sl_tp_fill` split** per W#5(b): 6 LOC dispatcher + `_drain_partial_tp` 47 LOC + `_drain_full_close` 35 LOC + 4 helpers (`_build_drain_context`, `_insert_synthetic_close_order`, `_insert_close_execution`, `_emit_close_events`); each method ≤50 LOC. **Synthetic paper_orders rows for SL/TP fills** per Decision #5 + H-024 binding: paper_executions.order_id FK to a real synthetic order so T-218 dispatcher exec_type derivation (DB execId → order_id match) works uniformly across paper + live. **`paper_orders.idempotent` mapping** (CONCERN 2): market → False (mirror @non_idempotent + H-003); sl/tp → True (mirror Bybit set_trading_stop @idempotent). **`correlation_id` sourcing** (BLOCKER 2): adapter generates `f"paper-corr-{uuid.uuid4()}"` per call (OQ-3 sub-question default A — decoupled from T-216). **`exchange_order_id`** UUID4 per Decision #6 (OQ-6 default A). **`paper_trades.notional_usd`** computed at INSERT time per BLOCKER 3 (NUMERIC(20,4); `qty * fill_price`). **Realized P&L on close** per Decision #8 (long: `(exit-entry)*qty - fees_open - fees_close`; short: `(entry-exit)*qty - fees_open - fees_close`; quantize NUMERIC(20,4)). **Partial TP semantics** per Decision #9 + OQ-3 default A: paper_trades stays OPEN with reduced qty + tp_hit=TRUE; partial_pnl = `(tp - entry) * tp_size - tp_fee` (TP fee only; entry fee RESERVED for full close). **`entry_fee` tracked separately** on `_active_positions` dict at open time per math-validator MATH FAIL → VERIFIED post-fix (Fix #1): full-close path uses `fees_open=position["entry_fee"]` NOT `position["fees_paid"]` to prevent partial-TP fee double-subtraction; `position["fees_paid"]` continues to accumulate for paper_trades.fees_paid persistence. **`stream_executions` / `stream_positions` AsyncIterator** per Decision #11/#12 + T-201 OQ-1: per-instance bounded `asyncio.Queue` (DI'd maxsize), `def`-not-`async-def` returning generator; OQ-5 default A: round-robin caveat documented via `test_two_consumers_split_events_does_not_broadcast`. **`cancel_order`** UPDATE paper_orders status='cancelled' (idempotent on already-cancelled); **`set_leverage`** no-op (paper has no leverage). **`OrderRejected` reused** (BLOCKER 4) for caller-mistake cases (`position_already_open` / `no_position_to_close`); no new exception class. **`update_paper_trade_partial` helper** (BLOCKER 5): UPDATE paper_trades SET qty/fees_paid/realized_pnl WHERE id (PK) for partial-TP path. **`asyncpg==0.30.0` dependency** declared in `packages/exchange/pyproject.toml` (CONCERN 3 fix; matches packages/db pin). **Hand verification §E.1** (full close long realized_pnl 460.7000 + notional_usd 32500.0000 + fees_paid 39.3000), **§E.2** (partial TP 46.0700 / qty=0.4 / tp_hit=TRUE / fees_paid=23.4300 / OQ-3 default A entry fee reserved), **§E.3** (aggregate partial-TP-then-SL close: -188.9100 / fees_paid=38.9100 — corrected derivation post math-validator MATH FAIL). **All 4 review gates passed**: plan-reviewer 1st REVISE 6 BLOCKERS+4 CONCERNS → 2nd APPROVE with W#1-W#7 Write-time guidance (W#1-W#4 doc-coherence resolved at consolidation; W#5-W#7 implementation-time). drift-checker 1st DRIFT (LOC inflation + 6 missing tests) → 2nd ON TRACK after operator-approved option A refactor (split drain methods + 7 tests added). brief-reviewer SHIP. math-validator 1st MATH FAIL (§E.3 partial-TP fee double-subtracted at full close path) → 2nd VERIFIED after Fix #1 (entry_fee tracked separately on `_active_positions` dict; `_drain_full_close` + `_persist_close` use `position["entry_fee"]` instead of `prior_fees`). 7 missing tests added: `test_persist_failure_rolls_back_full_chain` (§9.5 step 8 atomicity), `test_emit_happens_after_persist_commit` (Decision #2 ordering), `test_drain_called_after_t213a_enqueue_in_same_candle` (wiring), `test_pending_queue_empty_after_full_drain` (queue invariant), `test_partial_tp_then_sl_close_yields_correct_aggregate_pnl` (§E.3 -188.9100), `test_manual_reduce_only_close_after_partial_tp_uses_entry_fee_not_fees_paid` (math regression guard, 410.7300), `test_execution_event_shape_matches_protocol_dataclass` parametrized over (open/close/sl/tp); plus `test_close_paper_trade_uses_pk_not_symbol_status` (H-018 white-box SQL assertion) + `test_active_positions_dict_matches_paper_positions_after_each_mutation` (CONCERN 4 — _active_positions ↔ paper_positions parity over 4 mutation sites). **Hazards bound**: H-013 tpsl_mode propagation chain end-to-end (set_trading_stop → `_active_positions` dict → `PendingSLTPFill` → drain branch on `fill.tpsl_mode == 'Partial'`; no `'Full'` default baked); H-018 close paper_trades by PK only (`WHERE id = $`); H-024 DB-driven exec_type via synthetic paper_orders FK. **ci-full GREEN on branch HEAD `ba331b3`** (run 25063111500). Plan: `docs/plans/T-213b.md` (746 LOC). 14 integration + 11 unit tests + adjustments to T-211/T-213a tests for new ctor (pool kwarg) + drain-after-cross capture pattern. Repo-wide pytest 685 + 36 → 688 + 54 (+3 new pass + 18 new env-gated paper integration). **Calibration win continued** (T-204 → T-210 → T-214 → T-211 → T-206 → T-212 → T-213a → T-213b trajectory): 20 verbatim Decisions + 7 genuine OQs (all "use defaults") + Write-time guidance discipline + math-validator catch-and-recover (operator-flagged surface-expansion guard activated, mid-implementation refactor option A applied per L-007 spirit). **Unblocks T-213c** (reads + restart recovery — get_positions / get_fill_price / get_closed_pnl_cumulative DB read bodies; on-construct rehydrate of `_active_positions` from paper_positions). **Unblocks T-218** (execution dispatcher consumes `stream_executions()` per §9.5 step 5).
- [x] T-215: Adapter pool composition root (2026-04-30) — F2 eighteenth numbered work task; **ninth F2 task with active math-validator gate** (Gate 4 ACTIVE per CLAUDE.md gate-4 conditional rule — staged diff touched `services/execution/`; verdict VERIFIED — out-of-scope; `Decimal(env_str)` constructor string-exact pass-through to `PaperExchange` ctor, no arithmetic; `_parse_slippage_params` uses `Decimal(str(v))` defending against bare-numeric-JSON float-coercion; Settings rate-limiter polia typované `float` per ADR-0003 token-bucket rates/capacities, NIE money values; žiadne Decimal silent casts; žiadne `getcontext().prec` mutácie; žiadne library round-trip). T-215 deltas: `services/execution/app/pool.py` (NEW ~273 LOC; `MissingBotCredentialsError` + `AdapterPoolResult` frozen dataclass + `build_adapter_pool` async function + `_construct_bybit_adapter` + `_construct_paper_adapter` + `_require_env` + `_validate_slippage_model` + `_parse_slippage_params` + `_make_task_exception_logger` + 4 Bybit URL module constants); `packages/db/queries/execution.py` (NEW ~73 LOC; `BotRow` frozen dataclass + `ExchangeMode` Literal alias + `_validate_exchange_mode` + `select_active_bots` SQL `WHERE status='active' ORDER BY bot_id` per WG#3 deterministic-ordering invariant); `services/execution/app/main.py` (lifespan extends from 3 to 6 steps + reverse shutdown 4 steps: pool→bus→SharedRateLimiter→build_adapter_pool→state attach; reverse: bus.close→cancel/gather background tasks→per-bot adapter.close→pool.close); `services/execution/app/config.py` (+8 Settings fields: `bybit_leverage_cache_ttl_s=3600.0` per L-001 fix + 7 `rate_limit_*` per ADR-0003 §Decision 1 verbatim defaults; module-docstring extended per CONCERN #1 fix s 7 env-var families pod T-215 ownership); `services/execution/app/deps.py` (+`get_adapter_pool` provider returning `dict[BotId, ExchangeClient]`). **ADR-0004 NEW** (`docs/adr/0004-bot-credentials-env-var-source.md`, 80 LOC, status accepted, supersedes ADR-0003 §Decision 6 sub_account-from-column path) — operator-chosen path (b) per plan-reviewer 1st-pass REVISE [BLOCKER]: documents F2 env-var family decision authoritatively (`BOT_<ID>_BYBIT_API_KEY/SECRET/SUB_ACCOUNT` + 4 paper env vars) per H-022 symmetry, F5+ revisit trigger on env-var sprawl, demo-deployment routing (live URLs for demo sub-accounts per memory `deployment.md`). **ADR-0003 head amended** (1-line Status note cross-referencing ADR-0004; rest of ADR-0003 untouched per WG#2). **§0.3 LOC waiver operator-approved 2026-04-30 path A**: src diff 454 insertions vs 400 cap (13% over). Cohesion rationale: pool.py 4 helpers (`_require_env`, `_validate_slippage_model`, `_parse_slippage_params`, `_make_task_exception_logger`) load-bearing across both Bybit + Paper construction paths; pre-armed split per plan §LOC estimate (extract `pool/bybit.py` + `pool/paper.py`) by zaviedol cross-module helper imports + ZVÝŠIL total LOC. T-208a/b split precedent (separate REST surfaces, no shared helpers) NEAPLIKUJE. Plan §604 split-trigger language ("If pool.py exceeds 200 LOC during write, split per L-007") bola conditional contingency, nie hard mandate; operator má autoritu override-nuť na waiver pri jasnom rationale. **§N6 no globals**: pool.py má len 4 immutable URL string konstanty + `_VALID_SLIPPAGE_MODELS` frozenset + `__all__` + `MissingBotCredentialsError` class + `AdapterPoolResult` frozen dataclass — žiadne mutable module-level state. **§N7 thin adapter**: pool.py je composition-only; helpery sú pure. **§N9 configurability**: 8 Settings fields env-tunable; Bybit URL constants protocol-binding per L-001 (NIE operationally tunable). **Lifespan order load-bearing**: bus.close pred adapter.close (publish-after-persist contract z T-200 Q2); cancel + `asyncio.gather(*tasks, return_exceptions=True)` pred adapter.close aby ws_task neviedol nezachyteného CancelledError pri reconnect-loop sleep; `adapter.close()` per bot drainí ws + httpx (Bybit) alebo no-op (Paper); pool.close last. **H-022 verbatim test** `test_adapter_pool_uses_distinct_credentials_per_bot` (test_pool.py:248) — 2 boty alpha + beta, distinct env vars, assert distinct `sub_account` cez adapter ctor. **H-025 test pin** `test_adapter_pool_passes_same_rate_limiter_instance_to_all_bybit_adapters` — single SharedRateLimiter inštancia shared across all Bybit adapters per ADR-0003 §Decision 5 (test asserts `lim is rate_limiter` for both adapter ctor calls). **H-007 ws.run task spawn delegated** (T-209 owns reconnect loop; T-215 spawne `asyncio.create_task(ws.run(), name=f"bybit_ws_{bot_id}")` + `add_done_callback(_make_task_exception_logger)` — done-callback `task.cancelled()` early-out logs ERROR `execution.background_task_died` len pri non-cancelled exception per OQ-13 default A; `test_task_exception_logger_silent_on_cancellation` pinuje silent-on-cancel kontrakt). **H-024 deferred** (`exec_type` derivation = T-218 dispatcher scope). **`bot_id.upper()` env lookup pattern** per WG#5 dokumentovaný v pool.py module-docstring (lowercase DB → UPPERCASE env; example: `bot_id='alpha'` reads `BOT_ALPHA_BYBIT_API_KEY`). **`_require_env` actionable error** per WG#4 raises `f"Missing env var: {key}"` s konkrétnym key name; 3 missing-credentials testy assert key name v error message. **Done-callback** logs ERROR len ak `task.cancelled() == False` AND `task.exception() is not None`; `test_transport_reconnect_does_not_increment_auth_failure_count` ekvivalent v T-215 testoch je `test_task_exception_logger_silent_on_cancellation`. **Plan**: `docs/plans/T-215.md` (1st-pass plan-reviewer REVISE [1 BLOCKER ADR-0003 §6 drift + 3 CONCERNS] → operator path (b) ADR-0004 + 3 CONCERN fixes → 2nd-pass APPROVE 2026-04-30 [5 Write-time guidance items]). **All 4 review gates passed**: plan-reviewer 2nd-pass APPROVE → drift-checker DRIFT (§0.3 LOC overage operator-waivered path A) → brief-reviewer SHIP (5/5 WG addressed + flagged unused `logger`/`import logging` dead code v pool.py — odstránené pred Gate 4) → math-validator VERIFIED. **Repo-wide pytest 851 passed** (+26 new: 20 pool + 5 queries + 1 lifespan attach), 75 skipped — no regressions from T-209 baseline 825. **20 OQ defaults operator-approved 2026-04-30**: OQ-1 sub_account z env per H-022 symmetry (resolved cez ADR-0004 path b) — OQ-2 paper params z env (YAML deferred T-216+) — OQ-3 lifespan order pool→bus→limiter→adapter pool→state — OQ-4 reverse shutdown bus→cancel→adapter.close→pool — OQ-5 nový queries/execution.py s BotRow + select_active_bots — OQ-6 empty bots → empty pool, paused/archived filtered — OQ-7 fail-fast pri missing env — OQ-8 SharedRateLimiter env z Settings 7 fields — OQ-9 app.state attribute names per backlog — OQ-10 unit-only tests, integration deferred T-222 — OQ-11 keep T-215 unified — OQ-12 bybit_leverage_cache_ttl_s Settings field — OQ-13 ws task error log + continue per BinanceWsClient pattern. Branch: `feat/T-215-adapter-pool-composition` ff-merged to master. **Unblocks T-216** (order placement pipeline reads `app.state.adapters[bot_id]`), **T-217** (PositionLifecycle FSM consumes adapter handle), **T-218** (execution dispatcher consumes `adapter.stream_executions()`), **T-219** (reconciliation calls `adapter.get_closed_pnl_cumulative()`), **T-220** (P&L audit iterates `adapters.values()`), **T-221** (post-restart reconciliation iterates pool).
- [ ] T-216 (likely → T-216a + T-216b per §0.3 SPLIT candidate): Order placement pipeline (§9.5 steps 1-9). **T-216a**: request receipt → DLQ on missing adapter → `set_leverage` with LRU cache (Q9 default; **§9.5 step 3 references H-002 for set_leverage cache semantics, but H-002 is the cumulative-delta reconciliation hazard — drafting artifact in brief; cache implemented per its own semantics, not per H-002**) → `place_market_order` no-retry per H-003 → fetch fill_price. **T-216b**: `set_trading_stop` SL with 3× retry + emergency-close on exhaustion per H-004 → `set_trading_stop` TP with `tpslMode=Partial` per H-013 → persist orders+trades+position_state in single tx → emit `orders.events.<bot_id>` + `trading.events` post-commit (Q2/Q5 publish-after-persist). Hazards: H-003, H-004, H-013. **§N4 TDD discipline applies — write hazard-named test first, then implementation** (`test_place_order_on_timeout_never_retries_and_raises_unknown_state`, `test_sl_set_exhaustion_triggers_emergency_close_and_records`, `test_set_trading_stop_requires_explicit_tpsl_mode`). Blocked by: T-208, T-215, T-202, T-203, T-204. Trace: E1. Est: ~350 LOC + tests.
- [ ] T-217: PositionLifecycle FSM — `services/execution/app/lifecycle.py`. Per-trade monitor task spawned by T-216. Tick at `POSITION_POLL_INTERVAL` (default 1s, configurable per §N9). Read latest price from NATS KV `feature_latest`-like with REST fallback. MFE/MAE update. BE trigger: favorable move ≥ `be_trigger` → move SL to `entry ± be_sl_level`. Trail SL update if `trailing_active`. Hazard: H-018 (PK-keyed updates only via `packages/db/queries/trades.py` PK-keyed helpers). **Watch for cap pressure mid-write** (~280 LOC = L-006 threshold). Blocked by: T-216, T-203. Trace: E1. Est: ~280 LOC + tests.
- [ ] T-218: Execution event dispatcher — `services/execution/app/dispatcher.py`. `_on_execution` consumer of T-209 `stream_executions()` iterator + T-210 `DedupingConsumer` ringed by `execId`. Updates running P&L. Marks trailing on TP hit. Determines `exec_type` via DB `execId → order_id` match (NOT exchange order-link fields per H-024). On size-0 event → invoke T-219 close flow. Hazards: H-009 (T-210 reuse), H-024 (DB-driven exec_type). Tests: `test_duplicate_exec_event_is_ignored`, `test_sl_fill_after_partial_tp_labeled_sl_not_tp`. Blocked by: T-210, T-209. Trace: E1. Est: ~220 LOC + tests.
- [ ] T-219: Cumulative-delta P&L reconciliation — `services/execution/app/reconcile.py`. `_close_trade` flow per §9.5 lines 1594-1599. Snapshot `closed_pnl_total` BEFORE close (via `get_closed_pnl_cumulative`). Snapshot AFTER (with H-011 2s configurable sleep). Delta = attributable realized P&L. Apportion to trades in close order (works under partial-TP — multiple closed_pnl rows; works under concurrent closes — per sub-account isolation). Hazards: H-001, H-002, H-011, H-012. **§N4 TDD discipline applies — write hazard-named test first, then implementation** (`test_cumulative_delta_ignores_order_ids`, `test_close_with_identical_prior_trade_same_symbol`, `test_closed_pnl_snapshot_waits_before_reading`, `test_close_uses_closed_pnl_delta_over_ws_accumulation`). Blocked by: T-208, T-217, T-218; **ADR-Q-B prerequisite** (reconciliation strategy). **Watch for cap pressure mid-write.** Trace: E3. Est: ~280 LOC + tests.
- [ ] T-220: Periodic P&L audit loop + Migration 0009 `trade_pnl_deltas` — `services/execution/app/audit.py`. APScheduler in-process per §3.1 + H-021 (UTC only, no system cron). 5-min schedule. Fetch Bybit closed-pnl last 3h per sub-account. Compare to DB `trades.realized_pnl` for trades closed in window. Threshold $0.50 → write `trade_pnl_deltas` row + update `trades.realized_pnl`. **Migration 0009 inline** (table per Q6 baseline schema; **trade_pnl_deltas schema baseline per T-200 Q6; final lock at this plan-doc** — schema may iterate based on query patterns surfacing during implementation). Hazards: H-017 (cumulative attribution per sub-account+window — structurally prevents double-attribution), H-021 (UTC scheduler). **§N4 TDD discipline applies — write hazard-named test first, then implementation** (`test_audit_never_double_attributes_closed_pnl`, `test_daily_report_runs_at_configured_utc_time`). Blocked by: T-208, T-202; **ADR-Q-C prerequisite** (APScheduler operational pattern). **Watch for cap pressure mid-write.** Trace: E4. Est: ~250 LOC src + ~80 LOC migration + tests.
- [ ] T-221: Post-restart reconciliation — `services/execution/app/restart.py`. execution-service startup flow per H-020. Fetch open positions per sub-account from exchange + `position_state` rows from DB. Orphan DB → close with reason `reconcile_gone`. Orphan exchange → market-close (unmanaged). Matching → resume monitor task (rehydrate T-217 PositionLifecycle FSM from `position_state` per Q3 default). Hazards: H-020, H-026 (time-window+orderId-covering test, NOT qty approximations). Tests: `test_reconciliation_closes_db_orphans_and_markets_exchange_orphans`, `test_partial_non_5050_reconciliation_does_not_create_duplicate`. Blocked by: T-208, T-217, T-202, T-203. **Watch for cap pressure mid-write.** Trace: E1. Est: ~250 LOC + tests.
- [ ] T-222: F2 exit-criteria integration bundle — E1 manual playbook + E2 paper parity + E3 concurrent-bot cumulative-delta. **E1**: Bybit testnet smoke per §11.6 (manual; documented in `docs/runbooks/F2_E1_testnet_smoke.md` per Q10). Operator runs once before F2 close + chore commit references run output. **E2**: PaperExchange event-stream parity test (CI; same harness, parameterized `exchange_mode='paper'`, asserts identical event-stream shape to live for the same scenario). **E3**: concurrent-bot integration test (CI; 2+ bot adapter pool, parallel close events, asserts cumulative-delta apportions correctly without cross-bot bleed). Blocked by: all prior F2 tasks. **Watch for cap pressure mid-write.** Trace: E1, E2, E3. Est: ~250 LOC + harness wiring.

### F2+ opportunistic

- [ ] T-F2+: ADR-Q-B — cumulative-delta reconciliation strategy ADR. Snapshot timing, 2s configurable sleep rationale, partial-TP attribution order in case of multiple `closed_pnl` rows, sub-account scoping under concurrent closes. Prerequisite for T-219. Est: ~80 LOC ADR.
- [ ] T-F2+: ADR-Q-C — ADR for **APScheduler operational pattern (supervision + UTC enforcement + test fixtures)**, prerequisite for T-220. Brief mandates APScheduler library per §3.1 line 302 + H-021 line 2769 ("APScheduler in the service process, UTC only. No system cron."); ADR scope is **operational pattern**, NOT library selection: supervision strategy on scheduler crash, UTC enforcement audit single-point per H-021, test patterns (frozen-time vs FakeScheduler), dep-footprint accounting per §0.9. Est: ~80 LOC ADR.
- [ ] T-F2+: Bybit V5 instruments-info step-size cache. T-216a needs step-size to round qty before submit (else exchange rejects with retCode about precision). H-026 reconciliation NOT load-bearing for step-size (uses time-window+orderId-covering). Caching strategy + refresh cadence designed inline at T-216a plan-time per Q1 default. Re-litigate as standalone item only if cache TTL becomes non-trivial decision.
- [ ] T-F2+: Hazard catalog orchestrator test. Verifies all 18 F2-binding hazard tests exist by name in test suite. Skipped if any missing → CI red. Lives at `tests/integration/hazards/test_f2_hazard_catalog_complete.py`. Verification of E5 (§19 line 2531 "all hazard-catalog tests pass"), not new feature. Est: ~80 LOC.
- [ ] T-F2+: H-016 shadow task cleanup contract. F2 protocol stays as is (`stream_executions/positions` only); F5 shadow runtime owns full address with `async with adapter.subscribe(...) as feed:` wrapper class on top per Q8 default. Re-cited so F2 design does NOT bake conflicting assumptions.
- [ ] T-F2+: Bybit testnet credentials provisioning + secrets handling protocol. Operational, not code. Operator action: provision testnet sub-account, store keys in `.env.testnet` or vault, document in `docs/runbooks/`. Prerequisite for T-222 E1 manual playbook.
- [ ] T-F2+: per-bot Bybit API key vault migration (post-MVP). F2 reads from env per H-022 (`BOT_<ID>_BYBIT_API_KEY/SECRET`); production-grade secret store deferred to operator decision.
- [ ] T-F2+: NATS stream + KV bucket pre-flight check. Verify `ORDERS` stream exists per §8.2 (subjects `orders.requests.>` + `orders.events.>`) and `rate_limits` KV bucket exists per §8.2 line 1278. F0 nats-bootstrap should have provisioned both; confirm before T-216 / T-205 plan-review. ~10-line check or no-op confirmation.

### F1 numbered

(All F1 numbered tasks closed. F1 phase-gate satisfied 2026-04-26.)

### F1+ opportunistic

- [ ] T-F1+: per-bot HMAC secret selection via `X-Bot-Signal-Source` header with shared-secret fallback. F0 ships single shared `SIGNAL_GATEWAY_HMAC_SECRET`; §9.1 line 1438 calls per-bot secrets an "ADR-0001 topic" — needs ADR for secret store + header parsing + fallback semantics, then implementation. Discretionary enhancement, not phase-blocker. Est: M (~200-300 LOC + ADR).
- [ ] T-F1+: add `migrations/script.py.mako` standard Alembic async template. Currently each migration is hand-written; chore cleanup before F1's first migration task (T-103 or T-108 natural slot). ~20 LOC.
- [ ] T-F1+: `prometheus-nats-exporter` sidecar + nats scrape entry in `prometheus.yml`. Deferred from T-013a because NATS `/varz` returns JSON not Prom text format — direct scrape would produce permanently-DOWN target for healthy service (false-negative). Forward-refs in `infra/nats/README.md` and `infra/prometheus/README.md`. Relevant when F1 dashboards include NATS panels (post-T-017 follow-up or F1 task). ~40-60 LOC (service + image pin + UID probe + scrape job + README touch).
- [ ] T-F1+: install docker-buildx-plugin on lab host. Current compose falls back to legacy builder with Bake warning. Fine for F0 single-arch builds; becomes relevant for any future multi-arch. Out of T-015c scope.
- [ ] T-F1+: bump `docker/*` actions to Node 24-compatible versions when upstream publishes them. Currently pinned at `build-push-action@v6`, `login-action@v3`, `setup-buildx-action@v3` — all run on Node 20, deprecated per GHA runner Node 20 EoL. June 2nd 2026: Node 24 default but Node 20 still available via opt-in env var (`FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true`). September 16th 2026: Node 20 removed from runners, hard cutoff. Check upstream Docker action repos for new majors before then; bump accordingly. May require workflow syntax updates if maintainers ship breaking changes alongside the Node bump.
- [ ] T-F1+: align `tests/integration/migrations/conftest.py` env-gate with `tests/grafana/conftest.py` module-level skip pattern. Currently uses fixture-level skip with no-op `allow_module_level=True` flag (the flag is only honored when `pytest.skip` is called at module import time). Module-level guard gives true collection skip + non-misleading flag usage. ~10-line edit; behaviour-preserving. Slot: opportunistic.
- [ ] T-F1+: refine `SignalEnvelope._migrate_unknown_keys` docstring to distinguish None coalescing ("payload omitted" shorthand) from other non-dict pass-through. Currently the docstring implies all non-dict values pass through unchanged, but `None` is special-cased to `{}` so extras still merge. ~3-line docstring edit; behaviour locked by `test_payload_as_none_treated_as_missing` in `test_models.py`.
- [ ] T-F1+: GIN index on `features.value_json` for analytics-driven JSONB extraction (path-by-key queries, containment lookups). Deferred from T-108 because §7.2 line 914 specifies only the `features_latest` btree; F1 queries (T-110 dispatch + scoring-engine KV reads) go through the composite PK / `features_latest`. Add when F4+ analytics-api or dashboard surfaces a real consumer querying `value_json` paths (e.g. selecting all rows where `value_json->>'macd' > 0`). ~3-line migration (`CREATE INDEX features_value_json_gin ON features USING GIN (value_json) WHERE value_json IS NOT NULL`) — partial index avoids bloating from null rows.
- [ ] T-F1+: `market.ticks.binance.<symbol>` passthrough publish from `OhlcPipeline` (or sibling consumer). Brief §9.2 names the subject; §8.4 has no `MarketTick` schema. Needs schema design first (likely fields: symbol, exchange, timestamp_ms, bid, bid_qty, ask, ask_qty per Binance bookTicker frame shape). Add when paper-fill simulator (F2) or execution-svc surface a real consumer — §0.8 anti-hypothetical. SubscriptionManager already subscribes the `bookTicker` leg today (the §9.2 line 1455 pair), so frames are dispatched and silently ignored by T-104b until a consumer exists.
- [ ] T-F1+: add `duplicate_window: 5m` to `infra/nats/streams/MARKET_OHLC.json` when duplicate-message problems observed in production. T-104a/b ship with deterministic `Nats-Msg-Id` (UUID5 over `(symbol, bucket_start)`) on closed-candle publishes — forward-compat groundwork for server-side dedup once the stream config is updated. Current dedup belt-and-braces via DB `ON CONFLICT DO UPDATE` (T-104a) + downstream `(symbol, bucket_start)` keying suffices for F1; the stream config change is reactive, not preemptive. ~1-line JSON edit + nats-bootstrap re-run.
- [ ] T-F1+: replace `MARKET_DATA_SYMBOLS` env-var with `bots`+`bot_configs` YAML lookup once F3 ships the bot registry. Mechanical swap — `OhlcPipeline` accepts symbol list at start. Per-deployment env-var seed is the F1 stopgap (T-100); §9.2 line 1454 spec'd query lands when its prerequisite (operator-applied bot configs) exists.
- [ ] T-F1+: `/ready` grace-period for transient WS reconnects to avoid LB/k8s restart loops during Binance outages. Current F1 contract (T-100 `health.py`): RECONNECTING → 503 `reason="ws"` gives operator visibility but flips readiness during every Binance hiccup. Grace-period semantics (RECONNECTING < 30 s = still ready) restore stable readiness during normal reconnect cycles. Single-state-machine extension to `health.py`.
- [ ] T-F1+: market-data-svc end-to-end integration test — mock-WS server feeding closed-bucket frames, asserting `ohlc_1m` row + `market.ohlc.1m.<symbol>` publish. Lands after T-105 so backfill + live can be tested together. Defers ~200 LOC of network mockery until concrete payoff (full live-data round-trip exercising T-100 composition + T-104b pipeline + T-105 backfill).
- [ ] T-F1-005: surface `OhlcBackfill.per_symbol_pause_seconds` (and possibly `_REST_PAGE_LIMIT`) via `Settings`. Brief-reviewer nice-to-have from T-105 ship — 50 ms inter-symbol pause is a §N9 polling-interval that ought to be env-overridable; insignificant at sub-10-bot scale so deferred. ~10 LOC: add `backfill_per_symbol_pause_ms: int = 50` to `services/market_data/app/config.py` + thread through `OhlcBackfill` constructor in `main.py`. Bez závislostí.

## Parked
(none)
