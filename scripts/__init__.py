"""Operational CLI scripts (T-112 onward).

This package marker keeps ``scripts/`` import-discoverable for
``tests/scripts/`` and for ``python -m scripts.<name>`` invocations.
T-112 ships the first script: :mod:`scripts.backfill_features`.
"""
