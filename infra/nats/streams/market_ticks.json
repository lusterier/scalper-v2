{
  "name": "MARKET_TICKS",
  "description": "High-volume market.ticks.<exchange>.<symbol> feed from market-data-svc. max_age 1h (intentionally short — replay is unbounded and ticks are fan-out only); duplicate_window 60s sits well below max_age and guards publisher-side retries. Brief §8.1, §8.2.",
  "subjects": ["market.ticks.>"],
  "retention": "limits",
  "storage": "file",
  "num_replicas": 1,
  "discard": "old",
  "max_age": 3600000000000,
  "duplicate_window": 60000000000,
  "max_msgs": -1,
  "max_msgs_per_subject": -1,
  "max_bytes": -1,
  "max_msg_size": -1,
  "max_consumers": -1
}
