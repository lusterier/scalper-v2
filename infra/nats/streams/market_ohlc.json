{
  "name": "MARKET_OHLC",
  "description": "Closed-candle emissions on market.ohlc.<interval>.<symbol>. max_age 7d; duplicate_window 60s — candles are idempotent by (symbol, bucket_start, source) but server-side dedup catches republish-on-reconnect paths cheaply. Brief §8.1, §8.2.",
  "subjects": ["market.ohlc.>"],
  "retention": "limits",
  "storage": "file",
  "num_replicas": 1,
  "discard": "old",
  "max_age": 604800000000000,
  "duplicate_window": 60000000000,
  "max_msgs": -1,
  "max_msgs_per_subject": -1,
  "max_bytes": -1,
  "max_msg_size": -1,
  "max_consumers": -1
}
