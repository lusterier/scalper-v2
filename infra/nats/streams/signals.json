{
  "name": "SIGNALS",
  "description": "TradingView signal lifecycle: signals.raw (as-received), signals.validated (post-schema + symbol mapping), signals.rejected.<bot_id> (scoring-rejection shadow trail). max_age 7d; duplicate_window 2m covers the signal-gateway 10s idempotency ring plus upstream webhook retry slack. Brief §8.1, §8.2.",
  "subjects": ["signals.raw", "signals.validated", "signals.rejected.>"],
  "retention": "limits",
  "storage": "file",
  "num_replicas": 1,
  "discard": "old",
  "max_age": 604800000000000,
  "duplicate_window": 120000000000,
  "max_msgs": -1,
  "max_msgs_per_subject": -1,
  "max_bytes": -1,
  "max_msg_size": -1,
  "max_consumers": -1
}
