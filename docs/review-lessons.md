# Review lessons

Patterns the review system has caught. Each lesson encodes a watch-out point that reviewers should now check explicitly going forward. Reviewers consult this file at start of every invocation.

Format:
```
## L-NNN (T-XXX, <reviewer> <verdict>, <date>)
Pattern: <one-sentence description>
Active control: <what to check>
```

---

## L-001 (T-105, brief-reviewer SHIP nice-to-have, 2026-04-26)
Pattern: Polling intervals, pause durations, and similar hardcoded numbers in business logic violate §N9 (configurability invariant) even if they are sensible defaults.
Active control: At plan-reviewer stage, scan the plan for any numeric literals controlling timing, pacing, or periodic behavior. Require Settings/env exposure unless the value is mathematically fixed (e.g., "alpha = 2/(period+1)" formula constants).

## L-002 (T-107b, plan-reviewer REVISE, 2026-04-26)
Pattern: `name_template` literals in indicator implementations may drift from §B.2 spec when the indicator's `__init__` exposes a parameter that is hardcoded in the spec literal (e.g., `vwap_session` — session is fixed in spec, parameterizing it in the template breaks naming convention).
Active control: At plan-reviewer stage for any task touching `packages/features/builtins/`, explicitly check each indicator's `name_template` literal against §B.2 verbatim. Parametrized substitutions (`{symbol}`, `{interval}`) are universal; instance-baked parameters in the template (period, std_dev, etc.) are only valid where §B.2 shows them.

## L-003 (T-107b, plan-reviewer CONCERN, 2026-04-26)
Pattern: Helper functions extracted from indicator code (e.g., `_sma_seeded_ema_series`) need explicit alignment documentation showing they produce the same values as the standalone indicator class on the same input. Without this, a math drift between two implementations of the "same" computation can silently break golden tests.
Active control: At plan-reviewer stage, when a plan introduces a helper used by multiple indicators or by an indicator-internal computation, require a "golden cross-check" test in the plan that asserts helper output equals standalone indicator output on identical input.

## L-004 (T-107b, plan-reviewer CONCERN, 2026-04-26)
Pattern: `value_json` in FeatureValue is `Mapping[str, object]` (per T-106 contract) — this means dict literals work but the resulting FeatureValue is **not** hashable since dict isn't hashable. Plans involving `value_json` outputs should explicitly note this in the test strategy so a hash-check test exists.
Active control: At plan-reviewer stage, for any indicator producing `value_json`, require a test verifying that FeatureValue with `value_json` payload behaves correctly with respect to the documented `Mapping[str, object]` contract.

## L-005 (T-108, plan-reviewer REVISE, 2026-04-26)
Pattern: `sa.Float()` in SQLAlchemy/Alembic produces PostgreSQL `real` (4-byte single precision), NOT `DOUBLE PRECISION` (8-byte). Use `sa.Double()` for `DOUBLE PRECISION` columns. Spec literal `DOUBLE PRECISION` (e.g., §7.2 line 907 for features.value_num) requires `sa.Double()`, otherwise the migration silently degrades precision and integration tests of column type will fail.
Active control: At plan-reviewer stage, for any migration whose plan specifies `DOUBLE PRECISION`, verify the SQLAlchemy type is `sa.Double()`, not `sa.Float()`. At brief-reviewer stage, grep staged migration code for `sa.Float()` and flag if any `DOUBLE PRECISION` columns exist in the same migration.

## L-006 (general, observed across T-105/T-107a/T-107b, 2026-04-26)
Pattern: LOC estimates from Claude Code's plan drafts are systematically 20-40% optimistic for integration-heavy tasks (T-105 backfill: 240 → 344 = +43%; T-107b indicators: 216 → 280 = +30%). Skeleton or scaffold tasks (T-106, T-107a after split) match estimates more closely.
Active control: At plan-reviewer stage, if a plan's LOC estimate exceeds 280 LOC for an integration-heavy task, recommend pre-emptive split. If a plan's LOC estimate is 200-280 LOC, note it as "watch for cap pressure mid-write" but allow.

## L-007 (T-107, operator decision pre-plan-reviewer, 2026-04-26)
Pattern: Mid-write splits (Claude Code reaching 400-LOC cap mid-implementation and forced to split) are operationally expensive — they require git history rewrite, branch creation, and disrupt the implementation flow. Pre-emptive splits at planning time are far cheaper.
Active control: At plan draft stage, if the plan describes 5+ distinct components (e.g., 6 indicators) or has explicit "if I exceed cap mid-write I'll split" contingency language, recommend pre-emptive split into two tasks before invoking plan-reviewer.

## L-008 (T-218a, plan-reviewer REVISE BLOCKER #1, 2026-05-01)
Pattern: Plan-doc SQL literals using Python type names (`Decimal '0'`, `Datetime '...'`, `Bool 'true'`) are NOT valid PostgreSQL syntax — PG knows `numeric` / `timestamp` / `boolean` (lowercase, optionally with explicit cast `::numeric` or typed-literal `NUMERIC '0'`). The Python `decimal.Decimal` class name is not a PG type identifier and the SQL parser will raise `syntax error at or near "Decimal"` at runtime. Mock-level tests don't catch this because they intercept `conn.execute` before SQL is parsed.
Active control: At plan-reviewer stage, grep plan-doc + WG section for SQL literals containing Python type names (`Decimal '`, `Datetime '`, `Bool '`, etc.) — flag as BLOCKER. Require either implicit cast (bare `0` for NUMERIC, bare `true` for boolean), explicit cast (`0::numeric`, `'2026-01-01'::timestamptz`), or typed literal (`NUMERIC '0'`, `TIMESTAMPTZ '...'`). Additionally require a DB-level integration test pin against testcontainer PG when the helper exercises a non-trivial SQL expression (COALESCE/CASE/CAST), gated on `POSTGRES_TEST_DSN` env var per existing F1 integration pattern, otherwise mock-only tests pass while real path explodes.

## L-009 (T-409, ci-full pip-audit failure post-merge, 2026-05-04)
Pattern: Local pre-commit hooks do NOT run pip-audit; only ci-full does. New deps pinned at plan-time may carry unfixed CVEs (jinja2==3.1.4 had 3 CVEs with patches in 3.1.5 + 3.1.6 published before T-409 plan was drafted). Plan-reviewer Gate 1 + brief-reviewer Gate 3 + math-validator Gate 4 don't audit dep CVEs — gap surfaces only on master after CI-full run (~2 min latency post-push), requiring follow-up `fix(T-NNN)` commit on master.
Active control: At plan-reviewer stage, when a plan adds a NEW external dep (per §0.9), require explicit "verified latest patch version with no open CVEs at plan time" sentence in §0.9 justification — Claude Code can run `uv pip install --dry-run "<pkg>"` + `pip-audit --dry-run` locally before APPROVE. At brief-reviewer stage, run `uv run pip-audit --skip-editable` on staged uv.lock if any new dep added; reject SHIP if any unresolved CVE reported. Local pre-commit can't add pip-audit hook (~5s slow + network) without operator opt-in; gate is reviewer-side.

## L-010 (T-420, brief-reviewer FIX FIRST, 2026-05-05)
Pattern: Shared fetch wrapper that always calls `res.json()` will silently break the first 204/empty-body endpoint, while mocked tests pass because they short-circuit the json-parse path. T-420 introduced `DELETE /api/symbol-map/{id}` which backend returns 204 + empty body (per `services/analytics_api/app/routers/symbol_map.py:245`); `apiFetch` `return res.json()` would throw SyntaxError on empty payload, mutation falls into onError, query invalidation NEVER fires, deleted row stays visible — operator believes delete failed while backend actually deleted. Tests passed because they mocked `apiFetch` directly, bypassing `fetch`+`res.json()` path.
Active control: When introducing a NEW DELETE / PUT-no-body / 202-Accepted-empty mutation in a UI task, brief-reviewer MUST verify the fetch wrapper handles 204 + empty Content-Length BEFORE res.json() — short-circuit with `if (res.status === 204 || res.headers.get("content-length") === "0") return undefined as T;`. Plan-reviewer should flag this preemptively when reviewing plans that introduce DELETE/PUT-no-body. Test must exercise REAL apiFetch via fetch-level mock (`vi.fn() as typeof globalThis.fetch` returning `new Response(null, {status: 204})`), not apiFetch-level mock — apiFetch-level mocks bypass the json-parse trap entirely.

## L-016 (T-512a, drift-checker DRIFT pass-1 → SHIP via operator §0.3 over-cap waiver, 2026-05-08)
Pattern: Replay-recovery / restart-resume FSM tasks (shadow / rejected-signal / future task-restart) systematically under-budgeted at plan-time by ~150-180% — even after pre-emptive split per L-007. T-512a plan-reviewer pass-2 APPROVE budget was ~225 LOC src; reality post-implementation was ~625 LOC src (~155% over §0.3 400 cap; ~180% over plan target). Distribution: per-variant defensive parent-state check both modes (~25 LOC) + WG#5 window cap pre-check (~20 LOC) + WG#3 timer carry-over pre-check (~25 LOC) + WG#6 per-variant compute timeout wrap with diagnostic logging fields cursor_rows_consumed/last_bucket_start/elapsed_compute_ms/elapsed_io_ms (~50 LOC) + WG#11 replay variant_state seed for live continuation tracking (~15 LOC) + WG#12 own_sub subscribe live-continuation closure (~30 LOC) + WG#18 cascade-delete-race log format (~15 LOC) + structured logging events module-level (~50 LOC) + module docstring + imports + helpers (~100 LOC). All scope-aligned per drift-checker DRIFT verdict — no scope creep, plain calibration miss. 2nd data point with T-511b1 (shadow-worker FSM 70-80% over) confirms L-014 multiplier applies to ALL replay/restart-recovery FSM tasks, not just per-variant FSM body.
Active control: Plan-reviewer at gate 1 MUST flag any plan with budget <500 LOC src for replay-recovery / restart-resume FSM tasks (shadow restart per BRIEF §13.4, rejected-signal restart per §13.5, future signal-replay or risk-engine FSM tasks) as optimistic. Multiplier guidance for replay-recovery: enumerate plan's WG/AC items + count "defensive paths" (parent-state checks per mode, window cap, timer carry-over, per-variant timeout, structured logging diagnostic fields, cascade-delete race logging, live-continuation closure, replay state seeding); each defensive path contributes 15-30 LOC realistic. Specifically: if plan enumerates ≥2 mode-specific dispatches (live + paper) + ≥3 defensive cap/timeout/window pre-checks + ≥1 diagnostic logging event with structured fields + ≥1 nested closure for live-continuation, realistic budget is 500-700 LOC src for the replay module alone, NOT 150-225. Brief-reviewer at gate 3 MUST verify any §0.3 over-cap is accompanied by explicit operator waiver in commit msg per L-014 active control. Drift-checker at gate 2 MUST distinguish "scope drift" (DRIFT verdict — operator must REVISE) from "plan-budget calibration miss" (DRIFT verdict but operator may waive — replay-recovery pattern is the canonical case for this).

## L-015 (T-511b2a follow-up, ci-full integration regression post-merge, 2026-05-08)
Pattern: When a NEW migration modifies a table introduced in an earlier migration, the integration tests for the earlier migration (which run against ``migrated_db_dsn`` fixture upgraded to head) inherit the new schema state. Tests asserting old column shape, INSERT statements with NOT NULL columns, or FK behaviour will fail at head. T-511b2a's migration 0015 added ``parent_kind: TEXT NOT NULL`` to ``shadow_variants`` (introduced in 0014); 3 tests in ``test_0014_migration.py`` failed in ci-full: column shape (14 → 15 cols), 2 INSERTs (``NotNullViolationError`` on parent_kind), FK cascade test (FK dropped in 0015 → no cascade fires; original assertion expected ``after == 0``). Local pre-commit cannot catch this — integration migration tests are env-gated on ``POSTGRES_TEST_DSN`` and only run in ci-full (~2 min latency post-merge). L-009 sibling: ci-full-only checks surface failures post-merge requiring follow-up ``fix(T-NNN)`` commits.
Active control: When a plan adds a migration that modifies a table introduced in an earlier migration (drop column / add NOT NULL / drop FK / type change), plan-reviewer at Gate 1 MUST require a "Sibling migration test impact" section listing every earlier ``test_NNNN_migration.py`` whose assertions touch the modified table + the specific assertion that needs updating (column shape tuple / INSERT bind args / FK cascade behaviour). Brief-reviewer at Gate 3 MUST grep staged diff for ``test_NNNN_migration.py`` files matching the modified table's earlier migration + verify the relevant assertion lines match post-head state. If sibling test changes are not in scope of the migration task, plan must explicitly defer with a "Follow-up sibling test fix anticipated" note + estimate of ci-full latency. Math-validator + drift-checker do NOT catch this (env-gated tests skip locally). The plan-stage gate is the most reliable prevention point.

## L-014 (T-511b1, drift-checker DRIFT pass-1 → ON TRACK pass-2 with operator §0.3 over-cap waiver, 2026-05-08)
Pattern: FSM-style execution-service tasks (lightweight FSM modules running per-variant or per-trade against `PaperExchange` / dispatcher primitives — shadow worker, future risk-engine FSM, future signal-replay) are systematically under-budgeted at plan-time by ~70-80%. T-511b1 plan-reviewer pass-3 APPROVE budget was 225 LOC for `shadow_worker.py` + ~280 LOC total src; reality post-refactor was 383 LOC single-file + 505 LOC total src (+70% / +80% over plan; 105 LOC over §0.3 400 cap). Drift-checker pass-1 caught DRIFT (~42 LOC unscoped helpers); pass-2 confirmed scope-aligned + clean code post-refactor; remaining overage attributable to legitimate scope (FSM body lines 1-9 + `try/finally` H-016 finalizer + closure factory + 3 verbatim-copy helpers + Pydantic envelope + PE adapter delta with TerminalEvent dataclass + ctor kwarg + sl_type kwarg + `_drain_sl_tp_fill` callback fire). No trim path without compromising readability or splitting into operationally-expensive T-511b1a + T-511b1b. Operator over-cap waiver (2026-05-08).
Active control: Plan-reviewer at gate 1 MUST flag any plan with budget <300 LOC for shadow / risk / replay FSM tasks as optimistic. Multiplier guidance: enumerate the plan's listed members (helpers + class methods + closures + dataclasses + envelopes); apply 1.5-1.8× ceiling per item-count to set realistic budget. Specifically for FSM-style: if plan enumerates ≥3 helpers + ≥3 class methods + ≥1 closure factory + ≥1 dataclass / envelope, the realistic single-file budget is 350-450 LOC, NOT 200-225. PE adapter deltas adding `terminal_callback` + new dataclass + new ctor kwarg + new validation kwarg systematically run 50-70 LOC, NOT 25 LOC. Brief-reviewer at gate 3 MUST verify any §0.3 over-cap is accompanied by explicit operator waiver in commit msg; without explicit waiver, REVISE. Drift-checker at gate 2 MUST distinguish "scope drift" (DRIFT) from "plan-budget calibration miss" (ON TRACK with CONCERN noted to operator).

## L-013 (T-510b + T-520 cherry-pick — pre-emptive `_to_jsonable` wrapper as codec-state-immune JSONB-writer convention, 2026-05-07)
Pattern: F4 E1 audit_events double-encode regression (L-011) + audit_events `Object of type UUID is not JSON serializable` regression (intermediate `c241c15` fix) showed that `packages.db.queries.*` write helpers binding `$N::jsonb` parameters are sensitive to two orthogonal failure modes — (a) raw Python dict + asyncpg-without-codec → `InterfaceError: invalid input for query argument`; (b) pre-`json.dumps`-encoded text + asyncpg-WITH-codec → double-encode (string-scalar JSONB stored). The original L-011 active control split convention by per-service codec registration state (analytics-api / feature-engine register codec → pass dicts directly; signal-gateway / strategy-engine do not → use `json.dumps`), creating cross-service drift risk: when a service flips codec-registration state in F5+, all its existing JSONB writers silently flip into the corresponding bug class. T-510b (shadow.py for execution-service) + T-520 cherry-pick (signal_gateway.insert_signal) shipped a *codec-state-immune* convention: every JSONB writer wraps the dict via `_to_jsonable(value)` (recursive UUID/datetime/Decimal → str pre-stringification — defined at `packages/db/queries/audit.py:55`); the outer `json.dumps(...)` wrapper is added only when the consuming service does NOT register the codec. The `_to_jsonable` wrapper is the constant element across both modes; the `json.dumps(...)` wrapper is the codec-state-conditional bit.
Active control: When introducing or modifying any JSONB writer in `packages.db.queries.*` that binds `$N::jsonb` parameters, MUST: (1) wrap the dict via `from packages.db.queries.audit import _to_jsonable  # noqa: PLC2701` (option (c) explicit private-import per T-510b operator default A; alternatives extracted-shared-module / promoted-public flagged in plan-doc); (2) For services that do NOT register `_register_jsonb_codec` (signal-gateway / execution-service / strategy-engine today): emit `json.dumps(_to_jsonable(payload))` as the bind value. (3) For services that DO register codec (analytics-api / feature-engine today; grep `_register_jsonb_codec`): pass `_to_jsonable(payload)` dict directly to asyncpg; codec encoder serialises via internal `json.dumps`. (4) Module docstring MUST document the switch trigger when consumer service flips codec-registration state. Brief-reviewer must reject any `json.dumps(payload)` (raw, no `_to_jsonable` wrapper) on JSONB writers — pre-emptive wrapper is the standard convention going forward, regardless of current codec state. Plan-reviewer must surface this convention as Write-time guidance for any new task introducing a JSONB writer in `packages.db.queries.*`.

## L-012 (T-501 / T-505 ci-full regression — relative downgrade target breaks across head changes, 2026-05-07)
Pattern: Migration integration tests using `alembic downgrade -1` (relative target) silently break across master when a NEW migration lands — the relative target now points one revision later than the test author intended. T-501 added migration 0013 → `alembic head` shifted from 0012 to 0013 → `test_migration_0012_downgrade_preserves_pgcrypto` (which calls `downgrade -1` after `upgrade head` expecting to roll back 0012 → 0011) actually rolled back 0013 → 0012, leaving `backtest_runs` undropped, and the assertion `assert table_row is None` failed in master ci-full ~2 min after the chore(tasks) commit landed (`c11c767` ci-full failure). Local pre-commit only runs unit tests; integration migration tests are env-gated on `POSTGRES_TEST_DSN` and don't run in pre-commit, so the regression surfaced post-merge with no warning at brief-reviewer time. T-501 own test_0013 had the same latent fragility (its `downgrade -1` works today because head==0013 and target==0012, but would break when migration 0014 lands).
Active control: When writing migration integration tests that exercise `alembic downgrade`, ALWAYS use an explicit revision target (`downgrade <revision_id>`, e.g. `downgrade 0011`) instead of relative `-N` syntax. The test's intent is "rollback exactly this migration"; explicit target encodes intent and is robust against future migrations changing `head`. Plan-reviewer must flag any new migration test using `downgrade -1` / `downgrade -N` as a BLOCKER and require explicit target. Brief-reviewer must grep new migration test files for `downgrade.*-[0-9]` patterns. The fix in T-501/T-505 cohort retroactively rewrites both `test_0012_migration.py` (relative -1 → explicit 0011) + `test_0013_migration.py` (relative -1 → explicit 0012) for consistency. Future task `test_0014_migration.py` MUST start with explicit `downgrade 0013` from day one.

## L-011 (F4 E1 smoke double-encode under registered JSONB codec, 2026-05-07)
Pattern: When the connection has a registered JSONB codec (`conn.set_type_codec("jsonb", encoder=json.dumps, decoder=json.loads)` — present in analytics-api + feature-engine lifespans per T-401a / T-110d), pre-serialising a dict with `json.dumps(...)` BEFORE passing it to asyncpg as a `$N::jsonb` parameter triggers DOUBLE encoding: the codec re-runs `json.dumps` on the already-stringified value, storing a JSON string scalar (escaped `"{\"id\":1,...}"`) instead of a JSONB object. Read-back through the codec's `json.loads` decoder returns a string, which Pydantic dict-coercion then drops to `null` in the API response — so the reader sees empty `before_state`/`after_state` while the DB *does* contain (corrupted) data. Surfaced live during F4 E1 smoke at two layers: (a) `POST /api/backtests/` 500 when `_audit_state_dict(BacktestRunRow)` returned a dict whose UUID/datetime fields tripped the unguarded `json.dumps` (no `default=`); (b) `bot_config.apply` audit row showing `after_state: null` in the audit-log viewer because of the double-encode. Sibling `packages/db/queries/signal_gateway.py:110` uses the same pre-serialise pattern but its caller (signal-gateway service) does NOT register the codec, so single-encoding works there — the audit helper inherited the pattern from a different-service precedent and broke under analytics-api's codec. Existing mock-based tests captured the encoded string and assumed it round-tripped correctly; they never simulated the codec's second pass.
Active control: For any JSONB writer in `packages/db/queries/` that runs under a service registering `set_type_codec("jsonb", encoder=json.dumps)` (analytics-api, feature-engine — grep `_register_jsonb_codec`), pass Python dicts DIRECTLY to asyncpg (no `json.dumps` at the call site); pre-convert UUID / datetime / Decimal in the dict via a `_to_jsonable(v)` helper so the codec encoder sees only JSON-native types. Plan-reviewer must flag any new `audit_state_dict` / `payload_dict` helper that pipes into `$N::jsonb` and require: (1) call site passes dict, not `json.dumps(dict)`; (2) regression test that captures `bind_args` and asserts `isinstance(captured, dict)` (not str), AND that emulates the codec by calling `json.dumps(captured)` to prove no `TypeError`. Brief-reviewer must reject `json.dumps(state) if state is not None else None` patterns in JSONB-writer query helpers — even with `default=str` they double-encode under registered codec. Sibling `signal_gateway.insert_signal` is currently safe-by-accident (no codec on signal-gateway service) but should switch to the same pattern if/when the service registers a codec — flagged as latent at L-011 publish time.

## L-017 (fix(T-218b-open-fill-qty-bug), operator-discovered shipped-code bug, 2026-05-08)
Pattern: Test fixtures using artificial "post-update" mock values to bypass downstream state-checks can hide bugs in the pre-update logic. `test_process_open_fill_orders_lookup_to_open_branch` (test_dispatcher.py:366-386 pre-fix) used `_ps_row(remaining_qty=Decimal("5"))` with comment `"# post-update non-zero (no close trigger)"` — that comment assumed `update_position_state_after_fill` ran and decremented qty. The fixture value was artificial — realistic placement-time pre-update value would be the FULL qty (matching event qty), NOT `5`. The test asserted `reconcile_close.assert_not_called()` but did NOT assert `update_position_state_after_fill.assert_not_called()` — so the actual subtract bug (open fill decrementing remaining_qty in placement-time scenario) was masked behind the "post-update fake". Sibling B1-fix test `test_process_invokes_reconcile_close_when_remaining_zeroes` ENCODED the bug as expected behavior (exec_type='open' + remaining_qty=0 → reconcile_close called) — repurposed at fix-time as H-030 defensive-guard pin (assertion inverted to `assert_not_called`).
Active control: For state-mutation tests on functions that read state THEN mutate it, test fixtures MUST use REALISTIC pre-mutation entering values (e.g. placement-time `remaining_qty = full qty`, NOT artificial "post-update" placeholders). Test assertions MUST pin BOTH the mutation behavior (explicit `update_X.assert_called_once` / `assert_not_called` on EVERY mutating helper) AND the post-condition (downstream effect such as `reconcile_close` not called). Plan-reviewer Gate 1 MUST flag any test fixture with comments like `"post-update non-zero"` or `"artificial value to bypass X"` without explicit assertion pinning the SHOULD-NOT-BE-CALLED helper. Brief-reviewer Gate 3 MUST grep for `assert_not_called` count in tests touching `services/execution/` dispatcher / paper-exchange / reconcile paths — every state-mutation test must pin BOTH "what was called" AND "what was NOT called" sides of the contract.

## L-018 (fix(T-218c-paper-dispatcher-skip), operator-discovered shipped-code bug, 2026-05-08)
Pattern: Plan analysis claiming "bug dormant in <mode>" or "<mode> not affected by this issue" is itself a hazard if not backed by direct code-reading evidence. fix(T-218b) plan claimed "PaperExchange synthetic-fill flow does NOT emit ExecutionEvent for open fills via stream_executions (only SL/TP synthetic fills emit)" — this was reasoning, NOT verified evidence. Direct reading of `paper/adapter.py:820-831` shows `_persist_open` enqueues ExecutionEvent on every open fill (plus close at :929-940 + synthetic SL/TP at :1184-1194). The plan-doc's incorrect claim led to T-218b being framed as "live-mode-only" while paper mode had its own separate kill-path (`dispatcher.py:188` unattributable_fill RuntimeError) which would fire BEFORE the H-030 conditional even reached the `update_position_state_after_fill` call site. The H-030 fix is correct for live; H-031 (T-218c) addresses paper independently. Both fixes are needed.
Active control: For any plan claim of the shape "bug dormant in <mode>", "<mode> not affected by this issue", or "<feature> not exercised in <X> path", plan-reviewer Gate 1 MUST require explicit code citation (file:line + grep evidence) showing the relevant code path is not exercised. Reasoning-only "dormant" claims must be flagged as BLOCKER until code-verified. Brief-reviewer Gate 3 + math-validator Gate 4 should similarly verify any "X is dormant" claim against grep evidence at commit time. Operator-driven audit catches such claims; review system should catch them earlier at plan time. The pattern generalizes: any "no-impact" claim about a code path needs code-citation evidence, not reasoning.

## L-019 (fix(T-216c-fill-price-retry-exception), operator-discovered shipped-code bug, 2026-05-08/05-09)
Pattern: Retry loops over external adapter calls must wrap the `await` site with try/except matching the SAME error taxonomy as non-retried sibling calls in the same handler. `placement.py:205-232` retry block iterated `for attempt in range(fill_price_retry_attempts)` over `await adapter.get_fill_price(...)` with NO try/except — exceptions (`NetworkTimeout` / `RateLimitError` / `AuthError`) bypassed the retry counter AND the post-loop `FillPriceUnresolvedError + DLQ` contract. Sibling step 4 (set_leverage) + step 5 (place_market_order) DO have explicit `try/except (AuthError, OrderRejected, NetworkTimeout, RateLimitError)` blocks at their await sites — the retry block was written as if `get_fill_price` only ever returned None or Decimal, never raised. Existing tests covered only None-return retry; zero exception-path coverage. Reviewer T-216a / T-216b1 / T-216b2 focus was post-fill_price pipeline + paper fork + persist-tx + emit ordering — the retry block's exception coverage slipped through both plan-reviewer and brief-reviewer Gate 3 because no `for attempt in range(*retry*)` audit pattern existed.
Active control: For any plan / staged diff containing a retry loop pattern (`for ... in range(*retry*)` or equivalent counter-driven retry), plan-reviewer Gate 1 + brief-reviewer Gate 3 MUST grep the await sites inside the loop and verify they have explicit try/except matching either (a) the taxonomy of non-retried sibling calls in the same handler, or (b) the documented adapter contract for transient errors. ANY raw `await ext_call(...)` inside a retry loop without exception handling is a BLOCKER. The pattern generalizes: counter-based retry with no exception handling on the await site is a hidden silent-failure path.

## L-020 (fix(T-217c-position-state-trade-id-guard), operator-discovered shipped-code bug, 2026-05-08/05-09)
Pattern: Composite-PK SQL update helpers (e.g. `(bot_id, symbol)` for `position_state`) under concurrent INSERT/DELETE-then-INSERT need authoritative-id verification at dispatch site when row identity can change between the read that derives the id and the write that mutates the row. `update_position_state_after_fill(bot_id, symbol, ...)` was PK-bound on `(bot_id, symbol)` only; ExecutionDispatcher's `_derive_exec_type` Path A sourced `trade_id` from the `trades` table (via `select_trade_by_open_order_id` / `select_trade_by_close_order_id`); under a benign close→reopen race, the position_state row identity changed between derivation and write — the UPDATE silently mutated the wrong trade's row. Existing tests used SINGLE trade per (bot_id, symbol) lifetime fixtures, so the mismatch path was untestable. T-218b reviewer focus was the `exec_type` derivation branch table; the WHERE-clause omission for `update_position_state_after_fill` slipped through. Sibling lesson L-018 (T-218c) applies: T-217c plan does NOT make a "dormant in mode" claim — explicitly states "Either mode" impact (live = real-money phantom-close cascade; paper = corruption stays in DB / paper P&L drift).
Active control: For any SQL UPDATE helper bound to a composite PK that can be reused across distinct logical entities (e.g. `(bot_id, symbol)` reused across sequential trades), plan-reviewer Gate 1 + brief-reviewer Gate 3 MUST grep callers and verify they thread an authoritative identity (e.g. `trade_id` from the entity's source table) AND the helper includes that identity in the WHERE clause. Helpers that return `int` (rows updated count) MUST have callers check the return for 0 and halt-on-mismatch (mirroring T-217c/H-033 dispatcher pattern: ERROR log + raise RuntimeError; T-221 reconciliation owns recovery via NATS redelivery + Bybit-OPEN/DB-CLOSED divergence detection). The pattern generalizes: any composite-PK UPDATE under concurrent identity churn is a hidden silent-corruption path unless the WHERE clause anchors to an immutable identity column.

## L-021 (fix(T-537a1-sql-typecast), CI-discovered shipped-code bug, 2026-05-09)
Pattern: PG parameter type-inference fails (defaults to `text` or wrong-type resolution) when the parameter sits in a non-column-direct context — namely arithmetic with operator overloading (e.g. `$2 - make_interval(...)` where PG resolves to `interval - interval = interval` instead of `timestamptz - interval = timestamptz`) or `CASE WHEN ... THEN $N ELSE NULL END` where the result-type defaults to `text` because the NULL branch contributes no type hint and asyncpg's untyped-parameter binding doesn't reach into CASE branches. T-537a1 ci-full surfaced both modes simultaneously: `select_pending_outbox_events` failed with `operator does not exist: timestamp with time zone <= interval` (5 of 7 testcontainer tests); `mark_outbox_event_failed` failed with `column failed_at is of type timestamp with time zone but expression is of type text` (2 of 7). Mock-based unit tests passed (mock-level intercepts SQL before parsing); testcontainer-gated tests existed in `tests/integration/queries/test_outbox.py` per L-008 active control but were skipped locally without `POSTGRES_TEST_DSN` → CI was the first run that exercised them → master shipped broken twice (T-537a1 then T-537a2 propagated the same bug because T-537a2 didn't touch SQL). The fix is a 3-character `::timestamptz` cast at each problem site; the broader insight is that L-008's "testcontainer test exists" active control is necessary but not sufficient — the tests must be **executed** locally against a real PG before push, otherwise CI is the first signal and master can ship broken until next chore.
Active control: For any new SQL helper added to `packages/db/queries/` or `packages/outbox/queries.py` (or any other SQL helper module) that contains a parameter `$N` in a context other than direct column assignment (`SET col = $N` or `INSERT ... VALUES (..., $N, ...)`) — specifically arithmetic with operator-overloaded types (`$N - interval`, `$N + duration`, etc.), CASE branches (`THEN $N`), function arguments without explicit types, or comparison operators across unioned types — plan-reviewer Gate 1 MUST require explicit `::type` cast on the parameter literal in the SQL string. Brief-reviewer Gate 3 MUST grep the staged SQL for parameter sites in non-column-direct contexts and verify each has an explicit `::type` cast. Additionally: for any task adding a new testcontainer-gated test file in `tests/integration/queries/` or `tests/integration/migrations/`, the operator (or implementation Claude Code session) MUST run `POSTGRES_TEST_DSN=... uv run pytest <test_file>` locally before pushing the feat branch — relying on CI as the first execution surface is L-008 sub-gap. Mock-based SQL assertions MUST also pin `::type` cast literals (`assert "$2::timestamptz" in sql`) so SQL-string regression in future edits is caught at mock level even before testcontainer reaches them.

## L-022 (T-526, drift-checker DRIFT §0.3 LOC overage, 2026-05-15)
Pattern: Plans carrying many plan-reviewer Write-time guidance items systematically under-estimate src LOC because each WG item mandates a docstring / inline-comment / explicit-rationale block that does not appear in the line-count estimate (which is sized against logic body only). T-526 plan estimated ~245 src LOC; reality ~515 src (1.92×, ~115 over the §0.3 400 cap) — the overage decomposed almost entirely into documentation: cooldown_gate.py 241 LOC of which only ~130 is logic (WG#4 module docstring + CooldownDecision 5-field docstring + per-helper docstrings); trades.py 102 LOC of which only ~15 is logic (WG#1 charter-invariant docstring + WG#2 L-021 type-cast audit docstring). Compounding factor: AC-required first-of-its-kind scaffolding (metrics.py 63 LOC — first strategy-engine service-metric, build_registry + Metrics dataclass + builder) was AC#8-mandated but never line-budgeted because the plan enumerated only the gate's own files. Mid-impl drift-checker returned ON TRACK at the same LOC count, so the overage reached final drift-checker uncaught — the §0.3 hard cap is a brief gate independent of the drift-checker's plan-fidelity verdict, and ON-TRACK-at-1.9× should itself have been the escalation trigger.
Active control: plan-reviewer Gate 1 MUST add a +20-30% LOC contingency to any plan whose Write-time guidance list has ≥4 items OR whose ACs mandate first-of-its-kind scaffolding (first service-metric, first migration in a service, first adapter method), and MUST state the contingency-adjusted estimate explicitly in the plan §LOC budget so the §0.3 cap proximity is visible before coding. drift-checker MUST treat "ON TRACK but ≥1.5× plan src estimate" as an automatic NEEDS DISCUSSION (escalate to operator at the mid-impl checkpoint, not silently pass) — a plan-fidelity ON-TRACK does not absolve a §0.3 cap breach. brief-reviewer Gate 3 continues to require a declared per-file overage breakdown in the commit body for any cap miss (this part worked: T-526 commit body carried the 14-row table).

## L-023 (T-525a2 + T-525b, pre-commit mypy hook failure ×2, 2026-05-15)
Pattern: An inline `# type: ignore[<code>]` placed on a *specific kwarg line inside a multi-line call* is fragile under the `ruff-format` pre-commit hook. ruff-format reflows the call's argument layout; the trailing comment migrates to a *different physical line* than the argument that actually needs the suppression. mypy then fails the pre-commit hook with a SIMULTANEOUS pair: `unused "type: ignore"` (on the line the comment landed on, which has no error) + the original `[arg-type]`/etc error (on the line that needs it but no longer has the comment). This bit T-525a2 (integration test `bot_id=bot_id,  # type: ignore[arg-type]` → migrated onto `risk_config=...`) and then AGAIN T-525b (unit test, same shape, ~9 call sites, 18 mypy errors) — the local pre-`git commit` mypy/ruff runs passed because they ran BEFORE the ruff-format hook reflowed the file; the hook reformats then mypy re-runs on the reformatted source and fails. The commit does NOT happen (hook failure), costing a fix-and-recommit cycle each time.
Active control: do NOT use an inline per-argument `# type: ignore[...]` inside a multi-line/multi-argument call when the argument is a `NewType`/narrowed-type literal (the recurring case: `bot_id` is `BotId = NewType("BotId", str)` but tests pass a bare `str`). Use `cast("BotId", value)` (string-forward-ref cast — no runtime import needed; mypy resolves it; `cast` is a runtime no-op) or a pre-typed local variable instead — reflow-stable because it is a normal expression ruff-format will not split the suppression away from. plan-reviewer Gate 1 + brief-reviewer Gate 3: when a plan/diff has tests calling a `NewType`-typed parameter with a bare literal across multiple sites, require the `cast(...)`/typed-local form, not inline per-arg `# type: ignore`. The fix verbatim: add `if TYPE_CHECKING: from packages.core import BotId` + `from typing import cast`, replace `bot_id=<lit>,  # type: ignore[arg-type]` with `bot_id=cast("BotId", <lit>),`. (`cast` first arg as a string literal needs no runtime import under `from __future__ import annotations`.)

## L-024 (T-531, pre-commit mypy hook failure on test files, 2026-05-15)
Pattern: A local pre-commit verification of `uv run mypy <changed SOURCE modules only>` passes, but the pre-commit mypy HOOK (which type-checks the full staged set INCLUDING test files) fails — so the `git commit` does not happen and costs a fix-and-recommit cycle. Same hook-vs-local-scope gap class as L-023 (there: ruff-format reflow; here: scope of the local mypy run). T-531 hit two distinct mypy errors only in the new test file: (a) `[union-attr]` — an *explicitly-typed* `AsyncMock` fixture (`def patched_insert(...) -> AsyncMock`) makes `patched_insert.await_args` resolve to `_Call | None`, so `patched_insert.await_args.kwargs` is `Item "None" of "_Call | None" has no attribute "kwargs"`; sibling query tests that do `conn.execute.await_args.args` never tripped this because `conn = MagicMock()` leaves `conn.execute` as `Any` (mypy can't see the union). (b) `[unused-ignore]` — a `# type: ignore[arg-type]` on an `AccountBalance(**base)` `**dict[str, Decimal]` splat into a 5-Decimal-field dataclass is dead (mypy accepts the splat without it). Local source-only mypy + the targeted pytest both pass; the hook is the first surface that runs mypy over the test file.
Active control: before `git commit`, run mypy over the STAGED set INCLUDING test files — `uv run mypy $(git diff --cached --name-only --diff-filter=ACM -- '*.py')` — not just the source modules under change (this is the cheap local mirror of the pre-commit hook's scope; closes the L-023/L-024 hook-vs-local gap generally). For typed `AsyncMock`/`MagicMock` fixtures, narrow `.await_args`/`.call_args` with `assert <mock>.await_args is not None` before `.kwargs`/`.args` (reflow-stable; NOT an inline `# type: ignore`). Do NOT add `# type: ignore[arg-type]` on `<dataclass>(**dict)` splats — mypy accepts them and flags the ignore `[unused-ignore]` (per the `feedback_type_ignore_context` rule: `[unused-ignore]` = dead, remove). plan-reviewer Gate 1 + brief-reviewer Gate 3: when a task adds new test files with explicitly-typed mock fixtures asserting on `.await_args.kwargs`/`.args`, require the `assert ... is not None` narrowing form.

## L-025 (T-532a, math-validator VERIFIED-with-concern, 2026-05-16)
Pattern: A `## Hand verification` section in a plan-doc carried a wrong hand-computed epoch/ms literal (`since=2026-05-16T00:00:00Z → startTime = 1779897600000 ms`; the correct value is `1778889600000` — the stale number actually decodes to 2026-05-27T16:00:00Z, off by ~11.67 days). plan-reviewer Gate-1 APPROVE did NOT catch it (plan-reviewer does not auto-verify math per §6 — that is exactly Gate-4's job). It was harmless THIS time only because the shipped adapter computes `int(since.timestamp()*1000)` at runtime (verbatim mirror of the verified `get_closed_pnl_window`) and the adapter test asserts the window via that *same expression*, not the hardcoded literal — so the wrong number never reached code or a test. But this whole hardening cluster is mirror-tasks: a later windowed-pull task (T-532b or a future sibling) that copies the plan's Hand-verification ms literal into a test fixture would convert a doc-only error into a real capital-path math regression. This is an L-018 sibling (a plan-doc number is NOT code-verified evidence) specialised to numeric Hand-verification literals.
Active control: math-validator MUST independently recompute every `## Hand verification` epoch/ms/Decimal closed-form literal (≥2 methods for dates) rather than trusting the plan's stated number — a plan-doc Hand-verification value is an unverified claim until the reviewer reproduces it; flag any mismatch as a CONCERN even when (as here) the shipped code is correct, because the stale literal is a copy-trap for the next mirror task. plan-reviewer Gate-1: when a plan's `## Hand verification` states a derived constant (epoch ms, round-half-even result, seed value), require the derivation steps inline (not just the answer) so the number is self-checking; prefer code that computes the value at runtime + a test asserting via the same expression over a hardcoded literal. When fixing, correct the plan-doc number AND add the derivation trace (doc-only — no code/test change, no re-test, since the shipped code was already math-correct).

## L-026 (T-519, brief-reviewer FIX FIRST ×2, 2026-05-16)
Pattern: An audit / brief-correction task that fixes a stale/false claim at ONE source-of-truth site corrects only that site, while the SAME claim is duplicated verbatim at OTHER source-of-truth sites — leaving the corpus self-contradicting (§N10: the source of truth must not contradict the just-corrected statement). T-519 corrected the false "H-005 (opposite-signal guard) implemented as a scoring rule" claim in §20 H-005, but the identical false claim survived at `services/strategy_engine/app/consumer.py:30` (module docstring — caught as BLOCKER #1) and then again at `docs/CLAUDE_CODE_BRIEF.md:1565` (§9.5 strategy-engine "Known hazards addressed" — caught as BLOCKER #2, a different §section of the same authoritative file NOT in the staged §20 hunks). Cost: two FIX FIRST → fix → re-review cycles for one logical correction, because each grep stopped at the first hit instead of enumerating the full duplicate set up front.
Active control: when a task corrects a stale/false claim in any source-of-truth site (BRIEF §section, code docstring, ADR, schema comment), BEFORE the first commit run a repo-wide grep of the claim's distinctive tokens across the WHOLE brief (every §section, not just the targeted one) + `services/` + `packages/` (+ ADRs) — EXCLUDING immutable historical plan-docs (`docs/plans/T-*.md` are point-in-time records, NOT source of truth — never rewritten) and forward-looking config/examples (owned by the feature's own backlog ticket, not the audit). Enumerate ALL sites and correct them in the SAME commit; the audit/report records the full corrected-site list + the deliberate scope boundary (which sites are intentionally NOT touched and why). plan-reviewer Gate-1 + brief-reviewer Gate-3: for any claim-correction/audit task, require the plan to commit to "grep-all-source-of-truth-sites" as an explicit Write-time-guidance item, and brief-reviewer greps for residual duplicates of the corrected claim before SHIP (a single residual = FIX FIRST).

## L-027 (T-522, plan-reviewer Gate-1 APPROVE — sign-off/counter conflation caught at chore-close, 2026-05-17)
Pattern: A close-out / operator-sign-off / attestation task's plan that says "the chore-close bumps the phase counter to N/N + marks the phase COMPLETE" conflates "task deliverable shipped" with "the external actor has signed off" — executing it literally writes a FALSE ATTESTATION (phase/MVP recorded signed-off-complete while the operator sign-off blocks are still blank). plan-reviewer Gate-1 APPROVE'd the T-522 plan whose "Scope IN" said counter `64/66 → 66/66` + "F5 COMPLETE" at chore-close; the runbook's own "On sign-off complete" section (also approved) MORE-correctly gated the bump on operator §A/§B PASS — the contradiction surfaced only at chore-close time and was resolved by operator decision B (mark the task `[x]` deliverable-done, counter stays 64/66, the bump+COMPLETE is a future operator-triggered micro-chore). The audit-grade ledger (the very thing T-519/T-521/T-539 made trustworthy) would otherwise have shipped a false MVP-signed-off claim.
Active control: plan-reviewer Gate-1 — for any task whose deliverable is a sign-off / close-out / attestation artifact that a DIFFERENT actor (operator) executes/signs, REQUIRE the plan to gate the phase-counter bump + any COMPLETE / SIGNED / DONE-phase marker on the actual external action, NOT the task-deliverable close; the task-close marks only "deliverable shipped, pending <actor> sign-off". brief-reviewer Gate-3 — reject any staged chore that writes a phase-COMPLETE / counter-at-max / "signed off" marker while the corresponding sign-off block is blank/unfilled (false-attestation guard; sibling of L-018 — a COMPLETE/SIGNED marker is a claim whose evidence is the actual filled sign-off, not the task being closed).

## L-028 (T-522, operator-delegated F5 close-out run — 11 documented-command/prereq drifts, 2026-05-17)
Pattern: Operator-facing runbooks + BRIEF CLI docs + deployment prerequisites drift from shipped reality and are NOT caught by any review gate — plan/drift/brief/math review the code+plan, not whether a documented operator command actually runs end-to-end. The T-522 F5 close-out RUN surfaced **11** such drifts (D1–D11: alembic invocation/DSN, backtest module-form/`--config-path`, CI-gated tests presented as ad-hoc-runnable, missing shadow/paper-env/symbol-map prereqs, HMAC var name, a logging mislabel, scoring bound to an F4+-unimplemented feature) — none catchable short of executing the runbook against a real stack. A close-out/sign-off runbook authored from code-reading (even with WG1 commit-citations + drift ON TRACK + brief SHIP) verifies the EVIDENCE exists, NOT that the OPERATOR PROCEDURE runs; the four gates passed T-522 clean while every operator command in it was broken.
Active control: plan-reviewer Gate-1 — for any runbook / close-out / onboarding deliverable, the plan must state whether each operator command is *execution-verified* or *authored-from-code-reading*; the latter are marked "unverified-procedure" in the runbook body. brief-reviewer Gate-3 — for runbook deliverables the commit body must declare which steps are execution-verified vs authored-only; an all-authored operator runbook is SHIP-able only with that caveat explicit (not a silent omission). Phase close-out: the operator runbook MUST be executed end-to-end (or explicitly accepted as CI-grade-evidence-only with the procedure-drift recorded) BEFORE the phase sign-off — never sign a phase on an unexecuted runbook. Documented operator commands are unverified claims until run (L-018 generalized from "done"-claims to "runnable"-claims).

## L-029 (T-541, plan-reviewer Gate-1 pass-2 BLOCKER → operator phase-gate decision, 2026-05-17)
Pattern: Pulling a backlog item (`T-F5+`/`T-FN+`/"opportunistic") into active implementation when the phase is COMPLETE and TASKS.md records "no next phase unlocked" is a §0.10 phase-gate violation. The operator merely *requesting* the work ("chcem vyriesit H-005") is NOT a phase unlock — it is an intent that still requires formal governance. The main session-start treated "operator-requested" as the unlock and drafted/ran two plan-reviewer passes on H-005 before pass-2 BLOCKER-1 caught that (a) the backlog ticket was not a numbered task and (b) no phase was unlocked — wasted gate cycles, and a near-miss of ~180-230 LOC of trading-critical code entering a signed-off-MVP repo with no phase home. Resolution required operator governance, not a plan-text fix: promote backlog→numbered task + explicitly unlock the phase, mechanised via a §6.7 phase-opener ADR (ADR-0015) + a phase-populator meta-task (T-541, mirror T-523/T-500/T-200).
Active control: at SESSION-START, when the operator requests work on a backlog/`T-FN+`/opportunistic item, FIRST read TASKS.md phase state — if the current phase is COMPLETE or "no next phase unlocked", surface the phase-gate explicitly in the session-start summary ("this needs: backlog→numbered-task promotion + an explicit phase unlock, typically a §6.7 phase-opener ADR — proceed with that, or keep it as an accepted carve-out?") rather than treating the request as the unlock. plan-reviewer Gate-1 — for ANY task whose subject is a backlog/`T-FN+` item, verify TASKS.md shows an unlocked phase that owns it + a numbered task entry; if absent, return NEEDS DISCUSSION (operator governance), never APPROVE a plan that silently assumes the unlock. A signed-off-MVP repo is phase-gated HARDER, not softer — post-MVP work needs a deliberate post-MVP phase, not an ad-hoc pull.

## L-030 (T-542, drift-checker ON TRACK mid-impl §N10 blast-radius + plan-reviewer Gate-1 pass-1 REVISE BLOCKER-1, 2026-05-17)
Pattern: TWO recurring traps when adding a **config-default-ON** pre-scoring/pipeline guard that does an **unconditional per-signal DB read** (mirroring an existing default-OFF guard like the T-526 cooldown gate). (1) **§N10 blast radius is mis-scoped.** The T-542 plan's §N10 said "sibling tests stay green — no test has a bot with an open position + an opposite signal"; the real failure was unrelated to the hazard condition — because the new knob defaults `True` (vs cooldown/caps which default `0`=disabled and short-circuit before any DB hit), the gate did a `pool.acquire()`+focused-query for EVERY non-CLOSE signal, which `KeyError`'d on the scoring-path mock pools (`fetchrow` returns signal-id/scoring-shaped rows, not position rows) of **every consumer-driving test across multiple files** (`test_consumer.py` + 2 `packages/scoring/tests/` F3 files that `load_bot_config(real configs/bots/*.yaml)` — real configs inherit the `True` default). The plan's `## Files` list under-enumerated the test-isolation footprint. (2) **A whitelist-emptying / un-DEFER task breaks a meta-test that asserts the production constant directly.** `tests/test_hazard_catalog_coverage.py`'s synthetic parser self-test asserted `5 in _KNOWN_DEFERRED` and used the production constant in its gating logic — emptying the constant (the whole point of the un-DEFER) red-failed the self-test; the plan's first "update the fixture to resolved form" instruction was wrong and would have shipped a red suite.
Active control: plan-reviewer Gate-1 — for a new **default-ON guard with an unconditional per-signal external/DB read**, require the plan's §N10 to (a) enumerate EVERY consumer/pipeline-driving test file (grep the entrypoint symbol, e.g. `make_signal_handler`, repo-wide) and state the per-file mock-pool-shape impact, and (b) state the test-isolation mechanism explicitly (neutralise the orthogonal guard via the shared config builder + `model_copy` for real-YAML loaders — NEVER by mutating real `configs/bots/*.yaml`, which would weaken the guard in production; the production default stays as the BRIEF mandates). For ANY un-DEFER / whitelist-shrink / catalog-flip task, require the plan to enumerate every test that *asserts the mutated constant/catalog directly* (incl. synthetic parser self-tests) and decouple them onto a LOCAL literal before the flip — a parser/algorithm self-test must never reference the production whitelist it is meant to be independent of. drift-checker — treat "faithful impl of an approved plan that nonetheless breaks sibling tests the plan said were safe" as the signal to re-scope §N10 at the mid-impl checkpoint (it is ON TRACK iff the fix is mechanical pattern-consistent isolation with production untouched; otherwise NEEDS DISCUSSION).

## L-031 (T-544, plan-reviewer Gate-1 pass-1 REVISE BLOCKER-2 — L-026 first-hit recurrence on a dual-signed doc, 2026-05-17)
Pattern: L-026 ("enumerate ALL sites, never stop at the first hit") **recurred a second time** at the plan-stage of an F5-residual un-DEFER/forward-annotation: T-542's plan missed BRIEF §22-site-B; T-544's plan missed `docs/runbooks/F5_close_out.md` §B Result `:106`, enumerating only §A `:91`. The specific repeatable structure: **`F5_close_out.md` has TWO parallel operator-signed point-in-time sign-off sections — §A (paper-feature-complete, E1..E5, signed ~`:88-90`) AND §B (Live-ready, E6, signed `:111`) — and BOTH Result lines carry the same F5-residual context** (H-005 / strategy-engine-smoke / E3-CI-grade-per-decision-A). A plan that forward-annotates "the F5 residual" by grepping/citing one Result line will always miss the twin. Both passes were caught by plan-reviewer, not the author — a discipline-application gap on a *known* lesson, on a *specific recurring document shape*, so it warrants a sharpened, document-specific control (L-026 generic "enumerate all" was insufficient to stop the recurrence twice).
Active control: ANY task that touches `docs/runbooks/F5_close_out.md` to forward-annotate or reference an F5 residual/sign-off MUST treat it as a **dual-signed doc**: enumerate BOTH §A (`:~88-91`) and §B (`:~106-111`) Result/sign-off sections, state each is operator-signed point-in-time → BYTE-UNCHANGED (ADR-0015:35), and cover both with the single shared `## Deliberate residual` forward-annotation. plan-reviewer Gate-1 — for any plan citing an `F5_close_out.md` line, reject (REVISE) a §A-only OR §B-only enumeration; require the explicit "§A:NN AND §B:NN both checked, both byte-unchanged" pairing in the plan's site-set BEFORE approval. Generalize: when L-026 enumeration targets a document with a known parallel/mirrored structure (dual sign-off, alpha/beta service pair, A/B variant), enumerate the structure's PAIR explicitly by name, not by a single grep hit.

## L-032 (T-547, CI-fast unit RED on master from T-546-batch push — ops-framing skipped the suite on git-committed deletions, 2026-05-17)
Pattern: an "ops task" (CLAUDE.md ops-mode, "gates don't apply") that `git rm`/renames/edits git-tracked files OR removes `compose*.yaml` service-blocks which tests depend on silently regresses the suite — ops-framing is NOT a gate waiver for git-committed deletions (CLAUDE.md: "anything that goes into git commit = development task"). Commit `1750021` (demo-bot ops task) deleted `configs/bots/{alpha,beta}.yaml` + the `strategy-engine-alpha`/`-beta` compose blocks (operator-directed); 6 `packages/scoring/tests/test_f3_*` files hardcoded `_REPO_ROOT/"configs"/"bots"/{alpha,beta}.yaml` loads + a T-544 compose-pin asserted `strategy-engine-smoke == strategy-engine-beta` → 8 ci-fast `unit` tests red + 1 silent `pytest.skip` (lost coverage), surfaced only by the batched master push (the ops task never ran the suite). Sibling of L-030 (real-config↔test coupling) + L-028 (gates-green-while-reality-differs); the new edge = the *ops/dev classification gate itself* was the bypass, not a review gate.
Active control: before committing ANY ops-framed task: (1) `git diff --staged --name-status` → for every Deleted/Renamed path, `grep -rlF <basename> tests/ packages/*/tests/ services/*/tests/`; (2) for Modified `compose*.yaml`, `git diff --staged -- compose*.yaml | grep -E '^-[[:space:]]+[a-z0-9-]+:'` to list removed service-block keys, then `grep -rlF <removed-service-name> tests/`; any hit in (1) or (2) ⇒ run those tests (or the fast suite) before commit. The ops/dev label does NOT waive this. brief-reviewer Gate-3 (when an ops-framed task IS routed through it): treat any staged `D`/`R` of a git-tracked config/fixture/compose-block as FIX-FIRST unless the diff shows the dependent tests were updated in the same commit.

## L-033 (T-549a, brief-reviewer Gate-3 FIX FIRST — §N4/regression pin cloned mid-file silently relocated a trailing assert, 2026-05-17)
Pattern: a "clone the existing live/positive test for the new case" pin (§N4 / regression) is added via an `Edit` whose `old_string` ends at what the author *believes* is the original test's last line — but the original test had a further trailing assert the author's partial `Read` did not show. The new test gets inserted *between* the original's penultimate and last assert, so the original test **silently loses its trailing assert** (it moves onto the clone). The suite stays green (the assert still runs, just on the wrong test), so CI / a green-suite check does NOT catch it — only line-level review does. T-549a: the cooldown §N4 pin landed between `test_live_mode_queries_trades_table`'s 2nd and 3rd assert (the other 4 gate pins appended at EOF because their live-dispatch test was file-last → defect-free; only cooldown had a following test). drift-checker read it as "complete clone, file fine"; brief-reviewer's line-diff vs HEAD caught the live test's lost `assert captured[0][1:] == (...)`.
Active control: when cloning an existing test for a new pin, APPEND the clone at end-of-file (or immediately after the *entire* original test verified by reading to the next `def`/EOF), NEVER via an `old_string` that stops at a believed-last assert. Author: before the clone Edit, `Read` the original test through to the next top-level `def` (or EOF) to capture its FULL assert extent. drift-checker / brief-reviewer: for any "clone existing test" pin, diff the insertion-boundary against the original test's HEAD extent — verify the original test retains every assert it had at HEAD; a green suite is NOT proof of pin/original integrity (the moved assert still executes). Generalises L-026 "enumerate the full extent, not the first hit" to test-bodies: the unit of enumeration is the whole original test, not its first N asserts.

## L-034 (T-550, plan-reviewer Gate-1 pass-1 REVISE BLOCKER, 2026-05-18)
Pattern: a bug-fix plan that changes a literal / dict / param-shape / contract value asserted by an existing test, while claiming "no test pins X" or enumerating the affected sibling tests incompletely, will red-fail the un-enumerated sibling test on faithful implementation — the implementation Claude then either ships a red suite or "fixes" the sibling test with no plan mandate. T-550's plan asserted *"NO test pins the no-symbol params"* and listed 4 no-symbol get_positions tests as staying green, but `test_adapter.py:1101` (`..._query_shape_no_filter`) pinned exactly `params == {"category":"linear"}` — the Bybit-incomplete contract that WAS the bug (a mock-level params-pin froze an incomplete external-API contract → green locally / crash on the real API, L-021/L-008 sibling); faithful impl (adding `settleCoin`) would have red-failed it. Same class as L-018 (no-impact claim not code-evidenced) + L-015 (sibling-test impact, there migration-specific) generalised: ANY plan claim about existing test-coverage state ("no test covers/pins X"; "tests A/B/C are the only affected") is an unverified claim until grep-evidenced across the WHOLE relevant test module by the *assertion target*, not by the obviously-related test name.
Active control: plan-reviewer Gate-1 + brief-reviewer Gate-3 — for any fix that changes a pinned literal / dict / param-shape / contract value, require the plan to grep the whole relevant test module(s) for assertions pinning that exact value/shape (grep the asserted token itself, e.g. `{"category": "linear"}` or the changed param name — NOT just the test name), and enumerate EVERY such sibling test as either "updated in the same commit" or "stays green (return-value-mocked / different path)" with the reason stated. A plan sentence of the form "no test pins/covers X" is a BLOCKER until backed by that whole-module grep. Implementer: when a fix changes a pinned value, the sibling-test update is in-scope of the SAME commit (never a separate "test fix" commit); prefer correcting the existing canonical pin in-place over adding a parallel test (avoids divergent duplicate pins — the T-550 pass-1 resolution).
