# T-549b — `exchange.mode: demo` distinct Bybit endpoint routing + demo safeguard advisory

**Status:** Gate-1 APPROVE (plan-reviewer, single pass; no BLOCKERS/CONCERNS). Awaiting operator "proceed".
**Phase:** F6 numbered. Sibling half of operator-approved split T-549 → T-549a (DONE `bb62f79`) + **T-549b** (this; blocked-by T-549a ✅ DONE). ADR-0015 §70-81 decision-C standing basis (operator-directed post-MVP fix). §0 one-task-at-a-time. F6 ledger entry + counter 8/8→9/9 added at task close (L-027, mirror T-545..T-549a), NOT pre-written.

## Why T-549b exists (carried from T-549a deliberate boundary)

T-549a made `"demo"` a valid 4th `exchange_mode` (scoring/core/db/bus literals) + routed the 5 strategy-engine risk gates' table-dispatch to the real `trades`/`position_state` realm for demo. It DELIBERATELY left (L-031 PAIR by name) the execution-service adapter-construction + endpoint-URL half to T-549b: `pool.py:316` allowlist, `_construct_bybit_adapter` demo branch, the safeguard, demo URL constants, `configs/bots/demo.yaml` mode-flip, the endpoint ADR, BRIEF amendment, ops.md discharge. In T-549a-only state demo.yaml stays `mode: live` so `pool.py:316` never observes "demo" (internally consistent). T-549b completes the demo path so the parked demo bot can be armed against the correct Bybit endpoint (last operator arming attempt: `retCode=10003 API key is invalid` because demo keys were hitting live `api.bybit.com`).

## Verified external facts (Bybit V5 official docs — bybit-exchange.github.io/docs/v5/demo, fetched 2026-05-18)

- Demo REST base URL: `https://api-demo.bybit.com` (also confirmed by the T-549a-landed comment `packages/core/types.py:131`).
- Demo PRIVATE WS: host `wss://stream-demo.bybit.com`; the adapter's `BybitV5PrivateWs` appends the standard V5 `/v5/private` path (live=`wss://stream.bybit.com/v5/private`, testnet=`wss://stream-testnet.bybit.com/v5/private`) → demo = `wss://stream-demo.bybit.com/v5/private`. Demo PUBLIC WS uses mainnet `stream.bybit.com` — IRRELEVANT here (the adapter pool only uses the private WS).
- Demo requires a DEDICATED demo-account API key (independent account/user-id) — root cause of the operator's `10003`; ops.md discharge must state demo keys ≠ live keys.

## Operator-resolved OQs (baked in — no open questions remain)

- **OQ-1 = ADD DEMO ADVISORY LOG.** Demo bypasses `BOT_CONFIRM_LIVE` env-gate + Telegram (no real capital) BUT `_check_live_mode_safeguard` logs a `DEMO MODE ENGAGED` WARNING at startup (no env check, no `bus.publish`) for operator visibility (real Bybit demo account = real order lifecycle). testnet/paper still silently bypass; live unchanged.
- **OQ-2 = COMMENTS-ONLY compose fix.** Correct the stale "Bybit demo uses the live API endpoint" comment claims in the 2 compose.yaml blocks; KEEP `BOT_CONFIRM_LIVE: ${BOT_DEMO_CONFIRM_LIVE:-}` wiring untouched in both (inert-but-harmless once demo≠live; YAML parse ignores comments → the `test_compose_strategy_engine_services.py` demo↔smoke mirror-pin is unaffected → L-032 avoided).

## Source TOUCH (production)

1. `services/execution/app/pool.py` — add 2 module constants after :76: `_BYBIT_DEMO_REST_URL = "https://api-demo.bybit.com"`, `_BYBIT_DEMO_WS_URL = "wss://stream-demo.bybit.com/v5/private"` (L-001 protocol-binding constants class, mirror `_BYBIT_TESTNET_*`).
2. `pool.py` `_check_live_mode_safeguard` (:157-212) — insert a demo branch BEFORE the `if bot_row.exchange_mode != "live": return` (:181): `if bot_row.exchange_mode == "demo": bound_logger.warning("DEMO MODE ENGAGED", bot_id=bot_row.bot_id, exchange_mode="demo"); return`. No env read, no `bus.publish` (OQ-1). Update the function docstring (:165-180) to document the demo advisory branch. testnet/paper path (`!= "live"` return) unchanged; live path unchanged.
3. `pool.py` `_construct_bybit_adapter` (:228-233) — replace the 2-way `if testnet … else (prod)` with 3-way: testnet→testnet URLs, demo→demo URLs, else (live)→prod URLs. Keep the existing creds path (`BOT_<ID>_BYBIT_API_KEY/SECRET/SUB_ACCOUNT`) verbatim (H-022/ADR-0004 unchanged — only base_url/ws_url vary by mode).
4. `pool.py` `build_adapter_pool` (:316) — `if bot_row.exchange_mode in ("live", "testnet"):` → `("live", "testnet", "demo")` so demo constructs the Bybit adapter (not paper). The safeguard at :318 is already inside this branch and now logs the demo advisory.
5. `pool.py` module docstring (:21-29) — rewrite the "Bybit URL routing per exchange_mode" list: add `demo → REST https://api-demo.bybit.com + WS wss://stream-demo.bybit.com/v5/private`; DELETE the now-false "Demo deployment (per memory deployment.md) uses exchange_mode='live' … live URLs are the contracted F2 routing for demo sub-accounts (ADR-0004)" paragraph (that ADR-0004 contract is superseded by ADR-0017).

## Config TOUCH

6. `configs/bots/demo.yaml:17` — `mode: live` → `mode: demo`. `status` lives in the `bots` DB row, kept `paused` by operator — NOT touched by this task.
7. `configs/bots/demo.yaml` header comment (:1-11) — rewrite: the "exchange.mode: live because the Bybit demo account uses the LIVE API endpoint" rationale is now FALSE; replace with "mode: demo → routes to dedicated Bybit Demo Trading endpoint (api-demo.bybit.com) per T-549b/ADR-0017; requires demo-account API keys (not live keys)". Keep the "parked status='paused' pending operator arming" operational note.

## Config-comment TOUCH (compose — OQ-2=A comments-only; L-026 stale-claim set, L-032-safe)

8. `compose.yaml` execution-service block comment (~:344-351) — correct the "Bybit demo uses the live API endpoint" framing to the api-demo endpoint reality. `BOT_CONFIRM_LIVE: ${BOT_DEMO_CONFIRM_LIVE:-}` (:351) UNTOUCHED.
9. `compose.yaml` strategy-engine-demo block comment (~:386-388) — "exchange.mode: live (Bybit demo uses the live API endpoint)" → corrected to `mode: demo` + api-demo endpoint. env lines (:410) UNTOUCHED.
   (Comments are not YAML data → `tests/scripts/test_compose_strategy_engine_services.py` parses data only → demo↔smoke mirror-pin unaffected. Verified by RUNNING that test in §N10.)

## Docs TOUCH (markdown — ADR §0.3-exempt; BRIEF/ops doc; §6.7 amendment mechanism via the new ADR)

10. NEW `docs/adr/0017-demo-distinct-bybit-endpoint.md`: Decision = (a) `exchange_mode="demo"` routes to dedicated Bybit Demo Trading endpoints (`api-demo.bybit.com` / `stream-demo.bybit.com/v5/private`), superseding ADR-0004's "Demo / testnet routing clarification" demo=live-URL F2 contract (the explicitly-anticipated "F5+ may add a separate demo URL family" path at ADR-0004:75); (b) demo safeguard = `DEMO MODE ENGAGED` advisory, no env-gate/no Telegram (operator decision, OQ-1). §6.7 BRIEF-amendment mechanism (sibling-mechanism of ADR-0016 — the operator-decision-ADR precedent under F6/decision-C). Status/Supersedes: "Supersedes ADR-0004 §Demo/testnet routing clarification (in part)". Cross-refs: ADR-0015 (F6/decision-C), ADR-0016 (sibling), ADR-0004 (superseded-in-part), T-549a (foundation), L-026/L-031.
11. `docs/adr/0004-bot-credentials-env-var-source.md` — add a one-line head Status-note pointer: "Demo/testnet routing clarification § partially superseded by ADR-0017 (demo distinct endpoint) — see ADR-0017 for the active demo routing". Body UNCHANGED (point-in-time record; mirrors the ADR-0003↔ADR-0004 head-pointer precedent verbatim).
12. `docs/CLAUDE_CODE_BRIEF.md` (§6.7 amendment via ADR-0017) — L-026 site set, anchor by §-section + content (line numbers indicative, BRIEF shifts):
    - §7.2 bots DDL comment (~:854) `-- 'live' | 'testnet' | 'paper'` → `… | 'demo'`
    - OrderRequest schema (~:1334) `Literal["live", "testnet", "paper"]` → add `"demo"`
    - §B.1 alpha.yaml (~:3096) `mode: testnet  # live | testnet | paper` → comment `… | demo` (alpha's VALUE unchanged — only the valid-values enumeration comment)
    - §16.5 (~:2282) "Testnet and paper modes do not require this." → add: demo also doesn't require `BOT_CONFIRM_LIVE` but logs a `DEMO MODE ENGAGED` advisory (no Telegram) because it places real orders on the Bybit demo account.
    - WRITE-TIME re-grep BRIEF for every `live.*testnet.*paper` / `"live", "testnet", "paper"` enumeration; §2.5:~268 (paper→PaperExchange data-flow) + §2.6 (backtest) are DELIBERATELY-BOUND NON-sites (demo→Bybit is a different path; forcing a demo sentence there = scope creep) — record this boundary explicitly per L-026, unless the re-grep shows either para actually enumerates the mode set.
13. `docs/runbooks/ops.md` — MANDATORY ops-runbook trigger DISCHARGE (the T-549a plan recorded the trigger FIRED for the ExchangeSection.mode schema change, bound to T-549b as a BLOCKING acceptance item, 3 sub-conditions; precedent T-544 DISCHARGED-with-recording):
    - new-bot template (~:129) `mode: paper  # paper | testnet | live` → add `demo` + a `mode: demo` note: routes to Bybit Demo Trading (`api-demo.bybit.com`); requires DEMO-account API keys (NOT live keys — the `10003` root cause); does NOT need `BOT_CONFIRM_LIVE`; logs `DEMO MODE ENGAGED` advisory; writes real `trades`/`position_state` (T-549a) so all risk gates apply.
    - live-bot operational bullet (~:107-115) — add demo's distinction (demo: no `BOT_CONFIRM_LIVE`, needs demo keys, distinct endpoint).
    - "Pre live/testnet bot" env block (~:238) — note demo uses the same `BOT_<ID>_BYBIT_*` family but with demo-account keys; no `BOT_BOTID_CONFIRM_LIVE` needed for demo.
    - Append the ops.md-update note at the end of the task per the CLAUDE.md ops-runbook rule. This DISCHARGES the T-549a-bound trigger.

## Test TOUCH (§N4)

14. `services/execution/tests/test_pool.py` — NEW `test_bybit_demo_bot_constructed_with_demo_urls` mirroring `test_bybit_testnet_bot_constructed_with_testnet_urls` (:294-313): demo bot row, assert `client_factory.call_args.kwargs["base_url"] == "https://api-demo.bybit.com"` + `ws_factory.call_args.kwargs["ws_url"] == "wss://stream-demo.bybit.com/v5/private"`; env WITHOUT `BOT_CONFIRM_LIVE` (pins OQ-1).
15. `services/execution/tests/test_pool_live_safeguard.py:34` — `_bot_row` helper param `exchange_mode: Literal["live", "testnet", "paper"] = "live"` → add `"demo"` (the L-031 boundary-pair member T-549a deliberately left for T-549b).
16. `test_pool_live_safeguard.py` — NEW `test_safeguard_logs_DEMO_MODE_ENGAGED_for_demo`: `exchange_mode="demo"`, env `{}` → `logger.warning` called once with `("DEMO MODE ENGAGED", bot_id=..., exchange_mode="demo")`, `logger.error.assert_not_called()`, `bus.publish.assert_not_awaited()`, NO RuntimeError. Keep `test_safeguard_bypassed_for_testnet`/`_paper` UNCHANGED.
17. L-033 control: when adding tests 14/16, APPEND at end-of-file or immediately after the *entire* mirrored original test (read through to the next top-level `def`/EOF first) — never split a believed-last assert.

## §N10 / L-030 / L-032 blast-radius (explicit enumeration — the recurring trap T-542/T-547)

- demo.yaml `mode: live`→`demo`: repo-wide grep shows NO test loads `configs/bots/demo.yaml` individually. Config-loader/glob tests to RUN-and-confirm (demo is a VALID mode post-T-549a so any glob-all-configs validator still passes — but VERIFY, do not reason-only per L-018/L-030): `packages/scoring/tests/test_yaml_loader.py`, `test_f3_e1/e2/e3/e4` (T-547 relocated alpha/beta to `packages/scoring/tests/fixtures/`), `services/strategy_engine/tests/test_config.py`, `test_app_factory.py`, `conftest.py`, `tests/scripts/test_backtest_cli.py`, `services/analytics_api/tests/test_router_backtests.py`.
- compose comments-only: `tests/scripts/test_compose_strategy_engine_services.py` (all 3 fns) parses YAML data, not comments → unaffected; RUN to confirm (L-032 — the exact T-547 regression class).
- pool.py safeguard demo branch: existing `test_safeguard_bypassed_for_testnet`/`_paper` + all `test_pool.py` live/testnet/paper tests must stay green (demo branch is `== "demo"` guard, orthogonal).
- Full fast suite (`uv run pytest` ci-fast scope) must be 0-failed before commit.

## Hazards (§20)

No §20 hazard touched — URL string routing + an advisory log + the :316 allowlist token; zero P&L/order-placement/SL/WS-protocol/reconciliation logic. demo routes to the SAME real `trades`/`position_state` realm T-549a established (H-001/H-012 cumulative-delta path untouched). H-022/ADR-0004 per-bot creds env path verbatim-unchanged. Consistent with T-549a's "no §20 hazard touched" framing.

## §N8 / migration

No Alembic migration — `bots.exchange_mode` is `sa.Text()` (no PG enum DDL; T-549a established this). demo.yaml mode-flip is config, not schema. §N8 N/A.

## §0.3 LOC budget (L-022 contingency stated)

Logic src: pool.py ~12 + tests ~55-75. Markdown/doc: ADR-0017 new file (§0.3-exempt per markdown precedent), BRIEF 4 one-line edits, ops.md ~14 lines, demo.yaml comment ~8, compose 2 comment edits ~4, pool.py docstring ~6. L-022 +20-30% doc-contingency: realistic ~90-130 src+test LOC, well under §0.3 400 cap; no sub-split. L-026 enumeration discipline is the live risk, not LOC.

## Phase gate (L-029) — explicit

PASS, not assumed: operator-approved split T-549→T-549a/b; F6 unlocked (ADR-0015 §6.7); decision-C standing basis (ADR-0017 is a *technical/brief-amendment* ADR superseding ADR-0004, NOT a phase-governance ADR; sibling of ADR-0016 → correctly scoped). TASKS.md: T-549b has NO `- [ ]` numbered line yet; creating it is part of opening the task; F6 counter 8/8→9/9 at task close per L-027 (NOT pre-written).

## Gate plan

plan-reviewer APPROVE ✅ → drift-checker RUN (multi-site L-026/L-031/L-032 correctness; checkpoints: after pool.py, after first green test run, pre-brief) → brief-reviewer Gate-3 (verifies WG + ops.md-discharge + L-026 full site-set + L-033 test-append integrity) → math-validator Gate-4 ACTIVE (`services/execution/` IS in the Gate-4 trigger path) → expected `VERIFIED` (URL string routing + advisory log + enum token; zero Decimal/P&L/indicator math; `placement.py`/`pnl/`/`trades.py` NOT in diff) → `fix(T-549b)` + separate `chore(tasks)` F6 8/8→9/9 with L-027 prose. NO push (operator CI-minute gate — push only on explicit operator signal). Diff ≤400.

## Write-time guidance (plan-reviewer verbatim — brief-reviewer Gate-3 verifies each)

1. OQ-1 advisory-log exactness: the demo branch in `_check_live_mode_safeguard` MUST be inserted strictly BEFORE the `if bot_row.exchange_mode != "live": return` at :181, do exactly `bound_logger.warning("DEMO MODE ENGAGED", bot_id=bot_row.bot_id, exchange_mode="demo")` then `return` — NO `env.get(...)`, NO `MessageEnvelope`, NO `await bus.publish`, NO `now_fn()`. testnet/paper (`!= "live"` return) and the live path (env-gate + publish) byte-unchanged. Update the docstring numbered list (:165-180) to add the demo step as item 1 (renumber: demo-advisory, then != "live", then unset-env, then confirmed) without altering the existing §16.5-verbatim wording for the live clauses.
2. L-026 BRIEF site closure: before staging, re-grep BRIEF for every `live.*testnet.*paper` AND `"live", "testnet", "paper"` enumeration (the §7.2:854 DDL comment, §1334 OrderRequest Literal, §B.1:3096 alpha comment, §16.5:2282 are the known four). For §2.5:264-268 (paper-mode data-flow) and §2.6 (backtest) record them explicitly as deliberately-bound NON-sites with the rationale "demo→Bybit is a distinct path; injecting a demo sentence into the paper-data-flow / backtest para is scope creep" — UNLESS the re-grep shows either para actually enumerates the mode-set token, in which case it becomes an in-scope L-026 site. State the final site-set (in-scope + bound-out-with-reason) in the commit body.
3. L-032 compose-comment safety + full-suite gate: edit ONLY comment text in the two compose blocks (~:348-351 execution-service safeguard, ~:386-388 strategy-engine-demo); the `BOT_CONFIRM_LIVE: ${BOT_DEMO_CONFIRM_LIVE:-}` lines at :351 and :410 stay byte-untouched. After edits run `tests/scripts/test_compose_strategy_engine_services.py` (all three fns incl. `test_smoke_mirrors_demo_exactly_modulo_bot_keys`) AND the full ci-fast suite (`uv run pytest`) to 0-failed BEFORE the commit gate — the demo.yaml `mode` flip + compose-comment edits are exactly the T-547 ops/dev-class regression shape; a green targeted subset is not sufficient evidence.
4. Test 14 env construction: `test_bybit_demo_bot_constructed_with_demo_urls` must use a NEW env dict containing only the `BOT_ALPHA_BYBIT_API_KEY/SECRET/SUB_ACCOUNT` creds and explicitly OMITTING `BOT_CONFIRM_LIVE` (do not reuse `_LIVE_ENV`, which carries `BOT_CONFIRM_LIVE: "yes"`) — this is what actually pins OQ-1 (demo constructs the Bybit adapter without the live env-gate). Assert `client_factory.call_args.kwargs["base_url"] == "https://api-demo.bybit.com"` and `ws_factory.call_args.kwargs["ws_url"] == "wss://stream-demo.bybit.com/v5/private"`. Keep `_make_bus_for_live_tests()` (harmless; demo path never reaches `bus.publish`).
5. L-033 clone-append integrity: before cloning `test_bybit_testnet_bot_constructed_with_testnet_urls` (:294-313) and `test_safeguard_bypassed_for_testnet` (:138-152), Read each original through to the next top-level `def`/EOF to capture its full assert + teardown extent (the testnet pool test has a `task.cancel()` / `asyncio.gather(...)` teardown after the URL asserts); append the new test as a whole block after the entire original — never via an `old_string` ending at a believed-last assert. `test_safeguard_bypassed_for_testnet`/`_paper` stay byte-unchanged (new branch is `== "demo"`, orthogonal to the `!= "live"` path); the new safeguard test asserts the warning IS emitted with `logger.error.assert_not_called()` + `bus.publish.assert_not_awaited()` + no RuntimeError.
