# T-550 — bybit_v5 `get_positions()` sends `settleCoin` for the no-symbol (reconcile) path

**Status:** Gate-1 APPROVE (plan-reviewer pass 2/2; pass-1 REVISE BLOCKER [missed existing `:1101` no-symbol params-pin + false "no test pins"] + CONCERN [L-033 on in-place edit] both resolved).
**Phase:** F6 numbered. Operator-directed post-MVP fix surfaced during the T-549b demo-arming ops retry (operator: "Založ T-550 a pokračuj"). ADR-0015 §70-81 decision-C standing basis (same governance as T-545..T-549b — no per-item ADR; F6 `/N` grows; T-541 opener excluded). §0 one-task. F6 9/9→10/10 + entry at THIS close per L-027 (NOT pre-written; mirror T-545..T-549b). Dev task — Gate-1 + Gate-3; drift-checker SKIPPED (trivial <30 src LOC, CLAUDE.md, T-543/T-546 precedent); math-validator OUT-of-scope (`packages/exchange/` not in Gate-4 trigger list, T-548 precedent).

## Bug (production-surfaced, T-549b demo-arming ops retry 2026-05-18)

T-549b demo endpoint routing VERIFIED in prod (`DEMO MODE ENGAGED` + `adapter_pool_built bots_loaded:1` + `GET https://api-demo.bybit.com/v5/position/list 200 OK`, no 10003), then execution-service crash-looped at lifespan:
```
packages.exchange.errors.OrderRejected: Missing some parameters that must be filled in, symbol or settleCoin
main.py:210 reconcile_on_startup → restart.py:78 await adapter.get_positions() → adapter.py:284 get_positions → client.py:194 raise mapped
```
`get_positions(symbol=None)` builds `params={"category": _CATEGORY}`, adds `symbol` only when non-None. Bybit V5 `GET /v5/position/list` for `category=linear` REQUIRES `symbol` OR `settleCoin` (verified verbatim bybit-exchange.github.io/docs/v5/position 2026-05-18: *"either `symbol` or `settleCoin` is required. `symbol` has a higher priority"*; camelCase `settleCoin`; `settleCoin=USDT` → all USDT-margined linear perp positions). `restart.py:78` `reconcile_on_startup` calls `get_positions()` with NO symbol. Latent because get_positions() never ran against a real Bybit endpoint before (demo is the first; operator's documented "first real exercise surfaces a latent bug" chain: alerting httpx → T-547 → T-548 → T-549b 10003 → this).

**Accurate test-state (pass-1 BLOCKER fix):** the no-symbol params ARE pinned today by `test_get_positions_calls_upstream_with_bybit_v5_query_shape_no_filter` (`packages/exchange/bybit_v5/tests/test_adapter.py:1101`, assertion `:1108`): `assert call.kwargs["params"] == {"category": "linear"}`. **That pinned contract is itself the bug** — it asserts the Bybit-incomplete param set (missing `settleCoin`) and stayed green only because `_make_client_mock()` intercepts `client.request` before real Bybit param validation (textbook L-021/L-008: a mock-level params-pin freezing an incomplete external-API contract → green locally, crash against the real endpoint). Symbol-path pin `:513` asserts `{"category":"linear","symbol":"BTCUSDT"}` (correct — symbol higher priority; unchanged). `:488/:495/:524/:540` mock the return value, do NOT assert params → stay green.

**Severity:** high but latent. Beyond the crash: if get_positions() returned empty/wrong instead of crashing, `restart.py:87 ex_positions={p.symbol:p for p in positions if p.size>0}` → empty → ALL db position_state rows → `orphan_db` (restart.py:92) → phantom-close cascade (H-033/T-221). Current crash is fail-safe (crash-loop > silent phantom-close) but blocks ALL real-exchange bots (live/testnet/demo) at reconcile-on-startup.

## Fix (minimal)

1. `packages/exchange/bybit_v5/adapter.py`: add module constant `_SETTLE_COIN = "USDT"` adjacent to `_CATEGORY = "linear"` (:64), L-001 rationale comment (§N9).
2. `get_positions` (:287-289): keep `params={"category": _CATEGORY}`; `if symbol is not None: params["symbol"]=symbol` UNCHANGED; add `else: params["settleCoin"]=_SETTLE_COIN`. Symbol path stays exactly `{"category","symbol"}` (Bybit symbol-priority; do NOT also send settleCoin → preserves `:513` byte-for-byte). Net new logic: 1 constant + `else:` + 1 line.
```python
params: dict[str, Any] = {"category": _CATEGORY}
if symbol is not None:
    params["symbol"] = symbol
else:
    params["settleCoin"] = _SETTLE_COIN
```

## §N9 / L-001 decision (codebase-determinable — not an operator OQ)

`_SETTLE_COIN = "USDT"` module constant (NOT Settings/env), mirror `_CATEGORY="linear"` (:64) — v2 scope = USDT-margined linear perps; every configured symbol (BTCUSDT/ETHUSDT/SOLUSDT) USDT-settled; ZERO settle/quote config anywhere (grep-evidenced, L-018). L-001 "protocol/market-binding constant, NOT operationally tunable" class (cited adapter.py:70-73 `_MAX_CLOSED_PNL_PAGES`; pool.py URLs T-549b). Comment: "v2 scope = USDT-margined linear perps (mirror `_CATEGORY='linear'`); non-USDT linear perps would need symbol-derived/configurable settleCoin — out of scope, future market-scope task, not this fix."

## §N4 TDD — UPDATE existing pin in-place, NO new test

Resolution = update the existing `:1101` test in-place; do NOT add a separate new test (a new test would be a second divergent no-symbol params-pin — rejected; `:1101` already IS the canonical no-symbol params-pin and its asserted contract is exactly what the bug corrects).

- `test_get_positions_calls_upstream_with_bybit_v5_query_shape_no_filter:1101` — change ONLY assertion `:1108`: `{"category": "linear"}` → `{"category": "linear", "settleCoin": "USDT"}` + a 2-line WHY comment so a future `/simplify` can't silently drop settleCoin. Test name stays accurate (pins the no-filter query SHAPE — now Bybit-correct). Symbol-path test `:513` unchanged. Only changed assertion is within `:1101`.

## §N4 in-place-edit guard (L-033 applied to :1101)

Edit MUST target exactly params assertion (file line 1108), NOT displace trailing `assert call.kwargs["retries"] == 3` (:1109) nor touch `# --- T-530: get_account_balance` header (:1112). `old_string` spans the two-assertion pair so the trailing assert is preserved verbatim (re-anchored, not relocated):
- old_string:
  ```
      assert call.kwargs["params"] == {"category": "linear"}
      assert call.kwargs["retries"] == 3
  ```
- new_string:
  ```
      # Bybit V5 /v5/position/list (category=linear) requires symbol OR settleCoin
      # (T-550); no-symbol reconcile path → settleCoin=USDT.
      assert call.kwargs["params"] == {"category": "linear", "settleCoin": "USDT"}
      assert call.kwargs["retries"] == 3
  ```

## Scope / hazards / blast-radius

Diff: `adapter.py` (1 const + `else:` + 1 line) + `test_adapter.py` (1 assertion line + 2 comment lines, in-place at `:1101`). NO protocol change (`protocols.py:114`), NO paper change (`paper/adapter.py:1307` DB-backed), NO `services/` change, NO restart.py change, NO new test file. ~15 src+test LOC, << §0.3 400. L-026: only `/v5/position/list` caller is `get_positions`; other `_CATEGORY` uses (adapter.py:183/210/240/270/328/395/430/476/542/654) are different endpoints with their own param contracts (grep-evidenced, L-018) — out of scope. get_positions callers: `restart.py:78` (no-symbol — fixed path), `lifecycle.py:341 get_positions(symbol)` (symbol path — unchanged). §20 no regression (fixes latent H-033/T-221). §N8 no migration. §N3 `get_positions` `@idempotent` read (adapter.py:282), no new external write. §N2 error path already structured-logged. No new dependency. No ADR (pre-governed ADR-0015 decision-C; T-543/546/547/548 precedent).

## Gate plan

plan-reviewer APPROVE ✅ → drift-checker SKIPPED (<30 src LOC, CLAUDE.md, T-543/T-546) → brief-reviewer Gate-3 (verify `:1101` in-place correction + adapter else-branch + WG) → math-validator Gate-4 OUT-of-scope (`packages/exchange/` not in trigger list, T-548) → `fix(T-550)` + separate `chore(tasks)` F6 9/9→10/10 (L-027). NO push (operator CI-minute gate; currently unpushed: T-549b fix+chore + this). Post-merge (separate ops, not this gate): retry demo arming from `up -d --force-recreate execution-service` with demo `status=active` (DB already `exchange_mode=demo`) → expect clean reconcile + the gated test-LONG-signal step.

## Write-time guidance (plan-reviewer pass-2 verbatim — brief-reviewer Gate-3 verifies each)

1. `adapter.py`: pridaj `_SETTLE_COIN = "USDT"` bezprostredne za `_CATEGORY = "linear"` (:64) s L-001/§N9 market-scope-binding komentárom (zrkadli `_MAX_CLOSED_PNL_PAGES` L-001 komentár štýl :70-73). V `get_positions` pridaj IBA `else: params["settleCoin"] = _SETTLE_COIN` k existujúcemu `if symbol is not None:` — symbol vetva + `{"category": _CATEGORY}` base ostávajú byte-unchanged aby `:513` ostal zelený.
2. `test_adapter.py:1101`: in-place edit presne tým two-assertion `old_string`/`new_string` párom z §"in-place-edit guard" — zachovaj `assert call.kwargs["retries"] == 3` verbatim, nedotýkaj sa `# --- T-530:` header (:1112). Spusti `pytest packages/exchange/bybit_v5/tests/test_adapter.py -k get_positions` a over: `:1101` zelený s novými params, `:513` + `:488/:495/:524/:540` stále zelené, žiadny iný test nedotknutý.
3. Nepridávaj nový get_positions test — `:1101` updatnutý in-place je jediný kanonický no-symbol params pin (vyhýba sa divergentným duplikovaným pinom per pass-1 BLOCKER rezolúcia).
4. Spusti plný `packages/exchange/bybit_v5/tests/test_adapter.py` + `ruff check`/`ruff format`/`mypy` (vrátane test súboru, L-024)/`bandit` na 2 zmenených súboroch pred brief-reviewer gate.
5. F6 ledger 9/9→10/10 + T-550 `- [x]` entry idú do samostatného `chore(tasks)` pri close tasku (L-027 — NIE v `fix(T-550)` commite, NIE pre-written).
