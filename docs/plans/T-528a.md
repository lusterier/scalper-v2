# T-528a — risk-per-SL config discriminator + pure compute (foundation leaf)

**Status:** APPROVED (plan-reviewer Gate-1 single-pass APPROVE 2026-05-16; 0 BLOCKER / 0 CONCERN; all 8 technical claims source-verified; ADR-0013 §Consequences anticipates T-528 verbatim → no new ADR). Active foundation leaf of the operator-approved L-007 2-leaf split T-528→{a,b} (OQ-1=A). Recursive-split-record: `docs/plans/T-528.md`. Mirrors T-527b2a foundation-leaf posture EXACTLY.

## Purpose

Add the `sizing.method` discriminator + `risk_pct` to the config model, and the pure risk-per-SL compute function — Decimal-only, no I/O, **NO consumer** (T-528b is the first consumer). Zero behavior change: `SizingSection.method` defaults `"tier"`; no producer/wire/placement edits (those are T-528b).

## Operator OQ answers (T-528 plan-stage 2026-05-16, all recommended, baked)

- **OQ-1=A** — 2-leaf recursive split T-528 → T-528a (this leaf) + T-528b (cross-service wiring, blocked-by).
- **OQ-2=A** — additive `method` field on the EXISTING `SizingSection` (NOT a discriminated-union refactor): `method: Literal["tier","risk_per_sl"] = "tier"` + `risk_pct: Decimal | None = Field(default=None, gt=0)`. Backward-compat: no `method:` → `"tier"` → shipped tier behavior byte-unchanged. Rejected alt B (discriminated union — breaking refactor of just-shipped T-527 surface).
- **OQ-3=A** — risk-per-SL ignores `score_multipliers` (deterministic risk model; signal strength already gated upstream; scaling risk_pct by score would double-count). `max_notional_per_symbol` cap ALWAYS applies (safety rail both methods — baked from ADR-0013/T-527 precedent). `score_multipliers` stays tier-only.

## Scope / files touched (4 src files + tests)

**1. `packages/scoring/types.py` — `SizingSection` (extend; current state verified types.py:305-371)**
- Add `method: Literal["tier", "risk_per_sl"] = "tier"` (import `Literal`; confirm `Field` import).
- Add `risk_pct: Decimal | None = Field(default=None, gt=0)` (pydantic applies `gt=0` to the non-None value only).
- `tiers: list[SizingTier]` → `Field(default_factory=list)`; `score_multipliers: dict[str,Decimal]` → `Field(default_factory=dict)` — **solely so a `method: risk_per_sl` YAML may omit them**. `max_notional_per_symbol` stays REQUIRED.
- Rewrite `_structural_guards` method-conditional, **byte-preserving the shipped tier path (§N10)**:
  - `method=="tier"` (default; identical accept/reject vs shipped): `risk_pct is not None` → ValueError; `not self.tiers` → ValueError; `not self.score_multipliers` → ValueError (RESTORES the strictness field-requiredness used to give — no silent weakening); strictly-ascending balance_min guard unchanged.
  - `method=="risk_per_sl"`: `risk_pct is None` → ValueError (gt=0 Field covers a provided ≤0; validator covers None + defensive). tiers/score_multipliers MAY be empty.
  - Both (unconditional, after branch): `"default" not in max_notional_per_symbol` → ValueError (unchanged); non-digit score_multipliers keys → ValueError (unchanged; no-op for empty).
- Docstring: discriminator, `"tier"` backward-compat default, risk-per-SL semantics (`notional = total_equity*risk_pct/sl_pct`), OQ-3=A score_multipliers-tier-only, §N10 byte-preservation rationale.

**2. `packages/scoring/yaml_loader.py` — `_parse_sizing` (one required coercion block; verified yaml_loader.py:437-487 + `_to_decimal` 106-119)**
`method` (str) flows through `coerced` untouched → pydantic validates the `Literal` (zero change). `risk_pct` is a top-level scalar YAML parses as Python **float**; `SizingSection` is non-strict → an un-coerced float → `Decimal(0.01)` binary-float artefact = **§5.13/§N1 violation** corrupting the capital path. REQUIRED:
```python
if "risk_pct" in coerced:
    coerced["risk_pct"] = _to_decimal(coerced["risk_pct"])
```
alongside the existing coercion blocks. `_to_decimal` → `Decimal(str(0.01))=Decimal("0.01")` (clean); `None` passthrough → validator loud-rejects for risk_per_sl. Extend the `_parse_sizing` docstring's coercion sentence to name `risk_pct` + the intentional `method` non-coerced str passthrough.

**3. `packages/sizing/compute.py` — NEW `compute_qty_from_risk` (mirror `compute_qty_from_sizing` conventions; verified compute.py read in full)**
Keyword-only, Decimal-only, `Decimal | None`, single-rounding-point (NO round — T-529 `quantize_qty` is the sole downstream round in T-528b), in-docstring `## Hand verification`. Guard ordering mirrors `compute_qty_from_sizing:160-165` (skip-sentinel `None` BEFORE the fail-loud `ValueError`s):
```python
def compute_qty_from_risk(
    *,
    total_equity: Decimal,
    mark_price: Decimal,
    sl_pct: Decimal,
    risk_pct: Decimal,
    max_notional_per_symbol: dict[str, Decimal],
    symbol: str,
) -> Decimal | None:
    if total_equity <= 0:
        return None                          # no capital — skip sentinel (mirror sub-lowest-tier; T-528b → skip-before-place_market_order)
    if mark_price <= 0:
        raise ValueError(...)                # exchange/upstream anomaly — defensive fail-loud (mirror compute_qty_from_sizing:163)
    if sl_pct <= 0:
        raise ValueError(...)                # signal w/o usable SL — risk-per-SL undefined; T-528b `except ValueError` → skip (reason=compute_error), mirror T-527b2b
    if risk_pct <= 0:
        raise ValueError(...)                # SizingSection validator guarantees >0; defensive standalone (caller-agnostic)
    risk_amount = total_equity * risk_pct
    notional = risk_amount / sl_pct
    notional = cap_notional(notional, max_notional_per_symbol, symbol)   # reuse shipped; cap ALWAYS applies (OQ-baked safety rail)
    return notional / mark_price             # NO rounding (single-rounding-point)
```
NO `apply_score_multiplier` (OQ-3=A). Add `"compute_qty_from_risk"` to compute.py `__all__` + `packages/sizing/__init__.py` `__all__`/import. Extend the compute.py module docstring (risk-per-SL method alongside tier).

**4. `packages/sizing/__init__.py`** — +1 import + +1 `__all__` (mirror shipped 4-export shape).

NO consumer. Zero behavior change. 0 §20 regression, no new H-NNN (§0.8 — ADR-0011 reserved no T-528 hazard slot; ValueError = defensive fail-loud, mirror T-527b2a/T-534b1 no-H-NNN posture).

## §20 hazards
None new. No capital write/order/SL/reconcile/migration/SQL (L-021 N/A). Shipped tier path (method="tier" default) byte-preserved (validator restores the exact prior accept/reject surface).

## §N-invariants
- §N1/§5.13: `risk_pct` MUST be `_to_decimal`-coerced (headline — binary-float artefact otherwise). All compute arithmetic Decimal; no float casts.
- §N4: financial-math TDD — `## Hand verification` below + hand-computable golden fixtures (NOT impl-against-itself).
- §N5: ≥80% on `packages/sizing` via §N4 TDD verified locally. The §17.2 per-module `--cov-fail-under` gate is unwired repo-wide (PRE-DATES this task; report-only-elsewhere is the §17.2 posture → NOT N5-by-omission, NOT re-litigated); the T-527b2a-filed `T-F5+` ticket owns that gap. T-528a does NOT propose CI-gate wiring (T-527b2a pass-1 BLOCKER resolution applied pre-emptively).
- §N9: `risk_pct` operator YAML config (not hardcoded). No new Settings.
- §N6/N7/N8: pure functions, caller-agnostic, no globals, no migration.

## Gate-4 (math-validator) disposition — COMMITTED
**Gate-4 IN scope, COMMITTED, HEAVY** (no residual optionality — T-527b1 pass-1 open-disposition lesson applied). `packages/sizing/` not in the literal CLAUDE.md trigger list, but the shipped T-527b2a precedent adjudicated `packages/sizing` IN-scope COMMITTED HEAVY per the T-530 non-exhaustive-path-list precedent (real Decimal sizing arithmetic feeding a capital path). T-528a = exact same situation (risk-per-SL Decimal arithmetic feeding the T-528b capital path) → same disposition as T-527b2a (T-536/T-525b real-arithmetic class).

## Hand verification (math-validator Gate-4 cross-check; §B.1 caps {"default":3000,"BTCUSDT":5000})
1. equity=`Decimal("10000")`, risk_pct=`Decimal("0.01")`, sl_pct=`Decimal("0.02")`, mark=`Decimal("50000")`, "BTCUSDT": risk_amount=10000*0.01=`100`; notional=100/0.02=`5000`; cap BTCUSDT=5000→min(5000,5000)=`5000`; qty=5000/50000=`Decimal("0.1")`. Independent identity: loss-at-SL = qty*(mark*sl_pct)=0.1*(50000*0.02=1000)=`100`=risk_amount ✓.
2. equity=`Decimal("10000")`, risk_pct=`Decimal("0.02")`, sl_pct=`Decimal("0.01")`, mark=`Decimal("2000")`, "ETHUSDT" (no per-symbol→default 3000): risk_amount=`200`; notional=200/0.01=`20000`; cap default 3000→min(20000,3000)=`3000` (capped); qty=3000/2000=`Decimal("1.5")`.
3. Division precision: equity=`Decimal("1000")`, risk_pct=`Decimal("0.0333")`, sl_pct=`Decimal("0.03")`, mark=`Decimal("42500.75")`, "ETHUSDT": risk_amount=`33.300`; notional=33.300/0.03=`1110.000…`; cap default 3000→`1110.000…`; qty=1110/42500.75 at full Decimal context precision, NO round (math-validator verifies exact Decimal vs impl).
4. Skip sentinel: equity=`Decimal("0")`→`None`; equity=`Decimal("-5")`→`None` (≤0).
5. Fail-loud: mark=`Decimal("0")`→ValueError; sl_pct=`Decimal("0")`→ValueError; sl_pct=`Decimal("-0.01")`→ValueError; risk_pct=`Decimal("0")`→ValueError.
6. Validator matrix: risk_per_sl + risk_pct=None → ValueError; risk_per_sl + risk_pct=`0.01` + no tiers/score_multipliers → OK; risk_per_sl + risk_pct=`0` → pydantic gt=0 ValidationError; tier + risk_pct=`0.01` → ValueError; tier + empty tiers → ValueError; tier + tiers ok + empty score_multipliers → ValueError (byte-preserves shipped strictness); absent `method:` → "tier" (§B.1 alpha.yaml tier block validates exactly as before).

## Test strategy (§N4 TDD; ~140-200 test LOC)
- `packages/sizing/tests/test_compute.py` (sibling to shipped): NEW `compute_qty_from_risk` golden tests = Hand-verification 1-5 (hand-computed expected Decimals, parametrized; NOT round-tripping impl). Guard-ordering test (equity≤0 → None even if mark/sl_pct also invalid — sentinel precedes raises, mirror shipped guard-order test).
- `packages/scoring/tests/` SizingSection: NEW method-matrix (case 6). **Backward-compat regression pin**: existing shipped T-527a SizingSection validator tests MUST pass byte-identically; ADD a test that a `method:"tier"`/absent config omitting `score_multipliers`/`tiers` still raises (no silent weakening). L-015 sibling enumeration: list & re-run T-527a SizingSection/SizingTier validator tests + b1-extras/yaml_loader sizing tests; §B.1 alpha.yaml fixture (no `method:`) loads identically.
- `packages/scoring/tests/` yaml_loader: NEW `risk_pct` §N1-no-binary-float pin — load `risk_pct: 0.01`, assert `cfg.sizing.risk_pct == Decimal("0.01")` AND `str(cfg.sizing.risk_pct) == "0.01"`; `method:` round-trips as the str literal.

## LOC estimate / §0.3 / L-022
src ~90-140 (types.py +30-50 incl docstring; yaml_loader +5-12; compute.py +50-75 incl §N4 Hand-verification docstring per shipped compute.py per-function precedent; __init__ +2); tests ~140-200. Total ≪ §0.3 400. L-022 pre-acknowledged — pure-math leaf with §N4 docstrings runs hot (T-527b2a ~1.8×, point 30); base here smaller (1 new func + validator rewrite, not T-527b2a's 4 funcs) → ≪400, no §0.3 waiver, single leaf, no further split (L-007 boundary clean — cross-service wiring wholly T-528b).

## Open questions
None. All operator OQs (OQ-1/2/3=A) baked. §N5 §17.2-gate posture, Gate-4 COMMITTED, `risk_pct` _to_decimal requirement, §N10 tier-path byte-preservation all committed (no "to confirm" residue).

## Write-time guidance (must be addressed during implementation; plan-reviewer verbatim)
1. `SizingSection._structural_guards` method-conditional rewrite MUST byte-preserve the shipped tier accept/reject surface — specifically RESTORE the loud rejections that field-requiredness gave before default_factory: `method=="tier"` + empty `tiers` → ValueError AND `method=="tier"` + empty `score_multipliers` → ValueError (no silent weakening). Mandatory regression-pin test: a `method:"tier"` (or absent `method:`) config omitting `tiers`/`score_multipliers` still raises; the existing shipped T-527a SizingSection/SizingTier validator tests + the §B.1 alpha.yaml fixture (no `method:`) must still pass byte-identically (L-015 sibling enumeration: list & re-run them).
2. `risk_pct` `_to_decimal` coercion in `_parse_sizing` is REQUIRED (§N1/§5.13 — without it `SizingSection`'s non-strict `ConfigDict` does `Decimal(0.01_float)` = binary-float artefact corrupting the `notional = total_equity*risk_pct/sl_pct` capital path). Add the `if "risk_pct" in coerced: coerced["risk_pct"] = _to_decimal(coerced["risk_pct"])` block alongside the existing coercion blocks. Pin with a YAML-load test asserting BOTH `cfg.sizing.risk_pct == Decimal("0.01")` AND `str(cfg.sizing.risk_pct) == "0.01"`. Confirm `method` stays a non-coerced str passthrough (pydantic validates the `Literal`).
3. `compute_qty_from_risk` guard ordering = skip-sentinel (`total_equity<=0` → `None`) BEFORE the three `ValueError`s (`mark_price<=0`, `sl_pct<=0`, `risk_pct<=0`) — mirror shipped `compute_qty_from_sizing` lines 160-165 exactly. Single-rounding-point: NO rounding/quantize (T-529 `quantize_qty` is the sole downstream round in T-528b; a round here would double-round). NO `apply_score_multiplier` call (OQ-3=A — risk-per-SL ignores score_multipliers). `cap_notional` (shipped) ALWAYS applied (OQ-baked safety rail). Guard-ordering test: `total_equity<=0` returns `None` even when `mark_price`/`sl_pct` also invalid (sentinel precedes the raises).
4. §N4 TDD: test fixtures hand-authored from the `## Hand verification` Decimal literals (NOT implementation-against-itself / round-trip). Pin all Hand-verification cases 1-6 incl. the independent loss-at-SL identity sanity-check (case 1), division-precision case 3 (no round — math-validator verifies exact Decimal vs impl), skip sentinel (case 4), fail-loud matrix (case 5), validator matrix (case 6 incl. absent-`method:` backward-compat). Gate-4 IN-scope COMMITTED HEAVY (mirror T-527b2a) — no residual optionality.
5. Import + export hygiene: `Literal` (and confirm `Field`) imported in `packages/scoring/types.py` (verify against current `typing` imports); add `"compute_qty_from_risk"` to `compute.py` `__all__` AND `packages/sizing/__init__.py` `__all__` + import (mirror shipped 4-export shape). NO §17.2 CI-gate wiring; ≥80% `packages/sizing` via §N4 TDD only (the T-527b2a-filed T-F5+ ticket owns the unwired repo-wide gate — do NOT re-litigate). Before `git commit` run mypy over the STAGED set INCLUDING tests (`uv run mypy $(git diff --cached --name-only --diff-filter=ACM -- '*.py')` — L-024); for any `NewType`/Decimal-splat test site use `cast(...)` / typed-local / `assert ... is not None` narrowing, NOT inline per-arg `# type: ignore` (L-023/L-024).
