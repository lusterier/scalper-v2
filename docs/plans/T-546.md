# T-546 — correct stale "T-215 not implemented" comments

**Status:** Gate-1 APPROVE (plan-reviewer pass 2/2). Awaiting operator "proceed".
**Phase:** F6 (unlocked, ADR-0015). Operator-directed post-MVP fix → numbered F6 task per **ADR-0015 §70-81 decision-C** (standing basis; no per-item ADR). Precedent: T-545. T-546 = next free number (T-545 latest F6 numbered; **T-215 is a COMPLETED immutable F2 task — `TASKS.md:111` `[x] T-215 (2026-04-30)`; MUST NOT relabel — L-027**).

## Purpose

"T-215" (Adapter pool composition root) shipped 2026-04-30: per-bot Bybit key wiring is implemented & MVP-signed — `services/execution/app/pool.py` docstring:10-29 (H-022/ADR-0004) + `_construct_bybit_adapter:224-227` (`BOT_<ID>_BYBIT_API_KEY/SECRET/SUB_ACCOUNT` from env) + `build_adapter_pool:308`. Several comments/prose still wrongly state the wiring is unimplemented / "T-215 not done"; one of them generated a misframed "implement T-215" request. **No implementation work — comment/prose correction only.**

## Scope — 6 sites (L-026: ALL sites, fixed in ONE commit)

| # | Site | Stale text | Correction |
|---|------|-----------|------------|
| 1 | `compose.yaml:313` | execution-service header "T-215+ will extend the lifespan with the adapter pool composition root + ..." | past/shipped tense — T-215 shipped these 2026-04-30 |
| 2 | `compose.yaml:331` | env_file comment "T-215 will add BOT_<ID>_BYBIT_API_KEY/SECRET entries here per H-022." | per-bot `BOT_<ID>_BYBIT_API_KEY/SECRET/SUB_ACCOUNT` in secrets.env are consumed by `pool.py` (H-022/ADR-0004); shipped by T-215 (2026-04-30) |
| 3 | `compose.yaml:380` | strategy-engine-demo "per-bot Bybit key wiring into execution-service is T-215 (not yet implemented) — until then the bot cannot place live orders even with real keys" | wiring IS implemented (pool.py, T-215 done); demo parked `status='paused'` pending operator arming (real keys + execution-service recreate + un-pause), NOT pending code |
| 4 | `docs/runbooks/ops.md:107` | "sa do execution-service ešte **neprepájajú (T-215, neimplementované)**." | reason corrected to config/arming gap (T-215 done 2026-04-30); **operational advice preserved** |
| 5 | `docs/runbooks/ops.md:111` | "kým T-215 nie je hotové; inak zhodíš order execution pre celú platformu." | keep crash-loop warning + keep-paused advice; replace "kým T-215 nie je hotové" → "kým operátor nedoplní reálne kľúče + nerecreatne execution-service + neun-pausne (NIE kód — T-215 done 2026-04-30)" |
| 6 | `configs/bots/demo.yaml:8` | "per-bot Bybit key wiring into execution-service is T-215 (not yet implemented)..." | same correction as site 3 |

**ops.md sites 4–5: preserve operational correctness.** The live bot stays `status='paused'`; arming = real keys in `/etc/scalper-v2/secrets.env` + execution-service recreate + un-pause; an active+live bot without them crash-loops execution-service (`pool.py` `_require_env` + `_check_live_mode_safeguard`). Only the *reason attribution* changes. A correctness regression in operator guidance is worse than the stale ref. Matches corrected memory `t215_live_key_gap.md`.

## L-026 deliberate boundary (site-growth + exclusions)

- Site-set grew **3 → 5 → 6**: plan v1 = 3 (compose.yaml only); plan-reviewer pass-1 BLOCKER added ops.md:107,111; author's own repo-wide grep added `configs/bots/demo.yaml:8` (a recurring L-026/L-031 instance — verified independently, not trusting the reviewer count). plan-reviewer pass-2 independently re-grep'd and confirmed 6 is complete (no 7th).
- **Excluded as immutable / non-stale (bucket-c, never touched):** `docs/plans/T-214.md:376`, `docs/plans/T-310a.md:59,202`, `docs/plans/T-208a.md:26,28`, `docs/plans/T-523.md:60` (immutable point-in-time plan-docs; none carry stale "not implemented" language). `TASKS.md:111` = the `[x] T-215 (2026-04-30)` DONE ledger entry (source-of-truth proof the wiring shipped — the thing cited, not a stale claim).

## Hazards §20 / §N invariants

None. Comment/prose-only — no capital/execution path, no schema, no env, no behaviour. §N1–N10 untouched; §N10 (no §20 regression) N/A (no logic).

## OQ3 — SUPERSEDED (recorded for brief-reviewer Gate-3)

Plan-v1 closed OQ3 as "no ops.md update". **Superseded:** ops.md:107,111 + demo.yaml:8 are IN-SCOPE via L-026 all-sites enumeration — NOT via a mandatory CLAUDE.md ops-runbook schema-trigger (there is no BotConfig/RiskSection/SizingSection change, no new pool.py env var, no new service, no bots-table change). brief-reviewer Gate-3 must treat the ops.md + demo.yaml edits as **in-scope**, NOT out-of-scope drift.

## Test strategy

No new tests (comment/prose-only). Sanity: `docker compose config -q` must still parse post-edit. ops.md/demo.yaml are prose/comment.

## Gate plan

plan-reviewer APPROVE (pass 2/2) → drift-checker **SKIP** (<30 LOC trivial, CLAUDE.md) → brief-reviewer **Gate-3** (before commit) → math-validator **OUT-OF-SCOPE** (none of compose.yaml / docs/runbooks/ops.md / configs/bots/demo.yaml in the Gate-4 trigger list). Diff ≪400 LOC.

## Close

Write this doc; add `T-546` to `TASKS.md ### F6 numbered` (counter 4/4 → 5/5, mirror T-545 entry shape; T-541 opener excluded; do NOT alter the "ADR-0015 exit-criteria MET" sentence — independent of the numbered counter). Commit `fix(T-546): ...`, repeated `-m`, NO Co-Authored-By trailer (operator commit convention).

## Write-time guidance (plan-reviewer pass-2, verbatim — brief-reviewer verifies each)

1. **Fix all 6 sites in the SAME commit** (L-026): `compose.yaml:313,331,380` + `docs/runbooks/ops.md:107,111` + `configs/bots/demo.yaml:8`. Brief-reviewer must grep residual stale-claim tokens (`not yet|will add|will extend|neimplement|nie je hotov|neprepáj|pending|not implemented` against `T-215`) across the source-of-truth corpus and find zero before SHIP.
2. **ops.md:106-111 — preserve the operational warning verbatim in intent:** live bot stays `status='paused'`; arming = real keys in `/etc/scalper-v2/secrets.env` + execution-service recreate + un-pause; an active+live bot without them crash-loops execution-service (`pool.py` `_require_env` + live-mode safeguard). Only the *reason attribution* ("T-215 neimplementované" → config/arming gap, T-215 done 2026-04-30) changes. A correctness regression here is worse than the stale ref.
3. **In `docs/plans/T-546.md` record the deliberate scope boundary** (L-026 audit discipline): the full 6-site corrected list + the 3→5→6 site-growth history + the explicitly-excluded immutable sites (`docs/plans/T-*.md`, `TASKS.md:111`) and why. Carry the OQ3-superseded note so brief-reviewer treats ops.md + demo.yaml edits as in-scope, not drift.
4. **TASKS.md:** add `T-546` to `### F6 numbered`, grow the counter (4/4 → 5/5, mirror T-545 entry shape), keep T-541 opener excluded. Do NOT alter the "ADR-0015 exit-criteria MET" sentence (independent of the numbered counter — 3/3 mandatory items unchanged).
5. **Sanity check:** `docker compose config -q` must still parse post-edit (comment-only change to compose.yaml). No new tests (prose/comment). Commit `fix(T-546): ...`, repeated `-m`, no Co-Authored-By trailer (per operator memory).
