# T-527 — SPLIT RECORD (L-007 pre-emptive split)

**Status:** SPLIT 2026-05-16 (operator OQ-1=A at T-527 plan-stage; TASKS.md:224 + ADR-0011 pre-flagged "L-007 split-watch — confirm at plan stage", §B.1 sizing block large). Not an implementable plan — records the split; the active leaf plan is `docs/plans/T-527a.md`. T-527b plan written when T-527a merges.

## Why split (L-007)

T-527 reifies the BRIEF-deferred §B.1 `sizing:` block (BRIEF §22 Appendix B, `configs/bots/alpha.yaml` lines 3130-3149) — tier ladder + `score_multipliers` + `max_notional_per_symbol` + (deferred) `tier_promotion`/`tier_demotion`. TASKS.md:224 estimate ~300 src + ~250 tests pre-split with explicit "will trigger split-watch"; ADR-0011 + the TASKS entry pre-flagged L-007. The work spans **two distinct concerns**: (a) the config schema + score plumbing foundation (no behavior change, no consumer), and (b) the balance→tier→multiplier→cap→qty compute wired into the execution placement seam (capital-path behavior change, §N4 financial-math TDD, Gate-4 math-validator). Per L-007 active control, a pre-emptive split at plan-stage is far cheaper than a mid-write cap breach (T-533→{a,b} / T-534→{a,b} / T-525a→{a1,a2} precedent — foundation leaf then consumer leaf).

## Operator OQ answers (T-527 plan-stage 2026-05-16, all default A — baked into T-527a / T-527b)

- **OQ-1=A — 2-leaf split** into **T-527a** (config foundation: `SizingSection`/`SizingTier` schema + `yaml_loader._parse_sizing` + additive `OrderRequest.score` field + strategy-engine producer threading — zero behavior change, no consumer, independently testable) + **T-527b** (the sizing compute: balance→tier→score-multiplier→max_notional cap→notional÷mark-price→qty, wired into the execution-service placement seam, blocked-by T-527a). Mirrors T-533→{a,b} / T-534→{a,b} EXACTLY (schema/plumbing foundation leaf → capital-path consumer leaf).
- **OQ-2=A — tier_promotion / tier_demotion DEFERRED** (out of T-527 scope entirely). BRIEF §B.1 `tier_promotion.min_trades` + `tier_demotion.drawdown_pct` (alpha.yaml lines 3146-3149) are an under-specified *stateful* tier-adjustment layer on top of the base balance→tier selection. §0.7 (out-of-scope cruft → backlog ticket, not absorbed): a NEW backlog task **T-F5+ tier_promotion/demotion stateful adjustment** is recorded in TASKS.md. T-527a's `SizingSection` deliberately does NOT model `tier_promotion`/`tier_demotion` fields (they stay absorbed by `BotConfig.extra="ignore"` exactly as `sizing` is today, until that future task). No code, no schema for P/D in T-527.
- **OQ-3=A — `size`/`max_notional_per_symbol` = notional USD**; T-527b computes `qty = notional ÷ reference price` where reference = latest mark/ticker price via the adapter, immediately before the existing T-529 `quantize_qty` pre-flight. (Binds T-527b; T-527a's schema types `size`/`max_notional` values as `Decimal` USD accordingly.)
- **OQ-4=A — tier selected by `AccountBalance.total_equity`** (mirror T-531 `virtual_balance` gauge / equity-snapshot source); when `total_equity` < the lowest tier's `balance_min` the signal is **skipped** (no order placed; soft-block; structured log + Prometheus counter, mirroring the T-526 cooldown / T-524 caps silent-skip pattern). (Binds T-527b; T-527a's schema must allow a well-defined "lowest tier" — validator enforces ≥1 tier + ascending `balance_min`.)
- Still **additive-observable / backward-compatible** throughout: a bot YAML with no `sizing:` block → `BotConfig.sizing is None` → the static `execution.qty` path is byte-unchanged (T-528 later adds a `sizing.method: tiers | risk_per_sl` discriminator; T-527 is the `tiers` method only). Zero §20 regression; no new H-NNN; the `score` field is carried producer→wire but unconsumed until T-527b.

## Split (mechanical — config foundation vs compute consumer; mirror T-533→{a,b})

- **T-527a** (active leaf — `docs/plans/T-527a.md`): `SizingTier` + `SizingSection` Pydantic models in `packages/scoring/types.py` (mirror `RiskSection`/`ShadowConfig` net-new `extra="forbid"` precedent) + `BotConfig.sizing: SizingSection | None = None` (mirror `shadow: ShadowConfig | None = None` Optional-sentinel precedent, NOT `risk`'s default_factory) + `yaml_loader._parse_sizing` (mirror `_parse_shadow` absent→None + `_to_decimal` coercion) + additive `OrderRequest.score: float | None = None` (mirror T-511b2 additive `shadow_*` fields, `schema_version` stays `"1.0"`) + strategy-engine producer threading (`consumer._publish_order_request` gains a `score: float` param, call-site passes `result.total_score`, set on the `OrderRequest`). **No consumer** of `BotConfig.sizing`; **no compute**; **no balance fetch**; **no placement change**; **no fixture `sizing:` block** (strictly zero behavior change — `qty` still `bot_config.execution.qty`). T-530/T-533a-shaped no-consumer foundation leaf. Gate-4 **out of scope** (none of the touched files are in the CLAUDE.md trigger list; zero financial math — the arithmetic is all T-527b).
- **T-527b** (blocked-by T-527a): the sizing compute consuming `BotConfig.sizing` + `request.score` + `adapter.get_account_balance().total_equity` + a mark/ticker price; wired into the execution-service placement seam (`placement.py:_handle`, before the T-529 `quantize_qty`); skip-signal path for sub-lowest-tier balance. §N4 financial-math TDD + Gate-4 math-validator (touches `services/execution/`) + `## Hand verification` (tier select / score-multiplier / max_notional clamp / notional÷price worked examples). Own plan-reviewer Gate-1 cycle when T-527a merges; L-007/L-014 split-watch (re-confirm size at its own plan-stage).

## Sequence

T-527a plan-reviewer Gate-1 → implement → 4 gates → merge. Then T-527b (now unblocked) gets its own consolidated plan + Gate-1 cycle. Mirrors T-533a → T-533b / T-534a → T-534b.
