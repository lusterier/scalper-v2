# T-542 — H-005 opposite-side-position guard (consumer pre-scoring gate) + un-DEFER

**Status:** Gate-1 plan-stage (consolidated, operator architecture decision baked in). F6 numbered work task (1/4; F6 unlocked per ADR-0015; **Blocked-by T-541 ✅ DONE**). The original F6 driver (the H-005 arc that opened F6). Batch-push mode active (commits accumulate local; one push at batch end — operator CI-minute budget).

## Purpose

§20 **H-005 — Opposite-side signals**: live position LONG BTCUSDT, a SHORT signal arrives. v1 blocked it. v2 DEFERRED — no control exists (T-519 audit confirmed the genuine total gap; operator-acknowledged residual-risk carve-out at F5 sign-off, E4 reported 35/36 + H-005 DEFERRED). T-542 resolves it: a per-bot pre-scoring **consumer silent-skip gate** that blocks a new entry whose side is opposite to an already-open position for the same `(bot_id, symbol)`, then un-DEFERs H-005 across the ADR-0015 L-026 site-set → E4 36/36.

## Architecture decision — operator-chosen (2026-05-17), recorded as ADR-0016

BRIEF §20/§22 design-of-record was a **scoring condition** `opposite_side_open` (per-bot enable/disable, default blocked) used via a `block_opposite_position` YAML rule. **All 7 F5-shipped pre-scoring guards** (3a TTL / 3b symbol / 3b' source-filter / cooldown / caps / loss-limit / drawdown) are instead **consumer silent-skip gates**; the `opposite_side_open` scoring condition was never built (no evaluator in `packages/scoring/conditions/`; no `ctx.bot.*` position-state context-plumbing exists). The operator chose the **consumer-gate** architecture (consistent with shipped reality, smaller, lower-risk). This deviates from the BRIEF design-of-record → **ADR-0016** records the gate-vs-condition decision + the BRIEF §20/§22 amendment retiring the `opposite_side_open` condition design. Embedded verbatim in `## ADR-0016` below (written to `docs/adr/0016-h005-opposite-side-consumer-gate.md` on plan-reviewer APPROVE, mirror T-541/ADR-0015 precedent).

## Scope (IN)

1. **NEW `services/strategy_engine/app/opposite_side_gate.py`** (~80 src) — mirror `cooldown_gate.py` structure exactly:
   - `@dataclass(frozen=True, slots=True) OppositeSideDecision(active: bool, reason: str | None, open_side: str | None, signal_side: str | None)`; `_INACTIVE` sentinel.
   - `async def check_opposite_side(*, pool, bot_id, exchange_mode, symbol, signal_side, risk_config) -> OppositeSideDecision`.
   - **Disabled short-circuit BEFORE any DB hit**: `if not risk_config.block_opposite_side: return _INACTIVE` (mirror cooldown's both-knobs-0 short-circuit; here the knob is a bool, default `True` ⇒ short-circuit is the opt-out path).
   - **Table dispatch by `exchange_mode`** (mirror cooldown's `trades`/`paper_trades`): `position_state` (live/testnet) vs `paper_position_state` (paper). `position_state` holds OPEN positions only (T-219 deletes on close), composite PK `(bot_id, symbol)` ⇒ ≤1 row.
   - DB read via the NEW focused query helper (item 2). Block predicate: open row exists AND `open_side != signal_side` ⇒ `active=True, reason="opposite_side_open"`. No open row, or same side (pyramid/add — NOT opposite) ⇒ `_INACTIVE`.
2. **NEW focused query helper in `packages/db/queries/` (~30 src)** — `select_open_position_side(conn, *, bot_id, symbol, table_name) -> str | None` returning the open position's `side` (`"buy"`/`"sell"`) for `(bot_id, symbol)` or `None`. Mirrors the focused-boolean precedent `select_recent_open_trade_exists` (execution.py:649) and cooldown's `select_recent_closed_trades(table_name=...)` parameterised-table pattern. `table_name: Literal["position_state","paper_position_state"]` (whitelist constant, no user input — `# noqa: S608 # nosec B608` like the sibling SQL).
3. **`packages/scoring/types.py` `RiskSection`** (~3 src) — add `block_opposite_side: bool = Field(default=True)` (BRIEF §20 "per-bot enable/disable, **default blocked**"). `RiskSection` is `frozen=True, extra="forbid"` — net-new field, docstring block extended (mirror the cooldown/caps/loss/drawdown knob-doc paragraphs). Default `True` = guard ON by default; safe (no v2 bots deployed; matches BRIEF policy; existing `alpha/beta/smoke.yaml` have no open positions in any test ⇒ no behaviour regression — asserted by the sibling-regression test pass).
4. **`services/strategy_engine/app/consumer.py`** (~15 src + docstring rewrite) — insert the gate call **structurally AFTER the CLOSE-action block** (`if signal.action == "CLOSE": ... return` — CLOSE returns early, so by the gate call-site `signal.action ∈ {LONG, SHORT}`) and **BEFORE the T-526 cooldown gate**. Post-3b gate cluster becomes: 3a TTL → 3b symbol → 3b' source → **CLOSE** → **[NEW] opposite_side** → cooldown → caps → loss → drawdown → 3c. The consumer maps the side once and passes it in: `signal_side = _ACTION_TO_SIDE[signal.action]` — **REUSE the shipped `_ACTION_TO_SIDE` constant** (consumer.py:88 `{"LONG":"buy","SHORT":"sell"}`; total here because CLOSE already returned). NO refactor, NO circular import (gate receives the mapped `signal_side`, does not import the consumer). On `active`: `trading_logger.info("signal_blocked_opposite_side", bot_id=, idempotency_key=, symbol=, open_side=, signal_side=)` + `metrics.signals_blocked_opposite_side.labels(bot_id=, reason=).inc()` + `return` BEFORE scoring_evaluations / orders.requests / signals.rejected — **mirror the cooldown block EXACTLY** (post-3b gate class HAS a Prom counter, unlike the 3a/3b/source-filter no-counter class). Module-docstring (:29-34) H-005-DEFERRED prose rewritten → implemented.
5. **`services/strategy_engine/app/metrics.py`** (~8 src) — add `signals_blocked_opposite_side: Counter` field to `Metrics` + the `signals_blocked_opposite_side_total{bot_id, reason}` declaration in `build_strategy_engine_metrics` — mirror `signals_blocked_cooldown` verbatim (same post-3b gate-class counter pattern).
6. **§N4 TDD tests** — NEW `services/strategy_engine/tests/test_opposite_side_gate.py` (gate unit: the §Hand-verification truth table + disabled short-circuit asserts NO DB hit + paper/live table dispatch) + `test_consumer.py` wiring tests (opposite+open ⇒ silent-skip [scoring/publish NOT called, `signal_blocked_opposite_side` logged, counter inc]; same-side ⇒ proceeds; **CLOSE-ordering pin** ⇒ CLOSE reaches the CLOSE block NOT the gate, mirror T-545's pin; sibling regression: cooldown/caps/source-filter/symbol tests stay green). L-017 realistic fixtures (real `SignalValidated`/`bot_config`/position rows; pin called + not-called).
7. **ADR-0016** `docs/adr/0016-h005-opposite-side-consumer-gate.md` (full text in `## ADR-0016`).
8. **L-026 un-DEFER site-set — ONE commit** (ADR-0015:49 mandate). Enumerated in `## L-026 un-DEFER site-set` with the two immutability-boundary refinements explicit.

## Scope (OUT)

No `opposite_side_open` scoring condition / no `packages/scoring/conditions/` change / no `ctx.bot.*` context-plumbing (the rejected alternative). No in-memory `orders.events.<bot_id>` position view (§9.4 step 4 — unbuilt; out of scope; the gate derives state from DB per signal like cooldown). No exchange `get_positions()` call (wrong layer; consumer has no adapter). No change to the CLOSE-action block / cooldown / caps / loss / drawdown / 3b / source-filter logic. No mutation of operator-signed point-in-time records (F5_close_out §A/§B Result lines; T-541 status.md historical entry) — forward-annotation only (see boundary refinements). No new dependency.

## Files

NEW `services/strategy_engine/app/opposite_side_gate.py`; NEW `services/strategy_engine/tests/test_opposite_side_gate.py`; NEW `docs/adr/0016-h005-opposite-side-consumer-gate.md`; MOD `packages/db/queries/<execution|trades>.py` (focused helper — placement decided at write-time: alongside `select_recent_open_trade_exists` in `execution.py`, the open-position-read sibling); MOD `packages/scoring/types.py` (RiskSection field+doc); MOD `services/strategy_engine/app/consumer.py` (gate wiring + docstring); MOD `services/strategy_engine/app/metrics.py` (counter); MOD `services/strategy_engine/tests/test_consumer.py` (wiring tests); the L-026 un-DEFER docs (`docs/CLAUDE_CODE_BRIEF.md` §20/§22/§9.4/§19, `README.md`, `tests/test_hazard_catalog_coverage.py`, forward-annotations to `docs/runbooks/F5_close_out.md`); + this `docs/plans/T-542.md`; at close TASKS.md (T-542 `[ ]`→`[x]`; F6 1/4) + status.md (NEW entry, newest-first).

## §0.3 LOC

**Logic body** ~135 src (gate ~80 + query ~30 + RiskSection ~3 + consumer wiring ~15 + metrics ~8). **L-022 contingency-adjusted** (the exact-mirror precedent T-526 shipped 1.92× its plan estimate — overage was WG-mandated docstrings, NOT logic): + ~80-120 WG/charter docstrings (new module docstring + `OppositeSideDecision` 4-field docstring + the `select_open_position_side` charter + L-021 cast-audit docstring + per-helper docstrings) ⇒ **~215-255 src realistic, ≪ 400**. T-542 trims vs T-526 on two axes (metrics.py scaffolding already exists from T-526; `_ACTION_TO_SIDE` reused — no new constant) so it will not replay T-526's full 1.9×. Tests §0.3-excluded. ADR/BRIEF/README/runbook/docstring markdown §0.3-exempt. **1 task. L-007 split-watch noted but HOLDS**: one cohesive gate feature (gate+query+config+wiring+counter — same shape as the shipped single-task T-526); the un-DEFER is mechanical markdown (exempt). **L-022 escalation trigger:** if write-time src exceeds **~320** (drift-checker auto-NEEDS-DISCUSSION at ≥1.5× the ~215 floor) → stop + escalate, do not absorb. **L-021 discharge:** `select_open_position_side` SQL params `$1 bot_id` / `$2 symbol` are **column-direct equality** (`WHERE bot_id=$1 AND symbol=$2`) — not arithmetic / not CASE-branch / not operator-overloaded → no `::type` cast required; the new query carries an explicit L-021-cast-audit docstring stating this discharge (mirror the `trades.py` WG#2 audit docstring) so the L-021 active control is visibly satisfied at brief-review.

## Hazards (§20)

Resolves **H-005** (signal-acceptance gate, §15.2 / trading-correctness). §N4 TDD mandatory. §N10: sibling consumer gates (symbol/source/cooldown/caps/loss/drawdown/CLOSE) + the meta-test stay green — the gate is a leaf early-return with the `block_opposite_side` + `position_state`-exists guards; default-`True` is safe because no test has a bot with an open `position_state` row + an opposite signal (sibling-regression asserts this explicitly, not assumed — per WG of T-545 lesson). **math-validator OUT-of-scope**: `services/strategy_engine/`, `packages/db/queries/`, `packages/scoring/types.py`, `services/strategy_engine/app/metrics.py` — none in the Gate-4 trigger list (`packages/features/*`, `packages/pnl/`, `services/feature-engine/`, `services/execution/`, `services/scoring/`); the gate is a boolean side comparison, no financial math. The `## Hand verification` truth table is included regardless (trading-correctness, Gate-1 step-5 spirit). N1: query returns tz-aware where applicable; no time math in the gate. N3: read-only query, no external write.

## Hand verification — the opposite-side truth table (the correctness crux)

`signal_side = _ACTION_TO_SIDE[signal.action]` (`LONG→buy`, `SHORT→sell`; CLOSE never reaches the gate). `open_side` = `position_state.side` ∈ `{buy, sell}` for `(bot_id, symbol)` or absent.

| open position (bot,symbol) | signal.action | signal_side | predicate `exists ∧ open≠sig` | gate verdict |
|---|---|---|---|---|
| (none) | LONG | buy | F (no row) | allow → proceed |
| (none) | SHORT | sell | F (no row) | allow → proceed |
| side=buy (LONG open) | LONG | buy | F (buy==buy) | allow (pyramid/add — NOT opposite) |
| side=buy (LONG open) | SHORT | sell | **T** (buy≠sell) | **block** (silent-skip) |
| side=sell (SHORT open) | SHORT | sell | F (sell==sell) | allow (add — NOT opposite) |
| side=sell (SHORT open) | LONG | buy | **T** (sell≠buy) | **block** (silent-skip) |
| (any) | CLOSE | — | gate not reached | CLOSE block returns earlier |
| side=buy, `block_opposite_side=False` | SHORT | sell | short-circuit | allow (NO DB hit — disabled) |

The two **block** rows + the two same-side **allow** rows + the no-position **allow** rows + the disabled short-circuit + the CLOSE-ordering pin are the mandatory §N4 test cases (test_opposite_side_gate.py + the consumer wiring/ordering tests).

## L-026 un-DEFER site-set (ONE commit — ADR-0015:49 mandate; enumerated by Write-time grep, NOT first-hit)

**L-026 active control (verbatim, mandatory at write-time):** before editing, grep the WHOLE brief for `H-005`, `opposite[_ -]side`, `opposite_side_open`, `block_opposite`, `opposite-signal`, and `Known hazards addressed` — fix EVERY occurrence in this one commit OR record it deliberately-bound-with-rationale. The plan-stage grep (2026-05-17) found the complete set below; re-grep at write-time to confirm no drift.

DEFERRED→implemented, all in the T-542 feat commit:

- **`docs/CLAUDE_CODE_BRIEF.md`** (5 sites — §-section + distinctive-token anchored, line numbers indicative-only/volatile per L-026):
  - **§20 H-005 block** (`### H-005 — Opposite-side signals` header ~:2676; **Policy.** ~:2680; **Test.** ~:2682) — Policy rewritten "implemented as the `risk.block_opposite_side` consumer pre-scoring gate (T-542/ADR-0016)"; **Test.** rewritten to cite the canonical pin **`` `test_blocks_opposite_open_side` ``** (bare-fn token, collected by `pytest --collect-only` from `services/strategy_engine/tests/test_opposite_side_gate.py`); **DEFERRED + T-F5+ tokens removed**.
  - **§9.4 strategy-engine** (the `**Known hazards addressed:** H-005 (opposite-signal guard` line ~:1565 — **confirmed §9.4**: `### 9.4 strategy-engine` :1536 → this line :1565 → `### 9.5 execution-service` :1567; the plan-stage L-026 grep verified H-005 appears in ONLY this one of the seven §9.x "Known hazards addressed" blocks; the §9.1:1457 / §9.2:1489 / §9.5:1625 blocks do NOT mention H-005) — `H-005 (opposite-signal guard — DEFERRED…)` → `H-005 (opposite-signal guard — risk.block_opposite_side consumer gate, T-542)`.
  - **§22 site A** — the `# DEFERRED — the opposite_side_open scoring condition…` comment + `- name: block_opposite_position … condition: type: opposite_side_open, bot: self` YAML rule (~:3167-3176) — retired per ADR-0016: replace with `# H-005 is enforced by the risk.block_opposite_side consumer gate (T-542/ADR-0016) — not a scoring rule` and remove the YAML rule.
  - **§22 site B** — the SECOND opposite_side_open occurrence (~:3285-3291): `# DEFERRED — entry_point packages.scoring.rules.opposite_side:OppositeSideOpenRule code-verified non-existent…` + `- name: opposite_side_open / entry_point: packages.scoring.rules.opposite_side:OppositeSideOpenRule` plugin-entry-point example. **[Added at REVISE — the plan-stage grep's first version stopped at §22 site A; this is the exact L-026 first-hit failure mode the lesson exists for.]** Retire identically per ADR-0016 (replace with the same gate pointer; remove the plugin rule).
  - **§19 Phase F6 exit-criteria** (~:2631 `H-005 resolved — \`opposite_side_open\` guard implemented…`) → "`risk.block_opposite_side` gate implemented + tested; §20 H-005 no longer DEFERRED; E4 36/36". (The §19 F6 **task-list** line ~:2625 "T-542 — H-005 … resolve the deferred opposite-signal guard" is the task *definition* and stays — accurate as the task's own description, mirror other completed-task entries.)
  - **Deliberately EXCLUDED (L-026 false-positive, rationale recorded):** `:2841` — the H-024 `**Test.**` prose ("the next **opposite-side** full-close fill labels 'trail'") is ADR-0005 TP/SL-labeling, an unrelated use of the "opposite-side" string; NOT an H-005 site. Not touched.
- **`README.md`** :13-14 (the `opposite_side_open scoring condition` sign-off prose), :117 (`H-005 (opposite-side guard) DEFERRED`) → "implemented (`risk.block_opposite_side` consumer gate, T-542)".
- **`services/strategy_engine/app/consumer.py`:29-34** — module-docstring H-005-DEFERRED paragraph rewritten implemented (in the src diff anyway since consumer.py is wired).
- **`tests/test_hazard_catalog_coverage.py` — re-spec (BLOCKER-1; the prior "update fixture to resolved form" instruction was WRONG and would red the suite). Three precise, independent changes:**
  1. **Production constant** `_KNOWN_DEFERRED: frozenset[int] = frozenset({5})` → `frozenset()`; update its doc-comment (~:69-76) to "no §20 hazard is currently DEFERRED (H-005 resolved by T-542/ADR-0016); empty whitelist — ANY DEFERRED §20 entry is now a finding (cannot silently re-widen)". This alone makes the **real-brief** guard `test_every_hazard_has_a_resolvable_test_citation` (:195-235) go **36/36**: the `for hnum in sorted(_KNOWN_DEFERRED)` integrity loop is now empty; H-005 now has a resolvable `**Test.**` backtick citation → resolves via the collected-tests fixture. (No other edit needed for THIS test.)
  2. **§20 H-005 `**Test.**`** cites `` `test_blocks_opposite_open_side` `` (same token as the §20 site above) — `_BARE_FN_RE`-matching, collected from the new test file ⇒ `_extract_citations` resolves it ⇒ no finding.
  3. **Parser self-test DECOUPLE** (`_FIX_DEFERRED` literal :327-340 + `test_deferred_carveout_detected_and_tightly_gated` :343-361). The self-test currently asserts the **production** constant directly (`:360 assert 5 in _KNOWN_DEFERRED`, `:361 assert 6 not in _KNOWN_DEFERRED`) and uses it in its inline gating logic (`:358` via `hnum not in _KNOWN_DEFERRED`) — emptying the production constant breaks `:360` and flips `:358`'s expected `findings` (H-005 would now also be flagged). Fix = make the parser self-test self-contained, independent of the live whitelist: (a) relabel the synthetic literal's hazards to clearly-non-real ids — `### H-901 — synthetic acknowledged` / `### H-902 — synthetic rogue` (no collision with the now-live real H-005/H-006 — removes the confusing "H-005 deferred here / live in real §20"); (b) introduce a LOCAL `_synthetic_ack: frozenset[int] = frozenset({901})` inside/next to the test; (c) rewrite the asserts internally-consistent against the LOCAL set: `assert deferred == {901, 902}`, `cites[901] == [] and cites[902] == []`, the gating loop uses `hnum not in _synthetic_ack`, `assert findings == ["H-902: DEFERRED not acknowledged"]`, `assert 901 in _synthetic_ack`, `assert 902 not in _synthetic_ack`. The self-test now verifies the *parser+gating algorithm* with zero reference to the production `_KNOWN_DEFERRED` — empty or not. Tests/ file ⇒ §0.3-excluded, but logic-bearing ⇒ enumerated here assertion-by-assertion + §N4-pinned (see `## Test strategy`).

**Boundary refinement #1 — `docs/runbooks/F5_close_out.md` (operator-signed point-in-time; plan-reviewer CONFIRMED sound — NOT escalated):** ADR-0015:46 lists "F5_close_out.md E4-residual" as bucket-(b); ADR-0015:35 is the over-riding invariant ("Point-in-time F5 records … unchanged"; the §A/§B sign-off NOT reopened). :86/:90/:105 are operator-**signed** `luster`-attested §A/§B "Result: PASS WITH 3 PARTIALS … E4 35/36 + H-005 DEFERRED" timestamped point-in-time attestations — byte-mutating them falsifies a signed attestation (35/36 + H-005 DEFERRED *was* true as of `2026-05-17T05:17:35+00:00`). **Resolution (plan-reviewer-confirmed, recorded in ADR-0016 Consequences):** signed Result/sign-off lines stay **byte-unchanged**; T-542 adds ONE non-mutating forward-annotation footnote ("H-005 subsequently resolved 2026-05-17 in F6/T-542 — does not retro-change the F5 sign-off state"). This is a refinement *internal to* ADR-0015's own §35-invariant-over-:46-site-list hierarchy — the call plan-reviewer is empowered to make; **not** a NEEDS DISCUSSION.

**Boundary refinement #2 — `docs/status.md` (newest-first, T-541 entry immutable; CONFIRMED sound):** status.md :13/:15/:19 = the **T-541 point-in-time entry** (bucket-(c) immutable). The "status.md forward-line" un-DEFER is satisfied by T-542's OWN new newest-first entry at close (recording H-005 resolved) + the §20/README/meta-test updates. The T-541 historical prose stays byte-unchanged.

## Test strategy (§N4 TDD)

Test-first. **The canonical pin test is `test_blocks_opposite_open_side`** in `services/strategy_engine/tests/test_opposite_side_gate.py` — this exact bare-fn token is what §20 H-005 `**Test.**` cites; it MUST be `pytest --collect-only`-resolvable (the real-brief meta-test resolves the citation against the collected-tests fixture).

- **`test_opposite_side_gate.py`** (gate unit): the 8 `## Hand verification` truth-table rows — incl. `test_blocks_opposite_open_side` (buy-open + SHORT ⇒ block) and its mirror (sell-open + LONG ⇒ block), the two same-side allows, the two no-position allows, the disabled short-circuit **asserting NO `pool.acquire`** (no DB hit), and paper-vs-live `table_name` dispatch. Hand-computable fixtures (L-017): real `OppositeSideDecision`, real position rows; no implementation-against-itself.
- **`test_consumer.py`** (wiring): opposite+open ⇒ silent-skip (pin scoring/publish NOT called, `signal_blocked_opposite_side` logged, `signals_blocked_opposite_side` counter inc'd); same-side ⇒ proceeds; **CLOSE-ordering pin** (CLOSE → CLOSE block, NOT the gate — the only test proving insertion is *after* CLOSE / *before* cooldown); sibling regression — cooldown/caps/source-filter/symbol/CLOSE tests stay green (asserted, not assumed: explicitly run them).
- **Mandated meta-test verification case (BLOCKER-2):** an explicit assertion-level case that BOTH `tests/test_hazard_catalog_coverage.py` tests pass post-change: (a) `test_every_hazard_has_a_resolvable_test_citation` — H-005 ∉ `_deferred_hazards(real §20)` (because §20 H-005 carries no DEFERRED/T-F5+) AND its `**Test.**` token `test_blocks_opposite_open_side` resolves via the collected-tests fixture; with `_KNOWN_DEFERRED=frozenset()` the whitelist-integrity loop is empty ⇒ no finding ⇒ **36/36**. (b) `test_deferred_carveout_detected_and_tightly_gated` — green under the decouple refactor (synthetic `H-901`/`H-902`, local `_synthetic_ack={901}`, asserts internally consistent, zero reference to the now-empty production `_KNOWN_DEFERRED`). Run `uv run pytest tests/test_hazard_catalog_coverage.py -q` as the explicit E4 verification step (not a narrative claim).

## Open questions

**None.** Architecture decided by the operator (consumer gate). Remaining decisions are codebase-forced / BRIEF-grounded committed defaults: position-state source = per-signal DB query (in-memory view unbuilt; exchange = wrong layer); config knob = `RiskSection.block_opposite_side: bool = True` (BRIEF "default blocked"); insertion = after CLOSE, before cooldown (CLOSE returns early; structural anchor, not line number — T-545 lesson); side reuse = the shipped `_ACTION_TO_SIDE` (no refactor); the two L-026 immutability boundaries = forward-annotation only — **plan-reviewer CONFIRMED sound at the REVISE pass, NOT escalated to NEEDS DISCUSSION** (refinement internal to ADR-0015's own §35-invariant > :46-site-list hierarchy; recorded in ADR-0016 Consequences).

## ADR-0016 (full text — written verbatim to `docs/adr/0016-h005-opposite-side-consumer-gate.md` on plan-reviewer APPROVE)

# ADR-0016: H-005 opposite-side guard implemented as a consumer pre-scoring gate (BRIEF §20/§22 amendment)

**Status:** Accepted (2026-05-17; operator architecture decision "Consumer gate" at the T-542 Gate-1 plan-stage; F6 numbered T-542, the original F6 driver)

## Context

§20 **H-005** (live LONG, opposite SHORT arrives — v1 blocked, v2 DEFERRED). BRIEF §20 Policy + §22 (`block_opposite_position` rule, `condition: type: opposite_side_open, bot: self`, weight -999, required) specified the control as a **scoring condition** `opposite_side_open` (per-bot enable/disable, default blocked). T-519 audit (2026-05-16) confirmed it was never built: no evaluator in `packages/scoring/conditions/`, no execution-side equivalent, `consumer.py:30` defers it. F5 signed-off with E4 35/36 + H-005 DEFERRED (operator-acknowledged residual). F6 opened (ADR-0015); T-542 is the numbered task resolving it.

The shipped F5 reality: **all 7 pre-scoring guards** (3a TTL, 3b symbol, 3b' source-filter [T-545], cooldown [T-526], caps [T-524], loss-limit [T-525a2], drawdown [T-525b]) are **consumer silent-skip gates** deriving state from the DB per signal. The `opposite_side_open` scoring-condition design would be the *only* position-state-dependent scoring condition and requires `ctx.bot.*` position-state context-plumbing into the scoring evaluator that does not exist (a novel subsystem).

## Decision

Implement H-005 as a **consumer pre-scoring silent-skip gate** `risk.block_opposite_side` (default `True` = blocked, per the BRIEF policy intent), mirroring the shipped T-526 cooldown gate: a per-signal DB read of the open-positions table (`position_state` / `paper_position_state` by `exchange_mode`), blocking a new entry whose mapped side (`_ACTION_TO_SIDE[action]`) is opposite the open position's side for `(bot_id, symbol)`. Silent-skip (trading.log info + Prom counter + return before scoring) — same class as cooldown/caps. **Retire** the BRIEF §20/§22 `opposite_side_open` scoring-condition design + the `block_opposite_position` YAML rule.

## Consequences

- BRIEF §20 H-005 Policy/Test rewritten (gate, not scoring rule); **both** §22 occurrences retired — site A (`block_opposite_position` rule, `condition: type: opposite_side_open`) and site B (`opposite_side_open` plugin `entry_point: packages.scoring.rules.opposite_side:OppositeSideOpenRule`) — each replaced by a `risk.block_opposite_side` pointer; the §9.4 strategy-engine "Known hazards addressed" H-005 line + the §19 F6 exit-criteria mechanism wording updated.
- NEW `services/strategy_engine/app/opposite_side_gate.py` + a focused `packages/db/queries` open-position-side helper + `RiskSection.block_opposite_side: bool=True` + consumer wiring + a `signals_blocked_opposite_side_total{bot_id,reason}` counter.
- E4 35/36 → **36/36**: production `_KNOWN_DEFERRED → frozenset()` + §20 H-005 `**Test.**` cites the collectable `test_blocks_opposite_open_side`; the parser self-test `test_deferred_carveout_detected_and_tightly_gated` is decoupled from the production constant (synthetic H-901/H-902 + local `_synthetic_ack`). The L-026 un-DEFER site-set is rewritten in one commit.
- **F5 §A+§B Live-ready sign-off NOT reopened** — F6 additive; H-005 was an operator-acknowledged carve-out, now resolved, not a regression. **Two enumerated operator-signed/point-in-time records stay byte-unchanged (ADR-0015:35 invariant over ADR-0015:46 site-list — the §35-vs-:46 reconciliation):** (1) `docs/runbooks/F5_close_out.md` :86/:90/:105 signed §A/§B "Result: PASS WITH 3 PARTIALS … E4 35/36 + H-005 DEFERRED" attestations (true as of `2026-05-17T05:17:35+00:00`) — non-mutating forward-annotation footnote only; (2) `docs/status.md` :13/:15/:19 T-541 historical entry — untouched, the un-DEFER satisfied by T-542's own new newest-first entry. Neither rewrite falsifies a signed/historical record.
- math-validator OUT-of-scope (no Gate-4 trigger path; boolean side check).

## Rejected alternatives

- **Scoring condition `opposite_side_open`** (BRIEF design-of-record): needs nonexistent `ctx.bot.*` position-state context-plumbing into the scoring evaluator — a novel subsystem, larger, riskier, deviates from the uniform shipped gate architecture; triggers math-validator (`packages/scoring/`). Operator rejected in favour of the gate.
- In-memory `orders.events.<bot_id>` own-position view (§9.4 step 4): unbuilt; large; the consumer CLOSE-action block is deferred for exactly this reason.

## Relationship to ADR-0015 / ADR-0011

Under F6 (ADR-0015) as numbered T-542 (an original F5-residual carve-out, distinct from the decision-C operator-directed batch — H-005 was named in ADR-0015's F6 numbered set from the start). Extends BRIEF §20/§22 via the §6.7 ADR brief-amendment mechanism (sibling-mechanism of ADR-0011/ADR-0015). Does NOT supersede any ADR.

## Cross-references

BRIEF §20 H-005 / §22 / §9.4 / §19 / §6.7 / §15.2; ADR-0015 (F6; L-026 un-DEFER site-set :46/:49); T-526 cooldown_gate (the mirrored precedent); T-545 (the sibling silent-skip gate pattern + structural-anchor lesson); L-026 / L-029.

## Relevant paths

- precedent to mirror: `services/strategy_engine/app/cooldown_gate.py` (full structure), its consumer wiring `consumer.py:183-217`, `metrics.py` `signals_blocked_cooldown`
- `services/strategy_engine/app/consumer.py:88` `_ACTION_TO_SIDE` (reuse), `:172` CLOSE block (insert after), `:29-34` docstring (un-DEFER)
- `packages/db/queries/execution.py:649` `select_recent_open_trade_exists` (focused-query precedent), `analytics.py:515` `select_open_positions` + `_row_to_open_position` (`position_state` schema, `.side`)
- `packages/scoring/types.py:409` `RiskSection`
- `packages/bus/schemas/signals.py:65` `SignalValidated.action`; side vocab `packages/exchange/protocols.py:95` `Literal["buy","sell"]`
- L-026 un-DEFER (token-anchored, line numbers indicative-only — re-grep at write-time): BRIEF §20 `### H-005`:~2676 / §9.4 strategy-engine `**Known hazards addressed:** H-005`:~1565 [confirmed §9.4: hdr :1536 → :1565 → §9.5 :1567] / §22 site A `block_opposite_position`:~3167 / **§22 site B `opposite_side_open` entry_point:~3285 [the L-026-missed second site]** / §19 F6 exit-criteria:~2631; EXCLUDE :2841 (H-024, unrelated "opposite-side"). `README.md:13-14,117`. `tests/test_hazard_catalog_coverage.py`: `_KNOWN_DEFERRED`:76 → `frozenset()` + doc :69-76, `_FIX_DEFERRED` literal :327-340 (relabel H-901/H-902) + `test_deferred_carveout_detected_and_tightly_gated` :343-361 (decouple → local `_synthetic_ack`). `docs/runbooks/F5_close_out.md` :86/:90/:105 signed (forward-annotation footnote only). `docs/status.md` T-541 entry (untouched; new entry at close).

---

_(end of embedded ADR-0016 — the block above [`# ADR-0016:` … `## Relevant paths`] is written verbatim to `docs/adr/0016-h005-opposite-side-consumer-gate.md` in the T-542 feat commit. Everything below is plan-level, NOT part of the ADR file.)_

## Write-time guidance (plan-reviewer Gate-1 APPROVE pass-2 — brief-reviewer verifies each before SHIP)

1. Insert the gate call by STRUCTURAL ANCHOR, not line number: immediately after the `if signal.action == "CLOSE": … return` block (consumer.py ~:172-179) and BEFORE the T-526 `check_cooldown` call (~:183). The CLOSE-ordering pin test is the only proof of this placement — it must assert CLOSE reaches the CLOSE block, not the gate (mirror T-545's pin).
2. Mirror `cooldown_gate.py`/`consumer.py:183-217`/`metrics.py signals_blocked_cooldown` EXACTLY: `OppositeSideDecision` frozen+slots, `_INACTIVE` sentinel, disabled short-circuit (`if not risk_config.block_opposite_side: return _INACTIVE`) BEFORE any `pool.acquire`, table dispatch by `exchange_mode` (`position_state`/`paper_position_state`), and on `active`: `trading_logger.info("signal_blocked_opposite_side", …)` + `metrics.signals_blocked_opposite_side.labels(...).inc()` + `return` before scoring_evaluations/orders.requests/signals.rejected.
3. Meta-test 3-part decouple in `tests/test_hazard_catalog_coverage.py`, all three independent and assertion-exact: (i) `_KNOWN_DEFERRED → frozenset()` + doc :69-76 rewrite; (ii) §20 H-005 `**Test.**` cites the bare-fn token `` `test_blocks_opposite_open_side` ``; (iii) parser self-test relabel synthetic to `### H-901`/`### H-902`, add LOCAL `_synthetic_ack=frozenset({901})`, rewrite asserts internally-consistent (`deferred=={901,902}`, `cites[901]==[]`/`cites[902]==[]`, gating via `hnum not in _synthetic_ack`, `findings==["H-902: DEFERRED not acknowledged"]`, `901 in _synthetic_ack`, `902 not in _synthetic_ack`) with ZERO reference to the production constant. The synthetic literal must retain `DEFERRED` + `T-F5+` tokens (`_deferred_hazards` keys on them).
4. Mandatory write-time L-026 grep BEFORE editing docs: `H-005|opposite[_ -]side|opposite_side_open|block_opposite|opposite-signal|Known hazards addressed` across the whole brief. Confirmed plan-stage site-set: §20 :2676/:2680/:2682, §9.4 :1565, §22 site A :3167-3176, §22 site B :3285-3291, §19 exit-criteria :2631 (rewrite) — and §19 task-def :2625 STAYS (accurate task description). EXCLUDE :2841 (H-024 ADR-0005 trail-labeling, unrelated). README :13-14/:117, consumer.py:29-34. ALL doc un-DEFER in ONE feat commit (ADR-0015:49).
5. Both §22 sites retired identically per ADR-0016: replace the DEFERRED comment with the `risk.block_opposite_side` consumer-gate pointer and remove the YAML rule (site A `block_opposite_position`) / the plugin entry_point example (site B `opposite_side_open`).
6. Truth-table test completeness: all 8 `## Hand verification` rows are §N4 cases — the two block rows (`test_blocks_opposite_open_side` buy-open+SHORT, its sell-open+LONG mirror), the two same-side allows, the two no-position allows, the disabled short-circuit asserting NO `pool.acquire`, and the CLOSE-ordering pin. Plus the meta-test re-green case: `uv run pytest tests/test_hazard_catalog_coverage.py -q` green (both tests), `test_blocks_opposite_open_side` `pytest --collect-only`-resolvable, sibling regression (cooldown/caps/source-filter/symbol/CLOSE) explicitly run.
7. L-022 escalation trigger active: if write-time src exceeds ~320 → STOP and escalate, do not absorb (drift-checker auto-NEEDS-DISCUSSION at ≥1.5×). L-021 discharge: the new `select_open_position_side` carries an explicit cast-audit docstring stating column-direct equality params need no `::type` cast (mirror `trades.py` WG#2 audit docstring).
8. F5_close_out.md / status.md T-541 entry are FORWARD-ANNOTATION-ONLY: F5_close_out :86/:90/:105 signed §A/§B lines stay byte-unchanged + one non-mutating footnote; status.md T-541 historical entry untouched (un-DEFER satisfied by T-542's own new newest-first close entry). Per ADR-0016 Consequences §35-over-:46 reconciliation.
