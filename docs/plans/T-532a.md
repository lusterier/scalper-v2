# T-532a — funding-settlement read path + storage (foundation leaf)

**Status:** APPROVED (plan-reviewer Gate-1 single-pass APPROVE 2026-05-16; 0 BLOCKER / 0 CONCERN; every load-bearing code citation verified true; migration-chain correction confirmed). Active foundation leaf of the operator-approved L-007 2-leaf split T-532→{a,b} (OQ-2=A). Recursive-split-record: `docs/plans/T-532.md`. Mirrors the T-527b1/T-534a/T-533a foundation-leaf posture.

## Purpose
The funding-settlement read path + storage, NO consumer (T-532b is the first consumer — the poll tick + cumulative-delta audit integration). Zero behavior change: nothing polls or reads `funding_fees` yet.

## Operator OQ answers (T-532 plan-stage 2026-05-16, all recommended, baked)
- **OQ-1=A** — NEW `funding_fees` hypertable (migration 0021), NOT extend `executions` with `exec_type='funding'` (funding ≠ execution semantically — executions since 0005 require order_id/qty/price NOT NULL; ZERO sibling-migration-test churn vs extend-executions churning test_0005; cumulative-delta `SUM(trades.realized_pnl)` doesn't read executions anyway).
- **OQ-2=A** — 2-leaf split: T-532a (this foundation leaf) + T-532b (consumer: poll tick + audit integration + main wiring + config; blocked-by).
- **OQ-3=A** — funding enters the T-220 cumulative-delta audit as a SEPARATE cumulative funding term, NEVER folded into `trades.realized_pnl` (H-017-clean — ADR-0006). [Binds T-532b; T-532a is storage-only, no audit code.]
- **OQ-4=A** — a SEPARATE APScheduler `funding_fee_poll` tick (mirror the shipped T-531 `equity_snapshot`), own §N9/L-001 interval Settings, own `add_job`. [Binds T-532b.]

## Doubly-stale-doc correction (T-533a "0018→0020" precedent class, L-018)
TASKS.md:237 says "migration 0017" and ADR-0011 says "0016+0017" — BOTH stale (0017 = the shipped `0017_symbol_map_cleanup`; current migration head is `0020_trades_lifecycle_state` revision='0020' down_revision='0019', verified migrations/versions/0020:72-73). **T-532's migration is `0021`, down_revision='0020'.** Noted in-body in the migration file + commit.

## Scope / files

**1. `packages/exchange/types.py` — NEW `FundingFee` value type**
Frozen+slots Pydantic value type, verbatim-mirror the shipped `AccountBalance` (T-530) ConfigDict/shape. Fields: `symbol: str`, `settled_at: datetime` (UTC-aware — mirror the existing UTC-validated datetime field convention in types.py, §N1), `funding: Decimal` (signed — +credit / −debit). Add to `types.py __all__` ONLY (verified: `packages/exchange/__init__.py` does NOT re-export AccountBalance/InstrumentInfo — follow the ACTUAL precedent; T-530 plan-vs-reality lesson — NO `__init__.py` claim).

**2. `packages/exchange/protocols.py` — `ExchangeClient` Protocol extension**
Add `get_funding_fees_window(sub_account: str, since: datetime) -> list[FundingFee]` `@idempotent` (read; mirror the `get_closed_pnl_window` T-220a marker/declaration site). Conformance test: N PRECISE sub-edits NOT a blind count bump (verbatim T-530/T-527b1 discipline — count +1, explicit `get_funding_fees_window` `@idempotent` marker-pin mirroring the file `getattr`/`is_idempotent` convention, `_UNLABELED_METHODS` frozenset UNCHANGED ==3 [stream_executions/stream_positions/close, verified protocols.py:63], the read/write tally comment updated).

**3. `packages/exchange/bybit_v5/adapter.py` — NEW `get_funding_fees_window`**
Verbatim-mirror the verified `get_closed_pnl_window` (adapter.py:454-496): `@idempotent`, windowed pull `startTime` = `int(since.timestamp()*1000)` ms, pagination cursor capped by a `_MAX_*_PAGES` constant (mirror `_MAX_CLOSED_PNL_PAGES`=10, adapter.py:73), `Decimal(str(...))` no-float SIGNED decode (§5.13/§N1), `RateLimitError → _on_rate_limit_hit(bucket)` re-raise, via `self._client.request("GET", ..., retries=3)` (read = §N3-legal). Bybit `/v5/asset/transaction-log` `type=SETTLEMENT`. Returns `list[FundingFee]` (per-settlement granularity — deliberate divergence from `get_closed_pnl_window`'s `-> Decimal`, documented in the docstring: storage needs per-settlement records, OQ-1=A/OQ-3=A).
**WRITE-TIME-VERIFY (the single flagged item — MUST be resolved at write-time with a pinned fixture, NOT assumed):** (a) the exact Bybit v5 `/v5/asset/transaction-log` SETTLEMENT record JSON field names for funding-amount / symbol / settlement-time → decode into `FundingFee`; (b) the rate-limit bucket — `get_closed_pnl_window` uses `"positions"` but this is an `/v5/asset/` endpoint (the adapter uses multiple buckets — "positions"/"orders"); pick by the Bybit v5 rate-limit category at write-time, fixture-pin the decode.

**4. `packages/exchange/paper/adapter.py` — paper stub**
`get_funding_fees_window` → `return []` (deterministic; paper has no perpetual-funding model — mirror the T-534a paper `sl_price=None` / T-530 paper-limitation posture; documented as an explicit paper limitation, NOT a TODO).

**5. `migrations/versions/0021_funding_fees.py` — NEW hypertable**
Verbatim-mirror `0019_bot_equity_snapshots.py` (verified 0019:40-91): `revision='0021'`, `down_revision='0020'` (the corrected chain — NOT the stale "0017"). `id` BigInteger Identity(always=False) + `bot_id` Text NOT NULL + `symbol` Text NOT NULL + `settled_at` TIMESTAMP(timezone=True) NOT NULL + `funding` Numeric(20,4) NOT NULL (signed; L-005 money-precision round-half-even, mirror 0019's money cols), composite PK `(settled_at, id)`, `create_hypertable('funding_fees','settled_at', chunk_time_interval => interval '7 days')`, a bot/time-DESC index mirroring 0019, symmetric `downgrade()` (§N8 — DROP). In-body note the doubly-stale-doc correction.

**6. `tests/integration/migrations/test_0021_migration.py` — NEW**
Verbatim-mirror `test_0019_migration.py`: env-gated `POSTGRES_TEST_DSN` (skip if absent), column-shape (ordinal/data_type/is_nullable), **L-005 numeric_precision/numeric_scale tripwire on `funding` == (20,4)**, composite-PK + no-FK assertion, hypertable 7-day chunk-interval assertion, **L-012 explicit `downgrade` target '0020' (NOT relative `-1`)**.

**7. `packages/db/queries/<funding>.py` — NEW `@non_idempotent insert_funding_fee`**
Verbatim-mirror the shipped `insert_equity_snapshot` (verified equity.py:37-82): `@non_idempotent async def insert_funding_fee(conn: _DbExecutor, *, bot_id: str, symbol: str, settled_at: datetime, funding: Decimal) -> None` — single INSERT, column-direct VALUES (L-021 — no `::cast`; unit mock-pin only, no testcontainer in unit scope, mirror T-531/T-533b1), append-only no-retry. `@non_idempotent` + a marker-pin test asserting `is_non_idempotent==True` (mirror the insert_equity_snapshot §N3 contract; pre-empts the T-534b1 §N3-marker BLOCKER class).

NO consumer (T-532b first). Zero behavior change. 0 §20 regression, no new H-NNN (§0.8 — read-only adapter + new isolated table + writer-with-no-caller; mirror T-530/T-531/T-533a).

## §20 hazards / §N invariants
- No §20 hazard touched (no order/SL/capital write/reconcile; isolated new table + no-caller writer + read-only `@idempotent` adapter). No §20 regression.
- §N1: `settled_at` UTC-aware; all money `Decimal`, `Decimal(str(...))` decode (no float); Numeric(20,4) PG round-half-even (L-005 pinned).
- §N3: adapter `@idempotent` (read); writer `@non_idempotent` (append-only) + marker-pin.
- §N4: financial-math TDD — signed funding Decimal decode (no-float) + windowed-pull/pagination + migration precision; `## Hand verification` below.
- §N5: ≥80% on the new adapter/type/writer via §N4 TDD; the §17.2 per-module gate stays unwired repo-wide (T-527b2a-filed T-F5+ ticket owns it — NOT re-litigated, NOT N5-by-omission).
- §N8: forward-only migration + symmetric downgrade + per-migration test_0021.
- §N9: NO Settings in T-532a (the poll-interval knob is T-532b — OQ-4=A binds the consumer leaf).

## Sibling migration test impact (ADR-0011:102 MANDATORY section, L-015)
NEW isolated `funding_fees` table → modifies NO existing table → **ZERO churn to any earlier `tests/integration/migrations/test_NNNN_migration.py`** (test_0005 `_EXPECTED_TRADES_COLUMNS`/`_EXPECTED_EXECUTIONS_COLUMNS`, test_0008, test_0019, etc. all UNAFFECTED — the deliberate OQ-1=A clean-separation benefit vs the rejected extend-executions alternative). Only a NEW `test_0021_migration.py` is added. Stated explicitly per the ADR-0011:102 active control.

## Gate-4 (math-validator) disposition — COMMITTED
**Gate-4 IN scope, COMMITTED** (no residual optionality — T-527b1 open-disposition lesson applied). Not in the literal CLAUDE.md trigger list, but the T-530 non-exhaustive-path precedent governs: real signed-Decimal funding-money decode feeding the T-532b cumulative-delta P&L-audit capital path. Strictly SMALLER numeric surface than a full-arithmetic task (T-532a stores raw settlements; the cumulative-delta funding-sum arithmetic is wholly T-532b) → the T-530/T-527b1/T-534b1 "in-scope, decode+precision, no heavy arithmetic → fast/MODERATE VERIFIED" disposition, NOT the heavy T-536/T-528a class. Verifies: `Decimal(str(...))` no-float signed-funding decode, the Numeric(20,4) L-005 precision/scale, no silent Decimal→float, the windowed-pull boundary (since→startTime ms, pagination cap).

## Hand verification (math-validator Gate-4 cross-check)
- Signed decode: a Bybit SETTLEMENT record with funding `"-0.12345678"` → `Decimal("-0.12345678")` (debit, negative preserved exact, no float artefact); `"0.5"` → `Decimal("0.5")` (credit). [Exact field name write-time-verified + fixture-pinned.]
- Numeric(20,4): `Decimal("-0.12345678")` persisted to `funding numeric(20,4)` → PG round-half-even → `Decimal("-0.1235")` (mirror the 0019 Numeric(20,4) precedent; pinned in test_0021 L-005 tripwire).
- Window boundary: `since=2026-05-16T00:00:00+00:00` → Bybit `startTime` = `1778889600000` ms (UTC epoch ms; hand-computed + reviewer-recomputed two ways: 2026-01-01Z = 1767225600 s; +135 days [Jan31+Feb28(2026 non-leap)+Mar31+Apr30+15] = +11 664 000 s → 1 778 889 600 s ×1000. NOT `1779897600000` — that decodes to 2026-05-27T16:00:00Z, the pre-Gate-4 stale literal, corrected per math-validator L-018/L-025). Code computes `int(since.timestamp()*1000)` at runtime (verbatim mirror `get_closed_pnl_window`); the adapter test asserts via that same expression, NOT a hardcoded ms — so this illustrative number is doc-only and never reached the capital path. Pagination cursor stops at `_MAX_FUNDING_PAGES`.
- Empty window / paper: no settlements → `[]` (not None; paper always `[]`).

## Test strategy (§N4 TDD)
- `packages/exchange/tests/` adapter: a Bybit transaction-log SETTLEMENT JSON fixture (multi-record incl. a negative/debit + a positive/credit + a pagination cursor) → `list[FundingFee]` decode golden (hand-authored expected Decimals, NOT impl-against-itself); empty-window → `[]`; rate-limit→re-raise; the `since→startTime ms` boundary.
- paper: `get_funding_fees_window` → `[]` deterministic.
- conformance: the N precise sub-edits + the `@idempotent` marker-pin (mirror T-530/T-527b1).
- `tests/integration/migrations/test_0021_migration.py`: env-gated, column shape, L-005 (20,4) tripwire, composite PK, hypertable 7d, L-012 explicit downgrade '0020'.
- queries: `insert_funding_fee` mock-pin (L-021 column-direct, no testcontainer in unit scope) + `@non_idempotent` marker-pin.
- L-015: assert/state NO existing migration test is touched (the clean-separation benefit).

## LOC / §0.3 / L-022
src ~180-260 (adapter ~50-70; FundingFee ~15; protocol +1; conformance edits ~10; paper ~8; migration 0021 ~60-90 mirror 0019; queries writer ~40 mirror equity.py). tests ~180-220 (test_0021 ~140 mirror test_0019 the bulk; adapter decode golden ~50; conformance/writer ~30). ≪ §0.3 400 (foundation leaf — tick/audit/Settings wholly T-532b). L-022: migration/adapter tasks test-heavy NOT Hand-verification-docstring-heavy (T-533a ~124 src / T-530 ~147 closest precedents) → no §0.3 waiver, single foundation leaf, no further split. L-007 boundary clean: NO tick / NO audit / NO main wiring / NO Settings (all wholly T-532b).

## Open questions
None. OQ-1/2/3/4=A baked. Migration-number correction (0021/down_revision 0020), Gate-4 COMMITTED, §N5 §17.2-gate posture, paper-`[]` limitation, FundingFee `types.py __all__`-only (no `__init__.py`) all committed (no "to confirm" residue). The single WRITE-TIME-VERIFY item (Bybit transaction-log SETTLEMENT field names + rate-bucket) is a write-time fixture-pinned verification, NOT an unresolved design question.

## Write-time guidance (must be addressed during implementation; plan-reviewer verbatim)
1. Migrácia `0021_funding_fees.py`: `revision='0021'`, `down_revision='0020'` (NIE doubly-stale "0017"). Verbatim-mirror 0019_bot_equity_snapshots hypertable shape (id BigInteger Identity(always=False) + bot_id Text + settled_at TIMESTAMP(timezone=True) + funding Numeric(20,4); composite PK `(settled_at, id)`; `create_hypertable(...,chunk_time_interval => interval '7 days')`; bot/time-DESC index mirror; symmetric downgrade DROP). In-body note doubly-stale-doc correction (0017→0021 + ADR-0011/TASKS stale).
2. `test_0021_migration.py`: mirror test_0019 — env-gated POSTGRES_TEST_DSN; column-shape (ordinal/data_type/is_nullable); **L-005 numeric_precision/numeric_scale tripwire na `funding` == (20,4)**; composite-PK + no-FK assert; hypertable 7d chunk assert; **L-012 explicit `downgrade` target '0020' (NIE relatívne -1)**.
3. `FundingFee` → `packages/exchange/types.py __all__` ONLY. Verify ACTUAL AccountBalance/InstrumentInfo precedent — NErobiť `packages/exchange/__init__.py` re-export claim (overené: __init__.py neexportuje AccountBalance ani InstrumentInfo). Polia: `symbol: str`, `settled_at: datetime` (UTC-aware, mirror existujúci UTC-validated datetime field convention v types.py — §N1), `funding: Decimal` (signed +credit/−debit). frozen+slots Pydantic mirror AccountBalance ConfigDict.
4. Adapter `get_funding_fees_window` verbatim-mirror `get_closed_pnl_window` decode/pagination/rate-limit disciplína (@idempotent, since→startTime int ms, cursor cap `_MAX_*_PAGES` mirror _MAX_CLOSED_PNL_PAGES, `Decimal(str(...))` no-float signed, RateLimitError→_on_rate_limit_hit(bucket)→raise, self._client.request("GET",...,retries=3)). Return `list[FundingFee]` (zámerná divergencia od -> Decimal — zdokumentovať v docstringu: storage potrebuje per-settlement záznamy, OQ-1/OQ-3). **WRITE-TIME-VERIFY (jediný flagnutý bod — MUSÍ byť vyriešený pri kódení s pinned fixture, NIE predpokladaný):** (a) exact Bybit v5 /v5/asset/transaction-log SETTLEMENT JSON field names pre funding-amount/symbol/settlement-time → decode do FundingFee; (b) rate-limit bucket — get_closed_pnl_window používa "positions" ale toto je /v5/asset/ endpoint; vybrať podľa Bybit v5 rate-limit kategórie, fixture-pin decode. Paper stub → `return []` (deterministic; zdokumentovaná paper limitation, NIE TODO).
5. Conformance test = N precise sub-edits NIE blind count bump: count +1, explicit `get_funding_fees_window` @idempotent marker-pin (mirror file getattr/is_idempotent convention), `_UNLABELED_METHODS` frozenset UNCHANGED ==3, read/write tally comment updated. `insert_funding_fee` @non_idempotent + marker-pin test `is_non_idempotent==True` (pre-empts T-534b1 §N3-marker BLOCKER class). L-015: explicitne assertnúť/uviesť ZERO churn na existujúce test_NNNN_migration.py (clean-separation benefit — žiadna existujúca tabuľka sa nemení; ADR-0011:102 active control). Gate-4 COMMITTED. mypy strict nad staged set incl. tests pre-commit (L-024). grep všetkých nových symbolov vrátane scripts/ (T-527b2b backtest.py lekcia — T-532a síce no-caller, disciplína stojí).
