# T-525a — SPLIT RECORD (superseded by T-525a1 + T-525a2)

**Status:** Superseded — recursive L-007 split (plan-reviewer REVISE 2026-05-15).

T-525 was operator-split at plan stage into T-525a (daily-loss-limit) + T-525b (max-drawdown). Plan-reviewer Gate 1 on `T-525a.md` returned **REVISE**: self-assessed ~436 core src (excl. migration) exceeds the §0.3 400 hard cap; the T-524 0.77× undershoot ratio is invalid here (T-525a's per-file estimate was already L-022 docstring-inclusive, not logic-only-optimistic — so the overshoot risk is real, and T-526's 1.92× overshoot proves L-022 tasks overshoot even post-split). Recursive L-007 split applied (within the operator-approved L-007 split mechanism for T-525; split boundary is mechanical "infra vs gate", validated by T-524/T-526 precedent — no open architectural decision):

- **T-525a1** (`docs/plans/T-525a1.md`) — **ACTIVE**. Kill-switch persistence infrastructure: migration 0018 `bot_kill_switch_state` + `packages/db/queries/kill_switch.py` (CRUD, `@idempotent` writes) + `kill_switch_reconcile.py` (`reconcile_kill_switch_on_startup`, H-027) + main.py lifespan wiring + `RiskSection.daily_loss_limit_usd` Decimal field + yaml_loader Decimal-coerce (type dependency owned here so T-525a2's gate can consume it). **NEW H-027 formal §20 allocation here** (persistence + reconcile = core of H-027). plan-reviewer-estimated ~205-235 core src (excl. migration). Gate-4 math-validator: out of scope / fast no-op (declares the Decimal config field; no arithmetic — the P&L SUM lives in a2).
- **T-525a2** — follow-up, blocked-by T-525a1. The daily-loss gate: `packages/db/queries/trades.py::sum_realized_pnl_since` + `services/strategy_engine/app/loss_limit_gate.py` (`check_daily_loss_limit`) + consumer.py gate wiring (chain cooldown→caps→loss-limit) + `metrics.py` `signals_blocked_loss_limit` Counter + alpha/beta.yaml `daily_loss_limit_usd: "0"` + gate/consumer-ordering/integration tests. plan-reviewer-estimated ~215-245 core src. **math-validator Gate 4 IS in scope** (Decimal P&L sum + `sum <= -limit` threshold). L-021 BLOCKER resolved in a2's plan: `closed_at >= $2::timestamptz` explicit cast (NOT "verify at impl" — that is the T-537a1 ci-full-crash anti-pattern). Its own plan-reviewer cycle when T-525a1 merges.

- **T-525b** — `max_drawdown_pct`, P&L-drawdown from trades, NOT blocked-by-T-531 (operator OQ-4=A). Separate follow-up, own plan-reviewer cycle. Different reset semantic (hard-stop, not UTC-daily-auto-clear).

See `docs/plans/T-525a1.md` for the active consolidated plan.
