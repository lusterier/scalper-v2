# T-533 — SPLIT RECORD (L-007 pre-emptive split)

**Status:** SPLIT 2026-05-16 (operator OQ-2=A at T-533 plan-stage; ADR-0011-pre-flagged L-007/L-014/L-016 split-watch). Not an implementable plan — records the L-007 pre-emptive split; the active leaf plan is `docs/plans/T-533a.md`.

## Why pre-emptive split (L-007)

T-533 (named-state `TradeLifecycleState` FSM enum refactor) is the **largest hardening task** in the ADR-0011 cluster and the F5 critical-path bottleneck (TASKS.md:295, ADR-0011:35). ADR-0011:54/92/100 pre-flagged it for L-007 split-watch + L-014/L-016 (large refactor across multiple modules + migration + state population). TASKS.md:235 estimate "~400 LOC src (pre-split; **will trigger split-watch**) + ~350 LOC tests + 1 migration" — a self-declared mid-write cap breach. L-007 active control: pre-emptive split at plan-stage is far cheaper than an operationally-expensive mid-write split (T-525 / T-534b recursive-split precedent). Refactor blast radius confirmed: ~19 non-test files reference `trades.status` / `close_reason` / `position_state.{tp_hit,trailing_active,sl_type}` (reconcile / restart / placement_persist / dispatcher / lifecycle / execution.py / trades.py / analytics + analytics-api models + every sibling + migration test).

## Operator OQ answers (T-533 plan-stage 2026-05-16, all default A — baked into T-533a/T-533b)

- **OQ-1=A — ADDITIVE observable scope** (NOT authoritative-switch): `lifecycle_state` becomes a NEW canonical *observable* state (analytics / audit / UI / operator clarity); the legacy 4 columns (`trades.status`, `orders.status`, `position_state.{tp_hit,trailing_active,sl_type}`, `trades.close_reason`) **remain authoritative for all reads / decision logic**. Zero behavior change, hazard-test-neutral, low-risk right before live. The authoritative-switch + legacy-column deprecation/view is a deliberately-deferred F5+/F6 follow-up (out of T-533 scope entirely). This matches the ADR-0011 intent (named-state FSM for operational clarity pre-live) without a risky behavioral rewrite of capital-path code.
- **OQ-2=A — L-007 pre-emptive split** into **T-533a** (foundation, no behavioral consumer) + **T-533b** (dual-write wiring, blocked-by T-533a). T-533b may itself recursive-split by module-group at its own plan-stage if it trips §0.3 (mirror T-534b→{b1,b2}; decided at T-533b plan-stage, not now).
- **OQ-3=A — Python `StrEnum` + `trades.lifecycle_state` `sa.Text()` column** (repo convention — `trades.status`/`close_reason`/`orders.status` are all `sa.Text()`; `packages/core/types.py` already holds `Action`/`BacktestStatus`/`BotStatus`/`ShadowVariantTerminal`/`ShadowRejectedTerminal` `StrEnum`s; NO PG native ENUM — no `CREATE TYPE` exists anywhere in the repo; migration-friendly, no ENUM-ALTER pain).
- **Migration number = 0020** (TASKS.md:235 "0018" is doubly-stale — 0018=`bot_kill_switch_state` T-525a1, 0019=`bot_equity_snapshots` T-531 already landed; same stale-doc pattern as T-531's "0016"→0019 catch).

## Split boundary (mechanical — foundation vs consumer; mirror T-530→T-531 / T-534a→T-534b)

- **T-533a** (active leaf — `docs/plans/T-533a.md`): NEW `TradeLifecycleState` `StrEnum` (13 states) in `packages/core/types.py` + canonical `derive_lifecycle_state(...)` mapping helper in `packages/db` + migration 0020 (add `trades.lifecycle_state` `sa.Text()` nullable + backfill `UPDATE` mapping the steady-state subset from the legacy 4 columns + symmetric downgrade) + the **L-003 golden cross-check** (Python helper ≡ migration SQL CASE for every legacy combination) + the **L-015 sibling-migration-test-impact** (`test_0005_migration.py` — `trades` table shape) + `test_0020_migration.py` (L-021 run-locally + L-012 explicit `downgrade 0019`). **NO dual-write wiring, NO behavioral reader** — T-530/T-534a/T-531-migration-shaped no-consumer foundation leaf. ~130-180 src.
- **T-533b** (blocked-by T-533a): dual-write wiring — every state-transition WRITE site (placement → ORDER_REQUESTED/ORDER_PLACED/OPEN; dispatcher → PARTIALLY_CLOSED/CLOSING/CLOSED; lifecycle → TP_HIT/BREAKEVEN_SET/TRAILING_ACTIVE; reconcile/restart → ORPHANED/RECONCILED/FAILED; placement-fail → FAILED) ALSO writes `trades.lifecycle_state` via the shipped `derive_lifecycle_state` / direct enum, alongside the unchanged legacy columns. Forward-only transient states (SIGNAL_RECEIVED/ORDER_REQUESTED/ORDER_PLACED/CLOSING) become reachable here. Own plan-reviewer Gate-1 cycle when T-533a merges (mirror T-525a1→T-525a2 / T-534b1→T-534b2 sequence); recursive-split by module-group decided there.

## Sequence

T-533a plan-reviewer Gate-1 → implement → 4 gates → merge. Then T-533b (now unblocked) gets its own consolidated plan + Gate-1 cycle. Mirrors T-525a1→T-525a2 / T-534b1→T-534b2.
