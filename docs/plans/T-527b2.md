# T-527b2 — RECURSIVE SPLIT RECORD (L-007)

**Status:** SPLIT 2026-05-16 (operator OQ-1=A at T-527b2 plan-stage; code-inventory ~432 src EXCEEDS §0.3 400 cap; T-527b/ADR-0013/TASKS pre-flagged L-007/L-014 split-watch). Not an implementable plan — records the recursive split; the active leaf plan is `docs/plans/T-527b2a.md`. T-527b2b plan written when T-527b2a merges.

## Why recursive split (L-007)

T-527b2 (the §B.1 sizing COMPUTE + wiring, blocked-by the shipped T-527b1 `get_mark_price`) was inventoried at **~432 src LOC** — over the §0.3 400 cap *before* L-022 docstring contingency — across **two distinct concerns**: (a) a NEW `packages/sizing/` pure tier-ladder compute module (Decimal-only math, §N4 financial-math TDD + Hand verification, no I/O, independently unit-testable) and (b) the cross-service wiring (a `SizingSpecForWire` wire model + `OrderRequest.sizing` + strategy-engine producer mapping + the execution-service `placement.py` seam: balance fetch + mark-price fetch + tier compute + sub-lowest-tier skip-before-place + a Prom counter + the `sub_account` handler-kwarg plumbing — 5 placement error-paths). As one task this systematically breaches §0.3 (TASKS:T-527b2 + ADR-0013 pre-flagged L-007/L-014). Per L-007 active control, a pre-emptive split at plan-stage is far cheaper than a mid-write cap breach (T-527→{a,b} / T-527b→{b1,b2} / T-533→{a,b}→{b1,b2} / T-534→{a,b}→{b1,b2} precedent — pure foundation leaf then consumer leaf).

## Operator OQ answers (T-527b2 plan-stage 2026-05-16, all recommended — baked into T-527b2a / T-527b2b)

- **OQ-1=A — 2-leaf recursive split** T-527b2 → **T-527b2a** (NEW `packages/sizing/` pure compute module: `select_tier` / `apply_score_multiplier` / `cap_notional` / `compute_qty_from_sizing` — Decimal-only, §N4 hand-verified TDD; imports only `Decimal` + the `SizingTier` frozen value type from `packages.scoring.types` [acyclic — `packages.scoring` does NOT import `packages.sizing`; layering-clean, distinct from the b2b bus→scoring cycle]; NO consumer — foundation, independently testable) + **T-527b2b** (the wiring: NEW `SizingSpecForWire` in `packages.bus.payloads` + additive `OrderRequest.sizing` + strategy-engine producer maps `BotConfig.sizing`→wire + `placement.py:_handle` seam [`get_account_balance().total_equity` + `get_mark_price` + `packages.sizing` compute + sub-lowest-tier skip-before-`place_market_order` + Prom counter] + `sub_account` kwarg into `make_per_bot_handler`; blocked-by T-527b2a; Gate-4 IN scope — the real notional÷price capital path). Mirrors T-527a/T-527b1 foundation→consumer discipline EXACTLY.
- **OQ-6b=A (binds T-527b2b plan-stage) — `SizingSpecForWire` in `packages.bus.payloads`**: `OrderRequest.sizing` cannot import `SizingSection` from `packages.scoring` (`packages.scoring` already imports `packages.bus` — the reverse is an import cycle). Define a wire-facing `SizingSpecForWire` Pydantic model in `packages/bus/payloads.py` (tiers/score_multipliers/max_notional_per_symbol only) — the EXACT T-511b2 `VariantSpec` precedent (`packages.scoring.types` already imports `VariantSpec` from `packages.bus.payloads` for `ShadowVariant`). Producer maps `BotConfig.sizing`→`SizingSpecForWire` (mirror `shadow_variants`). Layering-clean, no cycle.
- **OQ-7=A (binds T-527b2b plan-stage) — `sub_account` kwarg into `make_per_bot_handler`**: `adapter.get_account_balance(sub_account)` at the placement seam needs the bot's sub_account (not in `placement.py:_handle` closure scope today). Extend `make_per_bot_handler()` with a `sub_account: str` kwarg, bound into the closure (mirror the existing `now_fn` binding); main.py lifespan reuses the shipped `_resolve_sub_account(adapter)` helper (T-531 equity-snapshot precedent). Idiomatic, ~+2-3 main.py LOC.
- Inherited (ADR-0013 Accepted + T-527/T-527b plan-stage, all default/recommended): compute home = execution-service placement seam; `SizingSection` threaded onto `OrderRequest` (as `SizingSpecForWire`, OQ-6b); balance via `get_account_balance().total_equity` (OQ-4=A); reference price via `get_mark_price` (T-527b1, shipped); **sub-lowest-tier balance → SKIP the signal before `place_market_order`** (no order; structured log + Prom counter; hard gate, mirror T-526 — OQ-4=A/ADR-0013, NOT a soft fallback to `execution.qty`); score→multiplier `str(floor(score))` clamped to the present-key range, `None`/absent→×1.0 (OQ-3=A T-527b); `tier_promotion`/`tier_demotion` DEFERRED (NEW `T-F5+` backlog ticket, T-527 OQ-2=A); backward-compat `BotConfig.sizing is None` → static `request.qty` byte-unchanged.

## Split (mechanical — pure compute foundation vs cross-service wiring consumer; mirror T-527b→{b1,b2})

- **T-527b2a** (active leaf — `docs/plans/T-527b2a.md`): NEW `packages/sizing/` (`__init__.py` + `compute.py`) — the 4 pure §B.1 tier-ladder functions (Decimal-only, no I/O, no caller). Mirrors `packages/exchange/quantize.py` (52-line pure-compute-helper precedent: module docstring §5.3-no-float + round-down semantic; caller-agnostic). §N4 financial-math TDD + `## Hand verification` (the core deliverable — every tier-boundary / floor+clamp / cap / notional÷price case hand-computed). **Gate-4 math-validator IN scope, COMMITTED** (this IS the §B.1 sizing financial math feeding the T-527b2b capital path — T-530/T-527b1 non-exhaustive-path precedent; HEAVY verification, NOT a fast pin — real tier-select boundary + floor+clamp + cap + Decimal division). NO consumer (T-527b2b first).
- **T-527b2b** (blocked-by T-527b2a): NEW `SizingSpecForWire` (`packages.bus.payloads`, OQ-6b) + additive `OrderRequest.sizing: SizingSpecForWire | None = None` (mirror T-527a `score`) + strategy-engine `_publish_order_request` maps `bot_config.sizing`→`OrderRequest.sizing` (mirror `shadow_variants`) + `placement.py:_handle` seam (before T-529 `quantize_qty`): `_resolve_sub_account`→`get_account_balance().total_equity` + `get_mark_price` + `packages.sizing.compute_qty_from_sizing` → `None`⇒skip-before-`place_market_order` (log + `signals_skipped_sizing` Prom counter on the T-531 `build_execution_metrics` scaffold) else substitute computed qty into the existing `quantize_qty` → `place_market_order` + `make_per_bot_handler` `sub_account` kwarg + main.py wiring. Backward-compat `request.sizing is None` → static path byte-unchanged. Own plan-reviewer Gate-1 cycle (OQ-6b/OQ-7 baked); §N4 + Gate-4 (touches `services/execution/` — literally in the trigger list) + `## Hand verification`.

## Sequence

T-527b2a consolidated plan → plan-reviewer Gate-1 → implement → 4 gates → merge. Then T-527b2b (now unblocked) gets its own consolidated plan + Gate-1 cycle (OQ-6b/OQ-7 baked from this record). Mirrors T-527b1 → T-527b2 / T-527a → T-527b.
