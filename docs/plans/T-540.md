# T-540 — F5 operator-runbook + BRIEF §12.2 accuracy fix (D1–D11)

**Status:** plan-reviewer Gate-1 APPROVE (2026-05-17). Promotes the TASKS.md:353 `T-F5+` backlog entry → numbered T-540. Operator approved 4 defaults via "use defaults": OQ-1=A numbered T-540 / OQ-2=A ship `configs/bots/smoke.yaml` / OQ-3=A D9 carved out / OQ-4 no §0.3 split.

## Purpose

Make the F5 operator-facing runbooks + BRIEF §12.2 CLI + deployment prereqs actually runnable, per the D1–D11 drifts empirically found during the 2026-05-17 T-522 close-out RUN (`b16d41a` / docs/status.md / L-028: documented operator commands are unverified claims until run). Doc/ops-accuracy + a runnable smoke fixture; NOT a capital-path change. Every corrected command is the verified-working form executed that session — cite the RUN per fix, never re-guess (L-028/L-018).

## Scope (IN)

1. **NEW `configs/bots/smoke.yaml`** — dedicated runnable smoke bot config: minimal schema-valid BotConfig with `scoring.mode: passthrough` + `rules: []` (deterministic `decision="passthrough"` with ZERO feature resolution — evaluator.py:155-156; sidesteps the F4+-unimplemented `oi_change` blocker that makes shipped alpha.yaml never accept), a `shadow:` block (`enabled: true`, ≥1 `ShadowVariant`, `max_duration_hours: 4`) [resolves D7], `trading.universe: [BTCUSDT, ETHUSDT]` (canonical — yaml_loader.py:531 maps universe→symbols; **NOT `.P`** here), `execution.*` fields mirrored from alpha/beta so the D5 `--override 'execution.sl_pct=…'` example targets an existing field. Config artifact (§N9-domain, like alpha/beta.yaml — not src).
2. **Fix the 4 runbooks** to verified-runnable forms:
   - `docs/runbooks/F5_E1_backtest_smoke.md`: D1 (alembic `-c migrations/alembic.ini` + `POSTGRES_URL`), D2 (`uv run python -m scripts.backtest`), D3 (`--config-path configs/bots/smoke.yaml`), D4 (host `--db-url postgresql://…@127.0.0.1:5432/…`), D5 (Step-2 `--override` → real `execution.*` path on smoke.yaml).
   - `docs/runbooks/F5_E2_shadow_smoke.md`: D1, D7 (use `configs/bots/smoke.yaml`), D8 (`$SIGNAL_GATEWAY_HMAC_SECRET`), D10 (document per-bot `BOT_<ID>_PAPER_SEED_BALANCE/SLIPPAGE_MODEL/FEE_RATE/SLIPPAGE_PARAMS_JSON` required for EVERY `bots` row), D11 (signal payload `BTCUSDT.P` — the TradingView-side input alias; canonical is `BTCUSDT`).
   - `docs/runbooks/F4_E1_dashboard_smoke.md`: D1.
   - `docs/runbooks/F5_close_out.md`: D1 (pre-flight), D2/D3/D4 (E1/E2 verify), D6 (E3 "Verify": the 2 named tests are CI-full/testcontainer-gated — need `NATS_TEST_URL`+`POSTGRES_TEST_DSN`; ad-hoc vs shared dev NATS is NOT a valid harness; sanctioned E3 verification = CI-full green OR the F5_E2 smoke with smoke.yaml), D7/D8/D11.
3. **Fix `docs/CLAUDE_CODE_BRIEF.md` §12.2** CLI lines (:1976 run-mode + :1995 `--compare`): D2 (`python -m scripts.backtest`) + D3 (`--config-path`).
4. **Record the deliberate boundary (L-026 — enumerate ALL sites + what is intentionally NOT touched + why):** D9 carved out to a separate `fix(...)`; `configs/bots/alpha.yaml` NOT modified (real bot; the `oi_change` scoring dependency = separate F4+ "built-in oi_change feature" backlog item); `docs/runbooks/F3_E1_dvoj_bot_smoke.md:25/27/133` (closed F3) + `docs/runbooks/F2_E1_testnet_smoke.md` (closed F2, uses `$WEBHOOK_HMAC_SECRET`) NOT edited despite carrying the same D8/D11 patterns (closed-phase operator docs, out of F5-corrective scope — flagged for operator).

## Scope (OUT)

D9 fix (separate `fix(...)`); `alpha.yaml`; F3_E1/F2_E1; implementing `oi_change`; ANY code/runtime change. Not absorbed into T-522 (§0.8 — T-522 shipped `ed54563`/`ef3cec2`).

## Files touched

NEW `configs/bots/smoke.yaml`; `docs/runbooks/F5_E1_backtest_smoke.md` / `F5_E2_shadow_smoke.md` / `F4_E1_dashboard_smoke.md` / `F5_close_out.md`; `docs/CLAUDE_CODE_BRIEF.md` (§12.2 :1976 + :1995 only). At chore-close: TASKS.md (promote :353 `T-F5+`→numbered T-540 `[x]`), status.md; review-lessons only if a NEW generalizable lesson.

## §0.3 LOC

runbooks + BRIEF = markdown (exempt). `smoke.yaml` = YAML config (§N9-domain, mirrors alpha/beta.yaml — not src). **0 src.** No split (OQ-4 confirmed by plan-reviewer).

## Hazards (§20)

None — no runtime/code, no capital path. N9: smoke.yaml IS config (compliant). N1: runbook timestamps already ISO-8601. N10: 0 code → no §20 regression.

## Test strategy

N/A code-wise (0 src). The fixes ARE the verified-working forms executed during the T-522 close-out RUN (`b16d41a`); cite the RUN per fix, no guessed commands. smoke.yaml must load schema-valid via the BotConfig+ShadowConfig pydantic loader; re-run the corrected F5_E1 backtest command with smoke.yaml to re-confirm runnable (closes L-028 on the fix itself).

## Open questions

None — OQ-1/2/3/4 resolved by operator "use defaults". Plan consolidated.

## Write-time guidance (verbatim from plan-reviewer Gate-1 APPROVE — brief-reviewer verifies each against staged diff before SHIP)

1. **(L-028/L-018 — re-verify, don't re-guess)** Every corrected command must be the form empirically executed during the T-522 close-out RUN; cite `docs/status.md` / commit `b16d41a` per fix. Before commit, actually re-run the corrected F5_E1 backtest command with `smoke.yaml` and load `smoke.yaml` through the yaml_loader — re-confirm runnable, not re-assert.
2. **(L-026 — full-set grep + boundary record)** Grep ALL source-of-truth sites per drift pattern across the 4 F5 runbooks + brief §12.2 (every bare `alembic upgrade head`, every `python scripts/backtest.py`, every `$HMAC_SECRET`, every `sizing.*`-override, every signal-payload `BTCUSDT` lacking `.P`) and fix the complete set in ONE commit. Record the deliberate NOT-touched boundary (D9, `alpha.yaml`, `F3_E1`, `F2_E1`) in the task + a runbook note. brief-reviewer will grep for residual duplicates before SHIP — a single residual = FIX FIRST.
3. **(smoke.yaml schema + semantics)** smoke.yaml must load schema-valid through the BotConfig + ShadowConfig pydantic loader. Use `trading.universe: [BTCUSDT, ETHUSDT]` (canonical — `yaml_loader.py:531` maps it to `symbols`; do NOT use `.P` here). `scoring.mode: passthrough` with `rules: []` (or a trivial always-noop rule) — verify the loaded config produces `decision="passthrough"`. Mirror `execution.*` fields from alpha/beta so the D5 `--override 'execution.sl_pct=...'` example targets a field that exists in the YAML.
4. **(L-027/§N1 — no false sign-off)** Do NOT fill F5_close_out §A/§B, do NOT bump the F5 counter (stays 64/66), do NOT mark F5 COMPLETE. T-540 closes as deliverable-shipped only; the F5 phase sign-off remains the operator's separate act.
5. **(D6 — no overstatement)** The F5_close_out D6 fix must state the sanctioned E3 verification (CI-full green OR the F5_E2 smoke with smoke.yaml) and explicitly that the 2 named E3 tests are CI-full/testcontainer-gated (need `NATS_TEST_URL`+`POSTGRES_TEST_DSN`; ad-hoc pytest vs shared dev NATS is NOT a valid harness). Do not present them as ad-hoc-runnable.

## Relevant paths

- `packages/scoring/evaluator.py` (passthrough :155-156), `packages/scoring/types.py` (ShadowConfig/ShadowVariant :239-300, BotConfig.symbols :456+, `_EXECUTION_OVERRIDE_FIELDS` :224), `packages/scoring/yaml_loader.py:531` (universe→symbols), `services/strategy_engine/app/consumer.py:349` (passthrough→order), `services/execution/app/pool.py:262-267` (D10), `packages/core/types.py:136` (D11 `.P` alias)
- `configs/bots/alpha.yaml` + `beta.yaml` (smoke.yaml schema reference; NOT modified)
- `docs/runbooks/{F5_E1_backtest_smoke,F5_E2_shadow_smoke,F4_E1_dashboard_smoke,F5_close_out}.md`
- `docs/CLAUDE_CODE_BRIEF.md` §12.2 :1976 + :1995
- Evidence source: `docs/status.md` "T-522 F5 close-out RUN" section (`b16d41a`) + L-028
