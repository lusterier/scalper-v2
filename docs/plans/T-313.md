# T-313: F3 exit-criteria integration bundle (E1 + E2 + E3 + E4)

**Status:** DRAFT (pre-Gate-1)
**Phase:** F3 (sixteenth and FINAL numbered work task; closes F3 phase exit-criteria gate)
**Predecessors:** T-300..T-312 (entire F3 catalog including T-308b + T-311 + T-312)
**Blocked by:** none (all F3 deps shipped)
**Blocks:** F4 phase unlock (operator-driven `chore(F3-close)` after T-313 ships + manual operator E1 dvoj-bot run)
**Estimate:** ~250 LOC tests + ~120 LOC runbook + ~80 LOC bot YAML fixtures

Mirror T-222 (F2 exit-criteria) precedent: pure verification + runbook + fixtures task; no new business logic; exercises existing T-300..T-312 surfaces.

---

## Purpose

T-313 satisfies the four F3 phase exit criteria from BRIEF §19:2546-2550:

- **E1**: Two bots with different scoring configs coexist and react differently to the same signal. (CI test + matching alpha.yaml/beta.yaml fixtures)
- **E2**: `scoring_evaluations` shows per-rule audit for every signal. (CI test verifying T-310b consumer's scoring_evaluations write across decisions)
- **E3**: Passthrough mode verified: every signal executes while `scoring_evaluations` shows decision='passthrough'. (CI test)
- **E4**: Plugin rule example (`oi_squeeze`) runs and contributes to score. (CI test using T-312 plugin)

T-313 is verification-and-runbook only:
- E1-E4 are pytest tests exercising existing T-310b consumer + T-301 audit + T-307 evaluator + T-312 plugin
- Bot YAML fixtures land in `configs/bots/alpha.yaml` + `beta.yaml` per BRIEF §B.1 + T-311 operator-setup-gate
- Plus optional manual operator runbook for end-to-end smoke (similar to T-222 E1 testnet runbook but paper-mode and CI-runnable as fallback)

This task does NOT add new business logic. All four E-criteria exercise existing surfaces.

## Scope

### In scope (T-313)

1. **`configs/bots/alpha.yaml`** (~40 LOC): per BRIEF §B.1:2911-2999 verbatim shape adapted for E1 dvoj-bot scenario. **BLOCKER#2/#3 fix — concrete rule shape pinned**:
   - `bot_id: alpha` (matches compose `BOT_ID=alpha` env per T-311 operator setup gate; filename matches `alpha.yaml`)
   - `exchange.mode: paper` + `account: sub_alpha` + `api_key_env: BOT_ALPHA_BYBIT_API_KEY` + `api_secret_env: BOT_ALPHA_BYBIT_API_SECRET`
   - `signals.ttl_seconds: 120` (§B.1 default)
   - `trading.universe: [BTCUSDT, ETHUSDT]`
   - **`execution.*` Decimal fields verbatim from §B.1:2934-2944**: `qty: 0.001` (T-310a OQ-4 deviation; §B.1 doesn't have qty), `leverage: 20`, `sl_pct: 0.01`, `tp_pct: 0.01`, `tp_qty_pct: 0.5`, `be_trigger: 0.005`, `be_sl_level: 0.003`, `trail_pct: 0.005`, `fee_rate: 0.00055`
   - `scoring.mode: active` + `trigger_threshold: 1.0` + **1 rule (concrete pinned shape)**:
     - `name: always_in_universe`
     - `weight: 2.0`
     - `feature: ind.${signal.symbol}.15m.oi_change`
     - `condition: { type: gt, feature: ind.${signal.symbol}.15m.oi_change, value: -999999.0 }` (always-True for any positive OI value via mock-resolver `value_num=Decimal("100")`)
   - **Test mock contract**: FeatureResolver mock returns `ResolverResult(value=FeatureValue(value_num=Decimal("100")), status="ok")` for `ind.btcusdt.15m.oi_change` → alpha rule fires → `applied_weight=2.0` → `total_score=2.0` >= threshold 1.0 → `decision="execute"`.

2. **`configs/bots/beta.yaml`** (~40 LOC): different scoring config so beta produces different decision. **BLOCKER#2/#3 fix — concrete rule shape pinned**:
   - `bot_id: beta` (matches compose `BOT_ID=beta`; filename `beta.yaml`)
   - `exchange.mode: paper` + `account: sub_beta` + `api_key_env: BOT_BETA_BYBIT_API_KEY` + `api_secret_env: BOT_BETA_BYBIT_API_SECRET`
   - `signals.ttl_seconds: 120`
   - `trading.universe: [BTCUSDT, ETHUSDT]` (same as alpha; E1 fixture both bots see same signals)
   - **`execution.*` verbatim same Decimal fields as alpha** (only bot_id + scoring differ)
   - `scoring.mode: passthrough` + `trigger_threshold: 1.0` + **1 plugin rule (concrete pinned shape per §B.1:2985-2991 verbatim)**:
     - `name: oi_squeeze_v2`
     - `weight: 1.0`
     - `feature: ind.${signal.symbol}.15m.oi_change` (rule-level per T-308 yaml_loader Path A operator-approved rejection of plugin-without-feature)
     - `condition: { type: plugin, name: oi_squeeze, version: 2, params: {lookback_candles: 5, oi_drop_pct: 1.0} }`
   - **Test mock contract**: same FeatureResolver mock as alpha; OISqueezeRule.evaluate returns `RuleOutcome(result=False, metadata={"reason":"history_insufficient"})` because T-306 resolver doesn't populate `feature_history` (T-312 documented limitation) → `applied_weight=0.0`. `scoring.mode == "passthrough"` overrides decision regardless of score → `decision="passthrough"`.

3. **`packages/scoring/tests/test_f3_e1_two_bots_diverge.py`** (~80 LOC, 1 test): **E1** verbatim per §19:2547. Loads alpha.yaml + beta.yaml via `load_bot_config`, builds `SignalValidated` for BTCUSDT, runs `evaluate(...)` against each bot. Asserts:
   - alpha decision == "execute" (active mode, score above threshold)
   - beta decision == "passthrough" (passthrough mode regardless of score)
   - alpha and beta receive THE SAME signal but produce DIFFERENT ScoringResult.decision values

4. **`packages/scoring/tests/test_f3_e2_audit_per_rule.py`** (~70 LOC, 2 tests): **E2** per §19:2548. **CONCERN#1 fix — pinning what E2 adds beyond T-310b's 14 consumer tests**: T-310b's `test_scoring_evaluations_write_runs_before_publish` pins ORDERING (`[db_insert, bus_publish]` call_log) only; T-313 E2 pins **per-rule audit content** (rule_results length matches `len(bot.scoring.rules)`, feature_snapshot non-empty). This is shape verification, not ordering — distinct granularity.
   - Test #1 `test_scoring_evaluations_records_one_rule_result_entry_per_scoring_rule` — load alpha.yaml (1 rule); evaluate via T-310b consumer flow with mocked `insert_scoring_evaluation`; assert `mock.call_args.kwargs["rule_results"]` is list of length 1 (matching `len(alpha.scoring.rules)`); load beta.yaml (1 plugin rule) → length 1
   - Test #2 `test_scoring_evaluations_feature_snapshot_non_empty_after_resolver_call` — same flow; assert `mock.call_args.kwargs["feature_snapshot"]` is non-empty dict (resolver populated at least one feature_ref → value entry)

5. **`packages/scoring/tests/test_f3_e3_passthrough_mode.py`** (~50 LOC, 1 test): **E3** per §19:2549. **CONCERN#3 fix — pinning what E3 adds beyond T-307's 32 unit tests**: T-307 `test_evaluator.py` already has 3 passthrough-mode unit pins (per T-307 done line). T-313 E3 net-new value is **E2E `evaluate()` on full beta.yaml BotConfig** + scoring_evaluations decision pin (verifies passthrough decision flows from BotConfig YAML → load_bot_config → evaluate → insert_scoring_evaluation call args).
   - Test #1 `test_passthrough_mode_records_decision_passthrough_in_audit_row` — load beta.yaml (mode=passthrough); evaluate via T-310b consumer flow with mocked `insert_scoring_evaluation`; assert `mock.call_args.kwargs["decision"] == "passthrough"` AND `result.rule_results` non-empty (full per-rule audit recorded even though decision is unconditional). Net-new vs T-307 unit pins: end-to-end YAML → audit row decision string pin.

6. **`packages/scoring/tests/test_f3_e4_plugin_runs.py`** (~80 LOC, 2 tests): **E4** per §19:2550. **Split per BLOCKER#1 fix** — T-307 `evaluate(...)` orchestrator builds internal `RuleContext` at evaluator.py:234-238 with `feature_history` defaulting empty (T-306 resolver doesn't populate today — F4+ deferred). OISqueezeRule.evaluate on empty history returns `RuleOutcome(result=False, metadata={"reason":"history_insufficient"})` → `applied_weight=0.0` → plugin does NOT contribute to score in v1. §19:2550 "runs and contributes to score" requires both pieces; T-313 splits:
   - **Test (a)** `test_oi_squeeze_runs_in_evaluator_pipeline` — full E2E via `evaluate(...)` orchestrator: load `configs/plugin_registry.yaml` real-repo path → registry contains `("oi_squeeze","2"): OISqueezeRule`; build BotConfig with single plugin rule per §B.1:2985-2991 verbatim shape (`condition: { type: plugin, name: oi_squeeze, version: 2, params: {lookback_candles: 5, oi_drop_pct: 1.0} }` + rule-level `feature: ind.${signal.symbol}.15m.oi_change`); mock FeatureResolver to return `ResolverResult(value=FeatureValue(value_num=Decimal("100")), status="ok")`; run `evaluate(...)`. Asserts: `result.rule_results` has one entry with `name="oi_squeeze_v2"`, `applied_weight == 0.0`, `result == "False"`, `error` dict containing `"reason": "history_insufficient"` (T-306 limitation explicitly acknowledged per §0.8 anti-hypothetical — plugin runs through pipeline but does NOT contribute to score in v1).
   - **Test (b)** `test_oi_squeeze_contributes_to_score_when_history_sufficient` — bypasses evaluator orchestrator; **Path A per pass-2 BLOCKER fix** — invokes `OISqueezeRule` directly (not `PluginCondition` adapter), mirror T-312 test pattern at `test_plugin_oi_squeeze.py:46-52`. `condition` field on a plugin-type ScoringRule is a `PluginCondition` instance whose `.evaluate(ctx)` returns `tuple[bool, dict | None]` (Condition Protocol contract per `packages/scoring/conditions/plugin.py:43`); to assert `RuleOutcome.result`/`.metadata` directly, access the wrapped `Rule` via `bot.scoring.rules[0].condition.rule.evaluate(populated_ctx)` — that returns `RuleOutcome`. Build `populated_ctx = RuleContext(signal=..., feature_snapshot={}, feature_ref="ind.btcusdt.15m.oi_change", feature_history={feature_ref: [FeatureValue(value_num=Decimal("100")), FeatureValue(value_num=Decimal("99.5")), FeatureValue(value_num=Decimal("99")), FeatureValue(value_num=Decimal("98.5")), FeatureValue(value_num=Decimal("98"))]})`. Asserts: `outcome.result is True` + `outcome.metadata["drop_pct"] >= Decimal("1.0")` + `outcome.metadata["oldest_oi"] == Decimal("100")` + `outcome.metadata["newest_oi"] == Decimal("98")`. This pins "contributes to score" semantics at the underlying Rule layer; full pipeline integration (orchestrator-level "contributes") is deferred to F4+ (T-306 resolver upgrade). Mirror T-312 test #7 (`test_evaluate_returns_true_on_drop_above_threshold`) Fixture A pattern verbatim.
   - **§19:2550 "contributes to score" deferral note**: T-313 documents that full E2E "plugin contribution" (orchestrator-driven applied_weight > 0) is achievable only after T-306 populates `feature_history`; today's verification splits "runs" (orchestrator) vs "contributes" (condition layer) per §0.8 anti-hypothetical. Operator may extend with F4+ E4-redux task once T-306 lands.

7. **`docs/runbooks/F3_E1_dvoj_bot_smoke.md`** (NEW; ~120 LOC, **REQUIRED deliverable** per CONCERN#4 fix — file ships in T-313; manual operator run is post-task optional, mirror T-222 F2_E1 runbook precedent): operator-runnable manual smoke checklist (mirror T-222 `F2_E1_testnet_smoke.md` pattern):
   - Prereq: `docker compose up -d strategy-engine-alpha strategy-engine-beta nats postgres ...` + alpha.yaml/beta.yaml fixtures present in `configs/bots/`
   - Step 1: POST same signal via signal-gateway webhook
   - Step 2: observe alpha emits `OrderRequest` (decision==execute) on `orders.requests.alpha`
   - Step 3: observe beta emits SignalRejected on `signals.rejected.beta` (or OrderRequest on .beta if passthrough → execute)
   - Step 4: query `scoring_evaluations` table → 2 rows, one per bot, with different `decision` values + per-rule audit JSONB
   - Step 5: verify oi_squeeze plugin entry in plugin_registry.yaml resolved during lifespan startup
   - Sign-off: record run timestamp + correlation_ids in `chore(F3-close)` commit

   **Note**: T-313 ships paper-mode E1 because T-311 multi-bot compose is wired but live testnet doesn't add value beyond F2's T-222 testnet smoke (which already verified Bybit testnet end-to-end for execution-service). F3 E1 is paper-mode 2-bot divergence verification.

### Out of scope (deferred)

- **F2 E5 hazard catalog orchestrator test** — T-F2+ backlog item (separate task)
- **F4 SSE / analytics-api integration tests** — F4 phase scope
- **Real-Bybit testnet 2-bot smoke** — operator may follow up if needed; F3 paper-mode E1 + F2 testnet E1 (T-222) jointly cover the surface
- **Property tests** for evaluator (BRIEF §10.7:1827-1828) — already covered by T-307 evaluator's 32 test pins; T-313 doesn't duplicate
- **`chore(F3-close)` commit** — operator-driven after T-313 lands + E1 manual run optional
- **CI workflow modification** for F3 phase gate — none needed; existing CI runs pytest which discovers all 4 E-tests via `packages/scoring/tests/`
- **E1 testcontainer-backed integration with full NATS + PG** — too heavy for F3 verification; E2 mock-based audit verification is sufficient (similar to T-222 E2 + E3 mock-based parity)

## Public interface

T-313 has no Python public interface — only:

1. 2 YAML fixture files at `configs/bots/alpha.yaml` + `configs/bots/beta.yaml`
2. 4 pytest test files at `packages/scoring/tests/test_f3_e[1234]_*.py`
3. 1 markdown runbook at `docs/runbooks/F3_E1_dvoj_bot_smoke.md`

Existing surfaces consumed (all shipped):
- `packages.scoring.load_bot_config` (T-308) — loads alpha.yaml/beta.yaml
- `packages.scoring.evaluate` (T-307) — runs scoring per-bot
- `packages.scoring.registry.load_plugin_registry` (T-305) — resolves oi_squeeze
- `packages.scoring.FeatureResolver` (T-306) — mocked in tests for deterministic feature values
- `packages.db.queries.scoring.insert_scoring_evaluation` (T-301) — mocked at consumer-call boundary

## Dependencies

No new external deps. All T-313 surfaces are pytest tests + YAML fixtures + markdown.

## Hazards relevant from §20

- **H-008** signal TTL — T-310b owns; T-313 fixtures use `expires_at = now + 60s` valid window
- **H-019** score fail-open — T-307 owns; T-313 doesn't introduce new fail-open paths
- **H-005** opposite-signal guard — N/A in T-313 (no scoring catalog extension)
- **H-022** per-bot creds — N/A (T-313 uses paper-mode bots; no Bybit API access)

No new hazards introduced.

## Test strategy

### Test file structure (4 NEW pytest files)

- **NEW** `packages/scoring/tests/test_f3_e1_two_bots_diverge.py` (~80 LOC, 1 test) — E1
- **NEW** `packages/scoring/tests/test_f3_e2_audit_per_rule.py` (~70 LOC, 2 tests) — E2
- **NEW** `packages/scoring/tests/test_f3_e3_passthrough_mode.py` (~50 LOC, 1 test) — E3 (CONCERN#3 fix: dropped duplicate test #1 from pass-1)
- **NEW** `packages/scoring/tests/test_f3_e4_plugin_runs.py` (~80 LOC, 2 tests) — E4 (BLOCKER#1 fix: split into orchestrator test + condition-layer test)

### Test count target

**6 new tests** covering 4 E-criteria (E1: 1, E2: 2, E3: 1, E4: 2 per BLOCKER#1 split). Mock-free where possible (load real YAML, build RuleContext directly); FeatureResolver mocked for deterministic values; T-310b consumer's `insert_scoring_evaluation` mocked at the boundary for E2 audit-row pin.

Repo-wide baseline 1434 → expected ~1440 passing.

### Test approach

- **E1**: end-to-end `evaluate()` on alpha + beta loaded from real YAML; assert different decisions for same signal
- **E2**: build minimal T-310b consumer-equivalent flow (call sequence: evaluate → insert_scoring_evaluation); assert call args via mock
- **E3**: BotConfig with `mode: passthrough` + scoring rules → verify decision regardless of score
- **E4**: load real `configs/plugin_registry.yaml` → build BotConfig with plugin condition → populate `feature_history` to trigger plugin's True branch → verify total_score reflects contribution

**Async + DI plumbing**: `evaluate(...)` is `async def` per `packages/scoring/evaluator.py:90` and requires `bound_logger: BoundLogger` kwarg. E1/E2/E3 tests using `evaluate(...)` MUST be `async def` with `@pytest.mark.asyncio` (or rely on existing `asyncio_mode = "auto"` in `pyproject.toml:124`). FeatureResolver is mocked via `MagicMock` with `.resolve = AsyncMock(return_value=ResolverResult(value=FeatureValue(value_num=Decimal("100")), status="ok"))` mirror T-307 `test_evaluator.py:_ok_resolver` pattern. `bound_logger` mocked via `MagicMock()` (no real structlog binding needed in tests). E2 + E3 audit-row pin via `monkeypatch.setattr` on `services.strategy_engine.app.consumer.insert_scoring_evaluation` (mirror T-310b `test_consumer.py` consumer-mock pattern at lines 130-183).

## Edge cases

- **alpha.yaml + beta.yaml load failures** — fixtures must round-trip cleanly through T-308 yaml_loader; CI catches at test discovery time
- **Different scoring outcomes from same signal** — E1 fixture must guarantee divergence (alpha active mode + beta passthrough mode different code paths)
- **Plugin without `feature:`** — T-308 yaml_loader rejects plugin condition without rule-level feature per Path A; beta.yaml fixture MUST set `feature: ind.${signal.symbol}.<interval>.oi_change` at rule level
- **`feature_history` empty in resolver** — T-306 doesn't populate today; E4 test must build RuleContext with `feature_history` directly populated to trigger plugin True branch (mock-side bypass T-306)
- **E1 fixture coupling to specific Decimal values** — keep YAML values from BRIEF §B.1 verbatim to avoid drift

## §N invariants

- §N1 N/A (no datetime arithmetic in fixtures or test bodies beyond now-snapshots)
- §N2 N/A (no logging code)
- §N3 N/A (no external write decorators; tests mock at boundary)
- §N6 no globals (test fixtures file-private)
- §N7 hexagonal (tests stay above scoring layer; mock at edges)
- §N9 N/A (no new business literals; YAML fixtures are operator-style examples)

## §0.3 LOC budget

| Component | LOC |
|---|---|
| `configs/bots/alpha.yaml` | ~40 |
| `configs/bots/beta.yaml` | ~40 |
| `test_f3_e1_two_bots_diverge.py` | ~80 |
| `test_f3_e2_audit_per_rule.py` | ~70 |
| `test_f3_e3_passthrough_mode.py` | ~50 |
| `test_f3_e4_plugin_runs.py` | ~50 |
| `docs/runbooks/F3_E1_dvoj_bot_smoke.md` | ~120 |
| **Src diff (excl tests + runbook)** | **~80 LOC** (YAML fixtures only) |
| Tests | ~250 (excluded from cap per §0.3) |

Tests + runbook excluded; src ~80 LOC = 20% of cap.

## Hand verification

T-313 is verification-only — no math, no Decimal arithmetic. Math-validator OUT OF SCOPE per CLAUDE.md Gate 4 (`packages/scoring/tests/` not in conditional list; YAML fixtures + runbook not in scope).

E1 + E2 + E3 + E4 each verifiable as boolean assertions:
- E1: `alpha_result.decision != beta_result.decision` (one specific value differs)
- E2: `mock.call_args.kwargs["rule_results"]` is non-empty list AND `len == len(bot.scoring.rules)`
- E3: `result.decision == "passthrough"` regardless of `result.total_score < threshold`
- E4: `result.total_score >= rule.weight` (plugin contributed; weight added to total)

No Decimal arithmetic in T-313 code path. E4 test (b) verifies OISqueezeRule's existing Decimal computation at the condition layer (already hand-verified in T-312 plan §"Hand verification" Fixtures A-F); T-313 fixture for test (b) uses Fixture A `[100, 99.5, 99, 98.5, 98]` with `oi_drop_pct=1.0` → drop=2.0 ≥ 1.0 → True.

E4 test (a) post-fix per BLOCKER#1: assert that after running `evaluate(...)` on full BotConfig with plugin rule, `result.rule_results[0]` has:
- `name == "oi_squeeze_v2"`
- `applied_weight == 0.0` (plugin returned False because feature_history empty per T-306 limitation)
- `result == "False"` (T-307 evaluator translates RuleOutcome.result=False to RuleResult.result="False" string per evaluator.py)
- `error` dict with `"reason": "history_insufficient"` (carried through metadata)

E1 / E2 / E3 each verifiable as boolean assertions:
- E1: `alpha_result.decision == "execute"` AND `beta_result.decision == "passthrough"` (decision strings differ; same input signal)
- E2 #1: `mock.call_args.kwargs["rule_results"]` length matches `len(bot.scoring.rules)` (1 for both alpha + beta in T-313 fixtures)
- E2 #2: `mock.call_args.kwargs["feature_snapshot"]` is non-empty dict
- E3: `mock.call_args.kwargs["decision"] == "passthrough"` for beta.yaml (mode=passthrough)

## Open questions

**OQ-1 (resolved by reading BRIEF):** Should E1 use Bybit testnet (mirror F2 T-222) or paper-mode?

**Decision:** paper-mode. F3's exit criterion is "two bots with different scoring configs coexist and react differently to the same signal" — this is a scoring-layer property, not an exchange-layer property. Paper-mode is sufficient + CI-runnable. F2 T-222 already covered Bybit testnet end-to-end. Operator may extend with manual Bybit testnet 2-bot run if needed (out of scope per §0.8).

**OQ-2 (resolved by reading T-310b plan):** Should E2 audit verification go end-to-end with real Postgres testcontainer or mock at consumer-call boundary?

**Decision:** mock at boundary. Mirror T-222 E2 + E3 pattern (mock-based parity verification — testcontainer overhead not justified for shape-pinning tests). T-301 already has Postgres-testcontainer integration test for `insert_scoring_evaluation` directly; T-313 E2 verifies the *call site* in T-310b consumer body, not the SQL layer (T-301 owns).

**OQ-3 (resolved by reading T-312 plan):** Should E4 build a real BotConfig + load real plugin_registry.yaml or use mocks?

**Decision:** load real `configs/plugin_registry.yaml` (mirror T-312 test #14 integration pattern). RuleContext.feature_history directly populated to bypass T-306 resolver (which doesn't populate history yet — F4+ deferred). E4 verifies the integration end-to-end at the evaluator level, not the resolver level.

**No outstanding questions for operator.**

## Acceptance criteria

- [ ] `configs/bots/alpha.yaml` ships with `bot_id: alpha` + `mode: active` + 2+ rules (one always-true) + paper exchange mode
- [ ] `configs/bots/beta.yaml` ships with `bot_id: beta` + `mode: passthrough` + oi_squeeze plugin rule
- [ ] 4 NEW E-test files ship 6 net new tests (E1: 1, E2: 2, E3: 1, E4: 2 per BLOCKER#1 split)
- [ ] All 6 tests pass; existing 1434 baseline → expected ~1440 passing
- [ ] mypy --strict packages/scoring/ clean
- [ ] ruff check packages/scoring/ clean
- [ ] `docs/runbooks/F3_E1_dvoj_bot_smoke.md` ships with operator-runnable checklist + sign-off section
- [ ] §0.3 LOC ≤ 400 (~80 src actual; 20% of cap)
- [ ] No new external deps; no hazard regressions
- [ ] No T-313 leakage (no F4 phase scope, no CI workflow change)

## Out of scope (re-statement for review)

- Real-Bybit testnet 2-bot smoke → operator may follow up if needed; F3 paper-mode E1 sufficient
- F2 E5 hazard catalog orchestrator → T-F2+ backlog (separate)
- F4 phase scope (analytics-api / SSE / UI) → F4 phase
- Property tests for evaluator → already covered by T-307 32 pins
- `chore(F3-close)` commit → operator-driven post-T-313 ship
- CI workflow modification → existing CI auto-discovers tests at `packages/scoring/tests/`
- Testcontainer-backed end-to-end E1 with full NATS + PG → too heavy; mock-based shape verification sufficient

## Write-time guidance (plan-reviewer pass-3 APPROVE 2026-05-02)

1. **alpha.yaml + beta.yaml `execution.*` Decimal fields verbatim per §B.1:2934-2944** — `qty: 0.001` per T-310a OQ-4 deviation. Use **string-quoted YAML values** (`qty: "0.001"`, `sl_pct: "0.01"`, …) to avoid float→Decimal precision artefacts through PyYAML's float parse path. T-308 `_to_decimal` helper handles both string + float input via `Decimal(str(value))`, but string-form removes the precision risk entirely. Keep YAML values verbatim from §B.1 (sl/tp/be/trail/fee_rate); document `qty` as T-310a OQ-4 deviation in the YAML header comment.

2. **E2 test #1 + E3 test #1 reuse T-310b `make_signal_handler` factory** — mirror T-310b `_build_handler` pattern at `services/strategy_engine/tests/test_consumer.py:130-183` (mock pool/bus/loggers + `monkeypatch.setattr` on `services.strategy_engine.app.consumer.insert_scoring_evaluation`). Do NOT directly call `insert_scoring_evaluation` outside the consumer flow — that would yield mock-only tests not pinning T-310b integration. The consumer-flow integration is the verbatim §19:2548 + §19:2549 contract.

3. **E4 test (a) inline comment justifying `applied_weight == 0.0`** — pin via inline `# T-306 limitation: feature_history empty → OISqueezeRule returns False → applied_weight=0.0; T-313 §0.8 anti-hypothetical acknowledges this. F4+ T-306 upgrade may flip this.` Future readers must understand this is intentional acknowledgment of T-306 deferral, not accidental coupling.

4. **Runbook step 3 explicit: passthrough → OrderRequest (NOT SignalRejected)** — `docs/runbooks/F3_E1_dvoj_bot_smoke.md` step 3 must read "beta emits OrderRequest on `orders.requests.beta` (passthrough mode goes through execute path per `services/strategy_engine/app/consumer.py:205`)" — T-310b consumer dispatches `decision in ("execute", "passthrough")` to OrderRequest publish branch. Operator manual smoke expects 2× OrderRequest (alpha + beta), NOT 1× OrderRequest + 1× SignalRejected. Drop any "or" ambiguity from the runbook.
