# T-527b — RECURSIVE SPLIT RECORD (L-007)

**Status:** SPLIT 2026-05-16 (operator OQ-2=A at T-527b plan-stage; ADR-0013 §Decision pt 4; T-527/TASKS pre-flagged L-007/L-014 split-watch). Not an implementable plan — records the recursive split; the active leaf plan is `docs/plans/T-527b1.md`. T-527b2 plan written when T-527b1 merges.

## Why recursive split (L-007)

T-527b (the §B.1 sizing COMPUTE, blocked-by the shipped T-527a config foundation) spans **two distinct concerns** per ADR-0013: (a) a NEW pre-trade reference-price source — `ExchangeClient.get_mark_price` protocol method + Bybit `/v5/market/tickers` + paper stub (no pre-trade price method exists on the protocol today), and (b) the `packages/sizing` pure tier-ladder compute + the execution-service wiring (`SizingSection`→`OrderRequest` producer thread, balance fetch, mark-price fetch, skip-before-place, `placement.py` consume) + §N4 financial-math TDD + Gate-4. As one task this is a new protocol method + 2 adapter impls + a new pure package + cross-service wiring + extensive hand-verified TDD — systematically over the §0.3 400 cap (TASKS:T-527b + ADR-0013 pre-flagged L-007/L-014). Per L-007 active control, pre-emptive split at plan-stage is far cheaper than a mid-write cap breach (T-534→{a,b} / T-533→{a,b} / T-527→{a,b} precedent — adapter-protocol foundation leaf then consumer leaf).

## Operator OQ answers (T-527b plan-stage 2026-05-16, all recommended — baked into T-527b1 / T-527b2 + ADR-0013)

- **OQ-1=A — compute home = execution-service placement seam** (NOT strategy-engine gate). Both sizing inputs (account balance, pre-trade reference price) are adapter-only; the adapter lives only in execution-service. ADR-0013 records the full rationale + rejected alternatives. (Architectural/brief-gap decision → ADR-0013 drafted per CLAUDE.md Gate-1 NEEDS-DISCUSSION → plan-reviewer reviews the ADR alongside this split + the T-527b1 plan.)
- **OQ-2=A — 2-leaf recursive split** T-527b → **T-527b1** (`get_mark_price` adapter-protocol extension: `protocols.py` + Bybit `/v5/market/tickers` + paper stub; NO consumer — foundation, independently testable; T-530/T-534a-shaped) + **T-527b2** (`packages/sizing` pure compute + `SizingSection`→`OrderRequest` producer thread + balance/mark-price fetch + skip-before-place + `placement.py` consume; blocked-by T-527b1; §N4 financial-math TDD + Gate-4 math-validator + `## Hand verification`). Mirrors T-534→{a,b} EXACTLY (protocol-extension foundation → consumer).
- **OQ-3=A — score→multiplier mapping** (baked into T-527b2): `key = str(floor(score))` clamped into the `[min,max]` of the present `score_multipliers` keys; `score is None` or no usable key → multiplier ×1.0 (notional = `tier.size` unmultiplied). Deterministic; never blocks a scored signal on a sizing-config gap; pinned in T-527b2's `## Hand verification`.
- Inherited from T-527 plan-stage (all default A, ADR-0013): OQ-3=A `size`/`max_notional_per_symbol` = notional USD; OQ-4=A tier by `AccountBalance.total_equity`, sub-lowest-tier → skip the signal before `place_market_order` (structured log + Prom counter). OQ-2=A (T-527) `tier_promotion`/`tier_demotion` DEFERRED (NEW `T-F5+` backlog ticket — not T-527b).

## Split (mechanical — adapter-protocol foundation vs sizing compute consumer; mirror T-534→{a,b})

- **T-527b1** (active leaf — `docs/plans/T-527b1.md`): NEW `ExchangeClient.get_mark_price(symbol: str) -> Decimal` (`packages/exchange/protocols.py`, `@idempotent` read mirror `get_account_balance`/`get_fill_price`) + Bybit `bybit_v5/adapter.py` impl (`GET /v5/market/tickers?category=linear&symbol=…`, public/unauth, `Decimal(str(...))` no-float, limiter-guarded, error taxonomy = sibling read methods) + paper `paper/adapter.py` deterministic stub + `_UNLABELED_METHODS`/conformance-test update (L-015 mirror T-530). **NO caller** (T-527b2 first consumer — T-530/T-534a-shaped no-consumer leaf). Gate-4: **IN scope, expected fast `VERIFIED`** (COMMITTED by plan-reviewer Gate-1 2026-05-16 — `packages/exchange/` not literally in the CLAUDE.md trigger list, but the T-530 non-exhaustive-path precedent governs: real `Decimal(str(markPrice))` price decode feeding the T-527b2 capital path. Strictly smaller numeric surface than T-530 — single decode, ZERO arithmetic [notional÷price is T-527b2] → fast VERIFIED on the no-float pin alone, the T-534b1/T-531 in-scope-zero-arithmetic precedent, not the heavier T-530).
- **T-527b2** (blocked-by T-527b1): NEW `packages/sizing/` pure module (`select_tier` / `apply_score_multiplier` / `cap_notional` / `compute_qty_from_sizing` — §N4 TDD, hand-verified) + additive `OrderRequest.sizing: SizingSection | None = None` (mirror T-527a `score`) + strategy-engine producer maps `BotConfig.sizing` → `OrderRequest.sizing` + `placement.py` seam: `get_account_balance` + `get_mark_price` + tier compute + sub-lowest-tier skip-before-`place_market_order` (structured log + Prom counter on the T-531 execution-metrics scaffold) + consume computed qty into the existing `quantize_qty` pre-flight. §N4 financial-math TDD + Gate-4 math-validator (touches `services/execution/`) + `## Hand verification` (tier-select / floor+clamp score-mult / max_notional clamp / notional÷price worked examples). Own plan-reviewer Gate-1 cycle; L-007/L-014 split-watch re-confirm at its own plan-stage.

## Sequence

ADR-0013 + this split + the T-527b1 plan → plan-reviewer Gate-1 (reviews the ADR alongside the consolidated T-527b1 plan per CLAUDE.md NEEDS-DISCUSSION→ADR path) → on APPROVE: operator §6.7 ADR acceptance, write the plan + WG, implement T-527b1 → 4 gates → merge. Then T-527b2 (now unblocked) gets its own consolidated plan + Gate-1 cycle. Mirrors T-534a → T-534b / T-527a → T-527b.
