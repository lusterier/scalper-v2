# T-545 — wire `SignalsSection.source_filter` into the strategy-engine consumer

**Status:** plan-reviewer Gate-1 APPROVE (2026-05-17, pass 3/3). F6 numbered work task (4/4; unlocked per ADR-0015 + the 2026-05-17 decision-C scope-clarification follow-up; **Blocked-by T-541** ✅ DONE). Governance for admissibility recorded in commits `1ada2b6` (TASKS.md `### F6 numbered` register + ADR-0015 follow-up) + `80896e6` (operator ops.md/CLAUDE.md, standalone). No new ADR (ADR-0015 already unlocked F6). No L-029 violation. Batch-push mode active (commits accumulate local; one push at batch end).

## Purpose

`SignalsSection.source_filter: list[str] | None = None` (`packages/scoring/types.py:182`, since T-310a) is in the schema but **never read at runtime** — a configured per-bot source allowlist is silently ignored (dead field). Wire it into the consumer pre-scoring pipeline as a source-allowlist silent-skip filter, mirroring the existing 3b symbol filter EXACTLY. Signal-acceptance correctness fix (§15.2).

## Scope (IN)

- `services/strategy_engine/app/consumer.py` (~10 LOC src) — insert the source-filter check **structurally between the 3b `signal_outside_universe` `return` and the `if signal.action == "CLOSE":` line** (current actual: 3b return ~:151, CLOSE ~:153 — anchor structurally, NOT by absolute line number). Mirror the 3b block EXACTLY: `if bot_config.signals.source_filter is not None and signal.source not in bot_config.signals.source_filter:` → `trading_logger.info("signal_outside_source_filter", bot_id=..., idempotency_key=signal.idempotency_key, source=signal.source, source_filter=bot_config.signals.source_filter)` → `return`. `signal.source` = `SignalValidated.source: str` (`packages/bus/schemas/signals.py:60`) — same parsed object 3b reads `.symbol` from; NO `original_source` twin (unlike `original_symbol:64`) → unambiguous. Silent-skip (NO scoring_evaluations / orders.requests / signals.rejected — same class as `signal_outside_universe`). **NO Prometheus metric, NO `metrics.py` change** (verified `metrics.py:47-50` — silent-skip 3a/3b filters emit `trading_logger.info` only; counters exist only for the post-3b gate class).
- TESTS (§N4 TDD; `services/strategy_engine/tests/test_consumer.py`, mirror `test_signal_outside_universe_is_dropped:225` / region §9.4 step 3b): (1) `source_filter=None` → pass-through (proceeds, no filtering); (2) `source_filter=["x"]` + `source="y"` → silent-skip (assert NO order/scoring_evaluation/signals.rejected; `signal_outside_source_filter` logged; metrics.py untouched — no metric assertion, mirror sibling); (3) `source_filter=["x"]` + `source="x"` → proceeds to next stage; (4) **CLOSE-ordering pin**: `source_filter=["x"]` + `source="y"` + `action="CLOSE"` → silent-skipped with `signal_outside_source_filter` (NOT `consumer.close_action_unsupported_v1`). L-017 realistic fixtures (real `SignalValidated`/`bot_config`; pin called + not-called). Sibling regression: `test_decision_close_action_logged_and_skipped_in_v1:432` + `test_signal_outside_universe_is_dropped:225` stay green.
- `docs/runbooks/ops.md` — "Pridanie nového bota" new-bot YAML template (~:100-165, currently NO `signals:` block): add a documented `signals:` block with `source_filter` (commented-optional + one-line semantics: allowlist of accepted signal sources; null/absent = accept all; non-listed source → silently skipped). §0.3-exempt markdown. Mandatory per the CLAUDE.md "Kedy aktualizovať ops runbook" rule (config-behaviour change: dormant field → runtime-effective).

## Scope (OUT)

No schema change; no `metrics.py` change; no change to the 3b symbol filter / CLOSE block / cooldown gate / any other stage; no new config field; no other ops.md edit; not absorbed elsewhere.

## Files

`services/strategy_engine/app/consumer.py`; `services/strategy_engine/tests/test_consumer.py`; `docs/runbooks/ops.md`; + this `docs/plans/T-545.md`; at close TASKS.md (T-545 `[ ]`→`[x]`; F6 numerator = done-count/4) + status.md. **No metrics.py.**

## §0.3 LOC

~10 src ≪ 400; tests §0.3-excluded; ops.md markdown-exempt. 1 task, no split.

## Hazards (§20)

Signal-acceptance gate (§15.2) — trading-correctness. §N4 TDD. §N10: sibling consumer tests (symbol-filter / CLOSE / cooldown / caps / loss_limit / drawdown) stay green — leaf early-return, no existing path changed; the CLOSE-ordering test confirms `test_decision_close_action_logged_and_skipped_in_v1` unaffected. math-validator OUT-of-scope (`services/strategy_engine/` not in the Gate-4 trigger list; boolean membership, no math). N1 N/A.

## Test strategy (§N4 TDD)

Test-first; the 4 cases above; L-017 realism (real entering state, pin called + not-called); sibling regression incl. the CLOSE green-check.

## Open questions

None — operator spec clear; ops.md exists (target = its new-bot YAML template `signals:` block); `.source` unambiguous; insertion point pinned; metric decision committed (NONE); phase-gate/governance resolved (decision C; commits `1ada2b6`/`80896e6`).

## Write-time guidance (verbatim from plan-reviewer Gate-1 APPROVE pass-3 — brief-reviewer verifies each before SHIP)

1. Insert the source-filter block **structurally** between the 3b `signal_outside_universe` `return` and the `if signal.action == "CLOSE":` line — NOT by absolute line number (plan's ~:149/:151 are ~2 off; current actual: 3b return :151, CLOSE :153). Guard EXACTLY mirrors 3b: `if bot_config.signals.source_filter is not None and signal.source not in bot_config.signals.source_filter:` → `trading_logger.info("signal_outside_source_filter", bot_id=..., idempotency_key=signal.idempotency_key, source=signal.source, source_filter=bot_config.signals.source_filter)` → `return`. No metrics.py touch.
2. The CLOSE-ordering pin test (case 4: `source_filter=["x"]` + `source="y"` + `action="CLOSE"` → `signal_outside_source_filter`, NOT `consumer.close_action_unsupported_v1`) is mandatory — it is the only test that proves the insertion is before the CLOSE block, not after. Do not drop it.
3. Sibling regression check: `test_decision_close_action_logged_and_skipped_in_v1` (test_consumer.py:432) and `test_signal_outside_universe_is_dropped` (:225) must stay green; the new early-return is a leaf with the `is not None` guard so allowlisted/None sources pass through untouched — assert this explicitly, do not assume.
4. ops.md `signals:` block must be added to the "Pridanie nového bota" new-bot YAML template only (~:100-165); `source_filter` commented-optional with the one-line semantics (allowlist; null/absent = accept all; non-listed → silently skipped). Markdown, §0.3-exempt; do not absorb any other ops.md edit.

## Relevant paths

- `services/strategy_engine/app/consumer.py` (insertion: between :151 3b `return` and :153 CLOSE block)
- `services/strategy_engine/tests/test_consumer.py` (sibling :225; CLOSE-regression :432)
- `packages/bus/schemas/signals.py:60` (`SignalValidated.source`); `packages/scoring/types.py:182` (`SignalsSection.source_filter`)
- `services/strategy_engine/app/metrics.py:47-50` (verified: no silent-skip counter — no metric)
- `docs/runbooks/ops.md` (new-bot YAML template — target of the `signals:` block); `docs/adr/0015-...md:70-81` (Follow-up — decision C); `TASKS.md:182-190` (F6 numbered + T-545 entry)
