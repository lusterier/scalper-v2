# T-200 — Populate F2 backlog

Status: **APPROVED by `plan-reviewer` 2026-04-26** (single-pass APPROVE with 5 Write-time guidance items, all addressed inline below).
Phase: F2 (unlocked 2026-04-26 per master HEAD `cf4b465`).
Branch: n/a — branchless on master per Q12 / T-019 F1-backlog-populate precedent.
Mirror precedent: T-019 (Populate F1 backlog, 2026-04-25).

## Goal

Single deliverable: **`TASKS.md` restructure adding `### F2 numbered` and `### F2+ opportunistic` subheadings under `## Backlog`, populated with F2 task entries.**

No code, no tests, no migrations. Pure metadata. Drift-checker skipped (no implementation surface). Math-validator skipped (no financial math touched).

After this task ships, T-201 becomes the first numbered F2 work and F2 implementation begins per standard four-gate flow.

## Spec references

- §0.10 phase-gate discipline (F2 unlocked, F3+ tasks ineligible).
- §9.5 execution-service — order placement pipeline + lifecycle FSM (BE/trail/partial-TP) + cumulative-delta reconciliation + P&L audit loop + WS handling + dedup.
- §11 ExchangeClient protocol surface + BybitV5Adapter + shared rate limiter + error taxonomy + adapter contract test.
- §12.1 PaperExchange — fill semantics + fee semantics + execution emission + paper_* persistence. **§12.2 backtest harness explicitly NOT in F2 scope** (brief §19 places it under F5 — Shadow Variants, Backtest Harness, Finishing).
- §19 F2 exit criteria — 5 bullets (E1 testnet flow / E2 paper parity / E3 cumulative-delta concurrent / E4 P&L audit corrects injected drift / E5 all hazard-catalog tests pass).
- §20 hazards H-001..H-026 — F2 deliverable per §19 line 2522 *"Full hazard catalog addressed (§20)"*. 18 hazards classified F2-binding below.
- §7.2 DDL — `orders`, `trades`, `executions`, `position_state`, `trading_events`, `paper_*` tables + `trade_pnl_deltas` (implied by §9.5 audit loop, schema not in §7.2 — open question Q6).
- §8.1 NATS subjects `orders.requests.<bot_id>`, `orders.events.<bot_id>`, `trading.events`. §8.4 `OrderRequest`/`OrderPlaced`/`OrderFilled`/`OrderClosed`/`SLMoved` payload schemas.
- §B (configs/bots/<id>.yaml) — read at adapter-pool composition for exchange_mode + per-bot fields.

## Survey

### F2-binding hazard classification (§20 H-001..H-026)

Every hazard classified as **F2-binding** (must be addressed within F2 scope) / **cross-phase** (touches F2 surface but full address spans phases) / **non-F2** (deferred / already addressed):

| ID | Title | Class | F2 owner task |
|----|-------|-------|---------------|
| H-001 | P&L orderId matching unsafe → cumulative-delta | F2-binding | T-219 |
| H-002 | Similar entries collide → cumulative-delta covers | F2-binding | T-219 (same approach removes matching) |
| H-003 | place_market_order non-idempotent, no retry | F2-binding | T-201 (protocol marker) + T-208 (adapter behaviour) |
| H-004 | SL-set failure → emergency close | F2-binding | T-216 (order pipeline step 6) |
| H-005 | Opposite-side signals | non-F2 | F3 strategy-engine (scoring rule `block_opposite_position_open`) |
| H-006 | Webhook rate limit | non-F2 | F0/F1 signal-gateway (already addressed) |
| H-007 | WS reconnect exp backoff | F2-binding (private WS) | T-209 (private WS) — public WS already F1 market-data-svc |
| H-008 | Signal TTL | non-F2 | F3 strategy-engine |
| H-009 | Execution WS dedup | F2-binding | T-210 (DedupingConsumer) + T-218 (dispatcher consumes) |
| H-010 | Fan-out before dedup | non-F2 | F0/F1 signal-gateway (already addressed) |
| H-011 | 2s sleep before closed-pnl query | F2-binding | T-219 (cumulative-delta) |
| H-012 | closed-pnl is source of truth | F2-binding | T-219 (cumulative-delta reads closed-pnl) |
| H-013 | Partial TP requires `tpslMode=Partial` | F2-binding | T-201 (protocol required param, no default) + T-208 |
| H-014 | Price subscription refcount | non-F2 | F1 market-data-svc PriceManager + T-110a BufferRegistry (already shipped) |
| H-015 | Orphan close uses exact qty string | F2-binding | T-208 (Decimal qty preservation in BybitV5Adapter) |
| H-016 | Shadow task cleanup | cross-phase | F2 protocol surface (no change needed; protocol stays as is); F5 shadow runtime owns full address |
| H-017 | Audit 1:1 matching → cumulative attribution | F2-binding | T-220 (P&L audit loop uses cumulative per sub-account+window) |
| H-018 | Close by PK only | F2-binding | T-202 (`packages/db/queries/trades.py` PK-keyed updates only) + T-217 (PositionLifecycle uses PK) |
| H-019 | Score fail-open is logged | non-F2 | F3 scoring evaluator |
| H-020 | Post-restart reconciliation | F2-binding | T-221 (execution-service startup) |
| H-021 | Scheduled jobs run UTC | F2-binding | T-220 (APScheduler in-process, UTC only) |
| H-022 | API keys per-bot, never shared | F2-binding | T-215 (adapter pool reads `BOT_<ID>_BYBIT_API_KEY/SECRET`) |
| H-023 | Shadow restart via OHLC replay | non-F2 | F5 shadow runtime |
| H-024 | Fill label derives from our order ID | F2-binding | T-218 (dispatcher matches `execId → order_id` from DB; exchange order-link informational only) |
| H-025 | Cross-bot IP rate limit coordination | F2-binding | T-205 (shared NATS KV rate limiter + coordinated pause flag) |
| H-026 | Bybit qty-step rounding in reconcile | F2-binding | T-221 (reconciliation uses time-window + orderId-covering test, not qty approximations) |

**Tally:** 18 F2-binding (H-001/002/003/004/007/009/011/012/013/015/017/018/020/021/022/024/025/026), 1 cross-phase (H-016), 7 non-F2 (H-005/006/008/010/014/019/023). All 18 F2-binding hazards have a named test per §20 catalog and each owner task carries that named test as part of its scope.

### F2 exit-criteria trace (§19 lines 2526-2531)

| ID | Bullet | Owner task(s) |
|----|--------|---------------|
| E1 | Test webhook against Bybit testnet → opens position, places SL+TP, manages BE+trail, closes correctly | T-222 (manual playbook + CI smoke harness; brief §11.6 says testnet integration is not-CI manual — confirm Q10) |
| E2 | PaperExchange identical event stream shape to live | T-222 (same harness, parameterized exchange_mode='paper') |
| E3 | Cumulative-delta reconciliation passes integration test under concurrent-bot simulated closes | T-219 (foundational test) + T-222 (concurrent-bot scenario) |
| E4 | P&L audit loop catches and corrects an injected drift | T-220 |
| E5 | All hazard-catalog tests pass | each F2-binding hazard's named test inline in its owner task; T-F2+ opportunistic catalog orchestrator verifies presence |

### Migration sequence (F1 ended at 0004; F2 adds 4 migrations)

| ID | Migration | Tables | Owner task |
|----|-----------|--------|------------|
| 0005 | Live execution ledger | `orders` + `trades` + `executions` (FK chain; `executions` hypertable) | T-202 |
| 0006 | Live position state | `position_state` (regular, PK bot_id+symbol) | T-203 |
| 0007 | Trading events | `trading_events` hypertable (event-stream mirror of orders.events for audit) | T-204 |
| 0008 | Paper mirror | `paper_orders` + `paper_trades` + `paper_executions` + `paper_positions` (mirror live shape, exchange='paper') | T-212 |
| 0009 | P&L audit deltas | `trade_pnl_deltas` (schema designed inline; not in §7.2 — open question Q6) | T-220 |

5 migrations total. Per-migration `test_NNNN_migration.py` per §N8.

### Numbered task table

| ID | Title | LOC est | Exit | Hazards | Blocked by | Split? |
|----|-------|--------:|------|---------|------------|--------|
| T-201 | ExchangeClient protocol + error taxonomy + types (`packages/exchange/`) | ~150 | E1/E2 | H-003, H-013 | — (top-of-DAG) | no |
| T-202 | Migration 0005 — orders+trades+executions | ~220 src + 250 test | E1 | H-018 | — (top-of-DAG) | no |
| T-203 | Migration 0006 — position_state | ~80 src + 120 test | E1 | — | T-202 | no |
| T-204 | Migration 0007 — trading_events | ~80 src + 120 test | E1 | — | — (top-of-DAG) | no |
| T-205 | Shared rate limiter (NATS KV `rate_limits` token bucket) | ~180 src + tests | E1 | H-025 | T-201 (errors), ADR Q-A | no |
| T-206 | Adapter contract test framework | ~100 src | E1/E2/E5 | covers H-003/H-013 marker conformance | T-201 | no |
| T-207 | BybitV5Adapter REST client + signing + retry matrix | ~280 src + tests | E1 | H-003 (no-retry on place_market_order) | T-201, T-205 | no |
| T-208 | BybitV5Adapter REST methods (set_leverage / place_market_order / set_trading_stop / cancel_order / get_positions / get_fill_price / get_closed_pnl_cumulative) | ~350 src + tests | E1 | H-013, H-015 | T-207 | **SPLIT candidate** per §0.3 / L-006 — ~7 methods × ~50 LOC + step-size lookup → likely ~400+; pre-flag T-208a (write methods set_leverage/place/set_stop/cancel) + T-208b (read methods get_positions/get_fill_price/get_closed_pnl) |
| T-209 | BybitV5Adapter private WS (auth re-handshake, exp backoff, stream_executions/stream_positions iterators) | ~250 src + tests | E1 | H-007 | T-207 | no |
| T-210 | DedupingConsumer (`packages/bus/dedup.py`) ring buffer 10k | ~120 src + tests | E1 | H-009 | — (top-of-DAG) | no |
| T-211 | PaperExchange skeleton + slippage models (fixed_pct / proportional_to_qty / half_spread) | ~280 src + tests | E2 | — | T-201 | watch-for-cap (~280 = L-006 threshold) |
| T-212 | Migration 0008 — paper_* mirror | ~250 src + 280 test | E2 | — | T-202, T-203 | no |
| T-213 | PaperExchange fill semantics (market-at-tick + SL/TP per-tick monitor + paper_* persistence + execution emission) | ~250 src + tests | E2 | — | T-211, T-212; **Q4 tick-source decision** | no |
| T-214 | execution-service skeleton (`services/execution/` FastAPI + lifespan + /health//ready//metrics, mirror T-109) | ~150 src + tests | E1 | — | — (top-of-DAG) | no |
| T-215 | Adapter pool composition root (loads bots table + per-bot env keys) | ~180 src + tests | E1/E2 | H-022 | T-201, T-207, T-211, T-214 | no |
| T-216 | Order placement pipeline (§9.5 steps 1-9; receive request → set_leverage → place_market → fetch fill_price → set_stop SL+TP → persist orders+trades+position_state in tx → emit orders.events + trading.events) | ~350 src + tests | E1 | H-003, H-004, H-013 | T-208, T-215, T-202, T-203, T-204 | **SPLIT candidate** per §0.3 / L-006; pre-flag T-216a (request → place + fill) + T-216b (set_stops + persistence + emit) |
| T-217 | PositionLifecycle FSM (per-trade monitor task; BE move + trail + MFE/MAE update; tick-rate POSITION_POLL_INTERVAL) | ~280 src + tests | E1 | H-018 | T-216, T-203 | watch-for-cap |
| T-218 | Execution event dispatcher (`_on_execution`; uses T-210 dedup; updates running P&L; marks trailing; exec_type from DB order_id) | ~220 src + tests | E1 | H-009, H-024 | T-210, T-209 | no |
| T-219 | Cumulative-delta P&L reconciliation (`_close_trade` flow; snapshot before/after closed-pnl; 2s sleep configurable; partial-TP attribution in close order) | ~280 src + tests | E3 | H-001, H-002, H-011, H-012 | T-208, T-217, T-218; **ADR Q-B prerequisite** | no |
| T-220 | Periodic P&L audit loop (APScheduler 5-min UTC; fetch closed-pnl 3h window; threshold $0.50 → trade_pnl_deltas + update trades.realized_pnl) + Migration 0009 trade_pnl_deltas | ~250 src + ~80 migration + tests | E4 | H-017, H-021 | T-208, T-202, **migration 0009 inline** | no |
| T-221 | Post-restart reconciliation (startup: fetch open positions exchange + DB; orphan handling per H-020 / H-026; resume monitor tasks) | ~250 src + tests | E1 | H-020, H-026 | T-208, T-217, T-202, T-203 | no |
| T-222 | F2 exit-criteria integration bundle (E1 testnet manual + E2 paper parity + E3 concurrent-bot cumulative-delta) | ~250 src + harness wiring | E1/E2/E3 | covers full F2-binding hazard set in scenario | all prior F2 tasks; **Q10 testnet-CI vs manual decision** | no |

**Total numbered F2 work tasks:** 22 (T-201 through T-222), with 2 pre-flagged splits (T-208 → T-208a/b, T-216 → T-216a/b) likely materialising at plan-stage. Including T-200 itself = 23 task IDs reserved.

### Top-of-DAG (no F2-internal deps; can start in any order after T-200)

- **T-201** ExchangeClient protocol + errors + types — pure types, no deps.
- **T-202** Migration 0005 — independent of execution-service code.
- **T-204** Migration 0007 trading_events — independent.
- **T-210** DedupingConsumer — `packages/bus/dedup.py`, no exchange deps.
- **T-214** execution-service skeleton — mirror T-109 feature-engine pattern, no F2 internal deps.

5 entries to start in parallel if operator runs more than one stream (single-developer flow likely serializes).

## ID scheme proposal

- F1 closed at T-113 (last T-NNN in Done section). Phase boundary clean.
- T-200 = backlog populate (this task; mirrors T-019 → F1 backlog populate).
- T-201..T-222 = F2 numbered work tasks per the table above.
- T-208a/T-208b, T-216a/T-216b reserved as pre-flagged split slots; if operator approves the split at T-208/T-216 plan-review time, the slot is used; otherwise the parent task ID consumes the slot and the split slot is freed back to the pool.
- T-F2+ prefix = opportunistic / cross-cutting / ADR-prerequisite items, no fixed numbering.
- T-300+ reserved for F3 (do-not-touch per §0.10 until F2 closes).

## Decomposition

(See "Numbered task table" above for the full list; this section restates one-line specs with exit-criterion trace for the TASKS.md entries.)

- **T-201**: `packages/exchange/protocols.py` + `errors.py` + `types.py`. ExchangeClient Protocol per §11.1 verbatim (idempotency markers via decorators baked into protocol; `set_trading_stop` requires `tpsl_mode` no default per H-013). Error taxonomy per §11.3 verbatim. Types: `Position`, `OrderPlaceResult`, `ExecutionEvent`, `PositionEvent`. Pure types; H-003+H-013 markers. Trace: E1, E2.

- **T-202**: Migration 0005. `orders` (regular, indexed by `bot_id, status` and `correlation_id`) + `trades` (regular, indexed by `bot_id, status` and `closed_at` partial WHERE status='closed') + `executions` (hypertable on `executed_at`, 7d chunks, UNIQUE `exchange_exec_id+executed_at`, FK to `orders` and `trades`). Per-migration `test_0005_migration.py`. H-018 enforced at query layer (T-216/T-217 use only PK-keyed updates). Trace: E1.

- **T-203**: Migration 0006. `position_state` regular table per §7.2 line 1058, PK `(bot_id, symbol)`. Per-migration test. Trace: E1.

- **T-204**: Migration 0007. `trading_events` hypertable per §7.2 line 1091, 7d chunks, indexes by `(bot_id, event_type, occurred_at DESC)` and `(correlation_id)`. Per-migration test. Trace: E1.

- **T-205**: `packages/exchange/rate_limiter.py`. NATS KV `rate_limits` bucket token bucket. Keys `bybit:<sub_account>:orders`, `bybit:<sub_account>:positions`, `bybit:ip:global`. Refill per §11.4 documented limits. Coordinated pause flag on `RateLimitError`. Hazard: H-025. Test: `test_one_bot_rate_limit_triggers_shared_pause_flag`. Trace: E1. **ADR prerequisite Q-A** (token-bucket budget tuning + Bybit V5 documented per-endpoint limits).

- **T-206**: `packages/exchange/tests/test_protocol_conformance.py` parameterized `pytest.fixture` runs every method-and-marker conformance test against any adapter. `BybitV5Adapter` and `PaperExchange` register as conformers. Trace: E5 (hazard catalog).

- **T-207**: `packages/exchange/bybit_v5/client.py`. HMAC-SHA256 signing, REST envelope with retry matrix per §11.2 ([0.5, 1.0, 2.0]s + jitter for idempotent; 0× for `place_market_order`). Error taxonomy mapping (retCode 10006/10016 → `RateLimitError`, etc.). Hazard: H-003 (zero-retry on non-idempotent). Trace: E1.

- **T-208** (likely → T-208a + T-208b): BybitV5Adapter REST methods. Each method on top of T-207 client + T-205 rate limiter. T-208a writes (`set_leverage` LRU-cached, `place_market_order` no-retry, `set_trading_stop` 3× retry, `cancel_order` 3× retry). T-208b reads (`get_positions`, `get_fill_price`, `get_closed_pnl_cumulative`). H-013 (no-default tpsl_mode), H-015 (Decimal qty round-trip). Trace: E1.

- **T-209**: BybitV5Adapter private WS. Auth handshake, exp backoff 1s→60s+jitter, auth re-handshake on reconnect. `stream_executions()` / `stream_positions()` async iterators. Hazard: H-007 (private WS portion). Trace: E1.

- **T-210**: `packages/bus/dedup.py`. `DedupingConsumer` base class with configurable key extractor and ring buffer 10k. Hazard: H-009. Test: `test_duplicate_exec_event_is_ignored`. Trace: E1.

- **T-211**: `packages/exchange/paper/adapter.py`. Implements ExchangeClient. Slippage models (`fixed_pct`, `proportional_to_qty`, `half_spread`) configurable per bot. State in `paper_*` DB tables (T-212) + in-memory cache. Trace: E2.

- **T-212**: Migration 0008. `paper_orders` + `paper_trades` + `paper_executions` + `paper_positions` mirror live shapes (reuse §7.2 columns) with `exchange='paper'` filter / no-FK to live tables. Per-migration test. Trace: E2.

- **T-213**: PaperExchange fill semantics. Market order at last observed tick + slippage; SL/TP per-tick monitor; persistence to paper_* tables; execution emission via same WS-like async iterator as live. **Q4** depends on tick-source decision (OHLC-1m monitoring acceptable in F2, OR tick stream first). Trace: E2.

- **T-214**: `services/execution/` FastAPI + lifespan + /health + /ready + /metrics. Mirror T-109 feature-engine skeleton. Trace: E1.

- **T-215**: Adapter pool composition root. Loads `bots` table (DB query), per-bot reads `os.environ["BOT_<ID>_BYBIT_API_KEY/SECRET"]` per H-022, instantiates `BybitV5Adapter` (live/testnet) or `PaperExchange` (paper) per `bots.exchange_mode`. Adapter pool dict `{bot_id: ExchangeClient}` attached to app.state. Hazard: H-022. Test: `test_adapter_pool_uses_distinct_credentials_per_bot`. Trace: E1, E2.

- **T-216** (likely → T-216a + T-216b): Order placement pipeline. T-216a (request receipt → DLQ on missing adapter → set_leverage cached → place_market_order). T-216b (fetch fill_price → set_trading_stop SL with 3× retry+emergency-close → set_trading_stop TP with tpslMode=Partial → persist orders+trades+position_state in single tx → emit orders.events + trading.events post-commit). Hazards: H-003, H-004, H-013. Trace: E1.

- **T-217**: PositionLifecycle FSM per-trade monitor task. Tick at `POSITION_POLL_INTERVAL` (default 1s). Read latest price from NATS KV `feature_latest`-like with REST fallback. MFE/MAE update. BE trigger check (favorable move ≥ `be_trigger` → move SL to entry ± `be_sl_level`). Trail SL update if `trailing_active`. Hazard: H-018 (PK-keyed updates only). Trace: E1.

- **T-218**: `_on_execution` dispatcher consuming T-209 stream_executions iterator + T-210 DedupingConsumer ringed by `execId`. Updates running P&L. Marks trailing on TP. Determines `exec_type` via DB `execId → order_id` match (NOT exchange order-link fields per H-024). On size-0 event → invoke T-219 close flow. Hazards: H-009, H-024. Trace: E1.

- **T-219**: Cumulative-delta P&L reconciliation. `_close_trade` flow. Snapshot `closed_pnl_total` BEFORE close (via `get_closed_pnl_cumulative`). Snapshot AFTER (with H-011 2s configurable sleep). Delta = attributable realized P&L. Apportion to trades in close order; works under partial-TP (multiple closed_pnl rows) and concurrent closes (per sub-account isolation). Hazards: H-001, H-002, H-011, H-012. **ADR prerequisite Q-B** (snapshot timing, partial-TP attribution order, sub-account scoping). Foundational hazard test: `test_cumulative_delta_ignores_order_ids`. Trace: E3.

- **T-220**: Periodic P&L audit loop. APScheduler in-process (T-220 scope adds APScheduler dep; ADR for choice if alternatives exist), 5-min schedule, UTC only. Fetch Bybit closed-pnl last 3h per sub-account. Compare to DB `trades.realized_pnl` for trades closed in window. Threshold $0.50 → write `trade_pnl_deltas` row + update `trades.realized_pnl`. **Migration 0009 (`trade_pnl_deltas` table) inline**. Hazards: H-017, H-021. Test: `test_audit_never_double_attributes_closed_pnl`, `test_daily_report_runs_at_configured_utc_time`. Trace: E4.

- **T-221**: Post-restart reconciliation. execution-service startup: fetch open positions per sub-account from exchange + `position_state` rows from DB. Orphan DB → close with reason `reconcile_gone`. Orphan exchange → market-close (unmanaged). Matching → resume monitor task (rehydrate T-217 PositionLifecycle from `position_state`). Hazards: H-020, H-026 (time-window+orderId-covering test, NOT qty approximations). Test: `test_reconciliation_closes_db_orphans_and_markets_exchange_orphans`, `test_partial_non_5050_reconciliation_does_not_create_duplicate`. Trace: E1.

- **T-222**: F2 exit-criteria integration bundle. E1 manual playbook (Bybit testnet credentials provisioned per Q10 decision; runs full TV-webhook → testnet → SL+TP+BE+trail+close; documented in `docs/runbooks/F2_E1_testnet_smoke.md`). E2 paper parity test (CI; same harness, parameterized exchange_mode='paper', asserts identical event-stream shape). E3 concurrent-bot integration test (CI; 2+ bot adapter pool, parallel close events, asserts cumulative-delta apportions correctly without cross-bot bleed). Trace: E1, E2, E3.

## Opportunistic items (T-F2+ — cross-cutting, ADR-prerequisite, deferred)

- **T-F2+ ADR-Q-A**: shared rate limiter design ADR. Token-bucket budget tuning + Bybit V5 documented per-endpoint limits + coordinated-pause-flag semantics on `RateLimitError`. **Prerequisite for T-205.**
- **T-F2+ ADR-Q-B**: cumulative-delta reconciliation strategy ADR. Snapshot timing, 2s configurable sleep rationale, partial-TP attribution order in case of multiple `closed_pnl` rows, sub-account scoping under concurrent closes. **Prerequisite for T-219.**
- **T-F2+ ADR-Q-C**: APScheduler choice ADR (or alternative). Process-internal vs external scheduler, UTC-only enforcement per H-021, supervision strategy on scheduler crash. **Prerequisite for T-220 if APScheduler is not the operator-default choice.**
- **T-F2+**: Bybit V5 instruments-info step-size cache. H-026 reconciliation uses time-window + orderId-covering (not qty approximations) so step-size is not load-bearing for reconcile, but order placement (T-216) needs step-size to round qty before submit. Caching strategy + refresh cadence — separate ADR or designed inline at T-216a.
- **T-F2+**: Hazard catalog orchestrator test. Verifies all 18 F2-binding hazard tests exist by name in the test suite. Skipped if any missing → CI red. Lives at `tests/integration/hazards/test_f2_hazard_catalog_complete.py`. Verification, not new feature.
- **T-F2+**: H-016 shadow task cleanup contract. F2 protocol stays as is; F5 shadow runtime owns full address with `async with adapter.subscribe(...) as feed:` wrapper. Listed here so F2 design does NOT bake conflicting assumptions.
- **T-F2+**: Bybit testnet credentials provisioning + secrets handling protocol. Operational, not code. Operator action: provision testnet sub-account, store keys in `.env.testnet` or vault, document in `docs/runbooks/`.
- **T-F2+**: per-bot Bybit API key vault migration (post-MVP). F2 reads from env per H-022 (`BOT_<ID>_BYBIT_API_KEY/SECRET`); production-grade secret store deferred to operator decision.
- **T-F2+**: NATS stream config check — verify `ORDERS` stream exists per §8.2 (subjects `orders.requests.>` + `orders.events.>`) and `rate_limits` KV bucket exists (§8.2 line 1278). F0 nats-bootstrap should have provisioned both; verify before T-216 / T-205 plan-review. ~10-line check or no-op confirmation.
- **T-F2+**: `MarketTick` schema design + `market.ticks.binance.<symbol>` passthrough publish. Already on F1+ opportunistic list — re-citing because Q4 (PaperExchange tick-source) may force resolution at T-213 plan time.

## Top-of-DAG identification (recap)

5 tasks have no F2-internal deps and can start immediately after T-200:

1. **T-201** ExchangeClient protocol + errors + types
2. **T-202** Migration 0005 — orders+trades+executions
3. **T-204** Migration 0007 — trading_events
4. **T-210** DedupingConsumer
5. **T-214** execution-service skeleton

Single-developer flow likely starts T-201 first (largest unblocker — protocol types feed adapters and pool). T-202 + T-210 + T-214 can fill in any order. T-205 (rate limiter) and T-207 (Bybit client) wait on ADR-Q-A and T-201 respectively.

## Risks / open questions

**Surface BEFORE TASKS.md Write so plan-reviewer + brief-reviewer can adjudicate first.** Some have proposed defaults; others need operator input.

- **Q1 — Bybit symbol-step-size lookup at order placement.** §H-026 references qty-step rounding. Reconciliation uses time-window+orderId so step-size is not load-bearing for reconcile, but T-216 needs step-size to round qty BEFORE submit (else exchange rejects with retCode about precision). Source: Bybit `instruments-info` REST. Caching strategy: process-local LRU keyed by `symbol`, refresh cadence (every ~24h?). **Proposed default**: design inline at T-216a plan time, not separate task. ADR if cache TTL > trivial choice. **Operator confirm or override.**

- **Q2 — Order placement transaction scope vs NATS publish ordering.** §9.5 step 8 "Persist orders, trades, position_state rows in one transaction"; step 9 emits NATS events. If NATS publish fails, DB has partial state (orders+trades+position_state present, but `orders.events.<bot_id>` not consumed downstream). **Proposed default**: T-104b precedent (publish-after-persist; DB is canonical, NATS is delivery channel; downstream recovers via DB read or next poll). T-216b inherits this contract verbatim. **Operator confirm.**

- **Q3 — PositionLifecycle task lifetime across restart.** Brief implies tasks ARE recreated on restart via H-020 reconciliation ("matching → resume monitor task"). Persistence: `position_state` table holds enough to resume (entry, qty, sl/tp, mfe/mae, trailing flags). T-221 rehydrates T-217 PositionLifecycle FSM from `position_state`. **Proposed default**: confirmed reading. **Operator confirm.**

- **Q4 — PaperExchange tick-source for SL/TP per-tick monitoring (F2 vs F5 boundary).** Brief §12.1 says "monitored per-tick; when price crosses, fill at trigger price + slippage." F1 deferred `MarketTick` schema (T-F1+ opportunistic — `market.ticks.*` passthrough publish from market-data-svc). **Two options:**
  - **Q4-A**: F2 PaperExchange uses OHLC-1m-based monitoring (intra-candle path generation deferred to F5 backtest harness §12.2). PaperExchange E2 parity test asserts shape, not tick precision.
  - **Q4-B**: F2 first lands `MarketTick` schema + passthrough (pulls T-F1+ item into F2 scope), then PaperExchange uses tick stream.
  - **Proposed default**: Q4-A (F2 stays narrow on PaperExchange tick precision; F5 backtest harness covers true intra-candle replay). Q4-B is significant scope creep for F2. **Operator decide.**

- **Q5 — `trading_events` vs `orders.events` persistence-vs-NATS split.** §8.1 line 1216 says `trading.events` is "same data as orders.events but persisted". So execution-service emits BOTH NATS publish (`orders.events.<bot_id>`) AND DB insert into `trading_events` per event. Persistence is part of order-pipeline transaction (T-216 step 8); NATS publish is post-commit (Q2 default). **Proposed default**: confirmed reading; T-216b implements both. **Operator confirm.**

- **Q6 — `trade_pnl_deltas` schema (not in §7.2 DDL).** Required by §9.5 line 1605 "writes correction to `trade_pnl_deltas` and updates `trades.realized_pnl`". **Proposed schema for T-220 migration 0009**:
  ```sql
  CREATE TABLE trade_pnl_deltas (
      id              BIGSERIAL PRIMARY KEY,
      trade_id        BIGINT NOT NULL REFERENCES trades(id),
      audit_run_at    TIMESTAMPTZ NOT NULL,
      delta_amount    NUMERIC(20, 4) NOT NULL,
      pnl_before      NUMERIC(20, 4) NOT NULL,
      pnl_after       NUMERIC(20, 4) NOT NULL,
      sub_account     TEXT NOT NULL,
      meta            JSONB NOT NULL DEFAULT '{}'::jsonb
  );
  CREATE INDEX trade_pnl_deltas_trade ON trade_pnl_deltas (trade_id);
  CREATE INDEX trade_pnl_deltas_audit ON trade_pnl_deltas (audit_run_at DESC);
  ```
  **Operator confirm or revise.**

- **Q7 — Adapter pool initialization symbol-set vs lazy.** T-215 reads `bots` table. Each bot's `exchange_mode` selects adapter class. Eager init (all adapters at startup) vs lazy (first-order-request). **Proposed default**: eager — single-process service, < 10 bots expected, startup cost trivial, pre-flight failure surfaces immediately not on first trade. **Operator confirm.**

- **Q8 — H-016 protocol surface decision.** F2 ExchangeClient protocol (T-201): does it expose `subscribe(symbol)` async-context-manager method NOW (preparing for F5 shadow runtime)? Or stays as `stream_executions()` / `stream_positions()` only and F5 wraps later? **Proposed default**: protocol stays as is in F2; F5 introduces wrapper class on top. Avoids speculative design (§0.8 anti-hypothetical). **Operator confirm.**

- **Q9 — `set_leverage` cache (H-002 reference; H-002 actual content is similar-entry collision).** §9.5 step 3 says "idempotent, cached — see H-002". H-002 itself is about cumulative-delta. The cache reference is **a brief inconsistency** — likely meant H-022 or generic idempotent caching. **Proposed default**: process-local LRU `(bot_id, symbol, leverage) → ts`, TTL 1h, on miss/expiry call API, on hit no-op. Avoids redundant API calls (rate-limit pressure). **Operator confirm or clarify brief.**

- **Q10 — E1 testnet integration: CI vs manual.** Brief §11.6 says "Integration: against Bybit testnet via E2E suite (not CI; manual)." But §19 line 2527 (E1 exit) says "test webhook against Bybit testnet opens position…". Reconcile: E1 is **manual playbook** (T-222 documents `docs/runbooks/F2_E1_testnet_smoke.md`), executed by operator before F2 phase-close-out. CI covers E2 (paper) + E3 (concurrent-bot simulated, no testnet). **Proposed default**: manual playbook for E1; operator runs once before F2 close + chore commit referencing run output. **Operator confirm.**

- **Q11 — `tpsl_mode` Bybit V5 API parameter shape.** H-013 says `set_trading_stop` requires `tpsl_mode` (no default). §11.1 protocol shows `tpsl_mode: Literal["Full", "Partial"] = "Full"` — but H-013 says "Compile error if omitted" (i.e., **NO default**). **Inconsistency in brief.** Proposed resolution: T-201 ships protocol with `tpsl_mode: Literal["Full", "Partial"]` **without default** per H-013 invariant. The §11.1 default `"Full"` is the bug we're fixing. **Operator confirm.**

- **Q12 — Single branch vs branchless on master for T-200 itself.** F1 T-019 ran branchless on master per the F0/F1 backlog-populate precedent. Per CLAUDE.md `One branch per task: feat/T-NNN-short-name` — but TASKS.md-only chores deviate from this elsewhere (chore commits land on master). **Proposed default**: branchless on master (mirror T-019). **Operator confirm.**

## TASKS.md Write outline

Do NOT edit `TASKS.md` until plan-reviewer Gate 1 APPROVE + brief-reviewer Gate 3 SHIP on this plan-doc. After approval, the edit is mechanical:

### Structural changes

1. Mark T-200 done in Done section with full prose entry (mirror T-019 pattern).
2. Add `### F2 numbered` subheading under `## Backlog`.
3. Populate with T-201..T-222 entries (one bullet each, 1-3 line spec).
4. Add `### F2+ opportunistic` subheading under `## Backlog`.
5. Populate with T-F2+ entries (cross-cutting / ADR-prerequisite list above).
6. Update `## Next` line: "(F1 backlog closed; T-200 promoted to Next... and on completion, T-201/T-202/T-204/T-210/T-214 are the top-of-DAG tasks for F2 — operator picks one to start.)"

### One entry format example (numbered)

```markdown
- [ ] T-201: ExchangeClient protocol + error taxonomy + types — `packages/exchange/protocols.py` + `errors.py` + `types.py`. Pure types per §11.1 verbatim (idempotency markers via decorators baked into protocol; `set_trading_stop` requires `tpsl_mode` no default per H-013 / Q11 resolution). Error taxonomy per §11.3 verbatim. Types: `Position`, `OrderPlaceResult`, `ExecutionEvent`, `PositionEvent`. Hazards: H-003 (non-idempotent decorator marker), H-013 (no-default tpsl_mode). Top-of-DAG. Trace: E1, E2. Est: ~150 LOC.
```

### One entry format example (opportunistic)

```markdown
- [ ] T-F2+: ADR for shared rate limiter design — token-bucket budget tuning per Bybit V5 documented per-endpoint limits, coordinated-pause-flag semantics on `RateLimitError`. Prerequisite for T-205. Est: ~80 LOC ADR.
```

### LOC target for TASKS.md edit

~22 numbered entries × ~150 char/entry + ~10 opportunistic × ~150 char + 5-10 lines structural = **~300-450 LOC TASKS.md diff**. Plan-doc itself is the main artifact (this file, ~480 LOC including survey table + decomposition + open questions).

## Verification plan

- `git diff TASKS.md` shows ONLY: F1 unchanged sections preserved + new `### F2 numbered` + new `### F2+ opportunistic` subheadings + T-200 Done entry + Next-line update.
- `grep -c "T-2[0-9][0-9]" TASKS.md` returns expected count (~22 numbered + 10 opportunistic + 1 T-200 entry).
- No code, no tests, no migrations changed.
- `uv run ruff format/check` clean (no Python touched).
- `uv run pytest` repo-wide unchanged from master HEAD `cf4b465` baseline.

## Decisions committed (operator-approved 2026-04-26 — "use defaults" on all 12)

1. **Q1 — Bybit step-size lookup**: design inline at T-216a plan time (LRU keyed by symbol; not a separate task). ADR only if cache TTL becomes non-trivial decision.
2. **Q2 — Order placement publish ordering**: T-104b precedent (publish-after-persist; DB canonical, NATS delivery channel; downstream recovers via DB read or next poll).
3. **Q3 — PositionLifecycle restart lifetime**: T-221 rehydrates T-217 PositionLifecycle FSM from `position_state` per H-020 reconciliation matching path.
4. **Q4 — PaperExchange tick-source**: Q4-A (F2 uses OHLC-1m monitoring; intra-candle path generation deferred to F5 backtest harness §12.2). PaperExchange E2 parity test asserts shape, not tick precision.
5. **Q5 — `trading_events` DB persistence + `orders.events` NATS publish**: BOTH per event from execution-service (persist part of T-216b tx; publish post-commit per Q2).
6. **Q6 — `trade_pnl_deltas` schema**: **proposed schema is STARTING POINT ONLY.** Final schema lock occurs at the migration task's own plan-doc (T-220 owns migration 0009 inline). Schema may iterate based on query patterns surfacing during T-220 P&L audit loop implementation. T-200 commits ONLY to "table exists with the proposed columns as a baseline"; T-220 plan-doc is the lock site.
7. **Q7 — Adapter pool init**: eager (single-process, < 10 bots, startup cost trivial, pre-flight failure surfaces immediately).
8. **Q8 — H-016 protocol surface**: F2 ExchangeClient protocol stays as is (`stream_executions()` / `stream_positions()` only); F5 introduces shadow-context-manager wrapper class on top. Avoids speculative design per §0.8.
9. **Q9 — `set_leverage` cache + brief H-002 cross-ref**: cache implemented as process-local LRU `(bot_id, symbol, leverage) → ts`, TTL 1h. **Brief inconsistency note codified for T-216a plan-doc**: *"§9.5 step 3 references H-002 for `set_leverage` cache semantics, but H-002 is the cumulative-delta reconciliation hazard (different scope). Treating as a drafting artifact in the brief; the cache rationale is correct but the H-NNN cross-reference is incorrect. Cache implemented per its own semantics, not per H-002. If we later find a pattern of similar brief cross-ref errors, we open a brief erratum task."* T-216a plan-doc author MUST include this exact note.
10. **Q10 — E1 testnet CI vs manual**: manual playbook (T-222 documents `docs/runbooks/F2_E1_testnet_smoke.md`); operator runs once before F2 close + chore commit references run output. CI covers E2 (paper) + E3 (concurrent-bot simulated, no testnet creds needed).
11. **Q11 — `tpsl_mode` no default**: T-201 ships protocol with `tpsl_mode: Literal["Full", "Partial"]` **WITHOUT default** per H-013 invariant. **Brief conflict resolution codified for T-201 plan-doc**: *"H-013 (hazard catalog) and §11.1 (protocol surface) disagree on `tpsl_mode` default. H-013 mandates no default (compile-error if omitted) — this is the authoritative bug-preventing invariant. §11.1's `default='Full'` is drafting artifact and gets overruled. Protocol ships per H-013."* T-201 plan-doc author MUST include this exact note.
12. **Q12 — T-200 branch vs branchless**: branchless on master (mirror T-019 F1 backlog-populate precedent).

### ADR-Q-C (APScheduler choice) — scope justification

Brief §19 F2 explicitly lists "ADRs for shared rate limiter, reconciliation strategy" (Q-A and Q-B). ADR-Q-C (APScheduler) is **operator-flagged for justification** at T-200 plan-review.

**Rationale for ADR-scope (not inline)**: APScheduler choice is **cross-module by design**:
- T-220 (P&L audit loop, 5-min UTC schedule)
- T-221 (post-restart reconciliation has scheduled re-checks if any orphan-resolution requires retry — TBD at T-221 plan time)
- F4 future (analytics-api or alerting-svc daily/hourly reports — multiple consumers per H-021 "scheduled jobs run UTC")
- F5 future (shadow-runtime restart cadence, backtest harness scheduling if any)

The choice (APScheduler in-process vs `asyncio.sleep` loops vs external cron) affects:
- Process supervision semantics (what if the scheduler thread dies?)
- UTC enforcement audit (single point vs scattered)
- Test patterns (frozen-time vs real-time vs FakeScheduler)
- Dependency footprint (APScheduler is non-trivial dep per §0.9)

If we keep this inline at T-220, every later scheduled-job consumer re-litigates the choice. ADR fixes it once.

**Counter-argument considered**: the only F2 consumer is T-220. Could defer ADR until F4 first additional scheduled-job consumer materializes — single inline decision at T-220 with note "if a second consumer arrives, retroactive ADR".

**Resolution**: **keep as T-F2+ ADR-prerequisite for T-220** (plan-doc default). Cost of ADR is ~80 LOC document, written once before T-220 implementation. Cost of deferring is risk that T-220 inline choice diverges from later F4 needs and forces a backwards-incompatible swap. Operator may override to inline-with-note if the dep is felt heavy — the Q12 default-acceptance implies keeping ADR-Q-C; explicit override invited.

## Write-time guidance (verbatim from plan-reviewer APPROVE 2026-04-26)

1. **L-006 watch-for-cap annotation in TASKS.md numbered entries.** Six integration-heavy entries at 250-280 LOC (T-207, T-209, T-213, T-219, T-220, T-221, T-222) are not yet flagged but sit at L-006 threshold. Each entry's TASKS.md bullet MUST include an inline "watch for cap pressure mid-write" note (≤ 8 words) so the per-task plan-doc author at T-NNN-stage applies L-006 active control without re-deriving it. Plan currently flags only T-211 and T-217 — extend the annotation set to the six listed entries.

2. **ADR-Q-C scope reframing.** APScheduler is brief-mandated (§3.1 line 302 + H-021 line 2769 "APScheduler in the service process, UTC only. No system cron."). ADR-Q-C is therefore NOT a library-selection ADR; the genuine ADR scope is **operational pattern**: supervision strategy on scheduler crash, UTC enforcement audit single-point, test patterns (frozen-time vs FakeScheduler), and dep-footprint accounting per §0.9. Reframe the T-F2+ ADR-Q-C entry in TASKS.md to read "ADR for APScheduler operational pattern (supervision + UTC enforcement + test fixtures), prerequisite for T-220" — not "APScheduler choice". Operator's "use defaults" already accepts the ADR; only the scope statement needs the tighten.

3. **N4 TDD discipline call-out for financial-math tasks.** T-216, T-219, T-220 entries in TASKS.md MUST explicitly cite *"§N4 TDD discipline applies — write hazard-named test first, then implementation"*. Plan references the named tests (`test_cumulative_delta_ignores_order_ids`, `test_audit_never_double_attributes_closed_pnl`, etc.) but doesn't bind the TDD ordering verbally. The bind is one phrase per entry, surfaces N4 to the plan-doc author at T-NNN draft stage.

4. **T-200 status header Q12 cleanup.** Plan-doc top status block says "Branch: feat/T-200-f2-backlog-populate (not yet created; per F1 T-019 precedent runs branchless on master after Gate-2 SHIP)". Q12 already committed branchless. Update the status header to drop the branch name proposal (e.g., "Branch: n/a — branchless on master per Q12 / T-019 precedent"). Cosmetic — applied during TASKS.md write or as a same-commit plan-doc patch. **APPLIED — status header updated 2026-04-26.**

5. **Q6 schema lock-site cross-reference in TASKS.md T-220 entry.** Plan commits "trade_pnl_deltas schema is starting point only; final lock at T-220 migration plan-doc". TASKS.md T-220 bullet MUST include the inline note *"trade_pnl_deltas schema baseline per T-200 Q6; final lock at this plan-doc"* so the T-220 plan-doc author knows the schema is editable, not frozen. Single phrase, prevents schema-drift confusion six tasks from now.
