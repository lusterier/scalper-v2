# T-519 — §20 hazard test audit (F5 E4 owner)

**Status:** APPROVED (plan-reviewer Gate-1 single-pass APPROVE 2026-05-16; 0 BLOCKER; 6-item Write-time guidance verbatim below; every "X never existed/stale" claim code-cited file:line — L-018 satisfied). E4 exit-criterion owner; gating for T-521/T-522 + the Live-ready MVP sign-off. master HEAD abfa775 (T-532b CI-GREEN; ALL ADR-0011 hardening clusters T-524..T-536 + T-501..T-518 shipped → T-519 unblocked). ADR-0011 governs E4/E6; no new ADR.

## Purpose
Satisfy F5 E4 ("All hazards in §20 have an associated test that passes" — BRIEF §19:2601): audit every H-001..H-036 entry's `**Test.**` citation, correct stale/missing citations to the actually-existing covering tests, ship a reproducible verification meta-test that permanently CI-guards §20-citation↔test consistency, and produce the operator-facing `docs/audit/hazard-test-coverage.md` report.

## Operator OQ answers (T-519 plan-stage 2026-05-16, all recommended, baked)
- **OQ-1=A** — test-NAME-pin reverse-map audit: parse the §20 `**Test.**` citations (H-001..H-036), verify each cited test is pytest-collectable + passes, produce the report. NO `@pytest.mark.hazard` retrofit, NO `pytest -m hazard` (the stale task-def mechanism is dropped — a pre-ADR-0011 artifact; code-verified non-existent: 0 tree-wide marker usages, pyproject.toml:116-121 `--strict-markers` + no `markers=[]` block would reject it anyway).
- **OQ-2=A** — T-519 closes E4 by correcting §20 citations to the actually-existing tests; adds a NEW test ONLY for a genuine TOTAL coverage gap (no existing test covers the hazard at all), and if that would breach §0.3/scope → a NEW `T-F5+` backlog ticket (§0.8 not absorbed). Citation corrections are cheap + in-scope; large test-writing is NOT absorbed.
- **OQ-3=A** — deliverable = `docs/audit/hazard-test-coverage.md` report (markdown, §0.3-exempt) + a reproducible verification pytest meta-test (parses §20 `**Test.**` citations, asserts every cited test is pytest-collectable) → CI-guarded permanent E4 regression guard.

## Plan-vs-task-def corrections (in-body, L-018 / T-532a–T-533a doubly-stale-doc class — drift/brief adjudicate SOUND, NOT NEEDS DISCUSSION; the test-NAME-pin convention is the established shipped reality, the task-def is a stale pre-ADR-0011 artifact)
1. Scope **H-001..H-026 → H-001..H-036** (catalog grew +10 post-task-def: H-027/028/029 allocated 2026-05-15/16 at T-525a1/T-534b2/T-535; H-030..H-036 via operator audit 2026-05-08/09 — all postdate the task-def; 36 entries, no missing integer).
2. Mechanism **`pytest -m hazard`/`@pytest.mark.hazard` retrofit → test-NAME-pin reverse-map** (the marker mechanism never existed).
3. §0.3 estimate **"~80 marker config + ~50 report generator" → "0 marker config + the verification meta-test (~150-195 src L-022-adjusted worst-case, ≪400, tests §0.3-excluded anyway) + the report markdown (§0.3-exempt) + the §20 citation fixes (BRIEF markdown-exempt)"**.

## Scope / files
**1. NEW `tests/test_hazard_catalog_coverage.py`** (the reproducible E4 guard — the only real src):
- Parse the LIVE `docs/CLAUDE_CODE_BRIEF.md` §20 section: `## 20.` heading at :2607, terminate at the next `## ` heading (`## 21.` :2940; `---` :2938). For each `### H-NNN —` entry capture its `**Test.**` line(s); extract every backtick-wrapped citation. 3 verified shapes: bare `` `test_name` `` (H-001:2617); `` `path/test_file.py::test_name` `` (H-035:2904, H-036:2914); `` `path/test_file.py` `` file-level no `::` (H-036:2914 → `packages/exchange/tests/test_quantize.py`). Multiple citations joined by `+` (H-027:2832, H-032:2924, H-033:2934). Tolerant regex over the backtick spans on the `**Test.**` line(s) only; non-backtick prose ignored.
- Collected-test set via subprocess `pytest --collect-only -q` parsed into node-ids (mirror the `test_0021_migration.py` subprocess-deterministic-tool precedent); collect-only never run (no recursion/self-timeout). Assert: every §20-cited test identifier (function name from `name`/`path::name`) and every bare-file citation path resolves to a collected test. On failure → assertion message lists each `H-NNN: <citation> (unresolved)` (the permanent E4 guard).
- **Contiguity = SORTED SET, NOT file-appearance order** (WG1 — the load-bearing correctness pin): parse all `### H-NNN`, collect the integer set, sort, assert `== range(1, max+1)` (max derived from the parsed entries — anti-L-025, NO hardcoded 36). MUST NOT assert monotonic file order (verified shipped reality: file order is `... H-029 H-030 H-031 H-034 H-035 H-036 H-032 H-033` — H-032/H-033 deliberately AFTER H-036 with `numbering note` prose, the audit-cluster grouping).
- §N4: this IS a meta-test; self-test its parser on hand-authored fixtures (synthetic gap → FAIL naming the gap; out-of-file-order-but-complete snippet mirroring the real H-035/036/032/033 layout → PASS; the 3 citation shapes; an unresolved citation → FAIL). No financial math.

**2. NEW `docs/audit/hazard-test-coverage.md`** (operator-facing E4 report, §0.3-exempt markdown; F4 E1 runbook precedent shape): regenerable-from-truth (the same parse the meta-test runs — documented as regenerable, NOT hand-maintained prose). Table H-001..H-036 → cited test(s) → file → status. Header: E4 statement, the test-NAME-pin mechanism, the dropped stale `pytest -m hazard`/`@pytest.mark.hazard` (code-verified non-existent), the H-026→H-036 catalog-grew correction, how to re-run (`uv run pytest tests/test_hazard_catalog_coverage.py -q` + the full suite for PASSING). Footer: the H-015 (+ any other) citation-correction record. Note (traceability, not a blocker): TASKS.md:204 task-def `Blocked-by` reads `ALL T-501..T-518` only while DAG 282/302/307 add `T-524..T-536` — flag the task-def line as the stale-narrower for a future TASKS.md tidy.

**3. `docs/CLAUDE_CODE_BRIEF.md` §20 citation corrections** (BRIEF markdown-exempt; the OQ-2=A E4 closure): for EACH entry whose `**Test.**` citation does not resolve, correct it to the actually-existing covering test(s). H-015 (:2729) confirmed stale — `test_orphan_close_uses_exchange_qty_string_not_float` grep tree-wide exit 1, zero files; related qty-string tests exist in `packages/exchange/bybit_v5/tests/test_adapter.py`. WRITE-TIME-VERIFY: read H-015's actual Policy/control + grep the real covering test(s); verify they pin H-015's specific "orphan close uses exchange qty string not float" control before citing; if NO existing test covers that specific control → trivially-small precise test ONLY if in-scope, else NEW `T-F5+` backlog ticket (§0.8) + cite partial pins + record the gap in the report. Same rule for any OTHER unresolved citation the full 36-entry audit surfaces (the RULE is committed; the exact set is the write-time output, recorded in the report + commit body).

NO source-module change (no marker config, no production code). 0 §20 regression (the audit only reads/cites; correcting a stale citation to the real test STRENGTHENS §20 — §N10 preserved). No new H-NNN. §N9 N/A. §N4 = the meta-test self-tests its parser. §0.3: src = the meta-test only ≪400 (and §0.3-excluded); report .md + BRIEF §20 edits markdown-exempt. Single task, no L-007 split.

## Gate-4 (math-validator) — OUT OF SCOPE, fast no-op (COMMITTED, no residual optionality)
NO file in the CLAUDE.md Gate-4 trigger list (no `packages/features|pnl`, `services/execution|scoring` compute, `packages/sizing`); zero Decimal arithmetic (the parser/asserts are string/set ops). `VERIFIED — out of scope, math-validator skipped` (the verbatim T-523/T-533a markdown-meta-task disposition).

## §0.3 / L-022 / L-007
src = the single verification meta-test; L-022 5-WG → +20-30% contingency → ~150-195 src worst-case, ≪ §0.3 400 (and tests §0.3-excluded; report .md + BRIEF edits markdown-exempt). NOT Hand-verification-docstring-heavy (a parser + assertions). Single task, no further split.

## L-015 sibling impact
No existing test pins §20 prose (the new meta-test is THE thing that pins §20-citation↔test consistency henceforth; confirm none at write-time). The H-015 (+others) §20 citation edits change BRIEF text only. test_app_factory/test_config etc. untouched. mypy over the staged set incl. the new test pre-commit (L-024).

## Hand verification (the meta-test's own correctness — Gate-4 out-of-scope, §N4-self-tested)
- Parser: `### H-001 — ...\n**Test.** \`test_cumulative_delta_ignores_order_ids\` verifies...` → citation `test_cumulative_delta_ignores_order_ids`. `**Test.** \`a/b/test_x.py::test_y\` + \`test_z\`` → `test_y` + `test_z`. `**Test.** \`pkg/tests/test_quantize.py\` 4 unit tests...` → file `pkg/tests/test_quantize.py` (bare-file, H-036).
- Contiguity: parsed {H-001..H-036} sorted → `range(1,37)` PASS. Out-of-file-order-but-complete fixture (H-035,H-036,H-032,H-033 layout) → PASS (set-based, not file-order). Synthetic gap (drop H-010) → FAIL naming H-010.
- Resolution: H-015-as-shipped (`test_orphan_close_uses_exchange_qty_string_not_float` absent) → FAIL `H-015: test_orphan_close_uses_exchange_qty_string_not_float (unresolved)` until the §20 citation is corrected → after correction, PASS (the meta-test is what proves E4 closed).

## Open questions
None. OQ-1/2/3=A baked. The 3 plan-vs-task-def corrections (scope H-036, mechanism test-NAME-pin, §0.3 reshape) are in-body stale-doc corrections (T-532a/T-533a class). The exact set of unresolved §20 citations beyond H-015 is the audit's write-time output (the RULE is committed: correct to real test; total-gap → trivial-test-or-backlog per §0.8/OQ-2=A; recorded in the report + commit body) — the single WRITE-TIME-VERIFY item, NOT an unresolved design question. Gate-4 out-of-scope COMMITTED.

## Write-time guidance (plan-reviewer verbatim — must be addressed during implementation)
1. **Contiguity check is on the SORTED SET of parsed H-integers, explicitly NOT on file-appearance order.** Verified shipped reality: §20 entries appear in file order `... H-029 H-030 H-031 H-034 H-035 H-036 H-032 H-033` (BRIEF:2918, 2928 — H-032/H-033 are physically AFTER H-036 by deliberate audit-cluster grouping, each with a `numbering note`). The verification test MUST parse all `### H-NNN` headings, collect the integer set, sort it, and assert it equals `range(1, max+1)` (max derived from the parsed entries — anti-L-025, no hardcoded 36). It MUST NOT assert entries appear in monotonically increasing file order. Self-test fixture MUST include an out-of-file-order-but-complete snippet (mirror the real `H-035, H-036, H-032, H-033` layout) and assert it **PASSES**; plus a synthetic-gap snippet (drop H-010) asserting it **FAILS naming H-010**. Both directions pinned.
2. **Collected-test set via subprocess `pytest --collect-only -q`** parsed into node-ids (mirror the `test_0021`/`test_NNNN_migration.py` subprocess-tool precedent for shelling out to a deterministic tool). The meta-test MUST be robust running inside its own suite — collect-only, never run; no recursion, no self-timeout. If `request.session.items` is demonstrably simpler and equally deterministic it is acceptable, but the subprocess form is the lower-coupling default.
3. **Citation parser handles exactly the 3 verified shapes from the `**Test.**` line**: bare `` `test_name` `` (H-001:2617), `` `path/to/test_file.py::test_name` `` (H-035:2904, H-036:2914), `` `path/to/test_file.py` `` file-level (H-036:2914 cites `packages/exchange/tests/test_quantize.py` with no `::`). Multiple citations on one line joined by `+` (H-027:2832, H-032:2924, H-033:2934). Tolerant regex over the backtick spans on the `**Test.**` line(s) only; non-backtick prose ("verifies correctness when…") ignored. Document the 3 shapes + the `+`-join + prose-ignore in the test module docstring.
4. **§20 citation corrections — apply the OQ-2=A rule per unresolved entry, fixture/grep-pinned at write-time:** for EACH entry whose citation does not resolve, grep the entry's actual Policy/control and cite the real covering test(s). H-015 is confirmed stale (`test_orphan_close_uses_exchange_qty_string_not_float` — grep tree-wide exit 1, zero files; related qty-string tests exist in `packages/exchange/bybit_v5/tests/test_adapter.py`): verify those tests pin H-015's specific "orphan close uses exchange qty string not float" control before citing them; if NO existing test covers that specific control → trivially-small precise test ONLY if in-scope, else a NEW `T-F5+` backlog ticket (§0.8 not absorbed) + cite partial pins + record the gap. The full 36-entry audit is the write-time output: every unresolved entry recorded in the report AND the commit body. Do NOT absorb large test-writing into T-519.
5. **`docs/audit/hazard-test-coverage.md` is regenerable-from-truth** (same parse the meta-test runs), documented as regenerable not hand-maintained prose. Header states E4, the test-NAME-pin mechanism, the **dropped stale `pytest -m hazard` / `@pytest.mark.hazard`** mechanism (code-verified non-existent: 0 tree-wide usages, pyproject `--strict-markers` + no `markers=[]` block would reject it), and the `H-026 → H-036` catalog-grew correction. Footer records the H-015 (+ any other) citation correction. State the L-022 contingency-adjusted src estimate explicitly (5 WG items → +20-30%: ~150-195 src worst-case, still ≪ §0.3 400 and tests are §0.3-excluded). Note in the report (traceability only, not a blocker): TASKS.md:204 task-def `Blocked-by` line literally reads `ALL T-501..T-518 passing` while the DAG lines 282/302/307 add `T-524..T-536` — the plan correctly treats T-524..T-536 as also-blocking (all shipped); flag the task-def line as the stale-narrower of the two for a future TASKS.md tidy.
6. **Gate-4 OUT-OF-SCOPE fast no-op** (T-523/T-533a markdown-meta-task class — no `packages/features|pnl`, `services/execution|scoring` compute, `packages/sizing` touched; zero Decimal arithmetic — the parser/asserts are string/set ops): `VERIFIED — out of scope, math-validator skipped`. The §N4 obligation is satisfied by the meta-test self-testing its own parser on hand-authored fixtures (synthetic gap + out-of-order-complete + 3-shape + unresolved-citation). Run `uv run mypy $(git diff --cached --name-only --diff-filter=ACM -- '*.py')` over the staged set INCLUDING the new test file before commit (L-024 hook-vs-local-scope closure); for any typed mock `.await_args`/`.call_args` use the `assert ... is not None` narrowing form, not inline `# type: ignore` (L-023/L-024).
