# T-552 — rate_limiter `_params` missing "market" key → KeyError on first real get_instrument_info (T-529 lockstep omission)

**Status:** Gate-1 APPROVE (plan-reviewer pass 4/4; pass-1 BLOCKER incomplete-L-026 + pass-2 BLOCKER missed rate_limiter.py:11-12 docstring + pass-3 CONCERN missed ADR-0003:59 — all resolved; §11.4 CONCERN + :93/:94 false-attribution bound-out confirmed-correct pass-2/3).
**Phase:** F6 numbered. Operator-directed post-MVP fix surfaced during the T-550 demo-arming ops retry (operator: "A – T-552 najprv, odblokuje demo test"). ADR-0015 §70-81 decision-C standing basis (governance as T-545..T-550 — no per-item ADR; F6 `/N` grows; T-541 opener excluded). §0 one-task. F6 10/10→11/11 + entry at THIS close per L-027 (NOT pre-written; mirror T-545..T-550). Dev task — Gate-1 + drift-checker RUN (~50 src+test LOC, multi-site L-026/L-034/L-015) + Gate-3 + math-validator Gate-4 (services/execution/ config.py+main.py in diff → triggered; expected VERIFIED — rate-limit float config wiring, zero Decimal/P&L/indicator math; rate_limiter.py itself out of math scope; 120/240 are config tier-mirrors, not derived math).

## Bug (production-surfaced, T-550 demo-arming ops retry 2026-05-18)

After T-549b+T-550 verified working, fresh test signal manual-test-003 flowed signal-gateway→strategy-engine-demo (decision=passthrough)→execution-service `orders.requests.demo` handler → `bus_handler_failed error: "'market'"` (= `str(KeyError('market'))`; bus handler logs only `error=str(exc)`, no traceback — secondary §N2 gap, T-551 family, NOT this scope). NO order placed.

Root cause code-verified: `packages/exchange/rate_limiter.py:94-97` `__init__` builds `self._params = {"orders":(...), "positions":(...), "_ip_global":(...)}` — NO `"market"`. `:57-59` `EndpointGroup = Literal["orders","positions","market"]` (T-529) + `bybit_v5/adapter.py:400` `get_instrument_info` calls `acquire(self._sub_account,"market")` → `acquire:117-120` → `_refill_and_debit(sub_key,"market")` → `:169 rate,capacity = self._params[group]` → **`KeyError('market')`**. Placement (`placement.py:271 get_instrument_info`) = first real exercise (paper bots don't use the limiter; testnet/live never armed; demo = first real order placement). T-529 added the `"market"` Literal member + `get_instrument_info` caller + `_on_rate_limit_hit("market")` but NOT `_params["market"]`, ctor params, Settings/env, nor ADR/docstring lockstep — classic lockstep omission (never-run-live-path chain: alerting httpx → T-547 → T-548 → T-549b 10003 → T-550 settleCoin → outbox-relay → THIS).

## Spec basis (determinable — no operator OQ)

ADR-0003:20 "orders/positions ~10 req/s; **market ~120 req/s per-IP**". ADR-0003:34-37 "Three bucket families" orders 10/20, positions 10/20, bybit.ip.global 120/240. ADR-0003:63 env convention `RATE_LIMIT_<GROUP>_RATE/_CAPACITY`. §N9 (ADR-0003:23/53/63): ALL bucket params = Settings env vars, NOT src literals. config.py:77-84 "ctor params per ADR-0003 §Decision 1 (defaults verbatim)".

- **Design = Design A** (4th sub-account bucket family `bybit.<sub>.market`): T-529 already shipped `acquire(self._sub_account,"market")` (sub-account-keyed, like positions); re-designing to market→ip.global-only = re-architecting shipped T-529 contract, rejected. Minimal completion of the omitted lockstep.
- **Defaults `rate_limit_market_rate=120.0`, `rate_limit_market_capacity=240.0`** — ADR-0003:20 "market ~120 req/s per-IP" → mirror ip.global 120/240 tier (rate=X cap=2X pattern of all 3 existing). NO ctor literal default (consistent w/ existing 6 params, single source = config.py Settings; ctor default = 2nd drift-prone location violating the design + §N9-location intent) — deliberately forces the L-034 lockstep across all test ctor sites.

## Fix (code)

1. `packages/exchange/rate_limiter.py` `__init__`: add `market_rate: float, market_capacity: float` keyword-only NO default, ADR-group-order (orders, positions, market, ip_global) + `self._params["market"]=(market_rate,market_capacity)`.
2. `services/execution/app/config.py` after :83: `rate_limit_market_rate: float = 120.0` + `rate_limit_market_capacity: float = 240.0` (mirror :77 ADR-0003 comment, group-order).
3. `services/execution/app/main.py:147-156`: add `market_rate=settings.rate_limit_market_rate, market_capacity=settings.rate_limit_market_capacity,` (group-order-consistent).

## L-026 site-set closure — FINAL (7 in-scope + bound-out)

**IN-SCOPE — same commit (7 sites):**
1. `BRIEF §11.4:1936` Keys bullet — +`bybit.<sub_account>.market` (§6.7; canonical spec ADR Context quotes; mirror §11.4's existing T-548-footnote shape, verify write-time).
2. `ADR-0003:9-12` Context blockquote — verbatim §11.4-quote sync (+`bybit.<sub_account>.market`); §N10, not a decision rewrite.
3. `ADR-0003:34-37` Decision-1 — +4th family bullet `- bybit.<sub_account>.market — refill 120 req/s, capacity 240 tokens.` + "Three bucket families"→"Four bucket families".
4. `ADR-0003:63` env-var list — +`RATE_LIMIT_MARKET_RATE=120` / `RATE_LIMIT_MARKET_CAPACITY=240`.
5. NEW `ADR-0003` T-552 footnote — sibling to existing T-548 footnote (:44-50), same style (defect-completion: 4th family T-529 already shipped in code, T-552 completes `_params`/ctor/Settings/env lockstep; no new ADR — design unchanged); MUST explicitly name Follow-up :93/:94 as pre-T-529 point-in-time T-205/T-208 scope (resolves §N10 in one authoritative place).
6. `packages/exchange/rate_limiter.py:11-12` module docstring — "1. Three bucket families:" → "1. Four bucket families:" + add `bybit.<sub_account>.market` (the edited file's own design-of-record docstring; line 9 "Design per ADR-0003"; zero-extra-diff).
7. `ADR-0003:59` Alternatives rationale — bucket-count prose "Three buckets match what the upstream actually enforces; the cost is 3 KV keys per sub-account vs 1" → "Four buckets match what the upstream actually enforces (orders, positions, market per endpoint group, plus IP-global); the cost is 4 KV keys per sub-account vs 1, which is negligible at sub-10-bot scale." — "OQ-1 default A" reference PRESERVED, ONLY bucket-count prose changed (same in-scope class as #3/#6, standing design-rationale).

**DELIBERATELY-BOUND NON-sites (rationale holds):** `ADR-0003:93` (T-205 Follow-up `Literal["orders","positions"]`) + `:94` (T-208 Follow-up call-list) — point-in-time T-205/T-208 original scope; `market` introduced by LATER T-529; rewriting = L-027 false-attribution; forward correction carried by NEW T-552 footnote (#5) which names them. `ADR-0003:44-50` existing T-548 footnote — T-548-scoped, untouched, new footnote separate sibling. `ADR-0003:16` ("4-bullet specification" — refers to the 4 §11.4 bullets, NOT bucket-count) — NOT a site, do not change. `compose.yaml`/`ops.md` — grep-confirmed no `RATE_LIMIT_*`.

**VERIFY-AT-WRITE-TIME:** `.env.example` — `.env*` Bash+Read deny; if it enumerates `RATE_LIMIT_*` add `RATE_LIMIT_MARKET_RATE=120`/`_CAPACITY=240` same-commit; if deny persists surface to operator (no silent-skip).

## Test TOUCH (§N4 + L-034/L-015 — every SharedRateLimiter ctor site, same commit)

`grep -rn "SharedRateLimiter(" packages services --include=*.py` = 1 prod (main.py:147) + 4 test ctor sites; adding required no-default ctor params breaks every test ctor site (L-034) → all same-commit; integration sites env-gated (L-015 — never defer):
- `test_rate_limiter.py`: `_make_limiter(...)` helper (~:40-63) — add `market_rate: float = 120.0, market_capacity: float = 240.0` sig defaults + pass to ctor (covers all helper-using tests). `test_constructor_accepts_settings_kwargs_per_adr_0003:67` — add `assert limiter._params["market"] == (120.0, 240.0)` + fix docstring "8 ctor kwargs (3 rate/capacity pairs..)"→"10 (4 pairs..)". NEW `test_acquire_market_group_debits_market_bucket` (mirror existing acquire orders/positions test; `await limiter.acquire("sub","market")` no KeyError + debits `bybit.sub.market`; L-033 APPEND after the ENTIRE mirrored original test, read to next top-level `def`/EOF first).
- `test_rate_limiter_integration.py` (env-gated, ci-full only): `_make_limiter:63` helper ctor + INLINE `:99` + INLINE `:177` — each +`market_rate=120.0, market_capacity=240.0` (NOTE: capacity literal = 240.0 NOT 120.0 — do not transpose rate/capacity). Same commit (L-015).

## Scope / hazards / phase

~50 src+test LOC + 7-site doc-sync (each ~1-3 lines, files already open), << §0.3 400. §20: no hazard regressed — FIXES a latent order-placement-blocking defect (rate-limiter KeyError aborts every real order); H-025 (single shared limiter) preserved; no new external write (`acquire` already `@non_idempotent`). §N8 no migration. §N9 satisfied (market params = Settings env vars per ADR-0003:63). §N6/N7 (DI'd ctor, thin) unaffected. §N10 self-consistency (the 7-site doc-sync) is the central axis. Phase-gate L-029: F6 unlocked (ADR-0015 §6.7); operator-directed post-MVP decision-C; promote numbered F6; 10/10→11/11 at close (L-027, not pre-written). NO new ADR (ADR-0003 footnote, T-548 precedent). L-026/L-031 4-pass recurrence — sharpened-lesson candidate for post-resolution learning loop (enumerate the bucket-count duplicate-set by widening the grep STEM across ALL THREE corpora ADR+BRIEF+packages/+services/ from the START, not iteratively).

## Gate plan

plan-reviewer APPROVE ✅ pass-4 → drift-checker RUN (multi-site L-026/L-034/L-015 lockstep; checkpoint after src, after first green test) → brief-reviewer Gate-3 (verify 7-site lockstep + footnote + §N4 + .env.example boundary + re-grep zero-residual) → math-validator Gate-4 (services/execution/ in diff → triggered; expect VERIFIED — rate-limit float wiring, zero Decimal/P&L; placement/pnl/trades not in diff) → `fix(T-552)` + separate `chore(tasks)` F6 10/10→11/11 (L-027). NO push (operator CI-minute gate; unpushed: T-549b+T-550 fix+chore = 4 + this). Post-merge (separate ops): rebuild+recreate execution-service, fresh test signal → expect order placement reaches Bybit demo (next never-run-live layer may surface — iterate). T-551 (outbox relay supervision) = operator-sequenced next task.

## Write-time guidance (plan-reviewer pass-4 verbatim — brief-reviewer Gate-3 verifies each)

1. **rate_limiter.py ctor + docstring (site #6, same commit):** `__init__` pridať `market_rate: float, market_capacity: float` keyword-only, BEZ defaultu (forsuje L-034 lockstep), ADR-group-order `(orders, positions, market, ip_global)` + `self._params["market"]=(market_rate, market_capacity)`. Súčasne module docstring `:11-12` "Three bucket families:" → "Four bucket families:" + vložiť `bybit.<sub_account>.market` v group-order. Zero-extra-diff (súbor už otvorený pre `_params` edit).
2. **config.py / main.py:** config.py po :83 `rate_limit_market_rate: float = 120.0` + `rate_limit_market_capacity: float = 240.0` (mirror :77 ADR-0003 komentár, group-order). main.py:147-156 pridať `market_rate=settings.rate_limit_market_rate, market_capacity=settings.rate_limit_market_capacity,` group-order-konzistentne. Defaulty 120.0/240.0 per ADR-0003:20 mirror-ip.global-tier — over že komentár pri config field menuje ADR-0003 ako zdroj čísla.
3. **L-026 7-site set v JEDNOM commite:** BRIEF §11.4:1936 (+`bybit.<sub_account>.market`) · ADR-0003:12 Context-quote-sync · :34-37 Decision-1 (4th family bullet + "Three"→"Four bucket families") · :59 Alternatives rationale (bucket-count próza "Three buckets match…" → "Four buckets match… (orders, positions, market per endpoint group, plus IP-global)", "OQ-1 default A" referencia ZACHOVANÁ, iba bucket-count veta menená) · :63 env list (+`RATE_LIMIT_MARKET_RATE=120`/`_CAPACITY=240`) · NEW T-552 footnote sibling k :44-50 (defect-completion, no-new-ADR, MUSÍ menovať :93/:94 ako pre-T-529 T-205/T-208 point-in-time bound-out) · rate_limiter.py:11-12 docstring. **Nepísať do :93/:94** (L-027 false-attribution — sú v `## Follow-up tasks`, viazané na pôvodný T-205/T-208 scope, `market` zaviedol neskorší T-529; forward correction nesie footnote #5). Over §11.4 existujúci T-548-footnote shape a mirror ho.
4. **Post-edit re-grep (scope = `ADR-0003 + BRIEF + packages/ + services/`, NON-test):** assert zero residual `[Tt]hree bucket` / `orders.*positions.*ip.global` mimo bound-out :93/:94 (tie nesené footnotom #5). Pozor: aktuálny grep nájde 6 bucket-count site (`:12`, `:16`, `:34`, `:49`, `:59`, `:63` + docstring) — všetky musia byť po edite "Four"/4-tuple konzistentné okrem :93/:94. `:16` ("4-bullet specification") sa NETÝKA bucket count (odkazuje na 4 §11.4 bullety) — nemeniť.
5. **Testy v tom istom commite (L-034/L-015):** test_rate_limiter.py `_make_limiter` helper (sig defaults `market_rate=120.0`/`market_capacity=240.0` + pass do ctor) + `test_constructor_accepts_settings_kwargs_per_adr_0003` (`assert limiter._params["market"]==(120.0,240.0)`, kwarg-count komentár "8…(3 pairs)" → "10…(4 pairs)") + NEW `test_acquire_market_group_debits_market_bucket` (L-033 append AŽ ZA celým zrkadleným originálom — read po next `def`/EOF pred vložením). test_rate_limiter_integration.py `_make_limiter:63` + inline:99 + inline:177 každý +`market_rate=120.0`/`market_capacity=240.0` — over že integration inline sites dostanú správny capacity literál (240.0), nie omylom rate (120.0). Env-gated, same commit.
6. **.env.example:** pokus o read; ak enumeruje `RATE_LIMIT_*` pridať `RATE_LIMIT_MARKET_RATE=120`/`_CAPACITY=240` same-commit; ak `.env*` deny pretrvá → surface operátorovi (no silent-skip).
7. **Close (L-027):** F6 10/10→11/11 + T-552 `- [x]` v SAMOSTATNOM `chore(tasks)` commite pri close (nie pre-written). Žiadny push (operator CI-minute gate). math-validator Gate-4: diff sa dotýka `services/execution/` (config.py+main.py) → trigger fires; `packages/exchange/rate_limiter.py` je mimo math scope; očakávaj `VERIFIED — out of scope` (konštanty 120/240 sú config tier-mirrors, nie odvodená matematika).
