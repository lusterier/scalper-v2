# T-553 — provision the missing `orders.dlq.>` JetStream stream (T-216a DLQ shipped without its stream)

**Status:** Gate-1 APPROVE (plan-reviewer pass 2/2; pass-1 BLOCKER `streams.yaml` 3-site under-enumeration + CONCERN §8.2:1402 `deadletter.<stream>` unclassified — both resolved via decouple option-b + grep-verified bound-out).
**Phase:** F6 numbered. Triage item **B** (operator-approved triage sequence B→A→C→T-551; "Otvor B teraz"). ADR-0015 §70-81 decision-C standing basis (governance as T-545..T-552). §0 one-task. F6 11/11→12/12 + entry at THIS close per L-027 (NOT pre-written; mirror T-545..T-552). Dev task — Gate-1 + drift-checker RUN (~45 LOC, multi-site L-026) + Gate-3 + math-validator Gate-4 OUT-of-scope (new test under `packages/bus/tests/`; NO Gate-4-path file).

## Bug (production-surfaced, T-552 demo-arming ops retry 2026-05-18)

First real demo order created on Bybit demo (`POST /v5/order/create 200 OK`); post-fill cascade incl. `bus_publish_failed subject=orders.dlq.demo error="nats: no response from stream"` + `execution.dlq_publish_failed`. The order-request handler's DLQ safety-net (fallback for ANY failed order-request) itself fails — no JetStream stream captures `orders.dlq.>`.

Root cause infra-verified: `placement.py:372 await bus.publish(subject_for_orders_dlq(bot_id), envelope)` (subject `orders.dlq.<bot_id>`, `packages/bus/schemas/orders.py:67-69`, T-216a OQ-3/OQ-8). `bus.publish` = JetStream publish (`packages/bus/client.py:141`); JS publish to a subject with no capturing stream → "no response from stream". `infra/nats/streams/orders.json` `ORDERS` `subjects:["orders.requests.>","orders.events.>"]` — NO `orders.dlq.>`; BRIEF §8.2:1233 spec identical; nats-init logs confirm 8 streams, none DLQ. **T-216a designed the DLQ subject + publish path (OQ-3: "DLQ on missing adapter"; OQ-8: get_fill_price None×3 → FillPriceUnresolvedError → "upstream handler … DLQs to orders.dlq.<bot_id>") but neither §8.2 NOR `infra/nats/streams/` was extended to capture it** — a lockstep omission identical in shape to T-529→T-552. Latent until first real order-request failure (paper/never-run-live never exercised DLQ; `services/execution/tests/test_placement.py` mocks `bus.publish` → missing stream never caught, L-021/L-008 class).

## Operator-resolved OQ (baked in)

**Dedicated `ORDERS_DLQ` stream** (operator decision; rejected: extend ORDERS subjects). A DLQ is a failure-forensics record the operator must inspect — audit-grade, NOT live order flow. §8.2 forensic streams (AUDIT 365d, TRADING_EVENTS 365d) long retention; live-flow (ORDERS 30d) short. ORDERS' `duplicate_window` 30s is H-003-non-idempotent-market-order-retry-specific — inappropriate for DLQ. Dedicated stream = clean forensic lifecycle + clear operator model.

## Decouple decision (pass-1 BLOCKER resolution — option b)

The `streams.yaml`→`streams/*.json` doc-drift is **pre-existing, NOT caused by B**, spans ≥4 cross-corpus sites. Claim-correction/audit concern = **T-519 audit-task pattern**. Per §0 (out-of-scope cruft → backlog, not absorbed) + operator minimum-ceremony: **B does NOT touch any `streams.yaml` claim at any site.** B amends ONLY the §8.2 streams-LIST block (adds `ORDERS_DLQ` entry) — orthogonal to the §8.2:1222 "where-defined" prose. The ADR-0001 footnote is NEW text about ORDERS_DLQ provisioning ONLY (does not reference/edit the ADR-0001:27 `streams.yaml` line). A **NEW F6+ doc-audit backlog item** is recorded at B's chore(tasks) close (T-519 pattern; site-list enumerated via broad-stem grep AT TICKET-EXECUTION TIME, NOT a frozen count — `grep streams.yaml` yields ≥4 non-test hits incl. `docs/plans/T-110b.md:66`).

## §8.2:1402 `deadletter.<stream>` classification (pass-1 CONCERN — BOUND-OUT, grep-verified)

§8.2:1402 ("after `max_deliver` retries → `deadletter.<stream>` + alert") is a **distinct, ZERO-code-impl server-side consumer-redelivery DLQ convention** (`grep -rniE "deadletter|max_deliver" packages/services/infra non-test` = NONE). NOT B's mechanism (T-216a app-level handler-explicit `orders.dlq.<bot>`). Per H-003/T-216a OQ-3 the orders path *explicitly forbids* max_deliver-redelivery ("DLQ would imply automatic retry-from-replay which H-003 forbids") → `deadletter.ORDERS` is inapplicable to orders anyway. **Bound-out**: distinct + unimplemented + H-003-inapplicable; reconciling its stale status folds into the SAME new F6+ doc-audit ticket (NOT B; §0 anti-scope-creep; identical to the ADR-0003:96/§8.2:1278 pre-flight-check bound-out).

## Fix

1. NEW `infra/nats/streams/orders_dlq.json` — copy `orders.json` structural template; `max_age` (`31536000000000000`) + numeric-field forms VERBATIM from `infra/nats/streams/audit.json` (the §8.2 365d stream — do NOT hand-type the ns literal): `{"name":"ORDERS_DLQ","description":"Dead-letter for failed order-requests (orders.dlq.<bot_id>, T-216a OQ-3/OQ-8 application-level handler-explicit DLQ; NOT a max_deliver consumer — H-003 forbids order auto-retry). Forensic max_age 365d like AUDIT/TRADING_EVENTS. duplicate_window 0 (ORDERS' 30s is H-003-market-order-retry-specific, irrelevant for one-shot DLQ). Provisioned T-553 — T-216a shipped the subject+publish without the stream. Brief §8.2.","subjects":["orders.dlq.>"],"retention":"limits","storage":"file","num_replicas":1,"discard":"old","max_age":31536000000000000,"duplicate_window":0,"max_msgs":-1,"max_msgs_per_subject":-1,"max_bytes":-1,"max_msg_size":-1,"max_consumers":-1}`. bootstrap.sh auto-globs `"$STREAMS_DIR"/*.json` (verified) → NO bootstrap.sh edit; auto-applied next nats-init run.
2. `docs/CLAUDE_CODE_BRIEF.md` §8.2 streams-LIST block ONLY (~:1224-1262, §6.7 amendment per ADR-0012/T-518 precedent): add grouped with AUDIT/TRADING_EVENTS: `- name: ORDERS_DLQ` / `subjects: ["orders.dlq.>"]` / `retention: limits` / `max_age: 365d` / `replicas: 1`. Do NOT touch §8.2:1222 (decoupled) or §8.2:1402 (bound-out).
3. `docs/adr/0001-nats-jetstream.md` — append a T-553 footnote (T-548/T-552 footnote-precedent style; NEW text about ORDERS_DLQ provisioning ONLY; does NOT reference/edit `:27`/`:38`): "ORDERS_DLQ stream provisioned — T-216a shipped `orders.dlq.<bot>` subject + placement.py DLQ-publish path (OQ-3/OQ-8) without the §8.2/infra stream lockstep; safety-net failed on the first real order-request failure (T-552 demo arming). Dedicated forensic stream 365d (mirrors AUDIT/TRADING_EVENTS) per operator decision. Defect-completion, design unchanged; No new ADR — pre-governed ADR-0015 decision-C (F6 task T-553). Cross-ref: the ADR-0003:96/§8.2:1278 NATS-resource pre-flight-CHECK (would have detected this) remains a separate deferred sibling."
4. NEW §N4 data-config test (sibling of `packages/bus/tests/test_orders_schema.py`): load `infra/nats/streams/orders_dlq.json`, assert `name=="ORDERS_DLQ"`, `"orders.dlq.>" in subjects`, `max_age==31536000000000000`; assert `subject_for_orders_dlq("anybot")` (`"orders.dlq.anybot"`) is prefix-matched by `"orders.dlq.>"`. The pin that would have caught the bug. `services/execution/tests/test_placement.py` (mocks bus.publish) + `test_orders_schema.py` verify-stay-green, NOT rewritten.

## L-026 site-set closure (broad-stem grep `orders.dlq|ORDERS_DLQ|dlq|dead.letter` docs/+infra/+packages/+services/ non-test; `streams.yaml` deliberately NOT a B site per decouple)

IN-SCOPE same commit: NEW `infra/nats/streams/orders_dlq.json` · BRIEF §8.2 streams-LIST entry · ADR-0001 T-553 DLQ footnote · NEW §N4 data test. BOUND-OUT: §8.2:1402 `deadletter.<stream>` (distinct unimplemented convention → F6+ doc-audit) · ADR-0003:96/§8.2:1278 pre-flight-CHECK (B provisions; check is separate deferred sibling) · `streams.yaml` ≥4-site drift (pre-existing, decoupled → F6+ doc-audit, T-519 pattern, §0 anti-absorb) · `infra/nats/bootstrap.sh` (auto-glob, topology-agnostic) · `test_placement.py`/`test_orders_schema.py` (verify-stay-green). WG MUST post-edit re-grep, zero unclassified residual.

## Hand verification (max_age literal)

365 d in ns = 365 × 86400 × 1_000_000_000 = **31_536_000_000_000_000 ns**. Reviewer-verified == `infra/nats/streams/audit.json` `"max_age": 31536000000000000`. WG: copy verbatim from audit.json (mismatch = config error). `duplicate_window:0` deliberate (ORDERS' 30s is H-003-specific).

## Scope / hazards / phase

~45 LOC (new JSON ~14 + §8.2 ~6 + ADR-0001 footnote ~6 + data test ~15), << §0.3 400. §20: no hazard regressed — FIXES the failure-safety-net (failed order-request → durable forensic DLQ record vs silent loss). H-003 (non-idempotent market order, zero-retry) unaffected — DLQ is NOT a max_deliver auto-retry consumer (T-216a OQ-3; this task provisions only the stream, adds no consumer). §N8 no DB migration. §N9 N/A (topology config = the §8.2 source-of-truth itself). §N2 improved. No new dependency. Phase-gate L-029: F6 unlocked (ADR-0015 §6.7); operator-directed post-MVP decision-C; triage-approved B; promote numbered F6 T-553; counter 11/11→12/12 at close (L-027, not pre-written) + the new F6+ doc-audit backlog item. NO new standalone ADR (ADR-0001 footnote, T-548/T-552 defect-completion precedent — reviewer-confirmed sound).

## Gate plan

plan-reviewer APPROVE ✅ pass-2 → drift-checker RUN (multi-site L-026; checkpoint after JSON+§8.2, after test green) → brief-reviewer Gate-3 (verify L-026 set + §8.2 streams-list-only + ADR-0001 footnote-only + max_age verbatim + decouple/bound-out adherence) → math-validator Gate-4 OUT-of-scope (no Gate-4-path file) → `fix(T-553)` + separate `chore(tasks)` F6 11/11→12/12 + NEW F6+ doc-audit backlog item (L-027). NO push (operator CI-minute gate; unpushed: T-549b+T-550+T-552 fix+chore = 6 + this). Post-merge ops (NOT this gate): recreate `nats-init` → `nats stream info ORDERS_DLQ` confirms created. B does NOT make a demo order succeed (A still blocks every order at get_fill_price); B's win = failed orders DLQ cleanly (de-noises A/C triage). A is the next triage task.

## Write-time guidance (plan-reviewer pass-2 verbatim — brief-reviewer Gate-3 verifies each)

1. NEW `infra/nats/streams/orders_dlq.json` — copy `infra/nats/streams/orders.json` as structural template; set name/description/subjects per Fix#1; copy `max_age` value (`31536000000000000`) AND the numeric-field forms VERBATIM from `infra/nats/streams/audit.json` — do NOT hand-type the ns literal. `duplicate_window:0` deliberate (state rationale in `description`). NO `bootstrap.sh` edit (auto-glob verified).
2. BRIEF §8.2 streams-LIST block ONLY (~:1224-1262): add `ORDERS_DLQ` (`subjects: ["orders.dlq.>"]`, `max_age: 365d`, `replicas: 1`) grouped with AUDIT/TRADING_EVENTS. Do NOT edit §8.2:1222 (`streams.yaml` prose — decoupled) nor §8.2:1402 (`deadletter.<stream>` — bound-out). ADR-0001: append T-553 DLQ footnote ONLY (new text; do not touch `:27` or `:38`).
3. NEW §N4 data test next to `packages/bus/tests/test_orders_schema.py`: load `infra/nats/streams/orders_dlq.json`; assert `name=="ORDERS_DLQ"`, `"orders.dlq.>" in subjects`, `max_age==31536000000000000`; assert `subject_for_orders_dlq("anybot")` is prefix-matched by `"orders.dlq.>"`. Verify `services/execution/tests/test_placement.py` + `test_orders_schema.py` stay green unchanged. Run full fast suite (sibling stream-config/test breakage check).
4. Post-edit broad-stem re-grep (`orders.dlq|ORDERS_DLQ|dlq|dead.letter|streams\.yaml` across docs/+infra/+packages/+services/ non-test): assert every hit is either in B's IN-SCOPE set OR an explicitly-bound item (§8.2:1402 / pre-flight-check / streams.yaml drift / bootstrap.sh / the 2 verify-green tests). Zero unclassified residual.
5. chore(tasks) at close (L-027, not pre-written): F6 11/11→12/12 + T-553 `[x]` + NEW F6+ doc-audit backlog item. **The backlog item's `streams.yaml` site-list must NOT hard-code "3 sites" — `grep streams.yaml` actually yields ≥4 non-test hits (BRIEF §8.2:1222, ADR-0001:27, packages/bus/client.py:387, AND docs/plans/T-110b.md:66; plus T-216a/T-216c plan refs for the `orders.dlq`/§8.2:1402 leg). Word the backlog text to enumerate the set via a broad-stem grep AT TICKET-EXECUTION TIME (T-519 audit pattern is corpus-wide), not a frozen count — otherwise the future ticket re-incarnates the L-026 under-enumeration one layer down.** NO push.
6. math-validator Gate-4: new test under `packages/bus/tests/` → no Gate-4-path file → expect `VERIFIED — out of scope`. If test lands elsewhere, re-confirm path is non-Gate-4.
