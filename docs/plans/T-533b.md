# T-533b — SPLIT RECORD (recursive L-007 split)

**Status:** SPLIT 2026-05-16 (operator OQ-1=A at T-533b plan-stage; ADR-0011/TASKS-pre-flagged L-007/L-014/L-016 recursive-split-watch). Not an implementable plan — records the recursive split; the active leaf plan is `docs/plans/T-533b1.md`.

## Why recursive split (L-007)

T-533b (dual-write wiring: every state-transition WRITE site also writes `trades.lifecycle_state`) spans **5 execution-service modules** — `placement_persist.py` (insert_trade→OPEN, emergency_close→CLOSED, placement-fail→FAILED), `dispatcher.py` (partial→PARTIALLY_CLOSED, close-flow→CLOSING/CLOSED), `lifecycle.py` (BE→BREAKEVEN_SET, trail→TRAILING_ACTIVE, TP→TP_HIT), `reconcile.py` + `restart.py` (orphan→ORPHANED, reconcile_gone→RECONCILED). TASKS.md:237 + the T-533a plan + ADR-0011:54/92 pre-flagged T-533/T-533b for L-007/L-014/L-016 recursive-split-watch (cross-module wiring across ~6 modules). Per L-007 active control, a pre-emptive split at plan-stage is far cheaper than an operationally-expensive mid-write split (T-534b→{b1,b2} / T-525a→{a1,a2} / T-533→{a,b} precedent). The wiring leaf alone touches every state-transition call site → systematically over the §0.3 cap as one task.

## Operator OQ answers (T-533b plan-stage 2026-05-16, all default A — baked into T-533b1 / T-533b2)

- **OQ-1=A — recursive L-007 split** into **T-533b1** (the `update_trade_lifecycle_state` write primitive, NO caller — foundation, independently testable) + **T-533b2** (wire it into all transition sites, blocked-by T-533b1; T-533b2 may itself recursive-split by module-group at its own plan-stage if it trips §0.3 — decided then, mirror T-534b2-being-the-consumer-after-T-534b1). Mirrors T-534b→{b1=helper, b2=consumer} EXACTLY.
- **OQ-2 (baked, implied by OQ-1=A)** — write mechanism = a NEW dedicated `update_trade_lifecycle_state` helper (additive: every legacy write helper — `insert_trade`/`update_trade_close`/`update_position_state_*` — stays byte-unchanged; the helper is called ALONGSIDE the unchanged legacy write at each site). NOT folding `lifecycle_state` into existing helpers' SET clauses (that would touch capital-path legacy writers — weaker OQ-1=A additive guarantee, larger blast radius, regression risk).
- **OQ-3=A — direct enum literal per site** (T-533b2): each forward FSM transition site writes its KNOWN `TradeLifecycleState.X` directly (placement→OPEN, BE-trigger→BREAKEVEN_SET, trail→TRAILING_ACTIVE, close→CLOSED, reconcile_gone→RECONCILED, error→FAILED). `derive_lifecycle_state` (T-533a) stays backfill/reconstruction-only — NOT reused for forward writes (the site knows its intent; reconstructing legacy inputs would be fragile + redundant).
- **OQ-4=A — post-trades-row states only** (T-533b2): the `trades` row is born at `insert_trade` (status='open'). T-533b2 wires only states with a live trades row: `OPEN` / `PARTIALLY_CLOSED` / `TP_HIT` / `BREAKEVEN_SET` / `TRAILING_ACTIVE` / `CLOSING` / `CLOSED` / `FAILED` / `RECONCILED` / `ORPHANED`. Pre-row transients `SIGNAL_RECEIVED` / `ORDER_REQUESTED` / `ORDER_PLACED` have no `trades` row → enum-defined but **deliberately not persisted** (no carrier; no consumer — §0.8; a future signals/orders-lifecycle task if one surfaces). `CLOSING`/`TP_HIT` wired only where a clean distinct site exists, else folded into `CLOSED`/`PARTIALLY_CLOSED` — the T-533b2 plan's site→state mapping table decides.
- Still **additive-observable** throughout (OQ-1=A from T-533: legacy 4 columns authoritative; `lifecycle_state` observability-only; zero behavior change; 0 §20 regression; no new H-NNN; no Settings).

## Split (mechanical — write-primitive vs call-site wiring; mirror T-534b→{b1,b2})

- **T-533b1** (active leaf — `docs/plans/T-533b1.md`): NEW `update_trade_lifecycle_state(conn, *, trade_id, state: TradeLifecycleState) -> None` in `packages/db/queries/execution.py` (the trades-writer module — beside `insert_trade`/`update_trade_close`; NOT `lifecycle.py` which T-533a established as pure/no-I/O). `UPDATE trades SET lifecycle_state = $1 WHERE id = $2` (H-018 PK-only, mirror `update_trade_close`). **UNDECORATED** (mirror sibling `update_trade_close`/`update_trade_fees_incremental`/`update_position_state_sl` — pure PK-`SET`-column UPDATE helpers carry NO marker; only INSERT/place-order non-idempotent writes are `@non_idempotent`; §N3 satisfied by the idempotent-by-construction PK-SET convention — T-534b1 plan-vs-reality precedent class). NO caller (T-533b2 first consumer — T-530/T-534b1/T-533a-shaped no-consumer leaf). ~15 src + mock unit test (SQL-string pin + bind args + marker-mirror pin).
- **T-533b2** (blocked-by T-533b1): wire `update_trade_lifecycle_state(state=TradeLifecycleState.X)` into every post-trades-row transition site (direct enum literal, OQ-3=A/OQ-4=A) alongside the unchanged legacy writes. Own plan-reviewer Gate-1 cycle when T-533b1 merges; **L-007/L-014/L-016 recursive-split-watch** (may pre-emptive-split by module-group — placement / dispatcher+lifecycle / reconcile — mirror T-534b2-after-T-534b1).

## Sequence

T-533b1 plan-reviewer Gate-1 → implement → 4 gates → merge. Then T-533b2 (now unblocked) gets its own consolidated plan + Gate-1 cycle (recursive-split decision there). Mirrors T-534b1 → T-534b2 / T-533a → T-533b.
