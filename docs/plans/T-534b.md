# T-534b — SPLIT RECORD (recursive L-007/L-022 split)

**Status:** SPLIT 2026-05-15 (plan-reviewer Gate-1 on the consolidated T-534b plan → REVISE: §0.3/L-022 cap-proximity). Not an implementable plan — this file records the recursive split; the active leaf plan is `docs/plans/T-534b1.md`.

## Why split

T-534b (SL watchdog APScheduler tick + `emergency_close_tracked_position` shared helper + formal H-028 §20 allocation) was drafted as a single consolidated plan. plan-reviewer Gate-1 honest L-022 decomposition: ~270 src logic + WG-mandated docstring inflation → **contingency-adjusted ~320–410 src**, AT/NEAR the §0.3 400 cap. Same profile as T-525a (L-022 first-application caught a ~436 pre-coding cap-breach → recursive split T-525a→{a1,a2}). L-022 triggers all present: WG-heavy (≥4 WG items), FSM-execution profile (ADR-0011:54 pre-flagged T-534 for L-007/L-014/L-016 split-watch), first-of-its-kind scaffold (`emergency_close_tracked_position` is the first "close-EXISTING-tracked-position" helper vs placement-time `emergency_close`). Empirical: T-534a planned "~10 src + docstring lines" → reality 42 src (plan-faithful WG-expansion, drift ON TRACK).

## Split (mechanical — infra/primitive vs consumer/gate)

Recursive L-007 split within the operator-approved T-534 split mechanism (T-534 OQ-1=A). Boundary is mechanical (infra/primitive leaf + consumer/gate leaf), validated by T-524/T-525a/T-526 precedent — **no open architectural decision, no new ADR/operator-OQ** (mirror T-525a SPLIT RECORD; plan-reviewer Gate-1 explicit).

- **T-534b1** (active leaf — `docs/plans/T-534b1.md`): `emergency_close_tracked_position` shared helper in `services/execution/app/placement_persist.py`. Flatten reduce_only + tx close of the EXISTING tracked trade (`update_trade_close` close_reason='emergency' + `delete_position_state`) + audit + emit `OrderClosed`. `@non_idempotent`. **NO caller** (T-534b2 is first consumer — T-530/T-534a-shaped no-caller leaf, independently testable). ~110 src.
- **T-534b2** (blocked-by T-534b1): SL watchdog tick (`sl_watchdog.py`) + 2 Settings fields + main.py 3rd `scheduler.add_job` + L-015-generalized sibling test_app_factory fix + **formal H-028 §20 catalog allocation** (mirror T-525a1 H-027 mechanism, §0.8). Consumes the shipped T-534b1 helper + T-534a `get_positions().sl_price`. ~210 src. Gets its own plan-reviewer Gate-1 cycle when T-534b1 merges (mirror T-525a1→T-525a2 sequence).

## Operator OQ answers (resolved, baked into T-534b1 / T-534b2 plans)

From T-534 split-stage: OQ-3=A H-028 = consecutive-threshold N=3, in-memory per (bot_id,symbol), transient `(NetworkTimeout|RateLimitError|AuthError)` → skip tick no-increment, SL-found → reset; configurable §N9; restart-safe via T-221; OQ-4=A live/testnet-only, paper skipped (`AdapterPoolResult.paper_bot_ids`, mirror H-031). T-534b plan-stage: OQ-A=A new shared helper (NOT reconstruct synthetic `emergency_close` args — placement-time signature mismatch; NOT restart.py startup-fail-fast machinery); OQ-B=A watchdog acts ONLY on size>0 + sl_price None/≤0 after N consecutive — `size==0` flat / position-absent → defer-log (T-221 reconcile territory, watchdog does NOT reconcile, §0 scope-discipline); OQ-C=A `trades.close_reason='emergency'` (reuse H-004) + distinct `trading_events.event_type='sl_watchdog_emergency_close'` + distinct structured log key for audit traceability.

## Sequence

T-534b1 plan-reviewer Gate-1 → implement → 4 gates → merge. Then T-534b2 (now unblocked) gets its own consolidated plan + Gate-1 cycle. Mirrors T-525a1 → T-525a2.
