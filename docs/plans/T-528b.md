# T-528b — risk-per-SL cross-service wiring (consumer leaf)

**Status:** APPROVED (plan-reviewer Gate-1 single-pass APPROVE 2026-05-16; 0 BLOCKER / 0 CONCERN; all technical claims source-verified). Consumer leaf of the operator-approved L-007 split T-528→{a,b} (split-record `docs/plans/T-528.md`; foundation leaf T-528a shipped & CI-green at master 1dce1fa). Mirrors the shipped T-527b2b sequence EXACTLY (foundation→consumer). Single task — NO further L-007 split. ADR-0013 (Accepted; §Consequences anticipates T-528 verbatim) governs — NO new ADR.

## Purpose
Wire the shipped T-528a risk-per-SL config + `compute_qty_from_risk` cross-service. `SizingSpecForWire` gains `method`/`risk_pct` (additive); strategy-engine producer maps them; the execution placement seam dispatches on `request.sizing.method`. Completes the risk-per-SL feature end-to-end.

## Operator OQ answers (baked)
- OQ-1=A (2-leaf split) / OQ-2=A (additive `method` field) / OQ-3=A (risk-per-SL ignores score_multipliers) — baked from the T-528 split-record `docs/plans/T-528.md`.
- OQ-B1=A (T-528b plan-stage 2026-05-16, operator-answered, recommended) — the risk-per-SL `total_equity<=0`→None skip reuses the shipped `signals_skipped_sizing{reason="sub_lowest_tier"}` + `execution.signal_skipped_sub_lowest_tier` log VERBATIM (split-record-consistent, minimal, one skip path; deliberate shared low-cardinality observability label). Rejected alt: distinct `sub_capital` reason (operator rejected as a split-record-"verbatim" deviation).

## Scope / files (3 src + tests)

**1. `packages/bus/payloads.py` — `SizingSpecForWire` (additive)**
Add `method: Literal["tier","risk_per_sl"] = "tier"` + `risk_pct: Decimal | None = None` (plain defaults — NO `Field`/`gt=0`; the wire is thin transport, the producer maps from an already-validated `SizingSection` per the shipped docstring; mirror the T-527b2b thin-transport posture; `Literal`@31/`Decimal`@30 already imported, `Field` deliberately NOT added). `tiers`/`score_multipliers` stay REQUIRED (producer always passes them explicitly — empty list/dict for risk_per_sl; do NOT switch to default_factory — that was a T-528a-SizingSection need for YAML omission, NOT a wire need). Additive optional-with-default → `OrderRequest.schema_version` stays "1.0" (mirror the T-527a `score` / T-527b2b `sizing` additive precedent; old payloads validate). Extend the SizingSpecForWire docstring (method discriminator; risk_pct; SizingSection is the validation authority).

**2. `services/strategy_engine/app/consumer.py` — `_publish_order_request`@436-446**
The shipped `SizingSpecForWire(tiers=[...], score_multipliers=..., max_notional_per_symbol=...)` gains `method=bot_config.sizing.method, risk_pct=bot_config.sizing.risk_pct`. For a risk_per_sl bot `bot_config.sizing.tiers==[]`/`score_multipliers=={}` (T-528a) → the existing comprehension/dict yield `[]`/`{}` carried faithfully. `bot_config.sizing is None` → `sizing_payload=None` unchanged.

**3. `services/execution/app/placement.py` — the seam method-dispatch@169-228**
Import `compute_qty_from_risk` (extend line 70 `from packages.sizing import compute_qty_from_sizing`). In the seam, after the fetch try/except (~:184), before `working_qty=computed_qty`, dispatch on `request.sizing.method`:
- `method == "risk_per_sl"`: **defensive narrowing guard (L-019)** — `if request.sizing.risk_pct is None:` → the verbatim `compute_error` skip (log + `signals_skipped_sizing{reason="compute_error"}` + return); satisfies mypy `Decimal|None`→`Decimal` narrowing AND is the defensive external-input guard (the wire does NOT re-validate the method↔risk_pct coupling — SizingSection is producer-side authority). Then `try: computed_qty = compute_qty_from_risk(total_equity=balance.total_equity, mark_price=mark_price, sl_pct=request.sl_pct, risk_pct=request.sizing.risk_pct, max_notional_per_symbol=request.sizing.max_notional_per_symbol, symbol=request.symbol) except ValueError → the verbatim compute_error skip`.
- `else` (tier — `method=="tier"`/default): the shipped `tiers=[SizingTier(...)...]` + `compute_qty_from_sizing(...)` + its `except ValueError → compute_error` BYTE-UNCHANGED.
- `if computed_qty is None:` → the shipped `sub_lowest_tier` skip path **reused verbatim for BOTH methods** (OQ-B1=A — risk-per-SL `total_equity<=0`→None maps to `reason="sub_lowest_tier"` + `execution.signal_skipped_sub_lowest_tier`; deliberate shared label, documented in-code).
- `working_qty=computed_qty` then the shipped `quantize_qty`@228 (sole pre-quantize change-point — single-rounding, no second round in the risk branch).
Backward-compat: `request.sizing is None` OR `method=="tier"` → byte-unchanged shipped T-527b2b tier path.

NO new H-NNN (skip-before-place = no capital at risk, mirror T-527b2b; §0.8). 0 §20 regression.

## §N invariants
- §N1: all Decimal; `request.sl_pct:Decimal` non-Optional (orders.py:101); `request.sizing.risk_pct` Decimal|None narrowed defensively before the Decimal-typed compute call; no float; wire round-trip preserves Decimal (the T-527b2b model_dump(mode=json)→str→model_validate pin extended to risk_pct).
- §N4: the financial math is shipped+VERIFIED in T-528a (`compute_qty_from_risk`); T-528b is wiring → tests assert the DISPATCH routing + the defensive risk_pct-None guard + backward-compat + wire round-trip; NO new/re-derived financial math.
- §N5: ≥80% via dispatch tests + L-015 sibling preservation; the §17.2 per-module gate stays unwired repo-wide (the T-527b2a-filed T-F5+ ticket owns it — NOT re-litigated, NOT N5-by-omission).
- §N9: no hardcoded values (risk_pct flows from YAML via T-528a). §N6/N7 unaffected.

## Gate-4 — COMMITTED IN scope, MODERATE
`services/execution/` is LITERALLY in the CLAUDE.md trigger list (mirror T-527b2b's disposition exactly — no residual optionality, T-527b1 open-disposition lesson applied). MODERATE: the diff is wiring/dispatch (the heavy risk-per-SL arithmetic was VERIFIED HEAVY in T-528a). math-validator verifies: dispatch routes correctly (risk_per_sl→compute_qty_from_risk, tier→compute_qty_from_sizing), no Decimal→float on the wire round-trip, the defensive risk_pct-None narrowing, `working_qty` remains the sole pre-quantize change-point.

## Hand verification (dispatch-level; arithmetic is T-528a-VERIFIED)
- risk_per_sl `SizingSpecForWire` round-trips model_dump(mode="json")→str→model_validate bit-identically: method=="risk_per_sl", risk_pct==Decimal("0.01"), str(risk_pct)=="0.01" (no binary-float on the wire — mirror the T-527b2b first-Decimal-on-wire pin).
- Dispatch: method=="risk_per_sl" → compute_qty_from_risk called with sl_pct=request.sl_pct, risk_pct=request.sizing.risk_pct; method=="tier"/default → compute_qty_from_sizing (byte-unchanged). Both-sides assertion (L-017 — chosen fn IS called AND the other is NOT).
- Defensive guard: method=="risk_per_sl" + risk_pct=None → compute_error skip, compute_qty_from_risk NOT called.
- OQ-B1=A: risk_per_sl + total_equity=0 → compute_qty_from_risk→None → the verbatim reason="sub_lowest_tier" skip.
- Backward-compat: sizing=None → static request.qty; tier wire (no method:) → method=="tier" default → compute_qty_from_sizing byte-unchanged (existing T-527b2b placement tests pass byte-identically — L-015).

## Test strategy
- `packages/bus/tests/test_payloads.py`: SizingSpecForWire method/risk_pct round-trip + method defaults "tier" absent + risk_pct Decimal-on-wire str-pin; existing tests byte-identical (additive defaults).
- `packages/bus/tests/test_orders_schema.py`: OrderRequest w/ risk_per_sl sizing round-trips; schema_version "1.0"; old-payload-without-method backward-compat.
- `services/strategy_engine/tests/test_consumer.py`: producer maps risk_per_sl SizingSection→wire method/risk_pct (model_copy(update={"sizing": SizingSection(method="risk_per_sl",risk_pct=...,max_notional_per_symbol=...)}) — mirror T-527b2b sizing producer test); tier bot byte-preserved.
- `services/execution/tests/test_placement.py`: dispatch matrix — risk_per_sl→compute_qty_from_risk (spy, both-sides L-017), tier→compute_qty_from_sizing (byte-preserved), risk_pct-None defensive→compute_error, risk total_equity=0→sub_lowest_tier (OQ-B1=A), sizing=None static; shipped T-527b2b placement sizing tests byte-identical.

## LOC / §0.3 / L-007 / L-024
src ~50-90 (payloads +~6+docstring; consumer +2 ctor args; placement +~25-35 dispatch+guard); L-022-adjusted upper ~110-120. tests ~120-170. ≪ §0.3 400. **Single task — NO L-007 split**: much smaller than T-527b2b (NO make_per_bot_handler signature change → minimal L-015 — the new wire fields are additive-with-defaults so no caller breaks; method-defaults-"tier" preserves shipped placement/consumer/payloads tests byte-identically). **L-024 active control applied** (the T-527b2b backtest.py scripts/-caller miss): SizingSpecForWire additive-with-defaults (no constructor breaks); compute_qty_from_risk new (only placement seam calls it — verified clean at review: only placement.py:189 + consumer.py:438 non-test callers, scripts/ clean); make_per_bot_handler signature UNCHANGED → scripts/backtest.py NOT impacted; still grep ALL SizingSpecForWire(/compute_qty_from_ callers incl. scripts/ pre-commit + project-wide-scope mypy. L-022: wiring task, not Hand-verification-docstring-heavy (mirror T-527b2b ~1.0× on-target).

## Open questions
None. OQ-2/3=A baked from the split-record; OQ-B1=A operator-answered 2026-05-16; ADR-0013 governs (no new ADR); single-task (no L-007); Gate-4 COMMITTED MODERATE baked (no "to confirm" residue).

## Write-time guidance (must be addressed during implementation; plan-reviewer verbatim)
1. `SizingSpecForWire` `method: Literal["tier","risk_per_sl"] = "tier"` + `risk_pct: Decimal | None = None` ADDITIVE with plain defaults — NO `Field`/`gt=0` (wire is thin transport; `SizingSection` is producer-side validation authority — payloads.py:31 already imports `Literal`/`Decimal`, do NOT add `Field`). `tiers`/`score_multipliers`/`max_notional_per_symbol` stay REQUIRED (NOT default_factory — that was a T-528a SizingSection-only need; producer always passes them explicitly, `[]`/`{}` for risk_per_sl). `OrderRequest.schema_version` stays "1.0" (mirror T-527a `score` / T-527b2b `sizing` additive precedent — old payloads validate). Extend the SizingSpecForWire docstring (method discriminator; risk_pct; SizingSection = validation authority).
2. Placement risk_per_sl branch MUST have the defensive `if request.sizing.risk_pct is None:` → verbatim `compute_error` skip (log + `signals_skipped_sizing{reason="compute_error"}` + return) BEFORE `compute_qty_from_risk` (L-019 — external-input guard: the wire does NOT re-validate the method↔risk_pct coupling; this also satisfies mypy `Decimal|None`→`Decimal` narrowing). Then `compute_qty_from_risk(total_equity=balance.total_equity, mark_price=mark_price, sl_pct=request.sl_pct, risk_pct=request.sizing.risk_pct, max_notional_per_symbol=request.sizing.max_notional_per_symbol, symbol=request.symbol)` (keyword-only — matches shipped signature compute.py:183-191) in `try: ... except ValueError → verbatim compute_error skip`.
3. `else` (tier — `method=="tier"`/default) path = shipped `tiers=[SizingTier(...)...]` + `compute_qty_from_sizing(...)` + its `except ValueError → compute_error` BYTE-UNCHANGED. `request.sizing is None` static `request.qty` path BYTE-UNCHANGED. `if computed_qty is None:` → shipped `sub_lowest_tier` skip path reused VERBATIM for BOTH methods (OQ-B1=A operator-answered 2026-05-16; distinct `sub_capital` reason explicitly rejected by operator) — document the shared-label decision in-code (note the observability trade-off: alerting/dashboards cannot distinguish tier-sub-lowest from risk-per-SL-zero-capital without correlating `sizing.method`). `working_qty=computed_qty` then shipped `quantize_qty` stays the SOLE pre-quantize change-point (no second round in the risk branch).
4. Consumer `_publish_order_request`: shipped `SizingSpecForWire(tiers=[...], score_multipliers=..., max_notional_per_symbol=...)` gains `method=bot_config.sizing.method, risk_pct=bot_config.sizing.risk_pct`; `bot_config.sizing is None` → `sizing_payload=None` UNCHANGED; risk_per_sl bot's `tiers==[]`/`score_multipliers=={}` carried faithfully by the existing comprehension/dict.
5. Tests: dispatch matrix BOTH-SIDES (L-017 — chosen fn IS called AND the other is NOT): risk_per_sl→`compute_qty_from_risk` spy, tier→`compute_qty_from_sizing` byte-preserved, risk_pct-None→`compute_error` (compute_qty_from_risk NOT called), risk_per_sl+total_equity=0→`sub_lowest_tier` (OQ-B1=A), sizing=None→static. Wire round-trip Decimal str-pin (`str(risk_pct)=="0.01"`, no binary-float — extend T-527b2b model_dump(mode=json)→str→model_validate pin). Enumerate & re-run byte-identically the existing SizingSpecForWire/OrderRequest/consumer/placement T-527b2b sibling tests (L-015). Before `git commit` run mypy over the STAGED set INCLUDING tests (`uv run mypy $(git diff --cached --name-only --diff-filter=ACM -- '*.py')` — L-024); typed mock `.await_args`/`.call_args` → `assert ... is not None` narrowing, NOT inline `# type: ignore` (L-023/L-024). Grep ALL `SizingSpecForWire(`/`compute_qty_from_` callers incl. `scripts/` pre-commit (verified clean at review: only placement.py:189 + consumer.py:438) + run project-wide-scope mypy (L-024 — T-527b2b backtest.py lesson). Gate-4 COMMITTED IN-scope MODERATE (`services/execution/` literally in trigger list) — no residual optionality.
