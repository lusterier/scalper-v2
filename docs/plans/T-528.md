# T-528 — RECURSIVE SPLIT RECORD (L-007)

**Status:** SPLIT 2026-05-16 (operator OQ-1=A at T-528 plan-stage; ADR-0011 pre-flagged L-007/L-014 split-watch — "sizing.method discriminator dispatcher"; structurally identical to the split T-527b2). Not an implementable plan — records the recursive split; the active leaf plan is `docs/plans/T-528a.md`. T-528b plan written when T-528a merges.

## Why split (L-007)

T-528 "risk-per-SL sizing" (ADR-0011 position-sizing cluster — the `sizing.method`-discriminated alternative to the T-527 tier ladder) was inventoried across **two distinct concerns** structurally identical to the already-split T-527b2: (a) config + pure compute — the `SizingSection.method`/`risk_pct` discriminator (with the §N10 method-conditional validator that byte-preserves the shipped T-527 tier path) + a NEW `compute_qty_from_risk` pure Decimal function (§N4 financial-math TDD + `## Hand verification`, no I/O, independently unit-testable) — and (b) the cross-service wiring (`SizingSpecForWire` `method`/`risk_pct` extension + strategy-engine producer mapping + the execution-service `placement.py` seam method-dispatch + the `except ValueError` skip-before-place path). T-527b2 at ~432 src needed a split; T-528's full inventory similarly exceeds the §0.3 400 cap once §N4 Hand-verification docstrings are counted (the T-527b2a pure-math leaf alone ran ~189 src at ~1.8× docstring-density, L-022 point 30). Per L-007 active control, a pre-emptive split at plan-stage is far cheaper than a mid-write cap breach (T-527→{a,b} / T-527b→{b1,b2} / T-527b2→{b2a,b2b} / T-533→{a,b}→{b1,b2} / T-534→{a,b}→{b1,b2} precedent — pure foundation leaf then consumer leaf).

## Operator OQ answers (T-528 plan-stage 2026-05-16, all recommended — baked into T-528a / T-528b)

- **OQ-1=A — 2-leaf recursive split** T-528 → **T-528a** (config discriminator + `compute_qty_from_risk` pure Decimal compute + §N4 Hand verification; `method:Literal["tier","risk_per_sl"]="tier"` + `risk_pct` on the EXISTING `SizingSection` with a method-conditional `_structural_guards` that byte-preserves the shipped tier accept/reject surface; NO consumer — foundation, independently testable; mirrors T-527b2a) + **T-528b** (the wiring: `SizingSpecForWire` gains `method`/`risk_pct`, additive — schema_version stays "1.0"; strategy-engine `_publish_order_request` maps `bot_config.sizing.method`/`risk_pct`→wire; `placement.py:_handle` seam dispatches on `request.sizing.method` — tier → `compute_qty_from_sizing` (shipped T-527b2b), risk_per_sl → `compute_qty_from_risk`; the `except ValueError` skip-before-`place_market_order` path reused verbatim from T-527b2b incl. `signals_skipped_sizing{reason=compute_error}`; blocked-by T-528a; Gate-4 IN scope — the real risk÷sl-distance capital path). Mirrors T-527b2→{b2a,b2b} EXACTLY.
- **OQ-2=A — additive `method` field on the EXISTING `SizingSection`** (NOT a pydantic discriminated-union refactor): `method: Literal["tier","risk_per_sl"] = "tier"` + `risk_pct: Decimal | None = Field(default=None, gt=0)`; `tiers`/`score_multipliers` gain `default_factory` SOLELY so a `method: risk_per_sl` YAML may omit them, with the method-conditional validator RESTORING the tier-path loud-rejection strictness (no silent weakening — §N10). Backward-compat: a bot with no `method:` → `"tier"` → existing shipped tier behavior byte-unchanged. Rejected alt B (discriminated union — a breaking refactor of the just-shipped T-527 `SizingSection` + every reference + `SizingSpecForWire` + `yaml_loader`).
- **OQ-3=A — risk-per-SL ignores `score_multipliers`** (deterministic risk model: `qty = risk_amount / sl_distance`; signal strength is already gated upstream in scoring — scaling `risk_pct` by score would double-count). `max_notional_per_symbol` cap ALWAYS applies (safety rail, both methods — baked from the ADR-0013/T-527 precedent, not a separate OQ). `score_multipliers` stays tier-only.
- Inherited (ADR-0013 Accepted — §Consequences anticipates T-528 verbatim: *"T-528 (risk-per-SL alternative, `sizing.method` discriminator) will inherit the same execution-service-resident, adapter-fed shape"*; **no new ADR**): compute home = execution-service placement seam (T-528b); balance via `get_account_balance().total_equity` (T-530, shipped); reference price via `get_mark_price` (T-527b1, shipped); SL distance fraction via `request.sl_pct` (already on `OrderRequest`, used post-fill at `placement.py:339 compute_sl_price`); sub-/zero-capital or unusable-SL → skip-before-`place_market_order` (T-528b reuses the T-527b2b skip seam verbatim — no order, no DLQ, no retry, log + Prom counter; no capital at risk).

## Split (mechanical — pure config+compute foundation vs cross-service wiring consumer; mirror T-527b2→{b2a,b2b})

- **T-528a** (active leaf — `docs/plans/T-528a.md`): `SizingSection` `method`/`risk_pct` + method-conditional `_structural_guards` (§N10 byte-preserve shipped tier path) + `_parse_sizing` REQUIRED `risk_pct` `_to_decimal` coercion (§N1 binary-float-artefact guard) + NEW `packages/sizing/compute.py:compute_qty_from_risk` (Decimal-only, single-rounding-point, guard-ordering mirrors `compute_qty_from_sizing`, NO score_multiplier per OQ-3=A, `cap_notional` reused) + `__all__` exports. §N4 TDD + `## Hand verification`. **Gate-4 math-validator IN scope, COMMITTED, HEAVY** (T-527b2a precedent — real Decimal sizing arithmetic feeding the T-528b capital path; T-530 non-exhaustive-path-list precedent). NO consumer (T-528b first). Plan-reviewer Gate-1 single-pass APPROVE 2026-05-16 (0 BLOCKER/CONCERN; 5 WG verbatim).
- **T-528b** (blocked-by T-528a): `SizingSpecForWire` additive `method`/`risk_pct` (schema_version stays "1.0", mirror the T-527b2b `SizingSpecForWire` additive shape) + strategy-engine `_publish_order_request` maps `bot_config.sizing.method`/`risk_pct`→wire (mirror the shipped `shadow_variants`/T-527b2b sizing conditional-build) + `placement.py:_handle` seam method-dispatch (`request.sizing.method == "risk_per_sl"` → `compute_qty_from_risk(total_equity, mark_price, request.sl_pct, request.sizing.risk_pct, …)` else the shipped `compute_qty_from_sizing` tier path) + the T-527b2b `except ValueError`/`None` skip-before-`place_market_order` seam reused verbatim (incl. `signals_skipped_sizing{bot_id,reason}` — `compute_error`/`sub_lowest_tier` already exist; risk-per-SL `total_equity<=0`→None maps to the existing skip path). Backward-compat `request.sizing is None` OR `method=="tier"` → the shipped T-527b2b tier path byte-unchanged. Own plan-reviewer Gate-1 cycle (OQ-2/3=A baked from this record); §N4 + Gate-4 (touches `services/execution/` — literally in the trigger list) + integration-level Hand verification of the dispatch.

## Sequence

T-528a consolidated plan (`docs/plans/T-528a.md`, APPROVED) → implement → 4 gates → merge. Then T-528b (now unblocked) gets its own consolidated plan + Gate-1 cycle (OQ-2/3=A baked from this record). Mirrors T-527b2a → T-527b2b / T-527a → T-527b.
