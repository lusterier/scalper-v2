# T-532 — RECURSIVE SPLIT RECORD (L-007)

**Status:** SPLIT 2026-05-16 (operator OQ-2=A at T-532 plan-stage; cap-proximate inventory — T-531 the closest sibling [no audit integration] was ~297 src single, T-532 = T-531-shape + cumulative-delta audit integration + paginated pull + new value type → §0.3-400-cap-risky once §N4 Hand-verification docstrings counted; NOT ADR-0011-pre-flagged but pre-emptively split per the cluster-wide foundation→consumer discipline). Not an implementable plan — records the recursive split; the active leaf plan is `docs/plans/T-532a.md`. T-532b plan written when T-532a merges.

## Why split (L-007)

T-532 "funding-fee tracking + migration 0021" (ADR-0011 account-balance/equity sub-cluster — sibling of the shipped T-530 `get_account_balance` + T-531 equity-snapshot) was inventoried across **two distinct concerns**: (a) the funding-settlement read path + storage — a Bybit `/v5/asset/transaction-log?type=SETTLEMENT` paginated windowed pull + a `FundingFee` value type + protocol/conformance + paper stub + migration 0021 `funding_fees` hypertable + test_0021 + a `@non_idempotent` queries writer (no I/O consumer, independently testable) — and (b) the cross-service consumer — a separate APScheduler `funding_fee_poll` tick + the T-220 cumulative-delta P&L-audit integration (a SEPARATE cumulative funding term, H-017-clean) + main wiring + the §N9/L-001 poll-interval Settings. T-531 (the closest sibling, NO audit integration) was ~297 src single; T-532 adds the audit integration + a paginated pull + a new value type on top → realistically §0.3-400-cap-proximate. Per L-007 active control a pre-emptive split at plan-stage is far cheaper than a mid-write cap breach (the T-527→{a,b}/T-527b2→{b2a,b2b}/T-528→{a,b}/T-533→{a,b}/T-534→{a,b} cluster-wide pure-foundation-leaf-then-consumer-leaf precedent).

## Operator OQ answers (T-532 plan-stage 2026-05-16, all recommended — baked into T-532a / T-532b)

- **OQ-1=A — NEW `funding_fees` hypertable** (migration 0021), NOT extend `executions` with `exec_type='funding'`. Rationale: funding ≠ execution semantically (executions since migration 0005 require order_id/qty/price NOT NULL — a funding settlement has no order); ZERO sibling-migration-test churn (only a NEW test_0021; vs extend-executions churning test_0005 `_EXPECTED_EXECUTIONS_COLUMNS` + every executions-touching test); the cumulative-delta `SUM(trades.realized_pnl)` does not read executions anyway. Schema (operator-approved): `id bigserial`, `bot_id text`, `symbol text`, `settled_at timestamptz`, `funding numeric(20,4)` (signed +credit/−debit), PK `(settled_at, id)`, 7d hypertable — verbatim-mirror `0019_bot_equity_snapshots`.
- **OQ-2=A — 2-leaf recursive split** T-532 → **T-532a** (foundation: adapter pull + `FundingFee` type + protocol/conformance + paper stub + migration 0021 + test_0021 + `insert_funding_fee` writer; NO consumer — independently testable; mirrors T-527b1/T-534a/T-533a) + **T-532b** (consumer: separate APScheduler `funding_fee_poll` tick + cumulative-delta audit integration + main wiring + §N9/L-001 poll Settings; blocked-by T-532a; mirrors T-531). Denominator 65→66 per L-007 (T-532 1-slot → 2 leaves = +1), numerator +1 when T-532a closes. Mirrors T-528→{a,b} EXACTLY.
- **OQ-3=A (binds T-532b) — funding enters the T-220 cumulative-delta audit as a SEPARATE cumulative funding term** (stored + observable + a distinct funding-delta attribution), NEVER folded into `trades.realized_pnl` (H-017-clean — ADR-0006 cumulative-only attribution; T-219 close flow stays the realized-pnl source of truth). T-532a is storage-only foundation — NO audit code.
- **OQ-4=A (binds T-532b) — a SEPARATE APScheduler `funding_fee_poll` tick** (mirror the shipped T-531 `equity_snapshot` tick: own `add_job`, own §N9/L-001 `execution_funding_fee_poll_interval_seconds=300` Settings, test_app_factory job-count 4→5), NOT folded into the T-220 `pnl_audit` tick (clean separation, independent cadence, ADR-0007 D1/D7).

## Doubly-stale-doc correction (L-018, T-533a "0018→0020" precedent class)

TASKS.md:237 says "migration 0017" and ADR-0011 says "0016+0017" — BOTH stale (0017 = the shipped `0017_symbol_map_cleanup`; current migration head is `0020_trades_lifecycle_state` revision='0020' down_revision='0019', verified migrations/versions/0020:72-73). **T-532's migration is `0021`, down_revision='0020'.** Corrected in the T-532a plan + to be noted in-body in the migration file + commit.

## Split (mechanical — funding read+storage foundation vs poll-tick+audit consumer; mirror T-528→{a,b})

- **T-532a** (active leaf — `docs/plans/T-532a.md`, APPROVED single-pass Gate-1 + 5 WG verbatim): `FundingFee` value type + `ExchangeClient.get_funding_fees_window` protocol/conformance + Bybit adapter `/v5/asset/transaction-log?type=SETTLEMENT` paginated pull (verbatim-mirror `get_closed_pnl_window`) + paper stub (`[]`) + migration `0021_funding_fees` (mirror 0019) + test_0021 (mirror test_0019; L-005 (20,4) + L-012 downgrade '0020') + `@non_idempotent insert_funding_fee` writer (mirror `insert_equity_snapshot`). NO consumer. **Gate-4 math-validator IN scope, COMMITTED** (signed-Decimal funding-money decode feeding the T-532b cumulative-delta capital-audit path — T-530 non-exhaustive-path precedent; decode+precision, NOT heavy arithmetic — the T-530/T-527b1/T-534b1 fast/MODERATE class). One WRITE-TIME-VERIFY item (Bybit transaction-log SETTLEMENT field names + rate-bucket — fixture-pinned at write-time, NOT an unresolved design question).
- **T-532b** (blocked-by T-532a): a separate APScheduler `funding_fee_poll` tick (mirror T-531 `equity_snapshot` — per-sub-account fan-out, two-tier resilience, `now_fn`) calling `get_funding_fees_window` → `insert_funding_fee` rows + the T-220 cumulative-delta audit integration (a SEPARATE cumulative funding term per OQ-3=A — H-017-clean, NEVER back-correct trades.realized_pnl) + main.py `add_job` + §N9/L-001 `execution_funding_fee_poll_interval_seconds` Settings + test_app_factory job-count 4→5 (L-015). Own plan-reviewer Gate-1 cycle (OQ-1/3/4=A baked from this record); §N4 + Gate-4 (touches `services/execution/` — literally in the trigger list — the cumulative-delta funding-sum arithmetic) + `## Hand verification` of the audit integration.

## Sequence

T-532a consolidated plan (`docs/plans/T-532a.md`, APPROVED) → implement → 4 gates → merge. Then T-532b (now unblocked) gets its own consolidated plan + Gate-1 cycle (OQ-1/3/4=A baked from this record). Mirrors T-528a → T-528b / T-527b1 → T-527b2.
