# T-523 — Extend F5 backlog with pre-live operational hardening (BRIEF §19 addendum + ADR-0011)

**Phase:** F5 (meta-chore that extends F5 scope; mirror T-500 / T-019 / T-200 pattern)
**Status:** DRAFT (pre-plan-reviewer Gate 1)
**Type:** TASKS.md restructure + BRIEF §19 addendum + new ADR (markdown-only)

## Context

Operator session 2026-05-08 surfaced 6 categories of pre-live operational gaps not covered by BRIEF §19 F5 scope:

1. Bot-level risk caps (max_open_trades, daily_loss, max_drawdown, cooldowns).
2. Automatic position sizing (balance-driven; risk-per-SL; qty_step rounding; min order; balance pre-check; altcoin cap).
3. Account balance / equity tracking (`get_account_balance()` adapter protocol gap; wallet/available/equity/margin/unrealized; historical snapshots).
4. PnL accounting refinements — **8/9 sub-items already shipped** in F2/F3 (cumulative-delta per ADR-0006, fee-per-fill, partial TP, reduce-only, realized/unrealized split, executions audit, T-220 audit loop). Funding fees is the only genuine gap → covered as T-532.
5. Named-state trade lifecycle FSM enum (currently free-text `trades.status` + `orders.status` + `position_state` flags + `close_reason` enum split across 4 columns).
6. SL/TP lifecycle verification gaps (periodic SL existence watchdog; SL overwrite protection; trailing SL audit pass).

Audit results (per `Agent` general-purpose research 2026-05-08): all bot-level risk caps, account balance tracking, named-state FSM, periodic SL watchdog — Bucket C ("silent everywhere", BRIEF + TASKS + ADRs none). §B.1 sizing block + qty_step rounding + virtual_balance gauge — Bucket B ("planned but unshipped", BRIEF-deferred to F4+ without task ID).

**Operator decisions 2026-05-08 (baked)**:
- OQ-1=B: Extend F5 scope (rename "Plný MVP" → "Live-ready MVP"; T-523..T-536 land within F5; not a new F6 phase).
- OQ-2=A: Formal addendum via BRIEF §19 update + ADR-0011 (mirror §6.7 ADR discipline).
- OQ-3=A: Hardening tasks ship after F5 existing tail (T-513b + UI T-516+T-517 + T-518..T-521) and BEFORE T-522 close-out runbook; T-522 scope expanded to include Live-ready sign-off (single runbook with 2 sign-off sections; default per operator OQ-1 reconciliation).

## Scope

**In scope:**
- TASKS.md updates: F5 phase counter narrative (22/22 → 22/44 pre-T-523-ship; 23/44 post-T-523-ship — T-523 meta-chore + 13 hardening tasks add 14 new entries to F5 cluster, joining 8 existing pending T-513b + T-516..T-522); NEW `### F5 mandatory hardening (T-524..T-536 — 13 tasks)` sub-section (T-523 itself is the meta-chore that adds them, counted separately as F5 done entry like T-500); F5 dependencies graph extension; F5 exit-criteria trace E6 row + Plný MVP → Live-ready MVP rename; F5+ opportunistic delta (absorbed items pointer to T-527/T-529/T-531).
- BRIEF §19 F5 block addendum: scope-expansion paragraph + new exit criterion E6 (Live-ready sign-off) + Plný MVP → Live-ready MVP rename in E5 row.
- NEW ADR-0011 `docs/adr/0011-f5-scope-extension-live-ready-mvp.md` capturing the scope-extension rationale + Decision/Context/Trade-offs/Rejected-alternatives sections per §6.7 protocol.
- TASKS.md `## Next` section restructure to surface hardening sub-cluster.

**Out of scope (each becomes a separate plan-reviewer cycle later):**
- T-524..T-536 individual task content (each gets its own `docs/plans/T-NNN.md` plan stage).
- T-522 close-out runbook content (T-522 plan stage handles 2-section runbook draft + Live-ready criteria; T-523 only declares the scope expansion).
- Implementation work for any T-524..T-536 task.

## Files touched

```
TASKS.md                                                UPDATE (~280 LOC delta)
docs/CLAUDE_CODE_BRIEF.md                               UPDATE (§19 F5 block; ~60 LOC delta)
docs/adr/0011-f5-scope-extension-live-ready-mvp.md      NEW (~280 LOC)
```

**No source code changes. No tests. No new dependencies.** Markdown-only meta-chore mirror T-500 pattern (T-500 = 0 src + ~580 LOC docs).

## Hardening task list (T-524..T-536; 13 tasks)

### Risk management (bod 1, 3 tasks)

- **T-524**: Bot-level concurrent-trades caps — `max_open_trades_per_bot` + `max_open_trades_global` config (§B.1 extension); enforcement at signal-acceptance gate (BRIEF §15.2 extension); reject-with-reason if cap hit. Per-bot YAML schema delta + signal-gateway / scoring gate hook + integration test injecting cap-hit scenario. **§N9: configurable per L-001** (caps in YAML, no hardcoded literals).
- **T-525**: Daily loss limit + max drawdown stop — `daily_loss_limit_usd` + `max_drawdown_pct` per-bot config; kill-switch fires when threshold crossed; bot enters `paused_loss_limit` state (extension to BotConfig FSM); state persisted across restart (mirror T-221 reconcile pattern). Test: simulated loss day + drawdown trajectory. **§N9: configurable per L-001**. **L-007 split-watch — confirm at plan stage** (FSM + persistence + reconcile pattern matches L-014 FSM-execution-service multiplier criteria).
- **T-526**: Cooldown after loss + losing-streak cooldown — entry-gate FSM at signal-acceptance; `cooldown_after_loss_minutes` + `cooldown_after_streak_n_losses_minutes` per-bot config; cooldown state persisted. Test: 3 losses + assert next signal blocked for cooldown duration. **§N9: configurable per L-001**.

### Position sizing (bod 2, 3 tasks)

- **T-527**: §B.1 sizing block reified — promote BRIEF-deferred (line 3006-3025) tier ladder + `max_notional_per_symbol` + altcoin cap + per-bot YAML schema + signal-acceptance compute path. **Absorbs F4+ opportunistic** TASKS.md line 265 verbatim entry "F4+ risk-based position sizing — replace `execution.qty: Decimal` with `sizing.tiers` block from §B.1:3006-3025." (T-310a OQ-4 was original deferral anchor — reified via this task). **§N9: configurable per L-001** (tier ladder + caps in YAML). **L-007 split-watch — confirm at plan stage** (§B.1 block is large; pre-emptive split likely T-527a tier ladder + T-527b max_notional/altcoin cap).
- **T-528**: Risk-per-SL sizing — alternative to tier ladder; `risk_per_trade_pct` config; computes qty s.t. `(entry-sl)*qty == risk_per_trade_pct * equity`. Per-bot opt-in via `sizing.method: tiers | risk_per_sl` discriminator. Coexists with T-527. **§N9: configurable per L-001**. **L-007 split-watch — confirm at plan stage** (discriminator dispatcher between tier and risk-per-SL paths matches L-014 multi-mode dispatch profile).
- **T-529**: qty_step rounding + min order size + available_balance pre-check — Bybit `instruments-info` cache (1h TTL parallel to T-215 leverage cache); `_round_qty_to_step(qty, step)` helper; min_order_qty validation; available_balance pre-check before place_market_order. **Absorbs F2+ opportunistic** TASKS.md line 269 verbatim entry "T-F2+: Bybit V5 instruments-info step-size cache." **Absorbs `placement.py:153` WARN-log stub** (`execution.qty_step_rounding_pending_t_f2_plus`). **§N9: configurable per L-001** (cache TTL + min_order_qty per-symbol; current 1h TTL cited as default).

### Account balance / equity tracking (bod 3, 3 tasks)

- **T-530**: `ExchangeClient.get_account_balance()` protocol extension — NEW method on `ExchangeClient` protocol returning `AccountBalance(wallet_balance, available_balance, total_equity, margin_balance, unrealized_pnl)` dataclass; Bybit `/v5/account/wallet-balance` impl; PaperExchange impl from in-memory state. Test: golden response decode + paper parity. **§N9: protocol fixed (no thresholds)** — read-only adapter method.
- **T-531**: Equity snapshot table + APScheduler tick + `virtual_balance{bot_id}` Prometheus gauge — NEW migration 0016 `bot_equity_snapshots` hypertable; per-bot N-min tick recorded via APScheduler; gauge exposed for Grafana. **Absorbs F5 BRIEF §15:2151** `virtual_balance` Prometheus mention. **§N9: configurable per L-001** (snapshot tick interval).
- **T-532**: Funding fee tracking — Bybit `/v5/asset/transaction-log?type=SETTLEMENT` adapter pull; storage in NEW migration 0017 `funding_fees` hypertable (or extension to `executions` with `exec_type='funding'`); cumulative-delta P&L audit integration. Bod-4 sub-item "funding fee tracking" gap. **§N9: configurable per L-001** (adapter poll interval).

### Trade lifecycle FSM (bod 5, 1 task)

- **T-533**: Named-state FSM enum refactor — single canonical `TradeLifecycleState` enum (`SIGNAL_RECEIVED`, `ORDER_REQUESTED`, `ORDER_PLACED`, `OPEN`, `PARTIALLY_CLOSED`, `TP_HIT`, `BREAKEVEN_SET`, `TRAILING_ACTIVE`, `CLOSING`, `CLOSED`, `FAILED`, `ORPHANED`, `RECONCILED`); migration 0018 adds `trades.lifecycle_state` column + populates from existing `status` / `position_state.tp_hit` / `position_state.sl_type` / `close_reason`; deprecates legacy text columns or maps via view. Largest task in cluster. **§N9: enum fixed (no thresholds)** — schema change. **L-007 split-watch — confirm at plan stage; L-014/L-016 watch + likely pre-emptive split** (large refactor across multiple modules + migration + state population).

### SL/TP verification (bod 6, 3 tasks)

- **T-534**: Periodic SL watchdog APScheduler tick — every N min verifies SL still exists on Bybit per active `position_state` row; emergency close if missing (mirror T-216b1 H-004 emergency_close path). Mid-session protection (currently SL only verified at create-time + restart). **§N9: configurable per L-001** (watchdog tick interval). **L-007 split-watch — confirm at plan stage** (APScheduler tick + emergency_close on miss matches L-016 restart-recovery / L-014 multi-mode dispatch profile).
- **T-535**: SL overwrite protection — DB-side SL state tracking; on adapter `update_sl` invocation, compare against last-known DB state; reject silent overwrite from manual exchange UI intervention; emit `sl_overwrite_detected` `trading_events` row. **§N9: protocol fixed (no thresholds)** — DB-side state diff check.
- **T-536**: Trailing SL audit pass — periodic (APScheduler tick) verification trail SL is correctly set per current `trail_pct` + entry slip + price action; emit `trail_sl_drift_detected` if Bybit-side SL diverges from expected. **§N9: configurable per L-001** (audit tick interval + drift tolerance pct).

## ADR-0011 outline

```markdown
# ADR-0011: F5 scope extension to "Live-ready MVP"

**Status:** Accepted (2026-05-08, T-523 plan-stage Gate 1)
**Context window:** F5 close-out + pre-live deployment hardening
**Authors:** Operator + general-purpose research agent (audit 2026-05-08) + plan-reviewer (T-523 Gate 1)

## Decision

F5 scope is extended with 13 mandatory hardening tasks (T-524..T-536). The
F5 exit criterion E5 is renamed from "Operator signs off Plný MVP scope"
to "Operator signs off Live-ready MVP scope". A new exit criterion E6 is
added: "all hardening tasks shipped + integration tests green + Live-ready
deployment runbook executed".

## Context

BRIEF §19 originally defined F5 as MVP feature-complete-on-paper:
shadow variants + backtest harness + UI extensions + close-out polish.
The audit-grade focus was on trading correctness (TDD financial math,
hazard catalog) — operational safety for live deployment was implicit.

Operator session 2026-05-08 surfaced 6 categories of pre-live ops gaps
post-F5-feature-complete: bot-level risk caps, balance-driven sizing,
account equity tracking, named-state FSM, SL/TP verification watchdog.
Most are silent in BRIEF + TASKS + ADRs (Bucket C); some are BRIEF-
deferred without task ID (Bucket B — §B.1 sizing block + qty_step +
virtual_balance gauge).

## Rationale

- "Plný MVP" semantic should match production-ready, not feature-
  complete-paper. Going live without bot-level risk caps risks capital
  loss; without periodic SL watchdog risks naked positions; without
  available_balance pre-check risks rejected-order pile-up.
- Single sign-off per operator preference — cleaner than two-stage
  (paper-MVP + live-ready) sign-off conceptually.
- BRIEF original F5 definition was incomplete vs operator intent; this
  ADR formalizes the scope correction without requiring a new phase.

## Consequences

### Accepted

- F5 close-out delayed by 13 hardening tasks (~3-4 weeks at current
  cadence).
- T-522 close-out runbook scope expanded to include Live-ready criteria
  (single runbook with 2 sign-off sections — paper feature-complete +
  Live-ready).
- BRIEF §19 F5 block updated: E5 renamed; E6 added; scope-expansion
  paragraph noting hardening sub-cluster.
- F5+ opportunistic items "F4+ risk-based position sizing" + "T-F2+
  Bybit instruments-info step-size cache" + BRIEF §15:2151 `virtual_balance`
  promoted to mandatory T-527 / T-529 / T-531.

### Trade-offs

- Single sign-off bundles paper-feature-complete + production-ready —
  operator cannot mark MVP shipped at intermediate state.
- Hardening tasks land late in F5 (after existing tail T-513b + UI +
  T-518..T-521) — reduces parallelism within phase but maintains
  ordering coherence.

## Rejected alternatives

- **Separate F6 phase**: rejected. Adds phase ceremony but doesn't
  change actual work; operator preferred single sign-off semantic
  (Q1=B 2026-05-08).
- **F5+ opportunistic only**: rejected. These are mandatory before
  live, not nice-to-haves.
- **Defer indefinitely / ship as-is**: rejected. Capital-loss risk
  before bot-level caps are in place.
- **Extend BRIEF §19 with new F6 phase block**: rejected per operator
  Q1=B. Would have moved hardening cluster post-F5 sign-off; preserves
  F5 narrow scope but doubles sign-off ceremony.

## Cross-references

- BRIEF §19:2575-2591 (F5 phase definition)
- BRIEF §B.1:3006-3025 (sizing block — promoted to T-527 mandatory)
- ADR-0007 (P&L audit loop — covers internal vs exchange comparison
  bod-4 sub-item; no task needed)
- T-220 (P&L audit loop, ADR-0007)
- T-221 (restart reconciliation, H-020 + H-026)
- T-310a OQ-4 (§B.1 sizing block F4+ deferral — now superseded by T-527)
- L-007 (pre-emptive split mechanic — applies to T-527 + T-533 sizing
  + FSM tasks at their plan stages)

## Follow-up

T-522 close-out plan stage decides 2-section vs split-T-522/T-537 runbook
structure. T-527 + T-533 plan stages decide pre-emptive split per L-007;
T-525 + T-528 + T-534 plan stages also flagged for L-007 split-watch.
T-530 + T-532 plan stages decide migration ordering (0016 + 0017 vs
combined).

**Per L-015 active control: T-531 (migration 0016) + T-532 (migration 0017)
+ T-533 (migration 0018) plan-docs MUST include "Sibling migration test
impact" section listing every earlier `tests/integration/migrations/
test_NNNN_migration.py` whose assertions touch any modified table.**
```

## Hazards relevant from §20

T-523 introduces no new code paths; no §20 hazard binding.

T-524..T-536 individual tasks may surface NEW §20 hazards:
- **H-027 (anticipated, T-525)**: Daily loss limit / drawdown stop must be persisted across restart and re-evaluated on startup (mirror T-221 reconcile pattern). Otherwise restart resets the kill-switch.
- **H-028 (anticipated, T-534)**: Periodic SL watchdog must distinguish "Bybit side dropped SL" from "Bybit side returned 'no positions' on transient error" — false-positive emergency_close would close real positions wrongly.
- **H-029 (anticipated, T-535)**: SL overwrite detection must NOT fire false-positive on legitimate trail SL updates (same trade_id + same direction) — only on out-of-FSM updates.

These hazards are listed in T-523 ADR-0011 as anticipated but **NOT added to BRIEF §20 catalog at T-523 time** — each surfaces during the relevant task's plan stage and gets formal H-NNN allocation then. Adding speculative hazards now violates §0.8 anti-hypothetical.

## Test strategy

N/A — markdown-only chore. No new tests. Existing test suite unaffected.

## LOC budget

- TASKS.md delta: ~280 LOC (F5 phase counter narrative + 14 new task entries + DAG update + exit-criteria + hazard binding + F5+ opportunistic delta).
- BRIEF §19 addendum: ~60 LOC (scope-expansion paragraph + E6 row + rename).
- ADR-0011 NEW: ~280 LOC (Decision + Context + Rationale + Consequences + Rejected alternatives + Cross-references + Follow-up).
- **Total ~620 LOC docs**. Markdown exempt §0.3.

Mirror T-500 pattern: ~580 LOC docs; 0 src; 0 tests. Single commit `feat(T-523): ...` per L-019 / T-019 / T-200 / T-500 named-task precedent.

## Acceptance criteria

1. TASKS.md F5 phase counter narrative updated: 22/22 → 22/44 (22 currently done; 1 new T-523 meta-chore + 13 new mandatory hardening + 8 existing F5 pending = 22 new entries beyond done; total = 44 entries in F5 cluster). Post-T-523-ship counter = 23/44. Fraction notation kept consistent with operator's session-end pattern.
2. NEW `### F5 mandatory hardening (T-524..T-536 — 13 tasks)` sub-section in TASKS.md with 13 task entries grouped by category (Risk / Sizing / Balance / FSM / SL/TP), each entry has scope-summary + estimated LOC + dependencies + L-007 split-watch flag where applicable.
3. F5 dependencies graph updated: T-524..T-536 block T-522 close-out (or are inserted before close-out per operator OQ-3=A); T-527 / T-529 / T-531 promoted-from-opportunistic markers visible.
4. F5 exit-criteria trace gets NEW row E6 = "All hardening tasks shipped + integration tests green + Live-ready deployment runbook executed" with owner T-524..T-536 + T-522.
5. F5 exit-criteria E5 renamed: "Operator signs off Plný MVP scope" → "Operator signs off Live-ready MVP scope" + foot-note pointing to ADR-0011 for rationale.
6. F5+ opportunistic items absorbed: F4+ "risk-based position sizing" → T-527 promoted; T-F2+ "Bybit instruments-info step-size cache" → T-529 promoted; BRIEF §15:2151 `virtual_balance` mention → T-531 promoted. Each absorbed item gets `→ promoted to mandatory T-XXX (T-523 reorg 2026-05-08)` pointer instead of deletion (traceability per operator default).
7. BRIEF §19 F5 block updated: scope-expansion paragraph (1-2 sentences) + E6 row added to exit criteria list + Plný MVP → Live-ready MVP rename in F5 narrative + footnote pointer to ADR-0011.
8. NEW `docs/adr/0011-f5-scope-extension-live-ready-mvp.md`: status Accepted 2026-05-08; Decision + Context + Rationale + Consequences + Rejected alternatives + Cross-references + Follow-up sections per §6.7 ADR template.
9. T-523 itself appears as a new entry in F5 done list (mirror T-500 pattern — meta-task counts toward F5 phase counter).
10. Commit message format: `feat(T-523): extend F5 with pre-live operational hardening backlog (BRIEF §19 addendum + ADR-0011)`. Single feat commit covering all 3 doc updates (mirror T-500 single-commit pattern). Chore commit follows for status.md.
11. Brief-reviewer single-pass SHIP expected (markdown-only; no code paths; no hazards introduced).
12. Math-validator: out of scope (markdown only).
13. status.md updated post-feat-commit with new "late-night VI" section describing T-523 ship + F5 scope extension rationale (per operator session-end pattern).
14. No source code changes — only TASKS.md, docs/CLAUDE_CODE_BRIEF.md, docs/adr/0011-f5-scope-extension-live-ready-mvp.md, docs/plans/T-523.md, docs/status.md touched.
15. No new dependencies.

## Open questions

All operator decisions baked from session 2026-05-08:

- **OQ-1 [BAKED, OPERATOR Q1=B 2026-05-08]**: Scope extension lives within F5 (rename "Plný MVP" → "Live-ready MVP") rather than new F6 phase.
- **OQ-2 [BAKED, OPERATOR Q2=A 2026-05-08]**: Formal BRIEF §19 update + ADR-0011 (most-formal option chosen).
- **OQ-3 [BAKED, OPERATOR Q3=A 2026-05-08, reconciled]**: Hardening tasks land within F5 after existing tail (T-513b + UI + T-518..T-521) and before T-522 close-out.
- **OQ-4 [DEFAULT, no operator follow-up needed]**: T-522 close-out scope = single runbook with 2 sign-off sections (paper feature-complete + Live-ready). Default is operationally simpler. T-522 plan stage can split into T-522 + T-537 if scope trip §0.3 cap.
- **OQ-5 [DEFAULT, no operator follow-up needed]**: F5+ opportunistic absorbed items keep their entries as `→ promoted to mandatory T-XXX` pointers (NOT deleted) for traceability. Future ADR-0011 readers can audit the absorption.
- **OQ-6 [DEFAULT, no operator follow-up needed]**: T-523 itself counts as F5 task entry in phase counter (mirror T-500 precedent).
- **OQ-7 [DEFAULT, no operator follow-up needed]**: T-524..T-536 entries in TASKS.md are scope-summary stubs (~15-25 LOC each); detailed plan-doc per task surfaces at its own plan-reviewer cycle. Mirror T-500 pattern (T-501..T-522 entries were similarly stub-shaped).

## Hand verification

N/A — no math; all operations are markdown edits.

## Sibling migration test impact (L-015)

T-523 adds NO migration. Future T-531 + T-532 + T-533 add migrations (0016 + 0017 + 0018) — sibling test impact handled at THOSE plan stages, not T-523.

## Write-time guidance (plan-reviewer APPROVE Gate 1 pass-2, 2026-05-08; verbatim)

1. **TASKS.md F5 phase counter narrative** musí byť konzistentný na všetkých miestach kde sa cituje (header counter, F5 sub-section header, end-of-file phase summary ak existuje). Použiť presne `22/22 → 22/44 (pre-T-523-ship) → 23/44 (post-T-523-ship)`. Brief-reviewer overí grep-om že žiadny zostatok starej `22/22`-only formulácie nezostal.
2. **TASKS.md F5+ opportunistic absorbed-pointer entries** (line 265 + line 269) MUSIA zostať na svojich pôvodných pozíciách s pridaným `→ promoted to mandatory T-527 (T-523 reorg 2026-05-08)` resp. `→ promoted to mandatory T-529 (T-523 reorg 2026-05-08)` pointerom — NEMAZAŤ pôvodný text, iba append pointer (per OQ-5 default + AC #6 traceability). BRIEF §15:2151 `virtual_balance` riadok dostane analogický `→ promoted to mandatory T-531` pointer.
3. **T-524..T-536 entries v TASKS.md** musia každá obsahovať: scope summary (1 veta), estimated LOC range (orientačný), explicit dependency note (čo blokuje / je blokované), `**§N9: ...**` tag verbatim per plan §Hardening task list, `**L-007 split-watch — confirm at plan stage**` flag IBA na T-525 + T-527 + T-528 + T-533 + T-534 (presne 5 entries — nie viac, nie menej; sleduj plan §Hardening task list).
4. **NEW `### F5 mandatory hardening (T-524..T-536 — 13 tasks)` sub-section header** — počet "13 tasks" musí matchovať doslovne plan §Hardening task list line 48 (`13 tasks`); akýkoľvek drift na "14" alebo "12" v sub-section header = brief-reviewer FIX FIRST.
5. **F5 exit-criteria trace** — E5 row musí byť premenovaný na `Operator signs off Live-ready MVP scope` s footnote pointer na ADR-0011; E6 row pridaný presne podľa AC #4 wording.
6. **BRIEF §19 F5 block update** — scope-expansion paragraph (1-2 vety, nie viac) + E6 pridaný do exit criteria zoznamu + `Plný MVP` → `Live-ready MVP` rename na všetkých miestach v F5 narrative + footnote pointer na ADR-0011. Žiadne ďalšie BRIEF sekcie nemodifikovať (out-of-scope per §0.4 silent-refactor cap).
7. **ADR-0011** musí obsahovať všetky sekcie podľa §6.7 template: Status / Context window / Authors / Decision / Context / Rationale / Consequences (Accepted + Trade-offs) / Rejected alternatives / Cross-references / Follow-up. §Follow-up sekcia MUSÍ obsahovať verbatim L-015 paragraph z plan §ADR-0011 outline lines 176–179 (sibling migration test impact directive pre T-531/T-532/T-533).
8. **Single feat commit** `feat(T-523): extend F5 with pre-live operational hardening backlog (BRIEF §19 addendum + ADR-0011)` (per L-019 / T-019 / T-200 / T-500 named-task precedent + AC #10). Chore commit pre status.md ide separátne (per L-019 split-commit semantics). NIE jeden mega-commit.
9. **status.md "late-night VI" sekcia** (per AC #13) — append, nemazať predchádzajúce sekcie; mention F5 scope extension + 13 hardening tasks + ADR-0011 link.
10. **Žiadne kódové zmeny, žiadne testy, žiadne nové dependencies.** AC #14 + #15. Brief-reviewer overí `git diff --stat` že zoznam zmenených súborov je presne `TASKS.md`, `docs/CLAUDE_CODE_BRIEF.md`, `docs/adr/0011-f5-scope-extension-live-ready-mvp.md`, `docs/plans/T-523.md`, `docs/status.md` — žiadny iný súbor.
