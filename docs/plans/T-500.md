# T-500 — Populate F5 backlog

Status: **APPROVED by `plan-reviewer` 2026-05-07** (single-pass APPROVE with 5 Write-time guidance items, all addressed inline below + reflected in `TASKS.md` per-task entries).
Phase: F5 (unlocked 2026-05-07 per master HEAD `4caa3d0` chore(F4-E1-smoke) sign-off; operator unlock signal *"pokracuj s F5"* this session)
Branch: n/a — branchless on master per Q12 / T-019 + T-200 phase-backlog-populate named-task precedent (F3/F4 used anonymous `chore(tasks)` commits `a07e46e` / `dec8c12`)
Mirror precedent: T-019 (F1, named-task path) + T-200 (F2, named-task path); F3/F4 used anonymous `chore(tasks): unlock FN phase + add NN-task plan` commits (`a07e46e` / `dec8c12`), T-500 reverts to T-019/T-200 named-task pattern for cleaner drift-checker / brief-reviewer visibility on a 22-task plan.

## Goal

Single deliverable: **`TASKS.md` restructure replacing the placeholder `## Next (F5 — pending operator unlock per §0.10)` section with `### F5 numbered` and `### F5+ opportunistic` subheadings populated with F5 task entries.**

No code, no tests, no migrations. Pure metadata. Drift-checker skipped (no implementation surface). Math-validator skipped (no financial math touched).

After this task ships, T-501 becomes the first numbered F5 work and F5 implementation begins per standard four-gate flow.

## Spec references

- §0.10 phase-gate discipline (F5 unlocked, F6+ tasks ineligible).
- §12.1 PaperExchange (F2-shipped at T-213a/b — T-506 wires it to HistoricalOHLCSource for replay mode).
- §12.2 Backtest harness — components: ReplayBus + HistoricalOHLCSource + HistoricalSignalSource + intra-candle path generator + comparison mode + CLI `scripts/backtest.py`. Results persisted to `backtest_runs.summary` + new `backtest_trades` table.
- §13 Shadow Variants — full spec: §13.1 Purpose, §13.2 per-bot YAML config, §13.3 runtime + `shadow.start.<bot_id>` NATS topic + `shadow_variants` table, §13.4 OHLC replay restart recovery (eliminates H-023), §13.5 rejected-signal shadow tracking (`shadow_rejected` table), §13.6 dashboard integration, §13.7 4 tests required.
- §14.3 dashboard sections — F5 extends T-415 backtest lab with results+comparison + T-414 trade drill-down with shadow variants panel + T-417 strategy editor with diff-against-live + new shadow rejected explorer.
- §19 F5 exit criteria — 5 bullets (E1 30-day backtest aggregates / E2 two-backtest comparison / E3 shadow restart persistence / E4 all §20 hazards have passing tests / E5 operator signs off Plný MVP).
- §20 hazards H-001..H-026 — F5 deliverable per §19 line 2580 *"All hazards in §20 have an associated test that passes"*. Hazard classification below identifies the small remaining-gap subset; bulk of hazards already covered by F2 (18) + F3 (3) + F4 (1) tests.
- §9.3 feature_definitions — auto-backfill on registration (T-518 owner).
- §B (configs/bots/<id>.yaml) — extend with `shadow:` block per §13.2 (T-514 owner).

## Survey

### F5-binding hazard classification (§20 H-001..H-026)

Every hazard classified as **F5-binding** (must be addressed within F5 scope) / **already-shipped** (F0/F1/F2/F3/F4 owner already merged + test passing) / **cross-phase** (F5 surface but spans phases):

| ID | Title | Class | F5 owner task |
|----|-------|-------|---------------|
| H-001 | P&L orderId matching unsafe → cumulative-delta | already-shipped (F2 T-219) | T-519 audit confirms test passing |
| H-002 | Similar entries collide → cumulative-delta | already-shipped (F2 T-219) | T-519 audit |
| H-003 | place_market_order non-idempotent | already-shipped (F2 T-201 + T-208) | T-519 audit |
| H-004 | SL-set failure → emergency close | already-shipped (F2 T-216) | T-519 audit |
| H-005 | Opposite-side signals | already-shipped (F3 strategy-engine `block_opposite_position_open`) | T-519 audit |
| H-006 | Webhook rate limit | already-shipped (F0/F1 signal-gateway) | T-519 audit |
| H-007 | WS reconnect exp backoff | already-shipped (F1 market-data + F2 T-209 private WS) | T-519 audit |
| H-008 | Signal TTL | already-shipped (F3 strategy-engine) | T-519 audit |
| H-009 | Execution WS dedup | already-shipped (F2 T-210 + T-218) | T-519 audit |
| H-010 | Fan-out before dedup | already-shipped (F0/F1 signal-gateway) | T-519 audit |
| H-011 | 2s sleep before closed-pnl query | already-shipped (F2 T-219) | T-519 audit |
| H-012 | closed-pnl is source of truth | already-shipped (F2 T-219) | T-519 audit |
| H-013 | Partial TP requires `tpslMode=Partial` | already-shipped (F2 T-201 + T-208) | T-519 audit |
| H-014 | Price subscription refcount | already-shipped (F1 market-data-svc PriceManager) | T-519 audit |
| H-015 | Orphan close uses exact qty string | already-shipped (F2 T-208) | T-519 audit |
| H-016 | Shadow task cleanup | **F5-binding** (F2 protocol stays; F5 owns full address) | T-511 + T-512 (shadow-worker + restart recovery) |
| H-017 | Audit 1:1 matching → cumulative attribution | already-shipped (F2 T-220) | T-519 audit |
| H-018 | Close by PK only | already-shipped (F2 T-202 + T-217) | T-519 audit |
| H-019 | Score fail-open is logged | already-shipped (F3 scoring evaluator) | T-519 audit |
| H-020 | Post-restart reconciliation | already-shipped (F2 T-221) | T-519 audit |
| H-021 | Scheduled jobs run UTC | already-shipped (F2 T-220 APScheduler) | T-519 audit |
| H-022 | API keys per-bot, never shared | already-shipped (F2 T-215) | T-519 audit |
| H-023 | Shadow restart via OHLC replay | **F5-binding** (eliminates `lost_on_restart` per §13.4) | T-512 |
| H-024 | Fill label derives from our order ID | already-shipped (F2 T-218) | T-519 audit |
| H-025 | Cross-bot IP rate limit coordination | already-shipped (F2 T-205) | T-519 audit |
| H-026 | Bybit qty-step rounding in reconcile | already-shipped (F2 T-221) | T-519 audit |

**Tally:** 2 F5-binding (H-016 + H-023, both shadow-runtime), 24 already-shipped. T-519 audit task is the single gating verifier — runs each hazard's named test from §20 and asserts pass; any gap surfaces a follow-up `fix(F5-hazard-<id>)` commit.

### F5 exit-criteria trace (§19 lines 2576-2580)

| ID | Bullet | Owner task(s) |
|----|--------|---------------|
| E1 | Backtest on 30-day historical window completes + reports aggregates | T-507 (CLI) + T-509 (worker connect) end-to-end smoke |
| E2 | Two backtests with different configs compared side-by-side | T-508 (comparison mode) + T-516 (UI per-trade variants drill-down acceptable as alt-surface) |
| E3 | Shadow variants persist across restart (verified by killing execution-service mid-variant) | T-511 (shadow-worker) + T-512 (OHLC replay restart recovery) — kill-during-variant integration test mandatory |
| E4 | All §20 hazards have passing tests | T-519 audit (gating task; one-batch run + report) |
| E5 | Operator signs off "Plný MVP scope" | T-522 close-out runbook + sign-off (mirror T-313 / T-423) |

### Migration sequence (F4 ended at 0012; F5 adds 2 migrations)

| ID | Migration | Tables | Owner task |
|----|-----------|--------|------------|
| 0013 | Backtest trades ledger | `backtest_trades` (FK to `backtest_runs.id` from T-407 migration 0012) | T-501 |
| 0014 | Shadow variants + rejected | `shadow_variants` + `shadow_rejected` (per §13.3 + §13.5) | T-510 |

Both forward-only per §N8 + L-007 (one migration per task — never bundled).

### Service-touch matrix (which services F5 modifies)

| Service | F5 changes |
|---------|------------|
| `services/execution/` | T-511 + T-512 + T-513 (shadow-worker in-process per §13.3 + OHLC replay) |
| `services/strategy_engine/` | none (already shadow-aware via T-513 rejected-signal observation hook?) — F2 contracts honored |
| `services/feature_engine/` | T-518 (auto-backfill on feature_definitions insert) |
| `services/analytics_api/` | T-509 (`/api/backtests/{id}/results` deepening) + T-516 + T-517 (shadow + rejected endpoints) |
| `services/signal_gateway/` | none — already shipped F0/F1 |
| `services/market_data/` | none — F1 PriceManager unchanged |
| `services/alerting/` | none — F4 T-409 already shipped |
| `packages/exchange/paper/` | T-506 (replay-mode wiring for HistoricalOHLCSource) |
| `packages/db/queries/` | T-501 + T-510 + T-518 (new `backtest_trades`, `shadow_variants`, `shadow_rejected`, `feature_definitions` query helpers) |
| `ui/` | T-515 + T-516 + T-517 (strategy editor diff + shadow drill-down + rejected explorer) |
| `scripts/` | T-507 (`backtest.py` CLI) + T-508 (comparison mode) |

## Decomposition (T-501..T-522 — 22 numbered tasks)

### Backtest harness cluster (T-501..T-509 — 9 tasks)

- **T-501**: Migration 0013 `backtest_trades` table (FK `run_id → backtest_runs.id`; per-trade columns mirror live `trades` schema). ~150 LOC migration + test_migration.py + ~50 LOC packages/db/queries/backtest_trades.py read helper.
- **T-502**: `ReplayBus` skeleton — in-process NATS-compatible publish/subscribe with timestamp-ordered delivery. Mirrors `packages/bus/nats_client.py` interface so consumers can swap. ~200 LOC src + ~150 LOC tests (replay determinism).
- **T-503**: `HistoricalOHLCSource` — TimescaleDB read + configurable replay pace (1x / 10x / max). Cursor-based iteration to avoid loading 30-day window into memory. ~180 LOC src + ~120 LOC tests.
- **T-504**: `HistoricalSignalSource` — chronological replay of `signals` table for bot's symbol universe. Read-only consumer. ~140 LOC src + ~100 LOC tests.
- **T-505**: Intra-candle path generator — deterministic O→(toward high if close>open else toward low first)→extreme→other extreme→C per BRIEF §12.2. **Pre-emptively split-flagged** per L-007 — generator alone may fit, but if joined with T-506 PaperExchange wiring it overshoots. ~120 LOC src + ~150 LOC tests (path determinism + TradingView "Replay" parity).
- **T-506**: PaperExchange wired to HistoricalOHLCSource for replay mode — refactor T-213b to accept either live `market.ohlc.1m.>` NATS or replay HistoricalOHLCSource source. ~200 LOC src delta + ~150 LOC tests.
- **T-507**: `scripts/backtest.py` CLI — `--bot <id> --from <date> --to <date> [--override 'path.to.field=value']` invocation; orchestrates ReplayBus + HistoricalOHLCSource + HistoricalSignalSource + PaperExchange + strategy-engine + execution-service into single in-process process. Summary stats persistence to `backtest_runs.summary` (total trades / WR / P&L / PF / MDD). ~250 LOC src + ~180 LOC tests. **Pre-emptively split-flagged** — orchestrator alone is large; if summary-stats compute is non-trivial (Pareto-efficient frontier metrics), split T-507a (orchestration) + T-507b (summary stats compute).
- **T-508**: Comparison mode — `python scripts/backtest.py --compare run_A_uuid run_B_uuid` outputs aggregate metrics diff + per-trade diff (same signal, different outcome). ~180 LOC src + ~140 LOC tests.
- **T-509**: Backtest worker connect to T-407 `/api/backtests/{id}` placeholder — when operator triggers backtest from UI (Step 4 of F4 E1 smoke runbook = `status=queued` row), worker picks up + runs T-507 CLI internally + writes summary back to row. Async background task in analytics-api lifespan or separate `services/backtest-worker/` (TBD; default A: in-process in analytics-api, separated later if perf demands). ~220 LOC src + ~140 LOC tests.

### Shadow variants runtime cluster (T-510..T-514 — 5 tasks)

- **T-510**: Migration 0014 `shadow_variants` + `shadow_rejected` tables — per §13.3 + §13.5. Schema includes terminal_outcome enum + MFE/MAE Decimals + variant_name + parent_trade_id FK. ~180 LOC migration + test_migration.py + ~80 LOC packages/db/queries/shadow.py read helpers.
- **T-511**: Shadow-worker skeleton in execution-service — per-variant `_step` FSM running against PaperExchange instance seeded with live entry. Subscribes to `shadow.start.<bot_id>` NATS topic. **Pre-emptively split-flagged** — runtime + persistence + emission may overshoot per F2 T-216 precedent; if so, T-511a (FSM + paper engine seed) + T-511b (persistence + emission). ~280 LOC src + ~200 LOC tests.
- **T-512**: OHLC replay restart recovery (§13.4) — on execution-service restart, for each pending variant query `ohlc_1m` from `created_at` to `now`, replay using same `_step` from T-511. Eliminates H-023 `lost_on_restart`. **Mandatory kill-during-variant integration test** per E3 exit criterion — kills execution-service mid-variant via signal, restarts, verifies variant resumes correctly. ~220 LOC src + ~250 LOC tests (kill-test is integration heavy).
- **T-513**: Rejected-signal shadow tracking (§13.5) — separate from variants. When signal rejected by scoring, 60-min observation task records MFE/MAE + terminal label (would_tp/would_sl/would_be/no_trigger) → `shadow_rejected` table. Restart recovery via OHLC replay (also v2 improvement). ~250 LOC src + ~200 LOC tests.
- **T-514**: Shadow variants config schema — per-bot `shadow.variants:` YAML block per §13.2; pydantic validation extends T-217 `BotConfig` model. Validation: variant names unique, overrides keys valid execution-config paths, max_duration_hours bounded. ~120 LOC src + ~100 LOC tests.

### UI extensions cluster (T-515..T-517 — 3 tasks)

- **T-515**: Strategy editor diff-against-live (BRIEF §19:2575 line 7) — extend T-416 strategy editor with side-by-side diff: "Current applied (read-only)" vs "Editing draft" with line-by-line YAML diff highlighter. ~200 LOC src + ~150 LOC tests.
- **T-516**: Shadow variants per-trade drill-down (T-414 8-section placeholder #4 → real renderer). Renders all variants alongside live outcome per §13.6. ~250 LOC src + ~150 LOC tests.
- **T-517**: Per-symbol best-variant aggregate + per-rejected-signal explorer (§13.6) — new dashboard sub-routes `/shadow/aggregate/$symbol` + `/shadow/rejected`. ~280 LOC src + ~180 LOC tests. **Pre-emptively split-flagged** — two independent surfaces; if combined overshoots, T-517a (aggregate) + T-517b (rejected explorer).

### Backend polish + ops cluster (T-518..T-522 — 5 tasks)

- **T-518**: Feature auto-backfill on registration (§9.3) — when a new `feature_definitions` row inserts, feature-engine schedules backfill job for historical OHLC range. ~200 LOC src + ~150 LOC tests.
- **T-519**: §20 hazard test audit — verify every H-001..H-026 has passing test (E4 enforcement). One-batch run via `pytest -m hazard --tb=short`; produces `docs/audit/hazard-test-coverage.md` report. **Late-F5 gating** per OQ-4 default A. ~80 LOC marker config + ~50 LOC report generator.
- **T-520**: Hardening shortlist — triage `T-F2+` / `T-F3+` / `T-F4+` opportunistic deferred items + 5 today-flagged tech-debts (ui nav persist, audit pre-fix row cleanup, signal_gateway latent fix, T-401c symbol_map cleanup, T-422 Playwright cache). Operator approves shortlist; rest defers F6+. ~150 LOC across small fixes + ~100 LOC tests; **multi-commit task** — each shortlisted item becomes a sub-commit with own brief-reviewer pass.
- **T-521**: Final docs pass — runbooks (`docs/runbooks/F5_E1_backtest_smoke.md`, `docs/runbooks/F5_E2_shadow_smoke.md`); glossary update; README rewrite reflecting MVP-complete state; `docs/CLAUDE_CODE_BRIEF.md` deltas if §B / §11 / §12 / §13 surfaced ambiguities during F5. ~400 LOC docs (markdown exempt from §0.3).
- **T-522**: F5 close-out runbook + E1..E5 sign-off (mirror T-313 / T-423 pattern). Operator-runnable smoke verifying 5 exit criteria; sign-off section with full ISO-8601 `+00:00` timestamp per §N1. ~280 LOC runbook + status.md + TASKS.md updates.

## Dependency DAG

```
T-501 (migration 0013) ──┐
                         ├─→ T-507 (CLI) ──┬─→ T-508 (compare) ──┐
T-502 (ReplayBus) ───────┤                 │                       │
T-503 (OHLCSource) ──────┤                 │                       │
T-504 (SignalSource) ────┤                 │                       │
T-505 (intra-candle) ────┤                 │                       │
T-506 (PaperEx replay) ──┘                 │                       │
                                           │                       │
T-509 (worker) ←───────────────────────────┘                       │
                                                                   │
T-510 (migration 0014) ──┬─→ T-511 (shadow-worker) ─→ T-512 (restart recovery)
                         ├─→ T-513 (rejected tracking)              │
T-514 (config schema) ───┘                                          │
                                                                   │
T-515 (strategy diff UI) — independent                              │
T-516 (variants drill-down UI) ←───────────────────────────────────┤
T-517 (aggregate + rejected UI) ←───────────────────────────────────┘

T-518 (feature backfill) — independent
T-520 (hardening) — independent

T-519 (hazard audit) ← all above passing
T-521 (docs final pass) ← T-519 passing
T-522 (close-out + sign-off) ← T-521 + T-507/T-508/T-512/T-518 all green
```

**Top-of-DAG (no F5-internal deps; can start in parallel after T-500 lands):** T-501, T-502, T-503, T-504, T-505, T-510, T-514, T-515, T-518, T-520. Operator picks one or two.

**Critical-path bottleneck:** T-512 OHLC replay restart recovery — kill-during-variant integration test is integration-heavy and may dominate session length.

## Pre-emptive splits (per L-007)

| Task | Split rationale | Suggested split |
|------|-----------------|-----------------|
| T-507 | CLI orchestration + summary stats compute may overshoot | T-507a (orchestration) + T-507b (summary stats) — confirm at planning |
| T-511 | FSM + paper engine seed + persistence + emission ~280 LOC est | T-511a (FSM + paper seed) + T-511b (persistence + emission) — confirm at planning |
| T-517 | Two independent UI surfaces (aggregate + rejected) | T-517a (aggregate) + T-517b (rejected explorer) — confirm at planning |

If any pre-flagged task plan-doc estimate exceeds ~280 LOC src per L-006 watch threshold, split before plan-reviewer Gate 1.

## F5+ opportunistic (carry-over candidates for F6 or hardening cherry-pick)

- **T-F5+-1**: ReplayBus extended to support pause/resume/seek (debug + interactive backtest UX); F5 ships 1x/10x/max only.
- **T-F5+-2**: Backtest comparison HTML report — pretty-printed metrics diff; F5 ships CLI text + UI side-by-side; HTML render is polish.
- **T-F5+-3**: Shadow variants statistical significance test — when N variants over M trades, Bonferroni-corrected best-variant claim.
- **T-F5+-4**: Feature backfill progress dashboard tile — currently log-only.
- **T-F5+-5**: Multi-bot backtest run (F5 ships single-bot per CLI invocation).

## Open questions resolved (operator default A applied 2026-05-07)

| OQ | Question | Default A (operator-confirmed) |
|----|----------|-------------------------------|
| OQ-1 | Backtest harness CLI vs HTTP endpoint | **Both** — CLI primary per BRIEF §12.2; HTTP `/api/backtests/{id}` calls CLI internally via T-509 worker |
| OQ-2 | Shadow worker placement (separate service vs in-process) | **In-process v execution-service** per BRIEF §13.3 ("part of execution-service in v1"); separation deferred to F6+ if perf demands |
| OQ-3 | F5 timing estimate | **2 weeks realistic** (vs BRIEF "2-3 weeks") with slip acceptance |
| OQ-4 | Hazard test audit (T-519) timing | **Late-F5 gating** — single batch run before T-522 close-out; rolling per-task audit deemed too noisy |
| OQ-5 | T-520 hardening shortlist scope | Operator-approved shortlist at T-520 plan-doc time; rest deferred F6+; 5 today-flagged items pre-included as candidates |
| OQ-6 | F5+ opportunistic placeholder | **Yes** — 5 items pre-listed above; new ones accumulate during F5 implementation |

No open questions remaining — plan is consolidated.

## Acceptance criteria

T-500 ships when:

- [ ] `TASKS.md` `## Next (F5 — pending operator unlock per §0.10)` placeholder is replaced with `### F5 numbered` (T-501..T-522 entries) + `### F5+ opportunistic` (T-F5+-1..T-F5+-5 entries).
- [ ] Each F5 numbered entry includes: brief summary + spec ref (BRIEF §) + estimate (LOC src + tests) + blocked-by + blocks downstream.
- [ ] Each F5-binding hazard (H-016 + H-023) is mapped to its owner task verbatim in TASKS.md entry text.
- [ ] Each F5 exit criterion E1..E5 is mapped to its owner task verbatim in TASKS.md entry text.
- [ ] Migration sequence (0013 + 0014) noted in TASKS.md.
- [ ] Service-touch matrix noted in TASKS.md per-task scope text.
- [ ] DAG ordering reflected via "blocked-by" / "blocks" cross-refs.
- [ ] Pre-emptive split flags noted on T-507 / T-511 / T-517 entries.
- [ ] F5 phase header line in TASKS.md updated: was "**24/24 tasks done + T-423 close-out runbook shipped + E1 SIGN-OFF**" → adds F5 unlock date + 22-task initial scope.
- [ ] Plan-reviewer Gate 1 APPROVE.
- [ ] No drift-checker (no implementation surface).
- [ ] No math-validator (no financial math).
- [ ] Brief-reviewer SHIP on staged commit.

## Out of scope (deferred to T-501+ owner tasks)

- Any source code (T-500 is metadata only).
- Any tests (T-500 ships zero new test files).
- Any migrations (T-501 + T-510 own those).
- ADR drafts for F5-specific architectural decisions — deferred to per-task ADR-need surface (e.g. T-506 PaperExchange replay-vs-live flag may need ADR; T-509 worker placement may need ADR).
- §B bot-config schema documentation update for `shadow:` block — T-514 owner.
- BRIEF §12 / §13 / §20 surface deltas if F5 implementation reveals ambiguities — T-521 final docs pass.

## Estimate

T-500 itself: 0 src LOC + ~330 LOC plan-doc artifact (this file) + ~250 LOC TASKS.md edit (22 numbered entries + 5 opportunistic + section restructure) = ~580 LOC docs total. **Documentation exempt from §0.3 cap** per BRIEF §0.3 ("excluding tests, generated code, migrations, vendoring").

F5 phase total estimate (per OQ-3 default A): **~2 weeks** at F4 / F3 cadence (~1.5 tasks/day operator + Claude Code pair); 22 tasks ÷ 1.5/day = ~15 working days ≈ 2 weeks calendar. Buffer for kill-test integration heavy tasks (T-512 + T-519) — slip acceptable.

## §N invariants check

- **§N1** UTC ✓ — runbook timestamps T-522 will use ISO-8601 `+00:00` per WG precedent (T-423).
- **§N2** structured logs N/A — no logging surface.
- **§N3** idempotent markers N/A — no writes.
- **§N4** TDD on financial math — T-501..T-518 owner tasks each carry §N4 obligation; T-500 doesn't introduce new src.
- **§N5** 80% coverage — same as N4; per-task gate.
- **§N6** no globals N/A — no DI surface.
- **§N7** hexagonal N/A — no architecture change.
- **§N8** forward-only migrations — T-501 (0013) + T-510 (0014) explicit forward-only with `test_migration.py` per migration.
- **§N9** YAML/env configurable — T-514 shadow config + T-507 CLI overrides honor §N9.
- **§N10** no §20 regression — T-519 audit gating is the explicit guard.

All invariants honored at the meta level; per-task plan-docs will re-assert at Gate 1.

## Write-time guidance (plan-reviewer APPROVE 2026-05-07 — must be addressed during implementation)

1. **Tighten mirror-precedent claim.** Plan-doc line 6 + TASKS.md F5-section narrative cite "T-019 + T-200 + T-300 + T-400" as named precedent backlog-populate tasks. Verified against git log: only T-019 (`33a02af`) + T-200 (`0b3c6da`) were named tasks; F3 backlog was `chore(tasks): unlock F3 phase + add 14-task plan` (`a07e46e`) and F4 was `chore(tasks): unlock F4 phase + add 24-task plan` (`dec8c12`) — both anonymous chore commits. Edit plan-doc line 6 + TASKS.md narrative to say "Mirror precedent: T-019 (F1, named-task path) + T-200 (F2, named-task path); F3/F4 used anonymous `chore(tasks): unlock FN phase + add NN-task plan` commits (`a07e46e` / `dec8c12`), T-500 reverts to T-019/T-200 named-task pattern for cleaner drift-checker / brief-reviewer visibility on a 22-task plan."

2. **Add missing T-517 ← T-513 DAG edge.** T-517 scope ("aggregate + rejected explorer") has a rejected-explorer half that consumes `shadow_rejected` populated by T-513. Plan-doc DAG line 155 only shows T-517 ← T-512. When writing TASKS.md `### F5 numbered` T-517 entry text per acceptance-criterion §4, mark blocked-by as `T-510 (schema) + T-512 (variant runtime → aggregate side) + T-513 (rejected tracking → explorer side)`. If pre-emptive split T-517a (aggregate) + T-517b (rejected explorer) lands at T-517 plan-doc time, T-517a blocked-by simplifies to `T-510+T-512`; T-517b blocked-by becomes `T-510+T-513`.

3. **L-011 latent-flag carry into T-501/T-510 migration tasks.** L-011 active control requires JSONB-writer query helpers to NOT pre-`json.dumps` payload under registered codec. T-501 (`packages/db/queries/backtest_trades.py` read helper) is read-only so safe; T-510 (`packages/db/queries/shadow.py` for `shadow_variants` + `shadow_rejected`) is write-side and runs under execution-service which currently does NOT register `_register_jsonb_codec` (per L-011 grep — only analytics-api + feature-engine do). Plan-doc should note in T-510 entry text: "**L-011 caveat**: shadow.py write helpers run under execution-service (no JSONB codec registered → safe-by-accident); if execution-service adds codec registration in F5+, switch to dict-direct pattern per L-011 active control." Mirrors T-200 line 171 H-016 latent-flag treatment.

4. **§N9 explicit per-task obligation note.** Plan-doc line 244 says "T-514 shadow config + T-507 CLI overrides honor §N9". T-505 intra-candle path generator depends on a deterministic-but-arbitrary 5-step sequence (O→toward-extreme-by-direction→extreme→other-extreme→C); per §N9 the path-step count + ordering rule itself is mathematically fixed (BRIEF §12.2:1961 verbatim) so NOT configurable, but any timing/jitter knobs are configurable. T-503 HistoricalOHLCSource pace ("1x / 10x / max") is explicitly env/CLI-configurable per BRIEF §12.2:1955. When writing TASKS.md per-task entry text, include "§N9: <fixed | configurable per BRIEF §X>" note for T-503 + T-505 + T-507 + T-509 + T-512 + T-513 + T-518 specifically (each has timing/threshold knobs). Mirrors L-001 active control (polling intervals + pause durations + thresholds in business logic violate §N9 even if sensible defaults).

5. **Acceptance-criterion §4 (E1..E5 mapping in TASKS.md entry text) clarification.** Plan-doc acceptance criteria line 207 says "Each F5 exit criterion E1..E5 is mapped to its owner task verbatim in TASKS.md entry text". E1 has TWO owners (T-507 + T-509); E2 has TWO (T-508 + T-516); E3 has TWO (T-511 + T-512). When writing TASKS.md per-task entries, include E-tag in BOTH owner-task entries (e.g. T-507 entry gets `(E1 owner — CLI side)`, T-509 entry gets `(E1 owner — worker connect side)`). Otherwise drift-checker can't trace ownership reliably during F5 implementation.
