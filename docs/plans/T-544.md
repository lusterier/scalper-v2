# T-544 — `strategy-engine-smoke` compose service (F6 final numbered task)

**Status:** Gate-1 plan-stage (consolidated; no open questions). F6 numbered work task, **last of the F6 /4** (F6-counted (3/4); on close F6 3/4 → **4/4** and the ADR-0015 F6 exit-criteria are MET). F6 unlocked per ADR-0015; **Blocked-by T-541 ✅ DONE**. The `strategy-engine-smoke` residual from the T-522 F5 close-out RUN (`b16d41a`), carved out of T-540 (§0.8). Batch-push mode active (local commits; one push at batch end).

## Purpose

The F5_E2 shadow-restart **deployment smoke** (`docs/runbooks/F5_E2_shadow_smoke.md`) cannot run end-to-end locally: `compose.yaml` ships only `strategy-engine-alpha` + `strategy-engine-beta` (the per-bot decision process is the *only* per-bot container; execution-service / analytics-api are singletons per BRIEF §2.2:234). The `smoke` fixture bot (`configs/bots/smoke.yaml`, shipped runnable by T-540 — `scoring.mode: passthrough`, `shadow:` block) has **no `strategy-engine-smoke` service**, so `docker compose … up -d … strategy-engine-smoke` (F5_E2 runbook step) has nothing to start. T-544 ships that service so the full local F5_E2 deployment smoke is runnable — the last F6 exit-criterion.

## Scope (IN)

1. **`compose.yaml`** (~22 yaml) — add a `strategy-engine-smoke` service block, **mirroring `strategy-engine-beta` EXACTLY** (same `build` context/Dockerfile, `image: scalper-v2/strategy-engine:dev`, `restart`, `networks: [backend]`, `env_file` `/etc/scalper-v2/secrets.env required:false`, `SERVICE_NAME: strategy-engine`, `LOG_LEVEL: INFO`, `NATS_URL`, `BOT_CONFIG_DIR`, `PLUGIN_REGISTRY_PATH`, the identical `depends_on` postgres/nats/nats-init + `healthcheck`) — the **only** differences: `BOT_ID: smoke` and `BOT_CONFIRM_LIVE: ${BOT_SMOKE_CONFIRM_LIVE:-}` (verbatim per-bot pattern, mirror alpha's `${BOT_ALPHA_CONFIRM_LIVE:-}` / beta's `${BOT_BETA_CONFIRM_LIVE:-}`). Placed immediately after the `strategy-engine-beta` block (ops.md "Pridanie nového bota" Krok 5 convention: "Pridaj za posledný `strategy-engine-*` blok"). A one-line block comment noting it is the F5_E2-smoke fixture bot (T-544), mirroring beta's intro comment density.
2. **`compose.dev.yaml`** (~3 yaml) — add the `strategy-engine-smoke` dev overlay mirroring the `strategy-engine-alpha`/`-beta` overlay verbatim (`env_file: !reset null` + `environment: DATABASE_URL: ${DATABASE_URL}`), so the F5_E2 runbook's `docker compose -f compose.yaml -f compose.dev.yaml up -d … strategy-engine-smoke` resolves the DSN from the project-root `.env` exactly like alpha/beta (without it the smoke service would EACCES on `/etc/scalper-v2/` + lack `DATABASE_URL` on the dev stack).
3. **`docs/runbooks/F5_E2_shadow_smoke.md`** (markdown, §0.3-exempt) — the `:29` line is the **L-028 honesty-note**, literally tagged `(L-028 / T-522 close-out decision A)` (an L-028 documented-procedure-drift / not-execution-verified note, NOT an L-026 duplicated-source-of-truth site; the `F5_close_out.md:48` cross-pointer also calls it "the **L-028** honesty note"). It states `compose ships strategy-engine-alpha/-beta only; a smoke bot also needs a strategy-engine-smoke service (documented residual)`. This is a non-signed checklist note — reword it to retire ONLY the **service-missing** blocker: from "documented residual / NOT re-run because the service is missing" to "the `strategy-engine-smoke` service ships as of T-544 — the service-missing blocker is removed; the full F5_E2 deployment smoke is now runnable **once the existing D7 (`bots`-row INSERT) / D10 (`BOT_SMOKE_PAPER_*` env) / `ohlc_1m`-seeding prerequisites in this checklist are met** (still the optional deeper confirmation, NOT the E3 sign-off gate — E3 stays CI-grade per T-522 decision A)". **Preserve the `(L-028 / T-522 close-out decision A)` tag verbatim** in the reworded line; do NOT relabel the note L-026; do NOT weaken/remove the D7/D10/`ohlc_1m` prerequisite checkboxes (`:27`, `:28`, `:31` — untouched); do NOT change the E3-is-CI-grade stance (T-522 decision A, not this task's call). (L-026 enters only as the rationale for *bundling* the F5_close_out forward-annotation into this same commit — see item 4 — not as a relabel of the `:29` note.)
4. **`docs/runbooks/F5_close_out.md`** (markdown) — **forward-annotation ONLY** (T-542 boundary-refinement #1 precedent). L-026 active control = enumerate the FULL duplicate set up front (the exact lesson — never stop at the first hit): the `strategy-engine-smoke` / F5_E2-deployment-smoke residual context appears in **BOTH signed point-in-time Result lines** — **§A `:91`** ("…blocked by the D1–D11 chain / `strategy-engine-smoke` residual…", signed `:88-90` operator `luster`) **AND §B `:106`** ("…the full F5_E2 kill/restart smoke stayed CI-grade per the T-522 close-out decision A…", signed `:111` `E6 … SIGNED OFF: operator: luster @ 2026-05-17T05:24:14+00:00`). **Both §A:91 and §B:106 are operator-signed point-in-time attestations → BYTE-UNCHANGED** (ADR-0015:35; rewriting either falsifies a signed attestation). Disposition: add exactly ONE non-mutating forward-annotation bullet to the `## Deliberate residual` section (the shared, non-signed forward-annotation site T-542 already annotated — it forward-covers the whole sign-off, BOTH §A and §B) noting the `strategy-engine-smoke` residual was resolved post-MVP by F6/T-544 (does not retro-change the signed F5 §A/§B sign-off). Net: 1 forward-annotation bullet, 0 signed-line edits, both §A:91 + §B:106 consciously checked + left verbatim. The non-signed `:48` pointer ("see its L-028 honesty note re the `strategy-engine-smoke` residual") stays accurate (points to F5_E2:29, reworded in item 3) — no edit there.
5. **NEW `tests/scripts/test_compose_strategy_engine_services.py`** (~35 test LOC) — the regression assertion, YAML-parse (not text-grep), no docker/subprocess, CI-cheap. **`yaml.safe_load` tag handling (verified at plan-stage):** `compose.yaml` has NO custom tags → plain `yaml.safe_load` works. `compose.dev.yaml` uses compose-merge custom tags (`!reset`, `!override`) → `yaml.safe_load` **RAISES `ConstructorError`** (confirmed: raises on `!override` line 79). The test therefore loads `compose.dev.yaml` with a tolerant `yaml.SafeLoader` subclass registering a multi-constructor (`add_multi_constructor("!", lambda loader, suffix, node: loader.construct_scalar(node) if isinstance(node, yaml.ScalarNode) else None)`) so unknown `!`-tags resolve to their underlying value (`!reset null` → `None`); `compose.yaml` uses plain `safe_load`. Assertions: (a) `services['strategy-engine-smoke']` exists in `compose.yaml` with `BOT_ID==smoke`, `image==scalper-v2/strategy-engine:dev`, `SERVICE_NAME==strategy-engine`, `depends_on` keys ⊇ {postgres,nats,nats-init}, a `healthcheck` present; (b) **mirror-exactness**: deep-copy both `strategy-engine-smoke` + `strategy-engine-beta` sub-dicts, overwrite `environment.BOT_ID` AND `environment.BOT_CONFIRM_LIVE` to a common sentinel on **both**, then assert the two normalized sub-dicts are `==` (full structural equality — build/image/restart/networks/env_file/SERVICE_NAME/LOG_LEVEL/NATS_URL/BOT_CONFIG_DIR/PLUGIN_REGISTRY_PATH/depends_on/healthcheck — a future drift from the parameterized pattern red-fails); (c) dev overlay: `compose.dev.yaml services['strategy-engine-smoke']` equals `services['strategy-engine-beta']` **verbatim** (NO key substitution — the dev overlay carries no per-bot keys, only `env_file: !reset null`→`None` + `environment.DATABASE_URL`). Sibling `tests/scripts/test_dev_up_service_identity.py` (T-543) unaffected.

## Scope (OUT)

- **No new Dockerfile** — `strategy-engine-smoke` reuses the existing single `services/strategy_engine/Dockerfile` / `scalper-v2/strategy-engine:dev` image (the T-309 parameterized-by-BOT_ID pattern). It is a new compose *service block*, not a new service *type*.
- **No `ops.md` change** — the per-bot `strategy-engine-BOTID` compose-block procedure is **already documented** in `docs/runbooks/ops.md` "Pridanie nového bota" Krok 5 (`:224-249`, the `strategy-engine-BOTID:` template). `strategy-engine-smoke` is a fixture instance of that already-documented parameterized pattern, **not a new service type / new procedure**, so the CLAUDE.md "Kedy aktualizovať ops runbook → Pridáva novú službu" mandatory trigger is **discharged, not skipped** (state explicitly for brief-reviewer). No new Dockerfile, no config-Pydantic-schema change, no `pool.py` env var, no `bots`/`bot_configs` table change either.
- No `configs/bots/smoke.yaml` change (T-540 shipped it runnable). No `execution-service` change (singleton, dispatches all bots' OrderRequests by `bot_id` — not per-bot). No actual F5_E2 smoke *execution* (the runbook's job, not this task — §0.8; T-544 only makes it *runnable*). No change to the E3-is-CI-grade stance (T-522 decision A). Not absorbed elsewhere.

## Files

`compose.yaml`; `compose.dev.yaml`; `docs/runbooks/F5_E2_shadow_smoke.md`; `docs/runbooks/F5_close_out.md` (forward-annotation only — signed §A `:91` AND §B `:106` both byte-unchanged); NEW `tests/scripts/test_compose_strategy_engine_services.py`; + this `docs/plans/T-544.md`; at close `TASKS.md` (T-544 `[ ]`→`[x]`; F6 3/4 → **4/4**; F6 exit-criteria MET — note all three: H-005 resolved [T-542] + D9 fixed [T-543] + strategy-engine-smoke shipped [T-544]) + `docs/status.md` (newest-first entry).

## §0.3 LOC

~25 yaml (compose block ~22 + dev overlay ~3) + ~30 test LOC ≪ 400. Markdown (F5_E2/F5_close_out/plan) §0.3-exempt. Trivial config, **1 task, no split**. No new dependency (`pyyaml` already a repo dep — used by `load_bot_config` etc.). No ADR (a 3rd instance of the gate-approved parameterized `strategy-engine-<bot>` pattern — no architecture decision, no BRIEF deviation; T-544 admissibility pre-governed by ADR-0015's F6-numbered set + TASKS.md:189, no per-item ADR per the ADR-0015 model).

## Hazards (§20) / non-negotiables

- No §20 hazard. **§N7** (hexagonal): compose is deployment topology, zero business logic — `strategy-engine-smoke` runs the identical T-310b consumer image as alpha/beta, parameterized only by `BOT_ID`. No new behaviour, no new code path.
- **math-validator OUT-of-scope**: the diff touches `compose.yaml` / `compose.dev.yaml` / `docs/` / `tests/scripts/` — none of the Gate-4 trigger paths (`packages/features/*`, `packages/pnl/`, `services/feature-engine/`, `services/execution/`, `services/scoring/`); no math.
- N1/N3/N4 N/A (no time/SQL/external-write/financial-math). The regression parse test is the mandated test (mirror the T-543 `tests/scripts/` precedent + the hazard-catalog parser-assertion style).
- **ops.md mandatory-rule check: discharged** (procedure already in ops.md Krok 5 `:224-249`; fixture instance of an already-documented pattern, no new Dockerfile/service-type) — explicitly stated so brief-reviewer's CLAUDE.md "Kedy aktualizovať ops runbook" check is satisfied without a runbook edit.
- **L-028** is the lesson tag of the `F5_E2:29` note itself (literally `(L-028 / T-522 close-out decision A)` — a documented-procedure-drift / not-execution-verified honesty note; NOT relabelled). **L-026** enters ONLY as the discipline for *bundling, in this one commit*, the forward-annotation of the residual's other authoritative refs + its active control "enumerate the FULL duplicate set": the residual context appears in BOTH `F5_close_out.md` signed Result lines — §A `:91` AND §B `:106` (both operator-signed point-in-time → BYTE-UNCHANGED, ADR-0015:35) — covered by ONE shared `## Deliberate residual` forward-annotation bullet (T-542 precedent). Net: F5_E2:29 reworded (non-signed, L-028 tag preserved, service-missing blocker only — D7/D10/ohlc prerequisites intact); F5_close_out §A:91 + §B:106 byte-unchanged + 1 forward-annotation.

## Test strategy

`tests/scripts/test_compose_strategy_engine_services.py` (see §Scope-IN-5 for the exact loader + assertion spec): (1) presence+fields of `compose.yaml services['strategy-engine-smoke']` (`BOT_ID==smoke`, image, `SERVICE_NAME==strategy-engine`, depends_on ⊇ {postgres,nats,nats-init}, healthcheck present); (2) **mirror-exactness** (load-bearing pin): normalize `environment.BOT_ID` + `environment.BOT_CONFIRM_LIVE` to a common sentinel on **both** `strategy-engine-smoke` and `strategy-engine-beta` sub-dicts, then assert full structural `==` (catches any drift from the parameterized pattern); (3) `compose.dev.yaml` (loaded via the tolerant `!`-tag SafeLoader subclass — plain `safe_load` raises `ConstructorError` on `!reset`/`!override`, verified at plan-stage) `services['strategy-engine-smoke']` equals the beta overlay **verbatim** (no key substitution — dev overlay has no per-bot keys). Hand-deterministic (parses two checked-in YAML files; no docker, no stack). Sibling `tests/scripts/test_dev_up_service_identity.py` (T-543) unaffected.

## Open questions

**None.** Design is fully determinable: mirror `strategy-engine-beta` verbatim with `BOT_ID: smoke` + `${BOT_SMOKE_CONFIRM_LIVE:-}` (the established per-bot pattern alpha/beta already instantiate twice); add the matching `compose.dev.yaml` overlay; reword the **L-028**-tagged F5_E2:29 honesty-note (non-signed; service-missing blocker only, D7/D10/ohlc prerequisites + the L-028 tag + the E3-CI-grade/T-522-decision-A clause preserved); F5_close_out **§A:91 AND §B:106** (both operator-signed point-in-time) byte-unchanged + ONE shared `## Deliberate residual` forward-annotation (T-542 precedent); YAML-parse regression test via the tolerant `!`-tag loader (T-543 `tests/scripts/` precedent; plain `safe_load` raises on compose.dev.yaml's compose-merge tags — verified at plan-stage). No library/scope/default ambiguity; no ADR; ops.md discharged (already-documented Krok 5 pattern).

## Relevant paths

- `compose.yaml:343-432` (strategy-engine section comment + alpha `:361` + beta `:396` blocks — mirror beta), insert after the beta block (`:417`-ish)
- `compose.dev.yaml:136-150` (strategy-engine-alpha/-beta dev overlay — mirror for smoke)
- `docs/runbooks/F5_E2_shadow_smoke.md:29` (the **L-028**-tagged honesty-note — reword: service-missing blocker only), `:27`/`:28`/`:31` (D7/D10/`ohlc_1m` prerequisites — UNTOUCHED), `:30` (the `up -d … strategy-engine-<bot>` command T-544 enables)
- `docs/runbooks/F5_close_out.md:48` (non-signed pointer — no edit), **`:91` (operator-SIGNED §A Result — BYTE-UNCHANGED)**, **`:106` (operator-SIGNED §B Result, signed `:111` — BYTE-UNCHANGED; the L-026-active-control duplicate the first pass missed)**, `## Deliberate residual` section (the single shared T-542 forward-annotation site covering BOTH §A+§B)
- `docs/runbooks/ops.md:224-249` ("Pridanie nového bota" Krok 5 `strategy-engine-BOTID` template — already documents this pattern ⇒ no ops.md edit)
- `configs/bots/smoke.yaml` (T-540, the bot the service loads — untouched); `tests/scripts/test_dev_up_service_identity.py` (T-543 parse-test precedent)
- TASKS.md:189 (T-544 entry); `docs/CLAUDE_CODE_BRIEF.md:2627` (§19 F6 task line), `:2633` (§19 F6 exit-criterion "strategy-engine-smoke compose service shipped"); ADR-0015 (F6; the strategy-engine-smoke residual in the F6-numbered set)

## Write-time guidance (plan-reviewer Gate-1 APPROVE pass-2 — brief-reviewer verifies each before SHIP)

1. `compose.yaml` `strategy-engine-smoke` block must mirror `strategy-engine-beta` (`:396-417`) EXACTLY — same build/image/restart/networks/env_file/SERVICE_NAME/LOG_LEVEL/NATS_URL/BOT_CONFIG_DIR/PLUGIN_REGISTRY_PATH/depends_on/healthcheck — only `BOT_ID: smoke` + `BOT_CONFIRM_LIVE: ${BOT_SMOKE_CONFIRM_LIVE:-}` differ; placed immediately after the beta block (ops.md Krok 5 convention).
2. `compose.dev.yaml` overlay for `strategy-engine-smoke` must mirror the alpha/beta dev overlay verbatim (`env_file: !reset null` + `environment: DATABASE_URL: ${DATABASE_URL}`); no per-bot keys.
3. Regression test must use a tolerant `yaml.SafeLoader` subclass with `add_multi_constructor("!", …)` for `compose.dev.yaml` (plain `safe_load` raises `ConstructorError` on `!reset`/`!override` — confirmed); plain `yaml.safe_load` for `compose.yaml`. No docker/subprocess.
4. Mirror-exactness assertion must be bidirectional: deep-copy both `strategy-engine-smoke` + `strategy-engine-beta` sub-dicts, overwrite `environment.BOT_ID` AND `environment.BOT_CONFIRM_LIVE` to a common sentinel on BOTH, then assert full structural `==`. Dev-overlay assertion is verbatim equality (no key substitution).
5. `F5_close_out.md` §A `:91` AND §B `:106` (signed at `:111`) must remain BYTE-UNCHANGED — both consciously checked; add exactly ONE forward-annotation bullet to the existing `## Deliberate residual` section; the non-signed `:48` pointer stays as-is.
6. `F5_E2_shadow_smoke.md:29` reword retires ONLY the service-missing blocker — preserve the `(L-028 / T-522 close-out decision A)` tag verbatim, do NOT relabel L-026, do NOT weaken/remove the D7/D10/`ohlc_1m` prerequisite checkboxes (`:27`/`:28`/`:31`), keep the "NOT the E3 sign-off gate — E3 stays CI-grade per T-522 decision A" clause.
7. No `ops.md` edit — Krok 5 `:224-249` already documents the parameterized `strategy-engine-BOTID` pattern; the mandatory ops-runbook trigger is discharged (state explicitly in the commit body for brief-reviewer).
