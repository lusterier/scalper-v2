# T-558b2 — Apply quantize_price at lifecycle BE/trail + trail_audit expected (finding #2 APPLY part 2 — FINAL)

**Type**: behavior apply (capital-safety; reuses the T-558a `quantize_price`; closes triage finding #2).
**Phase**: F6 numbered, ADR-0015 §70-81 decision-C. Operator pre-emptive split L-007: T-558 → T-558a (foundation DONE `2199278`) → T-558b → b1 (placement DONE `1f1ecf9`) + **T-558b2** (this — lifecycle BE/trail + trail_audit; the FINAL finding-#2 piece). blocked-by T-558b1 ✅ DONE. NO per-item ADR (decision-C pre-governed). All policy OQs baked at the T-558b OQ session (OQ-1 side-aware / OQ-2 quantize `expected` + KEEP tolerance / OQ-3 persist quantized / tick-source = `adapter.get_instrument_info(symbol).tick_size` cached). No new operator OQ.
**Gate-1**: plan-reviewer **APPROVE** 2026-05-18 (2 CONCERNs → 5-item Write-time guidance verbatim below; rulings a/b/c/d favorable; no BLOCKER).
**Date**: 2026-05-18.

## Problem (code-confirmed; T-556)

`lifecycle.py` BE/trail monitor: `new_be_sl = _compute_be_sl_price(side, entry_price, be_sl_level)` (~:209) / `new_trail_sl = _compute_trail_sl_price(side, new_best, trail_pct)` (~:248) flow UNQUANTIZED to `adapter.set_trading_stop(symbol=, sl_price=…)` (~:211/:250) + a `position_state` SL persist (~:232-236/:272-276) — same Bybit sub-tick snap/reject + DB↔exchange drift as the placement path (H-038; T-558b1 fixed placement only). **Coupling**: `trail_audit.py:185` recomputes `expected = _compute_trail_sl_price(...)` (real, NOT patched) and `:188` `drift = abs(exchange_sl - expected)/expected > tolerance` → emits `trail_sl_drift_detected`. If lifecycle quantizes the trail SL but `expected` stays raw, quantized `exchange_sl` vs raw `expected` drifts ~1 tick → **false-positive drift alert on every trailing position**. `lifecycle.py:44-49` carries a pre-existing "Known limitation (OQ-3=A): if Bybit normalizes the SL to the symbol price-tick … exact equality would false-positive; tick-tolerant compare is an F5+ follow-up" — T-558b2 RESOLVES it (we now send the tick-aligned value; exact-equality holds).

## Fix shape — files

1. `services/execution/app/lifecycle.py`: BE — `new_be_sl = quantize_price(_compute_be_sl_price(side, entry_price, be_sl_level), side, tick)`; trail — `new_trail_sl = quantize_price(_compute_trail_sl_price(side, new_best, trail_pct), side, tick)`. `tick = (await adapter.get_instrument_info(symbol)).tick_size` (cached 1h TTL — mirror placement; `symbol`/`side`/`adapter` in scope per `run_position_monitor_for_trade` :98-108). The quantized Decimal flows to BOTH `set_trading_stop` AND the `position_state` SL persist (same value — DB==exchange, OQ-3). Import `quantize_price` from `packages.exchange.quantize`. Update the `lifecycle.py:44-49` limitation comment (RESOLVED — tick-aligned sent; not an F5+ follow-up anymore).
2. `services/execution/app/trail_audit.py`: `expected = quantize_price(_compute_trail_sl_price(side, best, trail_pct), side, tick)` with `tick = (await adapter.get_instrument_info(ps.symbol)).tick_size` — fetched in **Phase B (conn-free, alongside `get_positions`)**, NEVER inside the Phase-C `pool.acquire()` block (preserves the `trail_audit.py:60-66` "conn never held across a network call" invariant); per-symbol 1h-TTL cache (same as lifecycle/placement; NOT per-candidate uncached). KEEP the `drift = abs(exchange_sl - expected)/expected > tolerance` math + tolerance byte-unchanged (OQ-2). NO retune.
3. `docs/CLAUDE_CODE_BRIEF.md` §20 H-038: AMEND the SAME block — (i) `**Test.**` line: APPEND the new b2 BE/trail/trail_audit bare `test_*` func-names (L-036; never a bare `*.py`); (ii) the `**Policy.**` parenthetical `(placement SL/TP: T-558b1; BE/trail enforcement + tests: T-558b2)` → b2-done; (iii) the trailing numbering-note sentence "…BE/trail enforcement + tests appended by T-558b2 to this same block." → b2-done. (WG#2: broad-stem grep `T-558b2` *within the H-038 block* — TWO forward-ref sites, L-026/L-035 class.) H-037/H-036/H-030..H-036-cluster-CLOSED byte-unchanged.

## L-034 grep-evidenced sibling enumeration (the T-558b1 pass-1 lesson applied at plan-draft)

**Zero-value-ripple test-fixture design (mirror T-557's `_ok_adapter` relax):** ALL existing lifecycle/trail_audit handler-flow + fixture values are exact multiples of 0.001 — grep-verified: `test_lifecycle.py:447/453/459/465` compute-unit (`100.300/99.700/109.450/90.450`), handler-flow `:492` (`== Decimal("100.300")`)/`:513`, `test_trail_audit.py:65` `_pos` sl_price `"99.5"`, `:96` `_ps` `Decimal("99.5")`, `:195` expected assert `"99.500"`. **Choose the test adapter-fixture `get_instrument_info` → `InstrumentInfo(..., tick_size=Decimal("0.001"))`**: `quantize_price(100.300,_,0.001)=100.300` (100300 int → ROUND_FLOOR → 100.300) etc. — every existing asserted value is **on-grid → quantize no-op → ZERO existing value-assert changes**.

- **Sibling-maintenance (the ONLY existing-test change) — L-034 site-set (grep `_adapter`/`_build_args`/`used_adapter`/`get_positions = `/`get_instrument_info` across BOTH test files; full set enumerated in-commit, mirror T-558a's 12-site discipline):** add `get_instrument_info = AsyncMock(return_value=InstrumentInfo(symbol=…, qty_step=…, min_order_qty=…, min_notional_usd=…, tick_size=Decimal("0.001")))` to `test_lifecycle.py` `_build`/`used_adapter` (~:197-206 — currently mocks `set_trading_stop`/`get_positions`, NOT `get_instrument_info` → `await adapter.get_instrument_info` would fail on the non-AsyncMock MagicMock) and `test_trail_audit.py` `_adapter` (:103-109 — mocks only `get_positions`).
- **Byte-unchanged carve-out** (T-558b2 changes NEITHER `_compute_be_sl_price` NOR `_compute_trail_sl_price`): `test_lifecycle.py:444-465` compute-unit tests (raw `100.300/99.700/109.450/90.450`) — UNCHANGED by construction. The handler-flow asserts `:492/:513` (`== Decimal("100.300")`) — UNCHANGED (on-grid at tick 0.001 → quantize no-op; value byte-identical, only the data-path now passes through quantize_price returning the same Decimal).
- **NEW tests** (the H-038 b2 citations + §N4): sub-tick fixture (tick `Decimal("0.1")`) proving quantize IS applied — lifecycle trail buy `109.450`→`109.4` (ROUND_FLOOR), trail sell `90.450`→`90.5` (ROUND_CEILING), a BE buy case; lifecycle persists the SAME quantized value (DB==exchange); trail_audit `expected` quantized → drift NOT false-positive vs a quantized exchange SL. Hand-literals, NOT `quantize_price(...)` re-calls.
- **NOT touched** (grep-evidenced): `test_placement.py`/`test_placement_persist.py` (b1/carve-out), `shadow_worker`/`test_shadow_*` (PaperExchange BE/trail = bound-out, no real exchange reject — rationale recorded), `test_paper_parity`/`test_dispatcher` (no lifecycle-SL literals).

## Hand verification (math-validator ACTIVE — reuses L-037-correct T-558a quantize_price)

- on-grid no-op (existing fixtures, tick 0.001): `quantize_price(Decimal("100.300"),"buy",Decimal("0.001"))` → `100.300/0.001=100300` (int) → ROUND_FLOOR `100300` → `*0.001` = `100.300` (UNCHANGED — proves zero-ripple). Same for `109.450/99.700/90.450/99.500/99.0` (all exact ×0.001).
- NEW sub-tick apply (tick 0.1): trail buy `_compute_trail_sl_price("buy",110,0.005)=110*0.995=109.450` → buy ROUND_FLOOR `/0.1=1094.5 → 1094 → 109.4` (LONG trail SL down = never tighter). trail sell `_compute_trail_sl_price("sell",90,0.005)=90*1.005=90.450` → sell ROUND_CEILING `/0.1=904.5 → 905 → 90.5` (SHORT trail SL up = never tighter). BE buy `_compute_be_sl_price("buy",100.07,0.003)=100.07*1.003=100.37021` → buy ROUND_FLOOR `/0.1=1003.7021 → 1003 → 100.3`. trail_audit: `expected` quantized identically → vs a quantized exchange SL, drift ≈ 0 (no false-positive).
All Decimal §5.3 (no float, no `//`; quantize_price unchanged from T-558a).

## Hazards / invariants

- §20: H-038 AMENDED (Test += b2 func-names; both forward-ref sites b2-done per WG#2). H-037/H-036/H-030..H-036-cluster byte-unchanged. **H-024** (ADR-0005 trail-after-partial-TP) NOT regressed — quantize rounds only the SL *price* side-aware conservatively (never tighter); the FSM `sl_type`-promotion + exec_type-derivation + partial-TP interaction are byte-unchanged; trail monotonic-tightening safe (quantize may be <1 tick *looser*, never tighter). **H-013** unaffected. The lifecycle watchdog exact-equality false-positive (lifecycle.py:44-49) RESOLVED.
- §5.3 Decimal-only; §N1 no datetime; §N3 no new external-write semantics (set_trading_stop/persist already `@idempotent`, mode unchanged); §N5 services/execution/ 80% list; §N6/N7 no DI/arch change (adapter already ctor param; get_instrument_info existing Protocol method, cached); §N9/L-001 no config literal; §N4 TDD. math-validator ACTIVE. ops.md MANDATORY schema-trigger NOT fired.
- §0.3: src ~<50 LOC (BE/trail quantize wraps + 2 get_instrument_info fetches + import + lifecycle:47 comment + H-038 amend); tests cap-exempt. No new dependency (quantize_price from T-558a). Commit `feat(T-558b2)`+`chore`; push operator-authorized; F6 17/18→18/18 at close (L-027 — finding #2 FULLY CLOSED).

## Out of scope (§0 anti-absorb)

shadow_worker BE/trail quantize (PaperExchange, no real reject — bound-out, rationale recorded); finding #3 / triage C / T-551 / T-555; `quantize_qty` false-`//`-docstring (T-558a bound-out); any drift-tolerance retune (OQ-2 KEEP).

## Acceptance criteria

1. lifecycle BE+trail: `new_be_sl`/`new_trail_sl` = `quantize_price(_compute_*, side, tick)`; tick from `await adapter.get_instrument_info(symbol)`; quantized value to BOTH set_trading_stop AND position_state SL persist. 2. trail_audit `expected` quantized (Phase B conn-free, per-symbol 1h cache); drift math + tolerance byte-unchanged. 3. lifecycle.py:44-49 limitation comment RESOLVED. 4. §20 H-038: Test line += b2 bare func-names; BOTH forward-ref sites (parenthetical + numbering-note) b2-done; H-037/cluster byte-unchanged; `tests/test_hazard_catalog_coverage.py` green. 5. test_lifecycle/test_trail_audit adapter fixtures += `get_instrument_info` AsyncMock(InstrumentInfo tick_size=0.001) — every adapter-construction site enumerated in plan-doc; existing value-asserts byte-unchanged (on-grid no-op); compute-unit `:444-465` carve-out untouched. 6. NEW tests (sub-tick fixture) prove quantize-applied at lifecycle set+persist + trail_audit expected; hand-literals not quantize_price re-calls. 7. repo-wide pytest 0 regressions incl. hazard-catalog. 8. ruff/format/mypy/bandit clean. 9. math-validator ACTIVE — §Hand verification holds. 10. F6 17/18→18/18 at close (L-027; finding #2 fully closed).

## Write-time guidance

(plan-reviewer APPROVE 2026-05-18 verbatim — binding for drift-checker Gate-2 + brief-reviewer Gate-3.)

1. **trail_audit get_instrument_info placement + caching (CONCERN-b):** the `get_instrument_info(ps.symbol)` await for the `expected` quantize MUST stay in Phase B (the conn-free section, alongside `get_positions`) — never inside Phase C `pool.acquire()` block — to preserve the module-docstring invariant (`trail_audit.py:60-66`: "The pool conn is never held across a get_positions network call"; a `get_instrument_info` HTTP call held across an acquired conn would violate it). trail_audit runs an APScheduler tick across ALL trailing trades/bots → fetch per *distinct symbol* with the same 1h-TTL cache as lifecycle/placement (NOT per-candidate uncached: N trailing trades = N network calls/tick). Record the chosen cache-key granularity (per-symbol) in the plan-doc.
2. **H-038 §20 block — TWO forward-ref sites in the SAME block (CONCERN-c, L-026/L-031/L-035 class):** before commit, broad-stem grep `T-558b2` *within the H-038 block itself* and update BOTH: (i) the `**Policy.**` parenthetical `(placement SL/TP: T-558b1; BE/trail enforcement + tests: T-558b2)`, AND (ii) the trailing "H-038 numbering note" sentence (per T-558b1 plan: *"...BE/trail enforcement + tests appended by T-558b2 to this same block."*). Enumerate the full b2-forward-ref set in the plan-doc; do not stop at the first parenthetical (this is the exact L-026/L-035 recurrence shape — duplicate forward-ref within one block).
3. **H-038 `**Test.**` L-036 control:** append ONLY bare `test_*` func-names (or `path.py::func`) that exist at b2-commit — NEVER a bare `*.py` filename (`tests/test_hazard_catalog_coverage.py::_PATH_RE` red-fails a bare filename). Run `tests/test_hazard_catalog_coverage.py` (repo-root meta-test, NOT in the services/execution suite) in the pre-commit verification. H-037/H-036/H-030..H-036-cluster byte-unchanged.
4. **Sibling-fixture enumeration (L-034) — grep-evidence in plan-doc:** enumerate EVERY adapter-construction site in `test_lifecycle.py` (`_build_args`/`used_adapter` ~:197-206 — currently mocks `set_trading_stop`/`get_positions`, NOT `get_instrument_info`) and `test_trail_audit.py` (`_adapter` :103-109 — mocks only `get_positions`) that needs an `get_instrument_info = AsyncMock(return_value=InstrumentInfo(... tick_size=Decimal("0.001")))` addition, by grepping `_adapter`/`_build_args`/`used_adapter`/`get_positions = `/`get_instrument_info` across BOTH files (mirror T-558a's 12-site `InstrumentInfo(` discipline). Without the mock, `await adapter.get_instrument_info` fails on the non-AsyncMock MagicMock. List the full site-set in the plan-doc.
5. **NEW-test hand-literals (math-validator discipline):** the sub-tick fixture proofs (tick `Decimal("0.1")`: trail buy `109.450`→`109.4` FLOOR, trail sell `90.450`→`90.5` CEILING, a BE buy case, trail_audit `expected` quantized → drift NOT false-positive vs quantized exchange SL) MUST assert hand-computed Decimal literals, NEVER a `quantize_price(...)` re-call (implementation-against-itself). math-validator is ACTIVE (services/execution/ + capital-sensitive price math) and will independently recompute §Hand verification incl. the SHORT-side CEILING direction (L-037 — recompute the formula, not just the target).

## Cross-references

BRIEF §5.3 / §9.5 / §20 H-038 (the T-558b1-created block this amends) / H-024 (ADR-0005, not regressed) / §6.7 / §19; ADR-0015 §70-81 (F6 decision-C); `docs/plans/T-558b1.md` (the H-038 block text + the two forward-ref sites) + `docs/plans/T-558a.md` (`quantize_price` foundation + L-037) + `docs/plans/T-556.md` (the live-diagnostic); ADR-0005 (H-024 trail-after-partial-TP); review-lessons L-007/L-026/L-034/L-035/L-036/L-037 (applied).
