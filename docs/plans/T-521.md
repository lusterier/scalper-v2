# T-521 — F5 Final docs pass

**Status:** APPROVED (plan-reviewer Gate-1 pass-2 APPROVE 2026-05-16; pass-1 REVISE 1 BLOCKER [L-026 incomplete opposite_side_open enumeration — missed §B.3 plugin_registry site] + 1 CONCERN [§19 filename↔bullet mapping must be baked] + 1 attribution note [§0.3-exempt source] — all resolved + re-verified against ground truth). Docs-only, single task (operator OQ-1=A). master HEAD 2d9adeb (T-519 CI-GREEN; blocked-by T-519 ✓ [x] commit 3d4fc38). ADR-0011 governs E4/E5/E6; no new ADR. Unblocks T-522 (E5/E6 Live-ready sign-off).

## Purpose
F5 final docs pass: the 2 operator-runnable smoke runbooks + the full MVP-complete README rewrite + the §21 glossary F5-term additions + the residual §B/§11 BRIEF deltas (incl. the 2-site opposite_side_open ANNOTATE-DEFERRED). Docs-only — every artifact is markdown; §0.3-exempt per **task-def TASKS.md:206 ("Est: ~400 LOC docs (markdown exempt §0.3)") + the established T-500/T-519/T-523 markdown-only precedent** (NOT the BRIEF §0.3 text, which exempts only generated code + tests). 0 src LOC.

## Operator OQ answers (T-521 plan-stage 2026-05-16, all recommended, baked)
- OQ-1=A — SINGLE task (not L-007 split; thematic close-out unity; precedent T-423/T-313 single ~350 LOC).
- OQ-2=A — opposite_side_open example/registry sites: ANNOTATE with a DEFERRED+pointer YAML comment (keep the design intent; do NOT remove; do NOT defer-entirely).
- OQ-3=A — README FULL MVP-complete rewrite (~250-350 LOC).
- OQ-4=A — §B.1 sizing example shows BOTH methods (explicit `method: tier` + commented risk_per_sl alternative).
- BAKED (not an OQ — doc-must-match-shipped-code): §11 protocol delta = add the 3 F5 @idempotent methods.

## Deliverables (5 markdown artifacts; §0.3-exempt)
1. **NEW `docs/runbooks/F5_E1_backtest_smoke.md`** — operator-runnable backtest smoke, verbatim-mirror the F4_E1 skeleton (Header[+verbatim §19 backtest bullet] / Purpose / Prereq `[ ]` checklist / numbered Steps with `[ ]` assertions: backtest CLI 30-day window run → verify `backtest_runs.summary` aggregates; compare CLI two-run → aggregate-diff + per-trade diff / Exit-checklist[verbatim §19 bullet] / Sign-off[`Run timestamp: …+00:00` §N1 / Operator / Master HEAD / Result PASS|PARTIAL|FAIL] / optional Discoveries).
2. **NEW `docs/runbooks/F5_E2_shadow_smoke.md`** — same skeleton; Steps: trade with `shadow.enabled` → kill execution-service mid-variant → restart → verify the `shadow_variants` row finalizes post-restart via OHLC replay recovery. Header quotes the verbatim §19 shadow-restart bullet.
3. **`README.md`** — FULL MVP-complete rewrite (~250-350 LOC, current 49 LOC stale "Phase F0"): status (F5 complete / Live-ready MVP) + architecture (multi-service: signal-gateway→feature-engine→strategy-engine→execution-service→analytics-api + market-data; backtest/paper/live data-flow) + features (backtest harness/ReplayBus, shadow variants+restart recovery, risk-mgmt caps, tier+risk-per-SL sizing, equity+funding tracking) + quick-start (deployment per BRIEF §18 + the F5_E1/E2 runbook cross-refs) + ADR-0011 hardening ref; retain the explicit "CLAUDE_CODE_BRIEF.md = source of truth" pointer.
4. **`docs/CLAUDE_CODE_BRIEF.md` §21 glossary** — +8-12 F5 terms (backtest harness, ReplayBus, intra-candle path, tier sizing, risk-per-SL sizing, equity snapshot, funding-fee tracking, kill-switch, concurrent-trades cap, daily-loss-limit, max-drawdown-stop, slippage model — each cross-ref'd to its §section/ADR), ~100-150 LOC additive, mirror the existing §21 entry format exactly.
5. **`docs/CLAUDE_CODE_BRIEF.md` residual §B/§11 deltas** (WRITE-TIME-VERIFY: read the actual current structure + line-nums verbatim before editing — L-018):
   - **opposite_side_open ANNOTATE — EXACTLY TWO in-scope blocks (definitive plan-stage L-026 sweep; no third)**: (a) §B.1/§22 alpha.yaml `block_opposite_position` rule (BRIEF:3123-3128; the `type: opposite_side_open` at :3127 is the same block — one comment covers it); (b) §B.3 `configs/plugin_registry.yaml` (BRIEF:3220-3221: `- name: opposite_side_open` + `entry_point: packages.scoring.rules.opposite_side:OppositeSideOpenRule` — same block, one comment). Each gets `# DEFERRED — not yet implemented; see §20 H-005 / the T-F5+ backlog ticket`; the §B.3 comment additionally notes the entry_point target (`packages.scoring.rules.opposite_side:OppositeSideOpenRule`) is unimplemented (T-519 code-verified non-existent). Do NOT remove either; do NOT defer-entirely. Closes the last residual H-005 brief self-contradiction the T-519 L-026 lesson flagged.
   - §B.1 alpha.yaml sizing block (BRIEF:3130-3150): explicit `method: tier` on the tier-ladder example + a commented `# method: risk_per_sl` / `# risk_pct: 0.01` alternative — matching the shipped SizingSection (`packages/scoring/types.py` — `method: Literal["tier","risk_per_sl"]` default "tier"; cite verbatim field-names/line-nums at write-time).
   - §11 ExchangeClient protocol: add the 3 F5 @idempotent methods — `get_account_balance(sub_account: str) -> AccountBalance` (T-530), `get_mark_price(symbol: str) -> Decimal` (T-527b1), `get_funding_fees_window(sub_account: str, since: datetime) -> list[FundingFee]` (T-532a) — signatures copied VERBATIM from the shipped `packages/exchange/protocols.py` @idempotent definitions, in the existing §11 listing style.
   - §12/§13: NO delta (pass-1-verified SOUND — intra-candle path + restart recovery + rejected-signal tracking all current; do NOT touch).

## Scope boundary (deliberate, L-026 recorded)
NOT touched: §20 H-005:2647/2649 + `consumer.py:30` (T-519-corrected, out of T-521 scope); §12/§13 (SOUND, no delta); immutable `docs/plans/` historical records (point-in-time, never rewritten); the opposite_side_open RULE IMPLEMENTATION (owned by the T-F5+ backlog ticket — T-521 annotates the two example/registry sites only, annotation ≠ implementation).

## §20 / §N / Gate-4
- No code change → 0 §20 regression, no new H-NNN, §N9 N/A, 0 src LOC. §N10 brief self-consistency (strengthening): §11-protocol + §B.1-sizing deltas make the BRIEF match shipped code; the 2-site opposite_side_open annotation closes the last H-005 self-contradiction (the §B.1/§B.3 examples were the deliberate T-519 boundary → T-521 makes the brief fully self-consistent on H-005-DEFERRED).
- **Gate-4 OUT OF SCOPE, fast no-op, COMMITTED** (no CLAUDE.md Gate-4 trigger-list path — zero code, zero Decimal arithmetic; verbatim the T-523/T-519/T-533a markdown-meta-task disposition; no residual optionality).
- T-519 meta-test (`tests/test_hazard_catalog_coverage.py`) UNAFFECTED — it parses only §20; T-521 edits §B.1/§B.3/§11/§21/README, never §20. Sanity-confirm green at write-time (cheap; expected green — no §20 touch).
- No L-021 (no migration). Markdown-only → no ruff/mypy/§0.3 (0 `.py` in the staged set).

## Test strategy
Docs-only — NO new tests, NO test churn. The runbooks ARE the operator-test artifacts (executed by the operator in T-522, mirror F4_E1→T-423 sign-off precedent). Sanity: the T-519 meta-test stays green (no §20 edit).

## §0.3 / L-022 / L-007
0 src LOC (markdown-exempt per task-def:206 + the T-500/T-519/T-523 precedent). ~700-900 LOC docs (the task-def's "~400" underestimated; markdown-exempt so NOT a cap breach; OQ-1=A SINGLE confirmed; precedent T-423/T-313 close-out single). L-022 N/A (no code/Hand-verification-docstring). No L-007 split.

## Open questions
None. OQ-1/2/3/4 + §11-protocol baked. The 2-block opposite_side_open enumeration is definitive (plan-stage L-026 sweep, no 4th site). The §19 filename↔bullet MAPPING is baked (header quotes the verbatim §19 bullet, no E-number; filenames verbatim task-def, sequential F5-runbook naming). §0.3-exempt attributed to task-def:206 + precedent. The 3 WRITE-TIME-VERIFY items are L-018 read-before-quote (exact §19 bullet text; exact §B.1/§B.3/§11 current structure+line-nums; exact shipped signatures/field-names) — NOT unresolved design. Gate-4 out-of-scope COMMITTED.

## Write-time guidance (plan-reviewer verbatim — must be addressed during implementation)
1. opposite_side_open ANNOTATE — exactly TWO in-scope blocks, no third: (a) §B.1 `block_opposite_position` rule BRIEF:3123-3128 (the `type: opposite_side_open` at :3127 is the same block, one comment covers it); (b) §B.3 plugin_registry BRIEF:3220-3221 (`- name: opposite_side_open` + `entry_point: packages.scoring.rules.opposite_side:OppositeSideOpenRule`, same block, one comment). Each gets a `# DEFERRED — not yet implemented; see §20 H-005 / T-F5+ backlog ticket` YAML comment. Do NOT remove either entry; do NOT defer-entirely. The §B.3 comment must additionally note the entry_point target (`packages.scoring.rules.opposite_side:OppositeSideOpenRule`) is unimplemented (T-519 code-verified non-existent). Do NOT touch §20 H-005:2647/2649 or consumer.py:30 (T-519-corrected, out of T-521 scope). The rule IMPLEMENTATION is owned by the T-F5+ backlog ticket — T-521 annotates examples/registry only.
2. §19 bullet copy is read-before-quote (L-018): at write-time read BRIEF:2597-2603 verbatim and copy the EXACT bullet text into each runbook header. F5_E1_backtest_smoke.md header quotes the backtest bullet(s) ("Backtest on a 30-day historical window completes and reports aggregates." + the side-by-side-compare bullet if grouped); F5_E2_shadow_smoke.md header quotes the shadow-restart bullet ("Shadow variants persist across restart (verified by killing execution-service mid-variant)."). NO E-number claim in any header — the filename "E1/E2" is sequential F5-runbook naming, decoupled from §19 ordinal.
3. §B.1 sizing delta + §11 protocol delta cite VERBATIM shipped source (L-018 read-before-edit): §B.1 sizing block — explicit `method: tier` + commented-out `# method: risk_per_sl` / `# risk_pct: 0.01`, matching the shipped SizingSection in packages/.../types.py (`method: Literal["tier","risk_per_sl"]` default "tier" — read current field names/line-nums at write-time, do not transcribe from memory). §11 protocol — add the 3 F5 methods with signatures copied verbatim from the shipped protocols.py @idempotent definitions (read current line-nums at write-time). §12/§13 = NO delta (pass-1 SOUND, do not touch).
4. Runbooks mirror the F4_E1 skeleton verbatim: Header[+verbatim §19 bullet] / Purpose / Prereq `[ ]` / numbered Steps with `[ ]` assertions / Exit-checklist[verbatim §19 bullet] / Sign-off (ISO-8601 `…+00:00` per §N1 + Operator + Master-HEAD + Result). F5_E2 Steps must exercise: trade with shadow.enabled → kill execution-service mid-variant → restart → verify the shadow_variants row finalizes post-restart via OHLC replay recovery.
5. README full MVP rewrite (~250-350 LOC): status / architecture / features / quick-start with runbook cross-refs / ADR-0011 reference; retain the explicit "BRIEF is source of truth" pointer. §21 glossary: +8-12 F5 terms cross-referenced, mirroring the existing §21 entry format exactly.
