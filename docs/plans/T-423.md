# T-423 — F4 exit-criteria integration bundle (CLOSE-OUT)

**Status**: DRAFT 2026-05-05 (pending plan-reviewer Gate 1)
**Phase**: F4 (LAST F4 task — verification + runbook bundle; T-422 Playwright E2E shipped commit `7dd72c4`)
**Estimate**: ~150 LOC runbook + ~50 LOC tasks-doc updates = ~200 LOC total
**Blocked by**: T-410..T-422 all shipped (✓ verified master HEAD `1bc0261`)
**Unblocks**: F4 phase exit-criteria operator sign-off + F5 phase unlock

---

## Purpose

Final F4 close-out task per BRIEF §19:2552-2571 — verify all 9 sections + scoring inspector + feature inspector + backtest lab + Playwright smoke green, ship `docs/runbooks/F4_E1_dashboard_smoke.md` operator-runnable checklist. Mirror T-313 (F3-close) + T-222 (F2 testnet smoke) verification-and-runbook pattern.

T-423 is **NOT a code-shipping task** — no new src/lib/components. It bundles:
1. **`docs/runbooks/F4_E1_dashboard_smoke.md`** — operator manual smoke runbook (mirrors F3_E1 pattern); operator-runnable checklist verifying BRIEF §19:2569-2570 exit criteria.
2. **TASKS.md F4 close-out narrative** — append F4 phase exit summary to current Done list; document 24/24 tasks shipped + 1 lesson added (L-010); 1789 pytest + 160 vitest + 23 dashboard-query parametrizations + 3 Playwright scenarios live.
3. **`docs/status.md` session-end snapshot** — F4 phase complete; F5 phase pick-up notes (per CLAUDE.md §6.6 session-end pattern).

**No backend test addition** — F4 exit criteria are operator-runnable verification, not pytest assertions. The 1789 pytest + 160 vitest already cover unit-level invariants; T-423 verifies end-to-end UX flow on live operator host.

**Per BRIEF §19:2569-2570 verbatim exit criteria**:
- Operator can navigate all 9 sections, see live data, drill into a trade end-to-end.
- Scoring inspector shows per-rule breakdown for any signal.
- Feature inspector charts historical feature values.
- Backtest lab can trigger a small backtest and show results.
- Playwright smoke journeys pass in CI.

T-423 runbook structures these into a sequential checklist with command snippets + expected outcomes per step.

**Explicitly out of scope for T-423**: net new src LOC (no production code; runbook + docs only); CI workflow changes (Playwright workflow shipped in T-422); pytest test addition (existing 1789 covers backend invariants); UI test addition (existing 160 vitest + 3 Playwright cover frontend); per-bot operator-tunable settings (F5+); production deploy (F5+ — auth + nginx + cloudflared per BRIEF §16.2); load testing (F5+); incident playbook (F5+); operator training material beyond smoke checklist; CEST/UTC toggle implementation (F5+); shadow runtime (F5+ per BRIEF §13).

---

## Public interface

T-423 ships **1 NEW runbook** + **TASKS.md narrative update** + **docs/status.md session-end update**.

### `docs/runbooks/F4_E1_dashboard_smoke.md` (NEW, ~150 LOC)

Operator-runnable smoke checklist covering BRIEF §19:2569-2570 verbatim. Structure mirrors `F3_E1_dvoj_bot_smoke.md`:

```markdown
# F4 E1 dashboard smoke runbook

**Phase:** F4 exit-criteria E1 (BRIEF §19:2569-2570)
**Mode:** dev (operator-host; production F5+)
**Owner:** operator (manual; T-423 ships this runbook as required deliverable)

§19:2569-2570 verbatim: 5 exit criteria.

## Purpose
End-to-end UX smoke verifying 9 dashboard sections + Playwright CI green.

## Prerequisites
- analytics-api running locally on http://127.0.0.1:8000
- Vite dev server running (cd ui && pnpm dev → http://localhost:5173)
- Operator browser open

## Step 1 — Open app + Overview renders 5 tiles
## Step 2 — Per-bot live view (bot selector → drill-in)
## Step 3 — Trade explorer + drill-down
## Step 4 — Backtest lab — trigger small run + see results
## Step 5 — Strategy editor — load + edit + apply (creates new version)
## Step 6 — Feature inspector — chart historical values
## Step 7 — Scoring inspector — select signal → per-rule breakdown
## Step 8 — Audit log viewer — chronological events + filter
## Step 9 — Settings — bot registry + symbol map CRUD
## Step 10 — Playwright CI smoke green (verify on master push)

## Exit checklist
- [ ] All 9 sections navigable
- [ ] Trade drill-down end-to-end (signal → scoring → close)
- [ ] Scoring inspector per-rule breakdown rendered
- [ ] Feature inspector chart populated for at least 1 numeric feature
- [ ] Backtest lab POST returns 202 + new row in versions list
- [ ] Playwright CI green on master HEAD (latest e2e workflow run)
```

Each step has 2-5 sub-bullets with expected outcomes (e.g., "5 tile titles visible: Open positions / Virtual balance / 24h P&L / Signals (24h) / Alerts (24h)").

### `TASKS.md` F4 close-out (~30 LOC update)

Append "F4 phase complete" narrative to current header block + append T-423 entry to Done list. Move all `## Next` F4 entries into `## Done` (T-423 was the last F4 entry). F4 sub-section headings (`### F4 — Backend / Frontend / Operations`) preserved with empty placeholders or pruned (operator preference).

### `docs/status.md` (~20 LOC update)

Append `## 2026-05-05 (session-end)` block per existing convention:
- F4 phase complete (24/24 tasks; 1789 pytest + 160 vitest + 23 dashboard-query + 3 Playwright passing).
- L-010 lesson added (apiFetch 204 No Content fix).
- Master HEAD reference + branch up-to-date.
- F5 pick-up notes (per BRIEF §19:2575+ — Shadow Variants + Backtest Harness + Finishing; est 2-3 weeks).

---

## Scope

### Files touched

| File | Op | Est LOC | Notes |
| --- | --- | --- | --- |
| `docs/runbooks/F4_E1_dashboard_smoke.md` | NEW | ~150 | Operator-runnable smoke checklist (10 steps + exit checklist) |
| `TASKS.md` | UPDATE | ~30 delta | F4 close-out narrative + T-423 entry to Done + `## Next` pruning |
| `docs/status.md` | UPDATE | ~20 delta | Session-end snapshot 2026-05-05; F5 pick-up notes |

### Tests in scope

NO new tests. T-423 is operator verification + documentation, not code. Existing test suite unchanged:
- backend pytest: 1789
- UI vitest: 160
- dashboard-query parametrizations: 23 (gated on PROMETHEUS_TEST_URL)
- Playwright scenarios: 3 (gated on master push trigger)

### Behavioral contract — runbook structure

10-step checklist. Each step:
1. Heading: `## Step N — <action verb> <expected outcome>`
2. 1-2 sentence prose context
3. Imperative commands (browser navigate / click / observe; NO bash unless infrastructure step)
4. Expected outcome bullets prefixed with `- [ ]` for operator check-off

Per BRIEF §19:2569-2570 5 exit criteria mapped 1:1 to runbook sections:
- "navigate all 9 sections" → Steps 1-9
- "drill into a trade end-to-end" → Step 3
- "scoring inspector per-rule breakdown" → Step 7
- "feature inspector charts historical values" → Step 6
- "backtest lab triggers small backtest + show results" → Step 4
- "Playwright smoke journeys pass in CI" → Step 10

### Behavioral contract — TASKS.md narrative

Header line 4: update F4 phase counter from "24/24" to "24/24 + T-423 close-out runbook shipped" (or similar wording).

Move T-423 entry from `## Next` (already removed when T-422 shipped — wait, T-423 not yet in `## Next` per current state; verify).

`## Next` F4 sub-section headings: prune empty `### F4 — Backend (analytics-api + alerting-svc)` + `### F4 — Frontend (Dashboard UI)` + `### F4 — Operations + Exit criteria` headings since no remaining F4 tasks. F5 phase placeholder section added with note "F5 pick-up TBD per BRIEF §19:2575+".

### Behavioral contract — docs/status.md update

Mirror existing 2026-05-04 session-end pattern:
- `## 2026-05-05 (session-end)` heading
- F4 phase summary (1-2 paragraphs): tasks shipped this session (T-412..T-423 = 12 F4 tasks across 1 marathon session), commits range (39d7ea8..1bc0261 + final T-423 commit), test count progression (1789 pytest + 4 vitest → 160 vitest + 0 → 23 dashboard parametrizations + 0 → 3 Playwright)
- Active lessons summary (L-001..L-010; new L-010 from T-420 apiFetch 204 fix)
- Watch-outs for next session
- F5 pick-up notes per BRIEF §19:2575+ (Shadow Variants + Backtest Harness + Finishing)

---

## Hazards

T-423 has **no §20 hazard binding** (documentation + runbook; no business logic, no exchange state, no math).

Reviewer cross-checks:

- **L-001 polling intervals**: N/A.
- **L-009 pip-audit**: NO new external deps.
- **§N1 UTC**: runbook timestamps use ISO-8601 with `+00:00` per §N1 (consistent with status.md convention).
- **§N3 idempotency**: N/A (read-only docs).
- **§N6/§N7/§N8/§N9**: N/A (markdown).
- **§16.6 LAN-only**: runbook mentions `http://127.0.0.1:5173` + `http://127.0.0.1:8000` per existing dev pattern.

---

## Test strategy

Operator-runnable verification. NO automated test added; existing test suites unchanged.

**Acceptance proof**:
1. Operator runs runbook locally, ticks each box, confirms F4 exit-criteria E1 passed.
2. Master push triggers `e2e.yml` workflow → 3 Playwright scenarios pass on chromium.
3. T-423 commit message references operator confirmation.

---

## §N invariants

- **§N1 UTC**: timestamps in status.md + runbook use ISO-8601 with `+00:00` offset.
- **§N2 logs**: N/A.
- **§N3 idempotency**: N/A.
- **§N6/§N7/§N8**: N/A (markdown).
- **§N9 configurability**: runbook command snippets reference dev defaults; no hardcoded production values.
- **§N5 80% coverage**: N/A.
- **§16.6 LAN-only**: runbook references local-host URLs only.

**Hazard mapping**: no §20 binding.

---

## §0.3 LOC budget

| File | Estimate (LOC) |
| --- | --- |
| `docs/runbooks/F4_E1_dashboard_smoke.md` | ~150 |
| `TASKS.md` | ~30 delta |
| `docs/status.md` | ~20 delta |
| **Subtotal** | **~200** |

**Per BRIEF §0.3** — documentation exempt from cap ("excluding tests, generated code, migrations, vendoring"); T-423 ships 100% docs/runbook content.

---

## §0.8 anti-hypothetical (deferred / out of scope)

- **NO new src code** (T-423 is verification + runbook bundle).
- **NO new pytest tests** (existing 1789 covers backend; F4 exit-criteria are operator-runnable, not automated).
- **NO new UI tests** (existing 160 vitest + 3 Playwright sufficient).
- **NO CI workflow changes** (T-422 e2e workflow already shipped).
- **NO production deploy artifacts** (F5+ — auth + nginx + cloudflared).
- **NO operator training material beyond smoke checklist** (F5+).
- **NO incident response playbook** (F5+).
- **NO load test scripts** (F5+).
- **NO shadow runtime initialization** (F5+ per BRIEF §13).

---

## Open questions

(Operator may opt to defer specific runbook step wording; no architectural OQs.)

- **OQ-1 Runbook command style** — bash commands vs browser-only steps. Default A: browser-only steps for UX verification (per BRIEF §19:2569 "operator can navigate"); bash only for prerequisites (start backend + dev server). No outstanding operator decision.

---

## Acceptance criteria

1. `docs/runbooks/F4_E1_dashboard_smoke.md` exists with 10-step checklist + exit checklist.
2. Each of BRIEF §19:2569-2570 5 exit criteria mapped 1:1 to runbook section.
3. Runbook prerequisites enumerate analytics-api + Vite dev server startup.
4. `TASKS.md` updated: T-423 entry in Done; F4 phase counter reflects 24/24 + close-out shipped; F4 sub-section headings pruned or marked complete.
5. `docs/status.md` updated with `## 2026-05-05 (session-end)` block.
6. Backend pytest 1789 unchanged (no Python files touched).
7. UI vitest 160 unchanged (no UI src files touched).
8. `pnpm typecheck` / `pnpm lint` / `pnpm build` clean (no UI changes).
9. NO new external deps.
10. Operator can run runbook end-to-end on dev host with backend + Vite running; 5 exit criteria check-boxes tickable.

---

## Hand verification

T-423 has **zero financial arithmetic**. Math-validator **OUT OF SCOPE** per CLAUDE.md Gate 4 line 121 (`docs/runbooks/` not in math-binding list; documentation only).

---

## Out of scope

(Already enumerated in §0.8.)

---

## Write-time guidance

(plan-reviewer Gate 1 APPROVE 2026-05-05; 6 WG items.)

1. **Step 10 Playwright CI semantics — pin "prerequisite vs closing loop"**: Plan §"Public interface" Step 10 reads "Playwright CI smoke green (verify on master push)" ambiguously. Pin in runbook body: T-423 commit itself triggers a master-push Playwright run; Step 10 reads "After T-423 commit lands on master, verify the latest `e2e.yml` workflow run on master HEAD is green (3 chromium scenarios passed)". Operator ticks Step 10 as last action post-commit, NOT pre-commit. This avoids chicken-and-egg (T-423 cannot block on its own future CI run).

2. **Each runbook step must have ≥2 `- [ ]` bullets**: Plan §"Behavioral contract" says each step has "2-5 sub-bullets with `- [ ]` for operator check-off". Verify ALL 10 steps ship with at least 2 expected-outcome bullets. Each BRIEF §19:2566-2571 exit criterion satisfaction is per-bullet (e.g., Step 7 needs at least: "select signal from list", "rule list renders with weight + result + applied_weight columns"), not per-section. Mirror T-313 F3_E1 step granularity.

3. **TASKS.md F4 sub-heading pruning — pin behaviour**: Plan §"Behavioral contract — TASKS.md narrative" leaves "preserved with empty placeholders or pruned (operator preference)" undecided. Per BRIEF §0.8 anti-hypothetical: prune the empty `### F4 — Backend (analytics-api + alerting-svc)`, `### F4 — Frontend (Dashboard UI)`, `### F4 — Operations + Exit criteria` sub-headings entirely (T-313 precedent: completed-phase sub-headings collapse into Done narrative). Add `## Next (F5 — pending operator unlock per §0.10)` placeholder section with brief note: "F5 phase scope per BRIEF §19:2575+ (Shadow Variants + Backtest Harness + Finishing). Tasks pending operator-driven plan-out. Phase gate not yet open."

4. **Test counts — read at commit time, not from session-end**: Plan §"Acceptance criteria" #6/#7 hardcodes "1789 pytest" / "160 vitest". These were last session's numbers. By T-423 commit time, T-412..T-422 may have added/changed tests. Replace literal numbers with: "Backend pytest count unchanged from `git log -1 master --pretty=%H` baseline" + "UI vitest count unchanged from same baseline". Actual count goes into commit message + status.md after running test suite locally pre-commit (mirror T-313 / T-222 sign-off pattern).

5. **status.md commit range + master HEAD anchor**: Plan §"Behavioral contract — docs/status.md update" cites range "39d7ea8..1bc0261 + final T-423 commit" — these SHAs must be re-verified at write time via `git log master --oneline | head -20`. status.md must reflect actual master HEAD at T-423 commit time, not plan-time guess. Pre-commit run: capture `git rev-parse HEAD` post-commit-prep (or use full `git log` of T-412..T-423 sequence) and substitute.

6. **§N1 verbatim form check on runbook timestamps**: Plan §N invariants asserts ISO-8601 with `+00:00`. Pin: any timestamp the operator records during runbook run (sign-off section, run timestamp) MUST use full ISO-8601 form `YYYY-MM-DDThh:mm:ss+00:00`, NOT abbreviated `Z` suffix or implicit UTC. Mirror T-313 sign-off line: `Run timestamp: 2026-05-02T20:15:30+00:00`.
