# T-527b2b — §B.1 sizing cross-service wiring (consumer leaf, capital-path)

**Status:** Consolidated plan, all OQ baked, ready for plan-reviewer Gate-1. Recursive-split consumer leaf 2/2 of T-527b2 (split-record `docs/plans/T-527b2.md`); the FINAL leaf of the T-527 tree (T-527a config + T-527b1 `get_mark_price` + T-527b2a `packages/sizing` compute all shipped). ADR-0013 (Accepted) governs the architecture — **no new ADR**. First & only consumer of `packages.sizing.compute_qty_from_sizing`.
**Phase:** F5 (ADR-0011 position-sizing cluster).
**Operator OQs (resolved, baked):** OQ-1=A **single task, NO further L-007 split** (T-527b2b plan-stage 2026-05-16 — code-inventory ~80 src + ~100 test = ~180 total ≪ §0.3 400 cap even at the recurring ~1.8× L-022 docstring density ≈ ~150 src; T-527a was the same schema+producer+wiring shape single-task at ~168 src; split would be ceremony without §0.3 benefit). OQ-6b=A `SizingSpecForWire` in `packages.bus.payloads`. OQ-7=A `sub_account` kwarg into `make_per_bot_handler`. **B1 (fetch-failure)** = skip-signal + log + counter (mirror the `get_instrument_info` transient-failure precedent). Inherited: ADR-0013 exec-service seam; OQ-3=A score→mult (done inside b2a); OQ-4=A sub-lowest-tier→skip-before-place. No unresolved questions.

## Purpose

Wire the shipped §B.1 sizing pieces end-to-end: thread `BotConfig.sizing` producer→wire→execution and replace the static `OrderRequest.qty` with `packages.sizing.compute_qty_from_sizing(...)` at the execution-service placement seam (ADR-0013). Backward-compatible: a bot with no `sizing:` block (`BotConfig.sizing is None` → `OrderRequest.sizing is None`) takes the byte-unchanged static `request.qty` path. Completes the T-527 §B.1 tier-ladder sizing system (still per-bot opt-in).

## Scope — files (1 new wire model + 5 wiring edits + tests)

### 1. `packages/bus/payloads.py` — NEW `SizingTierWire` + `SizingSpecForWire` (OQ-6b)

Mirror `VariantSpec` (`:66-79`) EXACTLY — the T-511b2 precedent (`packages.scoring.types` imports `VariantSpec` from here for `ShadowVariant`; `packages.bus` does NOT import `packages.scoring` — verified; `SizingSpecForWire` is bus-owned, no cycle):
- `class SizingTierWire(BaseModel)` — `model_config = ConfigDict(extra="forbid", frozen=True)`; `balance_min: Decimal`, `size: Decimal` (Decimal fields — VariantSpec.overrides carries `Decimal` on the wire; the bus envelope round-trips Decimal via `model_dump(mode="json")`→str / `model_validate`→Decimal, the established convention — NOT str-encoded).
- `class SizingSpecForWire(BaseModel)` — same `ConfigDict(extra="forbid", frozen=True)`; `tiers: list[SizingTierWire]`, `score_multipliers: dict[str, Decimal]`, `max_notional_per_symbol: dict[str, Decimal]` (the latter two are already `dict[str, Decimal]` in both `SizingSection` and the b2a function signatures — pass-through, no conversion). NO validators (the producer maps from the already-validated `SizingSection`; `extra="forbid"` catches wire corruption).

### 2. `packages/bus/schemas/orders.py` — additive `OrderRequest.sizing` (mirror T-527a `score`)

Add `sizing: SizingSpecForWire | None = None` after `score` (`:118`); import `SizingSpecForWire` from `packages.bus.payloads`. Extend the schema_version-stays-`"1.0"` docstring paragraph (`:80-85`) to name `sizing` alongside the T-527a `score` / T-511b2 `shadow_*` additive precedent. No `extra="forbid"` on `OrderRequest` → old payloads (no `sizing`) validate; `schema_version` stays `"1.0"`.

### 3. `services/strategy_engine/app/consumer.py` — producer maps `BotConfig.sizing`→`OrderRequest.sizing`

In `_publish_order_request` (`:395-471`), mirror the `bot_config.shadow`→`shadow_variants` conditional-build block (`:414-429`): build `sizing_payload: SizingSpecForWire | None = None`; `if bot_config.sizing is not None:` → `SizingSpecForWire(tiers=[SizingTierWire(balance_min=t.balance_min, size=t.size) for t in bot_config.sizing.tiers], score_multipliers=dict(bot_config.sizing.score_multipliers), max_notional_per_symbol=dict(bot_config.sizing.max_notional_per_symbol))`. Add `sizing=sizing_payload` to the `OrderRequest(...)` ctor (beside `score=score`).

### 4. `services/execution/app/placement.py` — the sizing compute seam + B1 + skip (capital-path)

Insert a sizing block **after the bot_id-mismatch warning (`:155`), before the `# 3a. T-529` comment (`:156`)**; substitute `working_qty` for `request.qty` in the existing `quantize_qty(...)` call (`:163`). Structure (mirrors the verified placement.py error-return posture):

```
working_qty = request.qty
if request.sizing is not None:
    try:
        balance = await adapter.get_account_balance(sub_account)
        mark_price = await adapter.get_mark_price(request.symbol)
    except (AuthError, NetworkTimeout, RateLimitError) as exc:          # B1 — verbatim the
        logger.error("execution.sizing_fetch_failed", bot_id=bot_id,    # get_instrument_info
                      symbol=request.symbol, error=str(exc))            # :175-182 precedent
        metrics.signals_skipped_sizing.labels(bot_id=str(bot_id), reason="fetch_failed").inc()
        return
    tiers = [SizingTier(balance_min=t.balance_min, size=t.size) for t in request.sizing.tiers]
    try:
        computed = compute_qty_from_sizing(
            total_equity=balance.total_equity, mark_price=mark_price, tiers=tiers,
            score=request.score, score_multipliers=request.sizing.score_multipliers,
            max_notional_per_symbol=request.sizing.max_notional_per_symbol,
            symbol=request.symbol)
    except ValueError as exc:                                           # b2a mark_price<=0 guard
        logger.error("execution.sizing_compute_failed", bot_id=bot_id,
                      symbol=request.symbol, error=str(exc))
        metrics.signals_skipped_sizing.labels(bot_id=str(bot_id), reason="compute_error").inc()
        return
    if computed is None:                                                # OQ-4=A sub-lowest-tier
        logger.info("execution.signal_skipped_sub_lowest_tier", bot_id=bot_id,
                     symbol=request.symbol, total_equity=str(balance.total_equity))
        metrics.signals_skipped_sizing.labels(bot_id=str(bot_id), reason="sub_lowest_tier").inc()
        return
    working_qty = computed
# 3a. T-529 ... quantize_qty(working_qty, instrument_info)   # request.qty → working_qty
```

`SizingTier` rehydration: `placement.py` imports `SizingTier` from `packages.scoring.types` (services/execution → packages.scoring is acyclic — scoring imports neither services nor packages.sizing) so `compute_qty_from_sizing`'s exact `tiers: list[SizingTier]` typed contract is honoured (no duck-type / no `cast`). **`SizingTier` has `model_config = ConfigDict(frozen=True, strict=True)` (`packages/scoring/types.py:314`)**: `SizingTierWire.balance_min`/`.size` are already `Decimal` after the bus `model_validate` (Decimal-on-wire → str via `model_dump(mode="json")` → `Decimal` on validate, the VariantSpec convention), so `SizingTier(balance_min=t.balance_min, size=t.size)` is a **Decimal→Decimal** construction — it satisfies `strict=True` with NO float/int intermediate and NO coercion (verified `types.py:314`). `score_multipliers`/`max_notional_per_symbol` are `dict[str, Decimal]` on both the wire model + the b2a signature → pass through unchanged. The B1 fetch `except (AuthError, NetworkTimeout, RateLimitError)` is the **complete** taxonomy for `get_account_balance`/`get_mark_price`: these are read RPCs and do NOT raise `OrderRejected` (unlike `set_leverage:186` which adds `OrderRejected` because it is an order-op) — verified against `protocols.py:138` (`get_account_balance`) / `:157` (`get_mark_price`); the B1 trio mirrors the `get_instrument_info:175-182` read-RPC precedent exactly. The `except ValueError` (b2a `mark_price<=0` guard) is defensive — `get_mark_price` returns a positive Decimal in practice, but the b2a contract raises and the seam must handle it (L-019 — every external-fed compute at an await-adjacent site has explicit handling).

### 5. `services/execution/app/placement.py` + `main.py` — `sub_account` kwarg (OQ-7) + metrics plumbing

- `make_per_bot_handler` (`:111-125`): add a `sub_account: str` kwarg AND a `metrics: Metrics` kwarg (both after `bot_id`), bound into the `_handle` closure **mirroring the existing `now_fn` kwarg→closure binding (`:119` / `:135-137`)** — NOT a T-531 precedent (T-531 threaded `exec_metrics` into the *equity-snapshot job* closure `main.py:379`, not into `make_per_bot_handler`; the correct precedent for a kwarg-into-this-handler is `now_fn`/`adapter`). **COMMITTED (not write-time-conditional)**: `placement.py` has NO `metrics`/`exec_metrics` in scope today (verified — grep `metrics` in placement.py = 0; `make_per_bot_handler:111-125` signature has none), so the `metrics: Metrics` kwarg IS required, unconditionally.
- `main.py` make_per_bot_handler call-site (`:174-187`): pass `sub_account=_resolve_sub_account(adapter)` (the shipped helper `:212-220`, already used for the dispatcher/audit bindings `:236,:330`) AND `metrics=exec_metrics` (the `exec_metrics` built at `main.py:127` inside `create_app`, in closure scope at the `:174-187` lifespan call-site — verified in scope via `:379`/`:456`). Both kwargs unconditional.

### 6. `services/execution/app/metrics.py` — `signals_skipped_sizing` Counter

`Metrics` dataclass (`:33-58`): add `signals_skipped_sizing: Counter`; `build_execution_metrics` instantiate `Counter("signals_skipped_sizing_total", "...", ["bot_id", "reason"], registry=...)` (mirror the `virtual_balance` Gauge declaration `:52-57`; `reason` labels: `fetch_failed` / `compute_error` / `sub_lowest_tier`). Verbatim metric-name convention (T-531 `virtual_balance` / the strategy-engine `signals_blocked_*` T-526/T-524 pattern).

### Tests (~7-8, exempt from §0.3)

- `packages/bus/tests/test_payloads.py`: NEW `SizingSpecForWire` Decimal-precision round-trip test — pattern = the existing `*_round_trip` tests (`test_payloads.py:62`/`:92`/`:149`, all `model_dump(mode="json")`→`model_validate`); **VariantSpec itself has NO round-trip test, and no existing `test_payloads.py` test pins Decimal precision (the parent_kind/shadow tests carry str/int, not Decimal) — this is the FIRST Decimal-on-wire precision pin**. Assert a `Decimal("0.001")`-class value survives `model_dump(mode="json")`→str→`model_validate`→`Decimal` **bit-identically** (no binary-float collapse) for `SizingTierWire.balance_min/size` + each `score_multipliers`/`max_notional_per_symbol` value; `extra="forbid"` rejects an unknown key on both `SizingTierWire` + `SizingSpecForWire`.
- `packages/bus/tests/test_orders_schema.py`: `OrderRequest.sizing` default `None`; explicit round-trip; **backward-compat pin** — payload omitting `sizing` validates → `sizing is None`, `schema_version=="1.0"` (mirror the shipped `score` backward-compat test).
- `services/strategy_engine/tests/test_consumer.py`: `_publish_order_request` with `bot_config.sizing` set → published `OrderRequest.sizing` mirrors it (tiers/multipliers/caps exact Decimal); with `bot_config.sizing is None` → `OrderRequest.sizing is None` AND `qty == bot_config.execution.qty` (both-sides no-behavior-change pin, L-017).
- `services/execution/tests/test_placement.py` (§N4 capital-path — the load-bearing tests): (a) happy-path — `request.sizing` set, `get_account_balance`/`get_mark_price` mocked → `quantize_qty` called with the **computed** qty NOT `request.qty`, and the computed qty flows to `place_market_order`; (b) sub-lowest-tier — `compute` returns None (mock balance < lowest tier) → early return, NO `place_market_order`, `signal_skipped_sub_lowest_tier` log + `signals_skipped_sizing{reason=sub_lowest_tier}` inc; (c) B1 fetch-fail — `get_account_balance` raises `NetworkTimeout` (and a second test: `get_mark_price` raises `RateLimitError`) → log `sizing_fetch_failed` + `{reason=fetch_failed}` inc + return, NO place (both-sides L-017: `place_market_order` not called); (d) `request.sizing is None` → `working_qty == request.qty`, existing path byte-unchanged (no `get_account_balance`/`get_mark_price` call); (e) backward-compat — OrderRequest without `sizing` → static path.
- `services/execution/tests/` main.py handler-registration (L-015 sibling — `main.py` is a touched §N5-enforced file [`services/execution/app/` is in the §17.2 set; whole-root coverage applies but the changed `:174-187` call-site lines need coverage]): identify the existing lifespan/handler-registration test (`test_main.py` / `test_app_factory.py` / `test_lifespan*`) and assert `make_per_bot_handler` is invoked with the new `sub_account` (= `_resolve_sub_account(adapter)`) AND `metrics` (= `exec_metrics`) kwargs; if no such test exists, add a focused smoke-assert (mirror the T-531 `test_app_factory` add_job-registration sibling-fix shape). Without this the changed `main.py` delta lines are uncovered (§N5 / L-015).
- mypy strict over staged incl tests (L-024); ruff + ruff-format (L-023).

## Hazards (§20)

No new H-NNN. ADR-0011 reserved no T-527 hazard slot (§0.8); ADR-0013 assessed the architecture (skip-before-`place_market_order` = no capital at risk on skip). Capital-path correctness is pinned by §N4 TDD (computed-qty substitution + the 3 skip seams) + Gate-4 math-validator. The B1 fetch-failure path is fail-safe (skip, no order). 0 §20 regression — `request.sizing is None` path byte-unchanged.

## §N invariants

- **N1.** Decimal end-to-end — `SizingSpecForWire` Decimal fields round-trip via the bus envelope (VariantSpec precedent, no float); `compute_qty_from_sizing` (b2a, VERIFIED) is Decimal-only; `working_qty` (computed Decimal) flows into the existing `quantize_qty` (Decimal). No `float()` introduced.
- **N2.** Structured logs: `execution.sizing_fetch_failed` / `.sizing_compute_failed` / `.signal_skipped_sub_lowest_tier` (mirror the existing `execution.*` placement log-key convention).
- **N3.** No NEW external write — reuses shipped `@idempotent` reads `get_account_balance`/`get_mark_price`; no order placed on any skip path.
- **N4.** TDD — the capital-path qty substitution + 3 skip seams + producer mapping are tests-first, hand-asserted.
- **N5.** ≥80% on the touched `services/execution/app/` (placement seam + the changed `main.py:174-187` handler-registration delta — covered by the §Tests main.py-handler-registration bullet, L-015), `services/strategy_engine/app/` (producer), `packages/bus/` (wire model) — the §17.2 whole-root coverage applies (no gate change; the §17.2 per-module-gate seam itself is the separate `T-F5+` backlog from T-527b2a, not this leaf). Every changed file (incl. `main.py`) has a test in the §Tests list.
- **N6.** No globals — `sub_account`/`metrics` bound via the handler closure (DI, mirror `now_fn`).
- **N7.** Hexagonal — placement orchestrates the shipped pure compute (b2a) + adapter reads (T-530/T-527b1); no business math here (it's in b2a).
- **N8.** No migration.
- **N9.** No hardcoded sizing literal — all numbers from `BotConfig.sizing` YAML (T-527a); B1/skip reasons are fixed enums-as-labels not thresholds.
- **N10.** 0 §20 regression — additive; `request.sizing is None` → static path byte-unchanged (pinned by test (d)/(e)).

## Gate-4 disposition (COMMITTED — not "to confirm")

**math-validator IN scope, expected `VERIFIED` (MODERATE — Decimal-round-trip + substitution, NOT the b2a-HEAVY-arithmetic class).** The staged diff touches `services/execution/` (literally in the CLAUDE.md Gate-4 trigger list — no precedent argument needed, unlike T-527b1/b2a). The §B.1 sizing **arithmetic** was T-527b2a (already math-validator VERIFIED HEAVY); T-527b2b's numeric surface is **plumbing of b2a's output**: (i) the `SizingSpecForWire` Decimal wire round-trip (producer `SizingSection`→wire→`OrderRequest`→placement) must preserve exact Decimal (the VariantSpec-Decimal-on-wire precedent — model_dump(mode="json")/model_validate); (ii) `working_qty = computed` (a Decimal pass-through, no transform) feeds the existing shipped T-529 `quantize_qty` unchanged (single rounding point — b2b adds NO rounding); (iii) the `SizingTier` rehydration is a field copy (`balance_min`/`size` Decimal, no arithmetic). Verification = no Decimal→float anywhere on the wire/substitution path; no second rounding; the producer→wire→rehydrate→compute Decimal chain is lossless. Hand verification:

- Producer `SizingSection.tiers[i].balance_min = Decimal("500")` → `SizingTierWire(balance_min=Decimal("500"))` → `model_dump(mode="json")` `"500"` → `model_validate` `Decimal("500")` → rehydrated `SizingTier(balance_min=Decimal("500"))` — exact, no float collapse (a §B.1 alpha.yaml round-trip on the full block: tiers {500,700}/{1000,1400}/…, multipliers "4":0.75…, caps default:3000/BTCUSDT:5000 — every value bit-identical end-to-end). The rehydration `SizingTier(balance_min=Decimal("500"), size=Decimal("700"))` is a **Decimal→Decimal** construction (`SizingTierWire` fields are `Decimal` post-`model_validate`) → satisfies `SizingTier` `model_config = ConfigDict(frozen=True, strict=True)` (`packages/scoring/types.py:314`) with NO float/int intermediate, NO pydantic coercion — math-validator confirms the rehydrated `SizingTier` is strict-accepted and `.balance_min`/`.size` are bit-identical to the producer-side `SizingSection` values (the lossless producer→wire→rehydrate chain ends in a strict-typed, precision-preserved value fed to the b2a-VERIFIED `compute_qty_from_sizing`).
- `working_qty = computed` where `computed = Decimal("1750.00")/Decimal("42500.75")` (b2a's verified output for the §B.1 example) → passed verbatim into `quantize_qty(working_qty, instrument_info)` (no `float()`, no pre-round); the qty_step round-down is the sole T-529 rounding.
- The 3 skip seams (`fetch_failed`/`compute_error`/`sub_lowest_tier`) return BEFORE any qty/price arithmetic — no numeric path on skip.

## §0.3 LOC budget (L-022)

`payloads.py` SizingTierWire+SizingSpecForWire ~14 · `orders.py` field+docstring ~9 · `consumer.py` producer block ~11 · `placement.py` seam block+substitute ~38 · `make_per_bot_handler`+main.py sub_account/metrics ~8 · `metrics.py` counter ~6 = **~86 src**; +L-022 docstring contingency (the seam block + Gate-4/§N4 rationale comments, the recurring ~1.5-1.9× pattern) ⇒ **~130-165 src ≪ §0.3 400 cap**, single task, NO further split (OQ-1=A — code-inventory confirmed; T-527a precedent ~168 src same shape). drift-checker L-022 tripwire (≥1.5× ≈ ≥~210, or any `packages/sizing` math edit / new ADR / scope beyond the 6 wiring files → escalate). Not a first-of-its-kind scaffold (every piece mirrors a shipped precedent: VariantSpec / T-527a score / shadow_variants producer / get_instrument_info error-return / T-531 metrics+_resolve_sub_account).

## Out of scope (§0 — no scope creep)

- Any change to `packages/sizing/compute.py` math (shipped T-527b2a, VERIFIED — b2b only CALLS it) or `get_mark_price`/`get_account_balance` (shipped) or `quantize_qty` (shipped T-529 — b2b feeds `working_qty` into it unchanged, single rounding point).
- `tier_promotion`/`tier_demotion` (T-527 OQ-2=A deferred → `T-F5+`); `sizing.method` discriminator (T-528); the §17.2 per-module coverage gate (`T-F5+` from T-527b2a).
- B2/B3 fetch-failure behavior (operator chose B1); DLQ/retry for sizing fetch (B1 = skip, no DLQ — explicitly).
- Migration / Settings / ADR / new H-NNN / BRIEF edit (ADR-0013 Accepted governs; no new decision).

## Acceptance criteria

1. NEW `SizingTierWire` + `SizingSpecForWire` in `packages.bus.payloads` (mirror `VariantSpec` `ConfigDict(extra="forbid", frozen=True)` + Decimal fields; bus-owned, no `packages.scoring` import — no cycle).
2. Additive `OrderRequest.sizing: SizingSpecForWire | None = None`; `schema_version` stays `"1.0"`; old payloads validate (backward-compat test pin); docstring updated.
3. Producer maps `bot_config.sizing`→`OrderRequest.sizing` (mirror `shadow_variants` block); `bot_config.sizing is None` → `sizing=None` AND `qty` unchanged (both-sides pin).
4. Placement seam: `request.sizing is not None` → fetch `get_account_balance(sub_account)` + `get_mark_price(request.symbol)`; rehydrate `SizingTier` (acyclic scoring import); `compute_qty_from_sizing` → substitute `working_qty` into the existing `quantize_qty` (the SOLE qty change-point; no other qty line touched). `request.sizing is None` → `working_qty = request.qty` byte-unchanged.
5. **B1**: `get_account_balance`/`get_mark_price` raising `(AuthError|NetworkTimeout|RateLimitError)` → log `execution.sizing_fetch_failed` + `signals_skipped_sizing{reason=fetch_failed}` + return, NO `place_market_order` (verbatim the `get_instrument_info` `:175-182` precedent). `compute` `ValueError` → `{reason=compute_error}` skip. `compute` `None` → `signal_skipped_sub_lowest_tier` + `{reason=sub_lowest_tier}` skip (OQ-4=A). All three: no order placed.
6. `sub_account: str` AND `metrics: Metrics` kwargs into `make_per_bot_handler` (both unconditional — mirror the `now_fn` kwarg→closure precedent; placement.py has NO metrics in scope today, verified) + main.py call-site passes `sub_account=_resolve_sub_account(adapter)` (shipped helper) + `metrics=exec_metrics` (in scope); `signals_skipped_sizing` Counter added to `build_execution_metrics`; main.py handler-registration delta covered by a `test_app_factory.py:299`-shape test (§N5/L-015).
7. §N4 TDD all 5 placement seams (happy/sub-lowest/B1-fetch-fail×2/none/backward-compat) + producer + wire round-trip; full pytest 0 regressions; mypy strict over staged incl tests (L-024); ruff+ruff-format (L-023).
8. **Gate-4 (COMMITTED)**: math-validator IN scope (services/execution/ literal), expected `VERIFIED` MODERATE — Decimal wire-round-trip lossless + `working_qty` pass-through no-second-round + no float; `## Hand verification` is the cross-check; NOT deferred.
9. §N1-N10; single task no split (OQ-1=A); no migration/dep/Settings/ADR/new-H-NNN/BRIEF; OQ-6b/OQ-7/B1/OQ-4=A baked; recursive-split-record `docs/plans/T-527b2.md` consistent; **completes the T-527 §B.1 tier-sizing system** (per-bot opt-in, backward-compat).

## Write-time guidance (plan-reviewer Gate-1 pass-2 APPROVE — verbatim; brief-reviewer verifies each addressed)

1. **SizingTier rehydration is Decimal→Decimal under strict=True.** `SizingTier` is `ConfigDict(frozen=True, strict=True)` (`packages/scoring/types.py:314`). `SizingTierWire.balance_min/.size` MUST be `Decimal` (not `Decimal | int`) so the post-`model_validate` values are already `Decimal` and `SizingTier(balance_min=t.balance_min, size=t.size)` passes strict=True with no float/int intermediate and no pydantic coercion. Test (c) sub-lowest-tier and the happy-path test must construct the wire model from real Decimal values and assert the rehydrated `SizingTier` fields are bit-identical (no float collapse).
2. **`metrics: Metrics` kwarg into `make_per_bot_handler` is unconditional.** `make_per_bot_handler` (`placement.py:111-124`, keyword-only) has NO `metrics`/`exec_metrics` in scope today — add `sub_account: str` AND `metrics: Metrics` after `bot_id`, bound into the `_handle` closure mirroring the `now_fn` kwarg→closure pattern (`:119` / docstring `:135-137`). At `main.py:174` call-site pass `sub_account=_resolve_sub_account(adapter)` and `metrics=exec_metrics` (`exec_metrics` built `main.py:127` in `create_app`, in scope at the call-site). Do NOT make either kwarg optional/defaulted.
3. **main.py handler-registration delta MUST be covered (§N5/L-015).** Extend the existing `test_app_factory.py:299` `test_lifespan_threads_settings_execution_orders_dedup_capacity_to_make_per_bot_handler` (or add a focused sibling test next to it, mirroring its `mocker.patch("services.execution.app.main.make_per_bot_handler")` shape at `:350`) to assert `make_per_bot_handler` is invoked with `sub_account == _resolve_sub_account(adapter)` AND `metrics == exec_metrics`. The changed `main.py:174-187` lines must not ship uncovered.
4. **First Decimal-on-wire precision pin (CONCERN1).** The new `test_payloads.py` round-trip test must assert a `Decimal("0.001")`-class value survives `model_dump(mode="json")`→str→`model_validate`→`Decimal` **bit-identically** (no binary-float collapse) for `SizingTierWire.balance_min/size` AND each `score_multipliers`/`max_notional_per_symbol` value, plus `extra="forbid"` rejects an unknown key on both `SizingTierWire` and `SizingSpecForWire`. Mirror the `*_round_trip` test shape (`:62/:92/:149`) but add the explicit Decimal-equality assertions (the existing round-trip tests do not pin bare-Decimal wire precision).
5. **B1 catch tuple is exactly the read-RPC taxonomy, no `OrderRejected`.** The fetch `except` for `get_account_balance`/`get_mark_price` MUST be `except (AuthError, NetworkTimeout, RateLimitError)` — verbatim the `placement.py:175-182` read-RPC precedent. Do NOT add `OrderRejected` to the B1 tuple (that belongs only to `set_leverage:186` because it is an order-op). Keep the separate `except ValueError` for the b2a `mark_price<=0` guard (L-019 — every external-fed compute at an await-adjacent site has explicit handling). Test (c) must cover both `get_account_balance` raising `NetworkTimeout` and `get_mark_price` raising `RateLimitError`, asserting `place_market_order` NOT called (both-sides L-017).
