# T-539 — F5 task-completion reconciliation audit

**Status:** plan-reviewer Gate-1 APPROVE (2026-05-17). Standalone task; next free number (highest existing = T-538). Sibling of T-519 (T-519 audited §20-hazard-test coverage before E4; T-539 audits task-completion before E5/E6). Operator approved 3 structural defaults via "pokracuj": standalone T-539 (not T-522a split) / F5-only scope / one-time report (no permanent CI guard).

## Purpose

Produce a trustworthy F5 task ledger before T-522 close-out / E1..E6 sign-off: verify every F5 task-slot is genuinely DONE by hard commit-evidence (not prose), reconcile the `64/66` counter to pinpoint exactly which slots are open, fix stale display (T-527/528/532 parent checkboxes), and record it in an audit report. Motivating discrepancy: counter says 64/66 (numerically 2 open) but only T-522 is visibly real-pending; TASKS.md has 4 parent `[ ]` (T-522, T-527, T-528, T-532) yet T-527/528/532 are done via L-007 split leaves (completing feat commits `96fae4f` / `610c8b4` / `2bfc6a3`). plan-reviewer verified the 2nd open slot is an **artifact**: T-522 (line 207) is explicitly a 2-section sign-off slot (paper feature-complete + Live-ready per OQ-4) → `66−64=2` origin.

## Scope (IN)

1. **F5 phase only**: base F5 numbered T-501..T-522, T-523, hardening T-524..T-536, late-adds T-537/T-538, + ALL L-007 / recursive split leaves (T-525a/b, T-527a/b1/b2a/b2b, T-528a/b, T-532a/b, T-533a/b1/b2, T-534a/b1/b2, T-537a*/b*, T-513b1/b2, UI T-515..517, etc.).
2. **Per-slot DONE predicate** (uniform evidence model): DONE iff ALL of (a) feat/fix commit exists; (b) reachable from master HEAD (`git merge-base --is-ancestor <sha> master` exit 0); (c) a `chore(tasks)` close commit with its counter bump exists in the trail; (d) TASKS.md checkbox `[x]` — OR, for a stale split parent, ALL its leaves satisfy a–d; (e) every deliverable file the task-def claims exists on disk + is git-tracked. Any clause failing → "OPEN / FORGOTTEN" finding.
3. **Counter reconciliation**: independently re-derive denominator = base F5 slots + Σ(L-007 split +1 increments harvested from the full `chore(tasks) … F5 counter X→Y` trail) and numerator = count of closed leaves; cross-check vs the live `**64/66**` line 5. Pinpoint the EXACT 2 open slots. Classify the 2nd (beyond T-522) as (i) genuine forgotten task → operator finding, or (ii) accounting artifact (expected: T-522 2-section sign-off slot) → documented; counter corrected ONLY if proven wrong.
4. **TASKS.md fixes**: T-527/T-528/T-532 parent `[ ]`→`[x]` + " — done via L-007 split leaves [explicit leaf list + completing feat SHA]"; add the T-539 self task-def line; counter line edited ONLY if reconciliation proves 64/66 wrong (then with full derivation in commit body + report).
5. **Report**: NEW `docs/audit/f5-task-completion.md` mirroring T-519's `docs/audit/hazard-test-coverage.md`: per-slot table (slot | base/split-leaf | feat SHA | merged? | chore SHA | checkbox | deliverable file(s) exist? | verdict), step-by-step counter derivation, the 2-open-slot identification + classification, the deliberate scope boundary, the findings list, and the exact re-runnable verification commands (regenerable like T-519's report).

## Scope (OUT — deliberate boundary, §0.8 + L-026, recorded verbatim in the report)

- NO re-audit of F0–F4 (closed + audited at their own phase-closes).
- NO permanent CI counter-invariant guard (operator default = one-time; noted as deferrable T-F5+, NOT built — distinct from T-519's meta-test).
- T-539 does NOT perform any genuine forgotten WORK. A real gap → finding + STOP + operator decision (new task / fold into T-522), never absorbed (§0.8). T-539 fixes only proven display-staleness + counter-arithmetic-iff-proven-wrong.
- Backlog T-F5+ items are post-F5, excluded from the 66.

## Files touched

- NEW `docs/audit/f5-task-completion.md` (markdown).
- `TASKS.md` (un-stale T-527/528/532 parents; T-539 self-entry; counter line iff proven wrong; mark T-539 `[x]` at close).
- `docs/status.md` (T-539 close section, at chore-close).
- `docs/review-lessons.md` ONLY if a generalizable lesson emerges (decided at close, not pre-committed).
- ZERO source code, ZERO migration.

## §0.3 LOC

0 src — all markdown/docs. Precedent: T-521 (markdown-only, §0.3-exempt), T-519 (markdown + §0.3-excluded meta-test; here no meta-test per operator default). Markdown-exempt per §0.3.

## Hazards (§20)

None — no runtime path touched (0 code). No §20 regression surface (N10-safe by construction; confirmed no `services/` / `packages/` touch). N1: any timestamp in the report uses the source commit's own ISO-8601 UTC.

## Test strategy

N/A code-wise (0 src). Audit reproducibility IS the test: every verdict cites a re-runnable command (git SHA / `merge-base --is-ancestor` exit / `git ls-files` of the deliverable); the report lists the exact commands so the operator can independently regenerate it (mirrors T-519's regenerable report).

## Open questions

None structural (3 majors resolved by operator). The "is the 2nd open slot a real forgotten task or an artifact?" question is a RUNTIME branch handled by WG4 (stop+surface), not plan-blocking — plan fully consolidated.

## Write-time guidance (verbatim from plan-reviewer Gate-1 APPROVE — brief-reviewer verifies each against staged diff before SHIP)

1. **WG1 (L-018):** every per-slot DONE verdict in the report table must carry hard evidence — feat SHA + chore-close SHA + `git merge-base --is-ancestor` result + `git ls-files` of each claimed deliverable. No verdict on prose/reasoning alone (L-018 is explicit: "done"-claims need code/commit citation, not reasoning).
2. **WG2 (L-025 spirit):** the counter reconciliation must show the full step-by-step derivation (base F5 slot set + each harvested L-007 denom+1 with its source `chore` SHA, summed to 66; numerator = enumerated closed leaves) — self-checking, not just the final number. Cross-check against the live line5 `64/66` and state the match/mismatch explicitly.
3. **WG3 (L-026):** enumerate the FULL F5 slot set up front (every base T-501..T-538 + every L-007/recursive split leaf incl. T-525a/b, T-527a/b1/b2a/b2b, T-528a/b, T-532a/b, T-533a/b1/b2, T-534a/b1/b2, T-513b1/b2, UI T-515..517) before any verdicts; record the deliberate scope boundary verbatim in the report (F0–F4 not re-audited / backlog T-F5+ excluded from the 66 / no CI counter-guard built — operator one-time default). Single-site stop is the L-026 failure mode — do not commit any TASKS.md fix until the whole slot set is enumerated.
4. **WG4 (§0.8):** if any slot fails the DONE predicate as a *genuine forgotten task* (not a stale display artifact), STOP — surface to the operator before any TASKS.md mutation of that slot; do not absorb the missing work into T-539 (this becomes a NEEDS-DISCUSSION outcome, not a silent fix). Only proven display-staleness (the 3 known stale parents) + counter-arithmetic-iff-proven-wrong are mutated by T-539. Repo evidence indicates the only real-pending slot is T-522 and the gap is the documented 2-section sign-off — so the expected path is "artifact, documented, no forgotten task"; WG4 still binds if reconciliation surprises.
5. **WG5:** the TASKS.md counter line (line 5) is edited ONLY if the WG2 derivation proves `64/66` wrong — and then with the full derivation in both the commit body and the report. If correct as-is (expected from the verified trail), leave line 5 byte-untouched and have the report explicitly explain the `66−64=2` as `T-522 (1 real-pending, inherently a 2-section E1..E5 + E6 sign-off slot)`.

## Relevant paths

- `TASKS.md` — stale parents at lines 224 (T-527), 231 (T-528), 237 (T-532); counter narrative at line 5; only real-pending `[ ]` at line 207 (T-522)
- `docs/audit/hazard-test-coverage.md` — T-519 report, structural mirror for the new `docs/audit/f5-task-completion.md`
- `docs/review-lessons.md` — L-018:82, L-025:110, L-026:114 (controlling lessons for WG1/WG2/WG3)
- `docs/CLAUDE_CODE_BRIEF.md` — §0.3 (markdown-exempt), §0.8 (no-absorb)
