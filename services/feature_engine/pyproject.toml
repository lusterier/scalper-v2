[project]
name = "scalper-v2-feature-engine"
version = "0.1.0"
description = "Indicator computation on candle close; publishes features to NATS KV and the features table. Scaffold stub; implementation lands in F1."
requires-python = ">=3.12"
