[project]
name = "scalper-v2-strategy-engine"
version = "0.1.0"
description = "Per-bot decision process: consumes validated signals + features, applies scoring, emits order requests. Scaffold stub; implementation lands in F3."
requires-python = ">=3.12"
