[project]
name = "scalper-v2-alerting"
version = "0.1.0"
description = "Consumes NATS events and forwards formatted alerts to Telegram. Scaffold stub; implementation lands in F5."
requires-python = ">=3.12"
