[project]
name = "scalper-v2-execution"
version = "0.1.0"
description = "Order placement, lifecycle FSM, SL/TP/BE/trail, cumulative-delta reconciliation, exchange adapter pool. Scaffold stub; implementation lands in F2."
requires-python = ">=3.12"
