[project]
name = "scalper-v2-analytics-api"
version = "0.1.0"
description = "Read-only REST + SSE API backing the dashboard; admin write endpoints. Scaffold stub; implementation lands in F4."
requires-python = ">=3.12"
