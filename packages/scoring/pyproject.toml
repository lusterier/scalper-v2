[project]
name = "scalper-v2-scoring"
version = "0.1.0"
description = "Rule language, condition types, and evaluator for per-bot scoring. Scaffold stub; implementation lands in F3."
requires-python = ">=3.12"
