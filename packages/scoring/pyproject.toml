[project]
name = "scalper-v2-scoring"
version = "0.1.0"
description = "Rule language, condition types, and evaluator for per-bot scoring. T-300 ships foundational Pydantic types; F3 follow-ups build on top."
requires-python = ">=3.12"
dependencies = [
    "pydantic==2.13.2",
]
