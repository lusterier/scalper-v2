[project]
name = "scalper-v2-bus"
version = "0.1.0"
description = "NATS JetStream client wrapper and message envelope helpers. Scaffold stub; implementation lands in T-008."
requires-python = ">=3.12"
