[project]
name = "scalper-v2-features"
version = "0.1.0"
description = "Feature protocol and built-in implementations (EMA/RSI/ATR/VWAP/Bollinger/MACD). Scaffold stub; implementation lands in F1."
requires-python = ">=3.12"
