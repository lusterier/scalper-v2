[project]
name = "scalper-v2-features"
version = "0.1.0"
description = "Feature port + FeatureValue/OhlcCandle domain types (§9.3). Built-in implementations (EMA/RSI/ATR/VWAP/Bollinger/MACD) land in T-107."
requires-python = ">=3.12"
